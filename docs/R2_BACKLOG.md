# R2 — Article-vs-Code Backlog

*The source of truth for Phase R2: implementing what the articles specify but the code does not yet do.*

**Status: R1 (parity) complete. R2 in progress — 10 of 14 core items resolved.**

> **Read this before opening an R2 slice.** Every item traces to an article. The
> articles are the spec; the code catches up to them, never the reverse. Where
> an article and the code disagree, level UP to whichever is the better design —
> do not quietly make the doc match the code.

---

## Where things stand

| | |
|---|---|
| Reference engine (Python) | live, in daily use, **2,319 tests** |
| Portable core (Rust, `sustena-core`) | **R1 parity complete** — all 5 slices; **R2 in progress** |
| Conformance vectors | R1 parity + R2 spec, incl. **22 approval**, **26 editing**, **29 constraint**, **31 compose**, **16 version**, **15 migrate**, **20 region**, **17 detect**, **20 controller**, **18 kernel**, **16 tenet**, **18 ensemble**, **17 signal**, **16 pincer**, **17 ooda**, **17 monitor**, **15 holarchy**, **14 population**, **17 consensus**, **13 router**, **16 vclock**, **16 disaggregation**, **21 division**, **20 crdt**, **17 operative**, **16 goodhart+presentation**, **14 mixture**, **16 criticality**, **15 cynefin**, **15 boundary**, **14 flow**, **8 obligation**, **18 clocks**, **17 watermark**, **20 period**, **16 checkpoint**, **19 dimension**, **14 belief**, **16 harmonics**, **13 windowing**, **14 observability**, **14 damping**, **11 semantic**, **14 capability**, **11 stranding**, **14 learned**, **13 lens**, **15 preattentive**, **14 widget**, **45 curated**, **14 strategy**, **15 attention**, **20 rollup (parity)**, **11 holon (parity)**, **9 authorization (spec)**, **60 transducer (parity)** cases (divergences recorded) |
| Rust tests | **1243 unit · 955 conformance tests** across 69 binaries, plus 3 doctests (two of them `compile_fail` proofs) — all green, and **`cargo clippy --all-targets -- -D warnings` is clean** |

**R1 slices at parity:** state · event fold · rules · operators + gate · council.

---

## Legend

- **Status** — `done` · `open` · `deferred` (travels with the economy layer)
- **Tier** — `CORE` (engine behaviour; land before it calcifies) · `follow`
- **Where** — which engine still needs it

---

## ✅ Closed since the last doc pass (8)

Five from the correctness pass, three found by the Rust port itself.

| # | Gap | Article | How |
|---|---|---|---|
| 1 | Fold fidelity — 3 operators changed state without recording it | CELL | Structural reconciler in Python; **unrepresentable in Rust** |
| 2 | Swallowed event-write failure | RECORD | Dead second writer removed; one transaction, fails loudly |
| 3 | Two rule evaluators disagreed | LAW / GENOME | Python unified onto one; **Rust has only one** |
| 4 | "Cannot parse" reported as "rule violated" | LAW | Raises a typed error instead |
| 5 | Cross-source double-count (KCB + M-Pesa) | RECEPTOR | Correlation on the transaction reference |
| 6 | **State did not own its data** — replaying a log mutated the state it rebuilt from | CELL | Found by building the vectors; deep-copy on write |
| 7 | **Number typing** — whole floats collapsed to integers | — | Found by the port; Python's promotion rule preserved |
| 8 | Gate failed open on an unparseable rule | TOLERANCE | **Rust refuses**; Python still skips → see #9 |

### Three gaps are now *impossible* in the Rust core

Not fixed — structurally unable to occur:

- **The live-reference write** (#1) — `get()` cannot hand out a mutable alias.
- **Two evaluators** (#3) — one parser, one evaluator, guards and invariants alike.
- **Gate failing open** (#8) — an unreadable rule refuses rather than being skipped.

---

## 🔴 CORE — before the core calcifies (14)

Ordered by dependency, not importance.

### Safety and admission

| # | Gap | Article | Where |
|---|---|---|---|
| 9 | Gate still fails open in Python (Rust already refuses) | TOLERANCE | Python |
| ~~10~~ | ~~`min_privilege` never read~~ — **DONE in Rust** (edge-based privilege, monotone across the holon path, enforced at the gate). Python unchanged. | ENZYME · TOLERANCE | Python |
| ~~11~~ | ~~Only `refuse` is built~~ — **DONE in Rust** (refuse / clamp / defer, with the paper's three clamp safety rules enforced at authoring time). Python unchanged. | LAW | Python |
| 12 | `simulate.run_path` advances a fork with **no gate** | TOLERANCE | Python — *structurally absent in Rust: there is one execution path, so a fork cannot skip it* |
| 13 | `api.get` / `api.post` make **real outbound calls** outside the egress boundary | TOLERANCE | Python — *structurally absent in Rust: the core has no network* |

### The primitives

| # | Gap | Article | Where |
|---|---|---|---|
| ~~14~~ | ~~State is untyped~~ — **DONE in Rust**: declared record type with bounds, load-time predicate binding, and organisational closure enforced at the gate. Opt-in per sustain. Python unchanged. | CELL | Python |
| ~~15~~ | ~~No `inverse`~~ — **DONE in Rust**: patch-level inverse derived from the mutation record. Honest about the three shapes that are not invertible from a record alone. Python unchanged. | ENZYME | Python |
| ~~16~~ | ~~No **checked composition** of operator pathways~~ — **DONE in Rust** (`compose.rs`, 2026-08-12): Hoare sequencing with a three-valued ⊨, `wp` pullback, and a `Pathway` that cannot be constructed around a refuted link. Python unchanged. | ENZYME | Python |
| ~~17~~ | ~~Ordering is `seq`~~ — **DONE in Rust**: event-time ordering with Lamport `(t_event, id)` tie-break, causal stamps, and a converging LWW join. Python unchanged. | RECORD · GAIA | Python |
| ~~18~~ | ~~No substrate dedupe~~ — **DONE in Rust**: uniform dedupe on the stable id, so at-least-once delivery gives exactly-once effect. Python unchanged. | RECORD | Python |

### Consistency

| # | Gap | Article | Where |
|---|---|---|---|
| 19 | Council keeps **two stores** (state-based and SQL) that can diverge | SLIME | Python |
| ~~20~~ | ~~Rollback registry in-memory~~ — **DISSOLVED in Rust**: the inverse is *derived* from the event record, so there is no registry to lose. Python unchanged. | Controller | Python |
| 21 | **Two database layers** — routes on SQLAlchemy, engine on sqlite3 | ADR-0001 D2 | Python |
| 22 | `seed.py` **bypasses the engine** — half-made sustains, no genesis event | — | Python |

---

## 🟡 FOLLOW (13)

### Observation

| # | Gap | Article |
|---|---|---|
| ~~23~~ | ~~No Kalman / EWMA / CUSUM — urgency is a single `pct`~~ — **DONE in Rust** across two slices (2026-08-12): `region.rs` made `urgency(s) = d(s,V)` real (replacing the `pct` proxy *and* its `allocated <= 0 → 0.0` blind spot), and `detect.rs` added **EWMA + CUSUM** over that series plus the severity classification that is the **formal Monitor→Controller boundary**. **Kalman is a declined import, not a gap** — MON-11's own fit caveat says it assumes a continuous ODE that discrete event-sourced state is not, and the Sustena-native form now ships end to end. Python unchanged. | Monitor · PERCEPT |
| ✅ 24 | ~~No MonitorEngine; scheduling external, no heartbeats~~ — **the engine is DONE in Rust** (`monitor.rs`, 2026-08-16): per-sustain chains keyed by id with `flatten_holarchy`, the **native** `ingest()` (`W = d(s,V) → EWMA → CUSUM`, no Kalman), and the escalation that makes **OBSERVE self-driving** into the OODA loop. The diagnosis behind this row was exactly right and is worth keeping: the reference watches by RECOMPUTING per request, so an accumulator had nowhere to live. **Scheduling stays external on purpose** — no clock or `tick()` in core, the same boundary CTL-9 draws. Python unchanged. | Monitor |
| 25 | Widget type-checker exists with **no application call site** | PERCEPT |

### Editing

| # | Gap | Article |
|---|---|---|
| ~~26~~ | ~~**No definition version history** — edits in place, cannot roll back~~ — **DONE in Rust** (`version.rs`, 2026-08-12): append-only DAG, head is a pointer, nothing overwritten. Python unchanged. | SLOPE |
| ~~27~~ | ~~3-valued rollback (Total / Partial / Unavailable) not built~~ — **DONE in Rust**: and `Total` has no Δ field, so reporting Partial as Total is unspellable. Python unchanged. | SLOPE |

### Grammar

| # | Gap | Article |
|---|---|---|
| 28 | No arithmetic in the DSL (blocks `ratio * balance`) | GENOME |
| 29 | No dynamic bracket indexing (`members[loan.member_id]`) | GENOME |

### Operatives and network

| # | Gap | Article |
|---|---|---|
| 30 | 4 dormant operatives call the LLM directly, propose operators that don't exist | SCOUT |
| 31 | Streaming protocol is a no-op | SCOUT |
| 32 | CRDT merge, Lamport ordering, quorum consensus — deferred | SLIME |

### Naming

| # | Gap | Article |
|---|---|---|
| 39 | **The physical code-rename has never been executed.** Pawa (line 11) states it as a live debt: *"In code the ratified names **Enzyme**, **Symbiont**, and **Embroidery** are still `operator`, `operative`, and `dsl` — aliases until the build-phase rename."* Retire Operator→**Enzyme**, Operative→**Symbiont**, DSL→**Embroidery**, and `operator-DAG`→**`Enzyme-DAG`** across ~500+ occurrences. The articles, the GLOSSARY and this WBD already use the ratified names; **only the code does not**, so every reader currently translates between two vocabularies. **Policy (standing, ratified):** aliases and re-export shims first, **gradual module-by-module**, **never a big-bang**, and **nothing merges without a green build** — locally *and* in CI. **Sequence it against the Rust port deliberately:** `sustena-core` is new code and can be born with the ratified names, which makes the Rust boundary the natural place to stop paying the translation cost. | Pawa (l. 11) · GLOSSARY |

### Housekeeping

| # | Gap |
|---|---|
| 33 | Legacy quarantine approved but **never executed** |
| 34 | `/events` queries a non-existent column → silently returns empty |
| 35 | Logout does not revoke — `sustains.py` skips `token_version` |
| 36 | 4 overlapping widget-render systems |
| 37 | Migrations hand-rolled; `alembic/` empty |
| 38 | Firestore sync is a mock — no real cross-device sync |

---

## ⏸️ DEFERRED — travels with the economy layer (6)

Held back with the economy layer per ADR-0001 Decision 5, pending legal review.

| # | Gap | Article |
|---|---|---|
| E1 | Two-type royalty split — `charge()` still runs the old 70/20/5/5 | Mycelium |
| E2 | `charge()` has **zero callers** — the ledger is never used | Mycelium |
| E3 | Pawa meter not wired to the ledger | Pawa |
| E4 | κ constants uncalibrated | Pawa |
| E5 | Arena `trust_score` and `downloads` are dead constants | Arena |
| E6 | Arena orders do not settle | Arena |

---

## What still needs a decision

**Nothing on the core list.** #17 was resolved by re-reading the articles: they
already specify event-time convergence, so it was settled spec rather than an
open fork.

**Standing rule, from that exchange:** if the articles already answer a
question, it is not an open decision — implement it and note it. Surface only
genuinely open forks: ones the articles are silent on, or where reality clashes
with the spec.

**#35 and #13 are live security items** sitting in FOLLOW: logout does not
revoke a session, and two operators make real outbound HTTP outside the egress
boundary. Both are real today on the live host. Pull forward on request.

---

## Working rules for R2

1. **Ground every slice in its article.** Cite it in the commit.
2. **Prove it with vectors.** A slice is not done until both engines agree, or
   until the vector records a deliberate, documented divergence.
3. **Python stays live and untouched** unless the item is a correctness fix to
   the reference itself.
4. **Build clean.** Several R1 gaps closed simply by not reproducing the old
   shape. Prefer making a defect unrepresentable over guarding against it.

---

---

## ⚠️ Whole-system reconciliation (2026-08-12) — checked against SUSTENA_UPGRADE_SPEC.md

This backlog covers the **foundation + walking skeleton** well. But the master spec (`SUSTENA_UPGRADE_SPEC.md`, §9.1) defines a three-module **operational spine — Monitor / Tenet / Controller** — and **two of the three are not yet tracked here.** Added below as pending so the backlog is comprehensive.

### ✅ SPINE — **all three modules now shipped in Rust** (2026-08-16)

**Monitor ✅ · Tenet ✅ · Controller ✅.** The spine the master spec puts at the centre is no longer a gap: `region.rs` + `detect.rs` (Monitor), `tenet.rs` + `ensemble.rs` (Tenet), `controller.rs` (Controller), with `kernel.rs` underneath all three. **And as of 2026-08-16 they are wired into one cycle** — `ooda.rs` (CTL-3) sequences them, with the human structurally at DECIDE. **And as of 2026-08-16 the loop feeds itself**: `monitor.rs` (MON-9) owns the detection chain per sustain and drives OBSERVE, so a state update reaches a surfaced decision without anyone stepping the machine by hand. **And as of CTL-5 the cycle reaches every level**: a breach at a nano-sustain climbs the holarchy until it finds a level that can act. What remains is the belief tracker, the preattentive encoder, the IoT bridge and panels — not the mathematics, no longer the loop, no longer its input, and no longer its reach.

| # | Gap | Article | Where |
|---|---|---|---|
| ✅ T1 | ~~**Tenet optimisation engine (§4)**~~ — **MATHEMATICS COMPLETE in Rust across three slices** (2026-08-16). `tenet.rs`: `T:S×A→Δ(S)`, the inversion point, **backward induction** → `J` + a complete policy, and the **CTL-12 join** (the `H=1` sweep IS the Controller's greedy Lyapunov step, checked by a vector). `ensemble.rs`: **scenario ensembles, invariant actions, decision nodes, dead drops** — the sweep run unmodified once per declared future, read for its disagreements. `pincer.rs`: **signal functions over the trajectory + the temporal pincer** — forward on a sampled `T`, backward on a declared cadence or a fired signal, consuming the Signal primitive so a persistent warning is debounced rather than re-triggering forever. **Only TEN-11 (pawa-efficiency) remains, and it is parked on the economy rather than on Tenet.** Python unchanged. | Tenet / Temporal Decision Architecture | Python |
| ✅ G1 | ~~**Controller decision-math (§3)**~~ — **the math, the loop AND the escalation are DONE in Rust** (`controller.rs` 2026-08-12; `ooda.rs`, `holarchy.rs` 2026-08-16): Lyapunov distance-to-V urgency, `should_surface`, `is_stable_intervention`, the Sheridan dispatch; **CTL-3's OODA machine** assembling OBSERVE→ORIENT→DECIDE→ACT with §III's separation made structural and no method that mints a token; and **CTL-5's holarchic escalation**, walking the `MonitorEngine`'s parent chain until a level can act — *"the escalation path IS the holarchy"*, so no escalation graph was introduced. **Still open: the IoT bridge (CTL-6), persistent panels (CTL-8), damping (CTL-11)** — none of which changes the loop or the escalation. Python unchanged. | Controller / The Expanse | Python |

### 🟡 Confirm coverage (built in Python — verify the Rust plan carries them, core vs app-layer)

- **Curated UI `compose(r)` / attention budget (§4H)** — built in Python (ORCHIE).
- **Ingest transducer / parse-rules (§4K)** — built in Python.

**Caveat (honest):** T1 and G1 were never in the walking skeleton either — not built in Python, not "dropped" by Rust; they are the spec's *next depth*. The risk is only that treating this backlog as "the whole system" would silently omit the simulate-optimise (Tenet) and govern-decide (Controller) depth the master spec puts at the centre.

### ✅ N1 — the approval token — SHIPPED IN RUST (2026-08-12)

`sustena-core/src/approval.rs`, grounded in **Operative §XVI** and **Capstone §VI.1 duty 3**.

The gate clause is now the article's, in full:

```text
admit(o,s) ⟺ g_o(s) ∧ o(s)∈A ∧ D(s,o(s))
             ∧ (effect_class = sandbox ∨ valid_token(approve(o, principal)))
```

Three things are **unrepresentable** rather than guarded against:

- **A live effect with no approval.** `EffectClass::Live` *carries* the token —
  the variant that would spell "live, unapproved" does not exist.
- **An approval that skipped simulation or the vote.** `ApprovalToken` has
  private fields and no public constructor; the only route is
  `Simulated → Voted → approve()`, each stage consuming the previous **by
  value**. That is OPV-31's trace invariant enforced by move semantics rather
  than by a runtime scan something could forget to run.
- **A token matching the wrong act.** The binding is kept structurally
  (operator + the real parameters, compared by equality), not as a digest, so
  there is no collision surface to find.

**Divergence, recorded not silent:** Python has no approval token
(`approval_token` / `valid_token` / `effect_class` are grep-0, re-confirmed).
`conformance/vectors/approval.json` carries an explicit `rust-ahead-of-python`
block, and a test asserts it still exists. These become parity vectors unchanged
if the reference catches up.

**Left open, deliberately, and named:** `execute_admitted` now takes nine
arguments. The fix is a context struct carrying registry/allowed/enforcement —
a real API refactor touching every call site, and not something to do as a side
effect of this slice.

### ✅ EDIT-12 — μ + Expand–Migrate–Contract — SHIPPED IN RUST (2026-08-12)

`sustena-core/src/migrate.rs`, grounded in **Editing §VIII**. **This completes
the engine half of M-EDIT.**

**μ finally has a representation.** Until now the classification was binary —
*compatible*, or *refused* — because "migratable" needs a migration function to
represent it with, and there was none. `Migration::Apply(Mu)` makes `Safe(e, μ)`
judge **μ(sᵢ)** rather than `sᵢ`, which is the whole difference between refusing
an edit and carrying the instances into it. A vector shows the same edit against
the same instances: refused under `μ = id`, admitted under a declared μ.

**The ordering is the safety property, so it is one call.** `Emc::run` executes
expand → migrate → contract; there is **no public way to run the contract
alone**. `D†` is committed as a real version node with both shapes present, so
the transition period is inspectable rather than a moment nobody can see, and the
one destructive step is sequenced last.

- **`e₊` is auto-safe with no scan** — and that required sharpening EDIT-6:
  `can_strand()` now names exactly `AddInv`, `ModifyInv`, `RetypeDim`, because
  `Safe` evaluates the candidate's invariants against live states, so an edit
  strands only if it adds or tightens an invariant.
- **`Safe(e₋, id)` is verified, not trusted.** The article says it holds by
  construction; if μ was wrong, saying so at the contract beats discovering it
  later.

**Compatibility is told at author time.** `Emc::plan` computes both directions
before anything runs — *forward* compatibility is required **because rollback
exists**, and an edit that is only backward-compatible is one-way. *"This commits
you"* is a fact the author is entitled to, not a discovery made during a
rollback.

**Knock-on to EDIT-7:** a μ that journals its pre-image collapses the `Partial`
rollback region to `Total`. `Emc::run` does it automatically — a bare `RetireDim`
of a genesis dimension rolls back `Partial`; the same reshape through EMC rolls
back `Total`.

**A correction found by testing:** `rollback` only consulted the *parent's*
journal, so a pre-image written on the node that did the erasing — where an
author would naturally put it, and where `commit_with_pre_image` puts it — was
not read. Fixed to check the erasing node's own journal first; the EDIT-7 unit
test that documented the old limitation was corrected rather than worked around.

**Divergence — rust-ahead, with the counterweight.** The reference has no μ, no
transition period, and names neither direction of compatibility. But its
refuse-if-unsafe **is** a real safety property and is at parity in shape with
`Safe(e, id)`. What it lacks is the escape: it can say no, and cannot offer a way
through.

### ✅ EDIT-7 / #26 / #27 / N3 — version history + the algebra of rollback — SHIPPED IN RUST (2026-08-12)

`sustena-core/src/version.rs`, grounded in **Editing §V**. **This closes N3**,
recorded as the sharpest remaining gap.

**The fix.** The reference engine runs `UPDATE sustain_templates SET spec_json =
?, version = ?` — an in-place overwrite that destroys the predecessor. The
counter increments and nothing is versioned, so there is no `D_{n-1}` and
therefore no `e⁻¹` to apply. Here `VersionDag::commit` **appends**, `head` is a
**pointer rather than a counter**, and each node records the full
`⟨D, parent, e, μ, author, t⟩`. Two edits can branch from one parent — which an
integer cannot express, and which is the structural reason the article says the
version field becomes a head pointer.

**Two inverses, and only one is easy.**

- `e⁻¹(e(D)) = D` — the document. Derived over the EDIT-8 taxonomy against the
  **parent** node (undoing a `DropInv` needs the expression that was dropped, and
  only the pre-state has it). Round-trips are applied and compared, not asserted.
- `μ⁻¹(μ(s)) = s` — the instances. **Not generally available**, because μ is
  frequently not injective (Fagin 2007).

**Three-valued rollback, and the lie made unspellable.** `Rollback::Total` has
**no field** for unrestorable dimensions, and `Rollback::classify` is the only
constructor — it returns `Partial` whenever Δ is non-empty. *Reporting Partial as
Total is the same class of lie as a clamp reported as an admit*, and here it is
not a mistake to guard against but a thing that cannot be written down.

**Both pre-image escapes work.** Free: **fold the log** — the `AddDim` that
introduced a dimension is already in the history, so `μ⁻¹` becomes a lookup at no
storage cost. Paid: **journal it explicitly**, which upgrades an otherwise-lossy
edit from `Partial` to `Total`. A dimension present since genesis has neither, and
that is an honest Δ rather than a contrived one.

**A groupoid, not a group.** `path_between` returns `None` for two versions on
branches that never meet, rather than inventing a route; each definition is its
own identity; and a composed path is **no more restorable than its worst link**.

**Divergence — rust-ahead, with the breadcrumb noted.** Python has no history
table, no parent pointer and no rollback of definitions at all. It *does* bump a
`version` integer and stamp `updated_at`, which tells you *that* something changed
— an audit breadcrumb, not a history. The μ⁻¹ entry also records that **neither**
engine can restore what was genuinely forgotten; the difference is that this one
says so, with Δ naming the dimensions. Tests assert both notes stay.

### ✅ #16 — checked composition of Enzyme pathways — SHIPPED IN RUST (2026-08-12)

`sustena-core/src/compose.rs`, grounded in **Operator §IV** (Hoare sequencing).

```text
post(A) ⊨ guard(B)                       when two Enzymes may chain at all
g_{A;B} = g_A ∧ wp(e_A, g_B)             guard pulled back through A's effect
e_{A;B} = e_B ∘ e_A                      function composition (associative)
ε_{A;B} = ε_A · ε_B                      concatenation in the free monoid E*
```

**The point: illegal-chain discovery moves from RUN time to WRITE time.** Python
chains dynamically, so a bad chain is found by running it and watching step three
refuse. And the property is **structural**, not a validation pass:
`compose(A,B) -> Result<Composed, Rejected>` means a refuted chain yields no
composite value, and `Pathway::try_chain` has **no constructor that takes steps on
trust** — a broken recipe cannot be *held*, not merely rejected on save.

**Three-valued ⊨, stated as such.** `Refuted` is the strictly stronger claim that
`post ∧ guard` is unsatisfiable — not "we failed to prove it", which is
`Undecided`. That distinction is what licenses refusing to build a chain rather
than warning about it, and a boundary pair of vectors pins it: `x <= 0` then
`x >= 0` touches at a point and is **undecided**; `x < 0` then `x >= 0` is
**refuted**.

**Decidable fragment:** conjunctions of path-vs-literal interval constraints.
Everything else — OR, NOT, quantifiers, `!=`, params — returns `Undecided` with
`runtime_guard_required` and a reason. **No SMT solver** was pulled in; a
dependency-free core is what lets this crate ship to a phone.

**`wp`, and the honest limit.** Effects are Rust functions, so `wp` is computable
only where an operator declares an `EffectSummary`: unchanged path (guard passes
through), `SetTo` (constant-folds), `ShiftBy` (moves the literal, staying in the
fragment). Where none is declared, `wp` is **not computed** and the composite
carries the runtime obligation instead of implying it was discharged.

**Divergence — rust-ahead, with the counterweight.** Python has no compose-time
check at all. But `OperativeGraph` *does* validate at load time that every node
names a registered operator — real, and worth not erasing: it checks a step
**exists**, not that it can **follow**. Both notes are asserted by a test.

**Partially advances #16's neighbour, OP-3:** `wp` now exists, but using it as an
author-time check on a *single* Enzyme's `g ⟹ wp(e,Q)` needs an `EffectSummary`
on `OperatorMeta` — a separate change to every operator declaration.

### ✅ CON-1 / CON-7 — transition constraints `D(s,s')` + conservation — SHIPPED IN RUST (2026-08-12)

`sustena-core/src/transition.rs`, grounded in **Constraint §I** (three predicate
families) and **§VI** (conservation).

The gate's **third conjunct**, and the last of the three to be built:

```text
admit(o,s) ⟺ g_o(s) ∧ o(s)∈A ∧ D(s, o(s))
```

`D` is a predicate on the ordered **pair**, so it can say things no state
constraint can. All three canonical shapes ship: **rate limit**, **monotonicity**,
**conservation**.

**"Money is conserved" is expressible for the first time.** A vector proves the
point directly: a step that credits a pocket with no matching debit produces an
after-state that is *individually valid* — every balance non-negative, every
pocket well-formed — and commits happily with `D` absent. Only the step is
wrong, and only `D` can see it.

Two things are **unrepresentable** rather than checked:

- **A clamped conservation law.** `TransitionRule` has **no strategy field at
  all**. Capping a transfer's outflow while leaving the inflow untouched creates
  money — it satisfies `C` by violating `D` — and the way to prevent that is to
  leave nowhere to write the clamp down.
- **Money summed in floating point.** `Tolerance::Exact` sums as `i128` over
  whole minor units, so tolerance 0 is genuinely exact.

**One correction found by testing, not inspection:** exact mode initially
refused `95000.0`, which would have made the law undeclarable over any real
ledger — the engine's arithmetic promotes to float (Python's rule, preserved in
R1). It now accepts an **integral** float (same count of minor units, differently
written) and still refuses a **fractional** one, which is the property that
actually matters.

**Divergence — rust-ahead, with the honest counterweight.** Python has no `D`.
But it *does* give `holon.transfer` transactional **atomicity**, which is not
nothing — and is also not conservation: it never checks the debit and credit are
equal, and it is hardcoded into one operator rather than declared over every
step. `constraints.json` records both halves so the gap is neither overstated
nor understated.

**Also closed:** CON-2's third conjunct. The §4E seam's **duty 2** now covers
`D` as well as the invariants.

### ✅ EDIT-8 — edit authority, the meta-gate — SHIPPED IN RUST (2026-08-12)

`sustena-core/src/editing.rs`, grounded in **Editing §VI** and **Capstone §VI.1
duty 5**. Built on the N1 token shipped the same day.

```text
admit(e, ⟨D,s⟩) ⟺ tok(α,e) ∧ ⊢e(D) ok ∧ Safe(e,μ) ∧ applicable(e,D)
```

The meta-Sustain proposition is the point: Σ↑ has state 𝒟, Enzymes the edit
taxonomy, and `V↑ = {D' : ⊢D' ok ∧ ∀i: μ(sᵢ) ∈ V_D'}`. `admit(e,·)` **is**
§4E's gate on Σ↑, so §4E's induction applies unchanged — a conformance vector
walks it rather than asserting it.

**Authority does not lift from below**, structurally:

- `EditEffect` has **no `Unchecked` variant**. The operator gate needed one for
  R1 parity; the meta-gate is new, so no bypass had to be preserved and none
  exists.
- `EditToken::mint` **refuses** a `Governance::Shared` definition from any
  single actor. The only other route is a `CouncilMint` — private fields,
  constructible only from a decision that **passed AND met quorum**.
- *Permission to spend from a pocket is not permission to redefine what a
  pocket is* — a compile-time fact, not a convention.

**The M-MUL composition point, made honest:** quorum *arithmetic* (who counted,
against what threshold) belongs to Multiparty and is unbuilt. `CouncilMint`
takes `quorum_met` and **refuses when false**, so the dependency is an
obligation the host cannot skip silently rather than a check this module
pretends to perform.

**Also closed by this slice:** EDIT-2 (the typed taxonomy), EDIT-3 (μ = id),
EDIT-4 (fail-safe on an uncompilable candidate), EDIT-6 (loosening skips the
scan — the concrete payoff of a typed `e`), EDIT-10 (whole-definition typing).

**Scope, named:** `Safe(e, μ)` is built for **μ = id** only. A real migration
function has no representation anywhere — that is **EDIT-12**
(expand–migrate–contract), and `Migration` names it rather than stubbing it.
`ModifyOp` and the peripheral family are absent because guards and effects are
Rust fns in this core, not data.

**Divergence — partial, term by term.** `tok(α,e)` and `applicable(e,D)` are
rust-ahead. **`Safe(e,μ)` is at parity in shape** — the reference genuinely
implements §III, witness set and all — and a test asserts the vector keeps
saying so, because claiming the whole module as rust-ahead would overstate the
gap.

### 🔵 Ten further gaps found by the full article reads — see the WBD

The completed WBD (`SUSTENA_UPGRADE_SPEC.md`) records **N1–N10**, found by reading the
last eight technical articles in full. They are not duplicated here; the WBD's
*"New gaps found by the final eight reads"* table is the live list. The three worth
knowing about before opening the next slice:

- **N1 — the approval token is grep-0.** One object discharges obligations in three
  articles (Operative §XVI, Editing §VI, Capstone §VI.1). The trace invariant is
  prefix-closed, so it can be **structural rather than promised**.
- **N2 — `publish` is a route, not an Enzyme**: a second write path into the Arena.
- **N3 — a definition edit destroys its predecessor.** Sharper than #26: rollback has
  nothing to roll back *to*.

*Last updated: 2026-08-17 — **the spine is assembled and the coordination layer is built end to end.** M-TEN's mathematics completed on **the Signal primitive**; **CTL-3** wired Monitor→Tenet→Controller into one cycle with the human at DECIDE; **MON-9** gave OBSERVE an engine that drives it; **CTL-5** made a violation climb until a level can act; and Multiparty went substrate → **Signal** → **Population** → **Consensus** (closing EDIT-8's `CouncilMint` loop) → **Router** (**completing MUL-15**) → **vector clocks** → **disaggregation**, which closed the aggregation bracket the Population opened: a hysteresis band that turns eight readings into two transitions instead of five, and *departure removes an edge, never a node* asserted rather than described → **division of labour**, which answered the question the coordination layer had been assuming away — *why a node would spend itself* — with `rb > c` enforced as a **refusal** (an unstable split cannot be constructed, only diagnosed), `r` **derived from the composition roll-up**, and a cheaper-covered-redundant split refused with `price_of_stability()` reporting what refusing it cost → **the wider CRDT family**, which closed the last row: G-counter, PN-counter, OR-set and RGA, with §VI's three laws *executed* (every permutation folded, then folded again with every update duplicated, one state out of all of it) and *never erase the node* turned into a **theorem about `max`** rather than a property of how the fold happens to be written. **Monitor ✅ · Tenet ✅ · Controller ✅ — and ★ M-MUL is COMPLETE, every row, the first article module finished end to end.** **★ M-OPV is now OPEN**: `ω` is built with no `S` and no `T` on it, so the sharing constraint is unrepresentable to violate — Prop 1 structural (a move outside `T` is unconstructible), Prop 2 at the type level (`reachable` is never passed a population) — and **`u_i` is a vector that stays one**, with a non-dominated option in a non-convex notch kept by the frontier and selected by **none of 1001 weight vectors**. That unblocked three agent-layer rows at once, and **two of them shipped the same day**: the **Goodhart guard** (OPV-27) — a gate-enforced invariant over `U`, in its own module beside the gate because §XIV's whole point is that the fix leaves the agent layer, with damage to `U` proven invisible to **every** operative and refused only by the gate — and **present-the-frontier** (OPV-29), where no method returns one option, collapsing needs a declared rule and reports what it hid, and the proof ties §XV back to §II by computation: the option 1001 weight vectors deleted is the option a single-winner surface hides. **And the MoE router (OPV-28) followed the same day**, closing §XV's third duty: a declared-weight top-`k` gate whose **sparsity is structural** — the router performs the expensive call, so an unselected operative's path is unreachable rather than merely skipped (a counting closure enters 2 of 4, never the other two) — with the **routing collapse walled off from the surface and the wall proven**, not asserted: the frontier is byte-identical before and after routing. It surfaces a coverage gap rather than routing to the least-bad scorer, and takes the mixture for **selection only**, since averaging expert outputs would be §II's collapse one layer on. **§XV's three duties are now all at least partial.** **And the criticality read (OPV-14) followed**, with the risk arithmetic built as a **type**: there is no method that returns a bare expected cascade size, because §VIII's whole point is that a mean at or above criticality would mislead rather than merely be uncertain — withheld *at* the band too, since being inside it is not knowing which side of `τ ≤ 2` you are on. It is read-only by construction (`&[Event]` in, reading out) and it **corrected three premises by checking them**: the reference has no `causes` column (the causal edge is known at runtime and thrown away — one column, not a redesign); the Monitor keeps no second moment, so detector 3 works over a supplied window; and **this backlog's own prior claim that OPV-14 would finish two duties was wrong** — σ̂ is *one input* to a domain reading. ★★★ **OPV-16 closed them, and §XV's THREE DUTIES ARE NOW ALL BUILT** — Orchie routes sparsely on a domain reading, holds the integrating view including the criticality signal, and presents the frontier rather than a winner. What closed Route specifically: an **indeterminate** `dom(s)` became representable, so a caller with no reading surfaces **disorder** instead of fabricating a regime to route at all. `dom(s)` itself stays **supplied** — §IX assigns the classifier to the Monitor (CAP-13), a separate **layer** rather than an agent-layer gap, and that distinction is what makes `Built` honest. The same slice made **disorder undeclarable by construction** (`Cynefin` has four variants, so there is no fifth to write) and **reconciled coverage to one source of truth** — `mixture`'s copy was removed, not left to drift. ★ **Phase 1 opened 2026-08-17 with SUS-7, the boundary `μ` + the autopoietic closure law** (`boundary.rs`): `B = ⟨scope, μ⟩` replaces the flat `owner_ids` the article names by its symptoms, and the closure law `μ_{o(s)} = μ_s ∧ schema(o(s)) = schema(s)` is enforced **at the gate in both halves** — a registered operator that appends to the roster is refused with state untouched. Boundary change is a **separate higher gate that reuses `editing.rs`'s authority** rather than forking it, so two paths mean **nothing to exempt**. It closes SUS-7, SUS-8 and OP-10 together, and **unblocked the firewall `F`** (CON-1) — **which shipped the same day** (`flow.rs`): all three predicate families now exist, and `F`'s irreducibility to `C` and `D` is **proven rather than asserted** — 500 between two own pockets versus 500 out and 500 back give byte-identical endpoints that **both conserve** under `Tolerance::Exact`, and only `F` separates them. A flow is defined relative to `μ` (the operator declares a movement, `μ` decides whether it crossed), flows are **declared not inferred**, composition is conjunction only, and the clamp trap is honoured by construction — `FlowRule` has no clamp strategy to declare. **And OP-3 closed the Operator layer the same day** (`obligation.rs`): the author-time obligation `g ⟹ wp(e,Q)` on a single Enzyme, reusing `compose.rs`'s `wp` and building no prover. ★ **It FLAGS, it does not block** — §III asks for a diagnostic, so nothing is called from `execute` and a test asserts an unsound operator is flagged at author time **and still runs**, caught dynamically after the fact. Three honest outcomes, and the third is the point: `Unavailable` exists because silently passing an unsummarisable effect is the *silently trusted* the row exists to prevent, and flagging one would report a defect not shown. ★★ **The finding:** every shipped operator is `Unavailable`, because Sustena's effects are parameter-driven — so the static check's reach today is zero operators proven either way, which is precisely why §III calls the dynamic check the honest position. Running it is what demonstrates that. ★ **And Phase 1's last block opened with EVT-5, the two clocks and the skew between them** (`clocks.rs`) — a **different** two-clock split from EVT-8's: that one separated physical from causal time, this separates **when it happened in the world** from **when the system heard about it**. ★★ The load-bearing property is that a **negative** skew survives: §III requires it *recorded and surfaced, never silently clamped*, and the clamp is made **unspellable** rather than merely refused — `Skew::Observed` is signed, `skew_of` is one subtraction that floors nothing, and `clock_findings` takes an **immutable** slice, so the surfacing path has no mutable access to the record it reports on. ★ Three readings, and two are deliberately not numbers: an absent arrival reads `Unknown`, a backfilled row reads `Inferred` (its two clocks read equal, so the arithmetic is 0 and *means nothing*), and `millis()` returns `Option`, so neither can decay into a `0` that passes for a punctual arrival. Skew is unbounded on purpose — no threshold, no rejection, no error type anywhere in the module — so §III's week-long forwarding-macro outage is accepted, a week-late event still orders back into its own past, and **EVT-6 is unblocked without being pre-empted**. ★★ **The divergence is the sharpest one yet, and most of the record is already there:** the reference has a real arrival clock (`ingest_messages.received_at`) and a real source (`source_id`), so the gap is the OTHER clock — and its transducer **captures the SMS's own date and time in 13 patterns and discards both one line later**, which makes ING-13 a `parsed_fields` change rather than a parser one. `trust`/`provenance` also already ship, on `ParseRule` rather than on the event: unapplied, not missing. ★ **Reconciling before building corrected three of this tracker's own claims:** `causes` and `provenance` were listed pending and were already present (re-read, not re-built), and `payload` was listed as present and **is not** — it lives on `EmittedEvent` and never reaches the durable record, so **EVT-1 stays 🔨 at six of seven fields** with that residual named rather than closed on paper. ★★ **And EVT-6 followed the same day — the row the whole substrate had been pointing at** (`watermark.rs`). *No window closes by wall-clock hope* is now **structural**: `close` takes a `&Watermark` and nothing else, there is no `close_at(tau)` and no `From<i64>`, so a caller holding only a processing clock cannot ask the question. §4J.3's failure is exactly `W(τ) = τ`, and it stays **possible but stops being accidental** — it requires declaring `SourceGuarantee { max_out_of_orderness: 0 }`, the claim *this source never delivers out of order*, in as many words. A case runs that zero bound and an honest 2h bound at the same τ over the same window: one closes, the other does not, and **the difference is entirely a declaration someone had to write down.** ★ Monotonicity is structural and **deliberately distinguished from EVT-5's clamp rule** — flooring a measurement hides a fact; non-decreasing *is* the definition of a watermark, a retreat would un-close a window that had already answered, and the rejected proposal is reported rather than discarded. ★ The heuristic is **estimated from EVT-5's surfaced skew** rather than invented, honouring its three readings — `Unknown` and `Inferred` are not zero skew, and with none observed the estimator **errors rather than returning a zero bound**, because *an unmeasured source is not a punctual one*. ★★ And **no volume of data promotes an estimate to a guarantee**: heavy-tailed skew means the largest ever seen is not a bound on the next, at 3 samples or 500. That is what keeps the **lateness policy mandatory** — `Lateness` implements no `Default`, so the compiler stops a window existing without one, and each cost is real in the type: only `AccumulateAndRetract` retains its emitted value, so `Drop` cannot retract even by mistake, while a dropped fact still lands in the late log. ★ **The divergence is a specific failure rather than an absence, with a real credit attached:** the reference has three genuine windows (`calendar.upcoming`, `tasks.due_soon`, `protege.urgent_window_hours`) and every one closes on `ctx.timestamp` — but that clock is **injected, not ambient**, so EVT-13's purity holds and the defect is the value assumed rather than an ambient read. A second correction to this tracker: **MON-4's row names `SlidingWindowAggregator` as if it ships and it is grep-0** — the row was describing the article, not code. ★★ **And EVT-11's mechanism landed the same day, completing §VIII's *same object seen twice* in one place** (`period.rs`): `Period` **wraps EVT-6's `Window`** rather than inventing a second interval type, so the half that says *which events belong* and the half that says *when we may answer* now meet, and EVT-6's mandatory `Lateness` rides through from the rule. ★★ The partition is **structural** — the expansion emits `[occ[i], occ[i+1])`, so each period's end IS the next one's start and a gap or overlap is unrepresentable — which buys Dijkstra's point (EWD831) exactly where roll-up needs it: **a year is the sum of its twelve months, not the months plus twelve boundary instants**, asserted both by summing durations against the span and by checking every boundary lands in exactly one month. ★★ **Store the rule, recompute the expansion** is a property of the type: `Recurrence` holds no instants, has no constructor accepting any, and the reason is *run* rather than described — **one rule under two tzdata releases yields two different instants with an identical local time**, so a persisted expansion would keep the older answer with nothing to detect it. That is exactly what `state.calendar.events` does in the reference today. ★★ **Zone id over bare offset**, with `Anchor::FixedOffset` kept as a labelled lesser thing — real data does arrive carrying only an offset — that consults no tz data and carries `tzdata_version: None`; its trap is demonstrated rather than warned about, since it agrees with the zone today and diverges under the revised release. ★ **The tz data is INJECTED, not bundled**, and §VIII's own argument is the reason rather than the ADR alone: a bundled database pins one release *invisibly*, hiding the very versioning the section warns about. The line is drawn where the versioning is — civil-calendar arithmetic is a fixed algorithm and is computed (Hinnant, round-trip tested); timezone offsets are versioned data and are only ever asked for. **`Cargo.toml` is unchanged.** ★ DST is exercised by a Europe/Berlin counterparty because Nairobi's case is genuinely vacuous: a real **23-hour** and **25-hour** day, both still partitioning, with a nonexistent local start refused and an ambiguous one resolved by a **declared** `DstPolicy` that implements no `Default`. ★ **The honest residual, kept 🔨:** the rest of the RRULE grammar (`COUNT`/`UNTIL`/`BYSETPOS`/`YEARLY`/`BYMONTH`/`EXDATE`/multi-value BY- lists), an `RRULE:` text parser, and migrating a real consumer onto the primitive — it is ready rather than in use. ★★ **And EVT-12 closed the same day** (`checkpoint.rs`): a checkpoint `(s_k, k)` out of §II's homomorphism, verified at **every** k rather than one convenient one, with `events_applied()` making the `O(n−k)` claim **countable** rather than asserted. ★★ Both of §IX's properties are **structural**: *always discardable* because `replay` takes an `Option<&Checkpoint>` and the fast and from-scratch paths are the **same function**; and *derived, never authored* because `Checkpoint::at` folds the log and there is **no constructor taking a state**. ★★ And **replay cannot reach an effect channel** — `&[Event]` in, `Value` out, no sink, no registry, no `execute` — so §IX's `ASSERT no_external_effects_were_issued()` is a fact about the signature rather than a check at the end; the contrast is the proof, since an operator body receives `&mut Vec<EmittedEvent>` precisely because emitting is what it is for. ★★ **It also found a real gap in §IX's own pseudocode:** `seen = set()` is per call, so ids already folded into the checkpoint never enter it, and a **late duplicate of an early event is skipped from scratch and applied from a checkpoint** — 380 versus 100, demonstrated by running the printed pseudocode literally. That quietly breaks the same section's discardability claim. Carrying the folded id set would make the checkpoint grow with `k`, which is most of what it was for, so the fix is taken where §V already puts dedupe, and `replay` **refuses** an un-deduped log rather than silently picking one of the two wrong answers. ★ **The counterweight is generous and exact:** the reference **already runs the homomorphism** — `sustain_states` is advanced one call at a time rather than re-folded, which is `fold(s_k, L[k+1..n])` at `k = n−1` — and its discardability is real and **tested**, since `rebuild_state()` regenerates the cache from the log and the law is asserted in its own suite. The genuine difference is small and sharp: `_persist_state()` makes a wrong cache **writable** and forbids it by docstring and project rule, where `Checkpoint::at` makes it unwritable. The `snapshot` grep (89 hits) is named as the false positive most likely to mislead — almost all of it is `StateAccessor.snapshot()`, a read, not a persisted `(s_k, k)`. ★★★ **And EVT-10 closed the block the same day** (`dimension.rs`) — the module's centrepiece stops being remembered and starts being checked. ★★ **Delta-accumulation on a snapshot dimension is a genuine TYPE ERROR, proven by a `compile_fail` doctest** that `cargo test --doc` executes: `SnapshotDim` has no `add`, so calling one on a balance read off an SMS does not build. The mistake is **unspellable**, not caught. ★ And the honest completion, because a kind declared in a *spec* with an update off a *wire* cannot be a Rust type error: `DimensionSchema::check` refuses it there too, with the reason travelling along. Both halves are needed, and claiming the compile-time one covered the other would have been the dishonest version. ★★ ***Never accumulate deltas* is derived and MEASURED**, not decreed: non-idempotence run as arithmetic (`x+2δ ≠ x+δ`), and made safe an accumulation **is** the grow-only counter — a redelivered delta leaves the state byte-identical, at an applied-id map of 200 entries after 200 deltas against LWW's constant **1** after 500 readings. §VII's cost argument as two numbers. ★★ **§VII's emphatic limit is structural:** three distinct kinds, `Contested` refusing automatic apply **entirely** and backed by `crdt.rs`'s OR-set, so the same two concurrent edits keep **both** as contested and exactly **one** as snapshot — **LWW cannot eat a contested edit**. ★ **The counterweight is the strongest in the whole set, and it belongs to the reference:** §VII's own field note records that the LWW-for-snapshot behaviour shipped in VOS **first**, derived empirically after out-of-order SMS clobbered a balance and before it had a name — *"the convergence theorem is not being proposed to VOS; it is being written down about VOS."* This row brought no behaviour; it turned a hard-won rule into a checked one. A second credit worth naming: `balance_after` is **already parsed out of every M-Pesa SMS** there and discarded, so the data for the snapshot kind is arriving and only the declaration is missing. ★ **Reconciled, not forked:** `vclock::Dimension` gained its third variant rather than a parallel enum appearing beside it — which forced `MergeResolution::resolve` open and turned a bare `Vec` meaning two things into a typed `Resolved::{Superseded, BothCount, NeedsReconciliation}`, three call sites corrected in place. The `(τ,id)` tie-break and the law checkers were reused, not re-derived.

★★★ **PHASE 1's ATTACK LIST IS COMPLETE.** Boundary (SUS-7/SUS-8/OP-10), firewall (CON-1/CON-2/IMM-1), Operator (OP-3/OP-5) and the whole Events & Time substrate (EVT-5/6/11/12/10) all closed on 2026-08-17. What remains in that layer is **declared residual rather than unstarted work**: EVT-11's RRULE-grammar remainder and EVT-10's DSL authoring surface (both Phase 4 / M-DSL), EVT-1's `payload`, EVT-14 effect journaling, and ~~**EVT-15 replay-under-`D′`, which blocks M-TEN and M-EDIT**~~ — ✅ **EVT-15 closed 2026-08-17 as Phase 3's opener**, and **EDIT-11 is unblocked** (see the Phase 3 paragraph below). **→ Phase 2 opened the same day, on MON-8** (`belief.rs`). ★★ The decisive move was declining §VIII's pseudocode where it calls `kalman_filter.update(observation)` **by name**: MON-2 stays the declined import, and the surviving `point_mass` is served by **EVT-10's snapshot collapse** — `Belief` *holds* a `SnapshotDim` and collapses through it, so *an observation arrived* means one thing in this codebase rather than two, and **a stale reading arriving late does not collapse the belief**. ★★ Silence widens monotonically (`σ = elapsed × DRIFT_RATE`, asserted across 24 consecutive hours) **without inventing dynamics** — `Dynamics::Unknown` holds the mean and grows only the band, since filling `predict_forward`'s shape with a fabricated model would put a made-up trajectory behind a real-looking mean. ★ The `SENSOR_SILENT` alert **reuses the boundary without reusing the shape** — it rides `Severity::escalates()` but is deliberately not a `detect::Alert`, because that type carries a `Shift` and nothing shifted; it is *we have lost sight of this*, not *this moved*, and it can never be `Info`. ★ The counterweight is real and correctly scoped: `ingest_engine`'s `is_stale` already flags a quiet source against a **declared** cadence and never guesses one — so the gap is precisely three things, of which only the third is new: **per-dimension** rather than per-source, **continuous** rather than binary, and **the estimate degrades** rather than a flag flipping. ★ Two limits worth carrying: the point-mass collapse **assumes the observation is authoritative**, which for a noisy sensor is a modelling assumption made and not checked — and modelling measurement noise is exactly where Kalman would come in, so the trap and the limit are the same decision seen twice; and the tracker is **not yet wired into `MonitorEngine`**.

★★ **MON-13 followed the same day** (`harmonics.rs`), and it **corrected the row that sent it there**: the row said harmonics *consumes `signal.rs`*, and it cannot — `signal.rs` is the excitable-medium primitive, a wave across a **graph of nodes**, with no time series in it. The Additions paragraph runs the harmonics sentence and a Signal **cross-reference** together, and the row read the second as a dependency of the first. What this reads is the `W = d(s,V)` window `detect.rs` already runs over, **supplied** rather than re-windowed — the same discipline `criticality.rs` follows. ★★ **The payoff is proven, not argued:** a conformance case runs the **real `Cusum`** over the on-schedule monthly bill and asserts it **fires**, while the frequency-domain reading calls the identical series an `ExpectedCycle`. *Complement, not veto.* The same magnitude **off-cycle** surfaces as a `GenuineShift`, and a **new rhythm on top of the known one** still surfaces — so suppression is not a blanket mute. ★★ **A cycle must be earned twice over:** **authorised** (declared — an EVT-11 monthly period is exactly this — or established through enough observed cycles) **and resolvable** (the window must carry two full periods). ★ Declaration authorises a cycle; **it does not make a short window able to measure it**, and subtracting energy at an unresolvable bin would remove a real shift under the name of a season. Both failures give `Indeterminate`, the same third-answer family as `Skew::Unknown` and `Soundness::Unavailable`. ★★ **And testing forced a correction inspection would not have:** attributing one bin per cycle reported an **on-schedule bill as a shift**, because a bill is a periodic **impulse** and an impulse train puts energy at every multiple of its fundamental — which is what the row is *named* for. Fixed by attributing the harmonic series, **with the cost named where it lives**: a genuine shift landing exactly on a harmonic of a known cycle is absorbed with it, inherent rather than a defect. ★ Counterweight: the **substrate is at parity** (`detect.rs` + `MonitorEngine`'s histories) and the **need is real there today** — `budget.allocate` carries `period: 'monthly'` and `record_income` a `frequency: 'monthly'`, so the household's rhythms are already named in the reference while nothing can see them. Direct `O(N²)` DFT, **no dependency**; an FFT is a named scaling slot. ★ **MON-7 moved to Phase 3** — a rendering row whose real consumer is the Curated UI's UI-13.

★★ **MON-4's typology followed the same day** (`windowing.rs`), and the reconciliation *is* the substance: **tumbling is not a third mechanism**. Calendar-aligned tumbling is a **one-line delegate** to EVT-11's `Recurrence::expand` — which matters concretely, since a month is not a fixed number of milliseconds and a duplicate would have had to get calendar length right a second time or quietly get it wrong — and **fixed-duration tumbling is the degenerate sliding case where `step == size`**, proven by `Sliding{DAY, DAY}` and an EVT-11 daily expansion producing **byte-identical** windows. So the genuinely new work was sliding and session. ★★ **Sliding overlaps honestly:** `windows_containing` returns a `Vec` and an event is counted in **each** window containing it, because that is what makes a rolling rate roll; a **`step > size` is refused**, since gaps between windows would silently drop every event that fell in one. ★★ **Session is data-driven:** a session ends **τ after its own last event**, not at the event that revealed the gap, and the last session in a batch is `Open` — *not* an empty session and *not* a closed one. ★★ **And EVT-6's guarantee carries to the one type that could have lost it:** a session's end is unknown in advance, so `close_on` takes a **`&Watermark` and nothing else** — a processing clock two hours past the gap, carrying an honest 90-minute bound, does **not** close it. **A clock is not evidence**, and there is deliberately no *expire after a while* path, which would be the wall clock wearing a different name. ★ **MON-4's own `SlidingWindowAggregator` wording corrected a second time** — grep-0 **on both sides**, and deliberately unbuilt: aggregation is a *consumer* of the typology, not part of it, and building one in would tie a window shape to a particular thing being summed.

★★★ **And MON-1 closed the Monitor block the same day** (`observability.rs`) — as a **declination plus a translation**, which is the honest shape for this row. §I's `rank([C; CA; …; CAⁿ⁻¹]) = n` is a **declined import**, the same position MON-2 takes on Kalman and on MON-11's own grounds — and with a concrete reason rather than only the categorical one: **`CAᵏ` needs a single `A` to take powers of, and there is not one**, because the dynamics here is a *choice* (which operator someone runs) rather than a fixed linear map. Picking an `A` would be a fabrication, and the row's own warning settles it: *a rough answer to a rank question is worse than none.* ★★ **The replacement is term-for-term, not an approximation:** `C` → dimensions a declared source measures; `A` → one operator (reads `X`, writes `Y` ⇒ `X → Y`); `CAᵏ` → a measurement reachable in `k` operator hops; `rank(O)=n` → every governed dimension reaches one. The verdict **names the operator chain** that carries a value, so the claim is checkable rather than trusted, and the search is breadth-first because *which block row* is a question about the smallest `k`. ★★ **Conservative where effects are unsummarised, and it says so in the verdict:** an operator with no `EffectSummary` contributes **no edges**, so a dimension can read unobservable that a summary would have shown observable — the safe direction, and `Unobservable` carries the unsummarised operators with `is_conservative()` to distinguish *unobservable* from *we could not see*. ★ **Over today's registry that is the whole picture** — no shipped operator declares a summary, which is OP-3's finding seen from a second angle, asserted rather than noted. ★ And it completes §I's implication where MON-8 began it, with the distinction kept sharp: `ungoverned()` is **runtime** (*nothing has spoken yet*), MON-1 is **structural** (*nothing ever could*). ★★★ **The Monitor block of Phase 2 is complete** — MON-8, MON-13, MON-4 and MON-1 all closed 2026-08-17, leaving MON-3 (traces; its Logs half already ✅) and MON-7 (moved to Phase 3).

★★★ **And CTL-11 closed Phase 2 the same day** (`damping.rs`). The row's substance is that **CTL-2 has a blind spot the article names**: under `e_{k+1} = (1−g)·e_k`, a gain in `(1,2)` makes `W` fall every step **while the state flips side every step**, and a gain of exactly **2** makes `W` never change — so `W(after) ≤ W(before)` holds **for ever, with equality**, while nothing settles. A conformance case runs **`is_stable_intervention` itself** over both trajectories and asserts `stable` at every step. Lyapunov is not wrong; it is answering a different question, which is exactly what *'Lyapunov says the gap closes; damping says it closes without oscillation'* means. ★★ **Honest fit on MON-1's precedent:** where a gain is **declared** the pole is exact and is computed (`Damped`/`Deadbeat`/`Underdamped`/`Undamped`, with `sustained` marking the pole exactly on the circle) — declining a real model would be as dishonest as fabricating an absent one; and **where there is none, nothing is fitted**, because there is deliberately **no `fit_gain(trajectory)`** and taking poles of a fitted model is the fabricated-`A` trap. The native form reads ringing from the trajectory instead, with `Indeterminate` below three points, and **overshoot** as the one-step form: 50 → 230 on a [100,200] band drops `W` and lands on the far side. ★★ **A finding from testing rather than design:** `W` is a **magnitude**, so a converging-but-ringing loop has a *monotonically falling* `W` — `read_trajectory` calls it `Damped` and is not wrong to; the **side** series is what carries the ringing. Both native readings ship because each sees what the other cannot, and shipping only the magnitude one would have **reproduced Lyapunov's own blind spot**. ★ **A framing correction the greps forced, recorded rather than smoothed:** the obvious counterweight would be *the time-domain half is at parity*, and it is **not** — `lyapunov` is grep-0 on the reference side, and its four `stable` hits are a stable sort, a stable fingerprint and two testables. CTL-2 is a **Rust** row, so the real counterweight is **intra-Rust**: the per-step check already existed here, and CTL-11 adds only the half Lyapunov cannot provide. ★ **Row phasing settled:** **CTL-6 relocated to a host-integration pass** (ADR-0001 keeps I/O out of the core; the core-side half is already the ingest door) and **CTL-8 moved to Phase 3** as a surface row, on the same argument that moved MON-7.

★★★ **PHASE 2 IS COMPLETE.** What remains in the cognition layer is declared residual: MON-3's Metrics/Traces (Logs already ✅), and — ~~the **`MonitorEngine` consolidation wiring**~~ ✅ **done 2026-08-17, closing Phase 2's assembly.** All four residuals discharged at once, **as wiring rather than new mechanism**: every module is the one it already was, reached through `Option` declarations on `SustainWatch` that leave an undeclaring sustain byte-identical. ★★ What runs when is stated rather than implied — harmonics **per tick**, belief **per tick but only through `ingest_at`** (a belief needs an event time and `ingest` has none; no clock in core, so the door is explicit and `ingest` documents that it updates no belief), windowing and observability **on demand**, the latter **structural** over the operator graph. ★★ And the wiring **found a real seam**, which is the argument for consolidating at all: `observation()` gated on the `OodaObservation`'s own `escalates()`, so a detection explained by a known season was suppressed by `Ingested::escalates()` **and still handed to OBSERVE** — two answers disagreeing about one reading. Fixed, with `suppressed_by_cycle()` making the suppression **visible**, since a suppression nobody can see is indistinguishable from a detector that failed. **→ Phase 3 (Surface + Immune) is next**, and has gained MON-7 and CTL-8 on the way. What is left elsewhere: TEN-11's pawa term (parked on the economy), the belief tracker, the preattentive encoder, the IoT bridge, panels and damping.*

★★★ **PHASE 3 OPENED 2026-08-17 ON EVT-15** (`semantic.rs`) — taken first because it was the one remaining item **blocking EDIT-11**. ★★ **The premise was confirmed in the code before anything was built on it, and it held in all three parts — with one correction.** (1) The patch fold really is `D`-independent: `checkpoint::replay` takes a log and a checkpoint and there is **no parameter through which a definition could enter** — a *property*, not a shortfall, and precisely why the semantic layer is built **beside** it rather than into it. (2) ★ The ingredient is **PYTHON-AHEAD**: `operators_log` carries `operator_name` + `input_json` per call, while the **Rust `Event` carries neither and there is no `operators_log` equivalent in this core** — so `EnzymeCall` had to be *defined* here, which is the one place the row's own phrasing (*the ingredients exist*) needed qualifying. (3) ★★ **`D′` needed no new type at all** — `editing::Definition` is already `⟨schema, invariants, operators⟩`, the gate's own three inputs, so replaying *under* `D′` is the same `execute` with a different `Definition` handed to it. ★★ **Same `D` reproduces; `D′` reinterprets.** The consistency check is **asserted, not assumed** — re-running the calls under the original definition lands where the patches did, and without it every `D′` verdict would be a claim about a different history. Under a `D′` that bites, the answer is **partly** inadmissible with the verdict pointed at the specific call: a floor of 1500 leaves the income admissible (1000→1500 clears it **exactly**, the boundary case chosen on purpose) and refuses the allocation that would take it to 1200. ★ **`NotPermitted` is kept separate from `Refused`**, because *the allow-list no longer carries this operator* and *the gate now rejects this state* are different repairs, and collapsing them would send an editor to fix the wrong one. ★★★ **Replay emits nothing, and the guarantee is doubled on purpose.** Structurally, this core has **no sink, no egress and no send** — **EVT-14 is not built in Rust**, stated plainly rather than assumed away, so the §IX assertion is currently a fact about the signature. But a signature-only guarantee evaporates the day an effect layer lands, so it is also **explicit**: `ReplayMode::Replay` **discards and counts** every emission, `suppressed_events` is asserted `> 0` so the guarantee is **exercised rather than vacuous**, and `Live` must be *named* — the same discipline `Authorization::Unchecked` follows. **Nothing here can re-fire a payment or an SMS.** ★ **Purity, stated as the limit it is:** `is_deterministic` runs the calls twice and compares, which **detects non-determinism and does not prove purity** — said out loud rather than left for someone to discover. ★ **Tenet's consumer was already served** by EVT-12 plus the simulator; the genuinely new work is Editing's. ★ **Divergence and counterweight:** `semantic` greps **4** hits on the reference side and **the first of them is the reference naming this very gap about itself**, which is the strongest form the counterweight takes — and the nearest real relative is `check_definition_edit_safety()`, which already asks *would the new definition refuse this?* of **live states**, where EDIT-11 asks it of **histories**. An instance can sit at a perfectly valid state it reached through a step the new definition would have refused; that difference is the whole row. ★ **Residuals, named rather than left open:** the call log is **not wired to `execute`** (nothing yet records an `EnzymeCall` when an operator runs), and `boundary`/`firewall` are not part of a `Definition`, so a `D′` replay checks schema, invariants and the allow-list — not `μ` or `F`. EVT-12's checkpoints deliberately do not transfer, because a checkpoint under `D` is not one under `D′`. **→ The Phase 3 body is next:** the Editing rows (EDIT-13/14/15/16, with **EDIT-11 now sequenceable**) and the Immune rows (IMM-7/10/11/12).*

★★★ **IMM-7 FOLLOWED, and STEP-0 turned the slice around before a line was written** (`capability.rs`). The row named three clauses and **two were already built**: the approval token shipped 2026-08-12 as **OPV-30/31** (`approval.rs` — no public constructor, `EffectClass`, `NonceLedger`, wired into `execute_admitted` with the nonce spent **at commit** so a gate refusal never consumes an approval), and **`permitted(α,o,Σ)` shipped the same day** as `principal.rs` with per-membership-edge privileges, weakest-link `⊕` across the nesting path, and `min_privilege` genuinely read. ★ The row's *status* had said `⬜ not started` for five days after the first of those landed — so the reconcile corrected **the tracker, not the code**, and corrected the brief too, which expected authorization to be the gap. IMM-6's row, by contrast, was already right. ★★ **The genuinely unbuilt clause was the confused deputy** (Hardy 1988), and it is the one that starts mattering the moment Symbionts can write: an operative acts on a person's authority over inputs **it did not choose** — a parsed SMS, an Arena artefact — so ambient authority applies everything that person holds to a name that arrived in untrusted text. The vulnerability is **asserted at the gate** rather than described: the same principal, the same untrusted target and the same operator **commit** under `Authorization::Principal` and are **refused** under `Authorization::Capability`, state byte-identical, with the authorised task still committing in the same test so the fix cannot quietly become an outage. ★★ **Miller-Yee-Shapiro's *no ambient authority* is a property of the type**: the `Capability` variant carries **no `&Memberships`**, so the fallback is *absent*, not merely unused — it cannot be written without changing the enum. ★★ **Three moves are unrepresentable rather than guarded:** redesignating a capability at another object (`Attenuation` carries no sustain — the deputy's own move removed from the vocabulary), widening rights (no such method), and forging one (no public fields, no constructor, **no `Deserialize`** — proven by a `compile_fail` doctest, the EVT-10 idiom). Asking for a stronger **tier** *is* representable, because a caller may sincerely ask, and is **refused and named** (`Amplification::Tier`) rather than silently bounded — clamping would hide that someone asked for more than they had, which is the trap `admission.rs` warns about one layer down. ★ **Issuing is a transfer, never a mint:** it consults a real edge (a stranger gets `NoEdge`) and reuses `effective_privilege_with`, so a capability cannot route around the monotone `⊕` that governs the ambient path — an owner of a habitat inside a household they only observe gets an **observer** capability. ★ **The same slice closed IMM-6's `skins`**, which had shipped as a struct `MembershipEdge` referenced and **nothing read** — *'a field, not a check'*, the exact defect `principal.rs`'s own header criticises `min_privilege` for in the reference, turned back on this codebase. Where an edge carries both a tier and a skin the **weaker wins**, so a bundle is not a promotion route; and an edge naming a skin nothing resolves is **REFUSED**, because falling back to the raw tier is how a typo'd role (`gest` for `guest`) becomes owner-by-accident — IMM-4's rule one layer up. ★★ **The counterweight is two things and the first is real parity:** authentication is the reference's and is good (PBKDF2-HMAC-SHA256, 260k iterations, per-user salt, constant-time, JWT with `token_version` = **real revocation**), which is precisely §IV's point that `auth` and `permitted` are separate conjuncts; and SUS-7's boundary `μ` is at parity on both sides, which is what gives a membership edge something to be an edge **of**. ★ And the reference's one real ownership check is credited exactly: `_assert_owns_sustain()` 404s for both not-found and not-owned so ids cannot be enumerated — careful work, and **single-owner, binary, at the route layer** rather than a conjunct of `admit()`. ★★ **The grep that would most mislead a re-reader is named:** `skin` returns **16 hits and zero real** — every one is the substring in *asking*, the sharpest false positive since CTL-11's `gain`/*again*; and `permitted`'s 11 hits are the **allow-list** (`sustain_engine.py:1618`, *'operator is permitted by this sustain's spec'*), which is §IV's conflation stated out loud in the reference's own comment. ★ **HRU's boundary is honoured rather than fought:** the safety question is undecidable in the general access-matrix model, so this model is four fields and two operations and stays that way — the same conclusion Rice's theorem forced on constraints, reached from another direction. **Residuals, named:** unforgeability is **in-process, not cryptographic** (a wire form needs a MAC and a key → **host-layer slot** under ADR-0001, the CTL-6 relocation — and a slot rather than a gap, because the deputy runs in-process); **this is not revocation** (a capability is valid until dropped, and revoking the underlying edge does not invalidate one already issued); skins bundle a **tier only**; and ★ **nothing issues capabilities to Symbionts yet** — the operative layer still calls with `Unchecked`/`Principal`, so the fix is **available rather than in force**, and threading it through `operative.rs` is the sequenced next step. **→ Phase 3 remaining:** **EDIT-11** (now unblocked, and it consumes EVT-15), **EDIT-13/14/15/16**, **IMM-10/11/12**, and the surface rows (UI-in-Rust, MON-7, CTL-8).*

★★★ **EDIT-11 FOLLOWED IMMEDIATELY, and for the third slice running STEP-0 narrowed the work before any of it was written** (`stranding.rs`). The mechanism was finished **twice over**: `editing::safe` has been the migration predicate over live **states** since 2026-08-12 — judging `μ(sᵢ)` rather than `sᵢ`, reporting a witness set that names instance *and* rule *and* reason, treating an uncompilable candidate invariant as a violation against every instance rather than skipping it, and wired into `admit_edit` as its fourth conjunct — and `semantic::replay_under` had been the reducer over **histories** since that morning. The rest of the engine (taxonomy, authority, version DAG, inverses, three-valued rollback, μ + Expand–Migrate–Contract) was already ✅, exactly as M-EDIT's own summary claimed. ★★ **So what was actually missing is two things and neither is a mechanism:** nothing called `replay_under` from the edit path — the history question was *answerable and unasked* — and `admit_edit` had exactly **two** outcomes, admitted or `WouldStrand`, with `Migration` an **input the caller had already chosen** rather than a **reported outcome**. Of §III's three classifications the engine could express *compatible* and *rejected* and had **no surface at all for the choice between them**. ★★ **The crux is now a running test rather than a note in two vectors:** `D′` adds `balance >= 500`; an instance recorded 300, then 700, and sits at **1000**. `safe` passes it — 1000 clears the floor. `replay_under` refuses call `c1`, because the balance was **300** the moment that call committed. Same instance, same `D′`, two correct answers to two different questions, kept in **separate lists and never summed**, because a stranded state wants a migration and a stranded history wants a policy decision about the past. ★★★ **And the distinction recurs in the REMEDIES, which is the stronger statement — migration cannot fix a history.** μ transforms where an instance *is*; it cannot reach back and make a past call admissible, so `Remedy::Migrate` carries the whole history-stranded list as residue even when it clears the states completely, and `leaves_residue()` says so. A surface that let `Migrate` read as *fixed* there would quietly lie about what the migration did. ★★ **Grandfathering is withheld while the state itself is stranded, and that is a soundness argument rather than a policy one:** scoping `D′` to new actions cannot help an instance already sitting somewhere `D′` forbids, because the very next call refuses regardless — so there, `Refuse` is the **only** option, which is precisely the case a silent default would have papered over. ★ **No remedy is recommended, structurally:** `remedies()` is the only accessor, it returns a `Vec`, there is no `best()` and no ordering that reads as a ranking, and `Remedy` implements no `Default` — the OPV-29 frontier discipline (no method returns one option) applied to editing, because which outcome is right is a policy question about a particular household and a library that picked one would be deciding it invisibly. ★★ **The counterweight belongs to the reference and it is unusually strong.** `check_definition_edit_safety()` is real, careful work: fail-closed (an uncompilable candidate invariant is a violation against every instance, not a skip), run **before** persistence, and it invalidates the per-instance spec cache on success — a fix made because an edit that silently failed to govern already-instantiated sustains is exactly the quiet non-application that discipline exists to rule out. Its `blocked_by` already carries a real **witness set** naming instance, invariant and reason, so ★ **the reference had the witness-set discipline before Rust did**, and `Stranded` is that idea typed. ★★★ And it **names this exact gap about itself**, in its own words: *"Only 'refuse' is built... there is no tool to transform a stranded instance's state to fit a new schema — the safety check can only say no, not offer a guided fix."* Same shape as EVT-15's counterweight — a sequenced, disclosed debt rather than an oversight. ★ **A false positive worth naming for being the inverse of one:** `check_definition_edit_safety` greps **2 hits and both are real** (the definition and its single call site) — a reviewer might read the low count as a stub and be exactly wrong. `grandfather`, `would_strand` and `migrate_instance` are **grep-0**, recorded as checked. **Residuals:** the remedies are **offered, not applied** — threading a chosen one back through `admit_edit`, and giving the engine somewhere durable to remember that a history was grandfathered, is host state and the sequenced next step; **grandfathering has no enforcement arm** (per-instance effective-definition pointers are host state); **nothing verifies that a supplied history is the instance's own**, which is EVT-15's *call log not wired to `execute`* seen from the consumer side; and ★★ the history check **cannot see `μ` or `F`**, because boundary and firewall are not part of a `Definition` — EVT-15's limit, carried unchanged rather than quietly dropped. ★★★ **With EDIT-11 closed, only EDIT-13 (`π : D → G`, a surface projection) remains in M-EDIT — the ENGINE half is complete with nothing outstanding.** **→ Phase 3 remaining:** EDIT-13, IMM-10/11/12, and the surface rows (UI-in-Rust, MON-7, CTL-8).*

★★★ **IMM-10 CLOSED M-IMM'S ENGINE HALF** (`learned.rs`), and STEP-0 settled the scope before anything was designed — for the fourth slice running, the reconcile changed what got built. ★★ **The ParseRule system is PYTHON-ONLY**: `ParseRule`, `parse_rule`, `transducer`, `OTP` and `R_fixed` are **grep-0** in this core. So the Rust work is not to port a subsystem across in order to have somewhere to hang a least-privilege bound — it is the **generic mechanism §V is really about**, of which a ParseRule is one instance: the fixed/learned split, the ordering, and the bound. The regex library, the seed set and the adoption verifier stay app-layer, said out loud rather than quietly imported. ★★ **`R_fixed` stays a fixed point, structurally rather than by enforcement:** `Edit::apply` is `(&Definition) -> Definition`, acting on `𝒮`/`Inv`/`T`, with **no parameter through which a rule set could enter and no return through which one could leave** — the same shape as EVT-15's finding about the patch fold — and a `FixedRule` is not part of a `Definition`, so `∀e, ∀r ∈ R_fixed: e(r) = r`, **exercised over all eight edit kinds** and compared byte-for-byte rather than rested on the signature argument. ★ A fixed rule also carries **no operator and no capability**: a pre-gate refuses, it does not act, so asking for one is an `Err` rather than an empty capability — which would read as *bounded to nothing* where the truth is *not applicable*. Ordered-before is structural too: `screen` is the only entry point, the fixed tier runs first, and the order is **not a parameter**, so a learned rule cannot see an OTP before the secret filter did. ★★ **Gap 1 — trust became a real decision input, and §V's finding was VERIFIED rather than quoted:** across **47** `trust` hits in the reference, **not one** is a comparison, a branch, a sort key or an ordering; filtering for `if`/`==`/`sort`/`key=` leaves a single hit and it is the `<...>` in a docstring listing the tuple's fields. `TrustPolicy` is the evaluator §V said was missing, and it drives **two** decisions — **precedence** (a `Shipped` rule wins a match over a `ProposedConfirmed` one even when declared second) and **privilege** (machine-synthesised-then-clicked gets a strictly weaker capability than authored-and-reviewed, because confirming is cheaper than authoring). ★ Both derive from **one** declared mapping, so they cannot drift into two orderings that disagree, and `TrustPolicy` implements **no `Default`** — what a trust level ought to reach is a deployment's policy, and a library that invented one would be deciding it invisibly (the same reason `Lateness` has none). ★★★ **Gap 2 — least privilege on the learned tier, the keystone, and it CONSUMED IMM-7 exactly as the row anticipated:** a learned rule derived from untrusted ingest maps text to an operator invocation, so with nothing constraining WHICH operators it may target the learned set widens the attack surface every time it grows. `IngressBound` declares what ingest may reach **at all** (small, declared, HRU), and the rule holds a capability **attenuated to that bound** instead of the ambient authority of whoever it acts for — the confused-deputy configuration with the learned tier as the deputy. Proven **end to end at the real gate**, not at this module's boundary: an ingest-tier rule invoking an out-of-bound operator reaches `execute_as` and comes back refused with `capability_carries`, no mutations, no events, **state byte-identical** — while the in-bound call still commits, asserted in the same test so a bound cannot quietly become an outage. ★★★ **No trust level lifts the bound**, asserted across all three, because a ceiling with an exception is not a ceiling; and ★ **the bound never exceeds the principal it acts for** — a capability is a transfer, so the policy sets a ceiling and never a floor, which also means this can never become an escalation route. ★★ **The counterweight is the strongest in the module and it belongs entirely to the reference:** the fixed/learned *split* is at parity, and the fixed tier there is its best security work — `contains_sensitive_secret()` runs **first in `parse_message()`** (`transducer.py:894`, before any parser) **and again in `IngestEngine.capture()`** (`ingest_engine.py:365`, before the raw text is ever written, so a rejected message leaves **no row and no `raw_payload`** rather than merely no log line); two independent layers, both ordered-first, one upstream of persistence; **non-authorable** (a module constant with no edit and no learning path) and mirrored in Java on the device so the secret never leaves the phone. ★ Adoption gating (verify-against-inducing-example plus regression-against-stored-messages) is real there too, and untouched. ★ **Two greps worth carrying:** `trust`'s **47 hits with zero decisions** — a count that reads like a live subsystem and is entirely storage and display — and `R_fixed`'s **grep-0 that is not an absence**, the most misleading zero in the set so far, because the *name* appears nowhere while the *thing* is excellent, so a reviewer grepping it would conclude the exact opposite of the truth. ★ A third, inward-facing: `event::Trust` **does** exist in Rust and already drives `ClockFinding::suspect` — but that is **source** provenance (*is this connector the authority for what it reports*) against §V's **rule** provenance (*how did we come to believe this rule*), which is why `RuleTrust` is a separate type and not an overload. **Residuals:** no matcher library (substring only, deliberately — a richer one would invite porting the reference's regexes); **adoption gating is not built here** and does not need to be, since it exists app-layer where the matcher and the corpus live; adoption and authority are kept **separate on purpose**, so a rule outside the bound is adopted-and-visible rather than silently dropped, refused at `capability_for` where the reason names the bound; and **nothing hands the capability to a running rule engine**, because this core has no ingest loop at all — the same threading residual IMM-7 already carries, and host work either way. ★★★ **M-IMM's ENGINE HALF IS COMPLETE.** What remains in the module is **app-layer only**: IMM-11 (a different-*kind* OTP detector — layering is independence of failure, not count) and IMM-12 (detection over the log, correctly last, because a detector over an unmediated system reports a hole it cannot close). **→ Phase 3 remaining:** EDIT-13, the surface rows (UI-in-Rust, MON-7, CTL-8), and IMM-11/12 app-layer.*

★★★ **EDIT-13 CLOSED THE WHOLE EDITING MODULE** (`lens.rs`), and it is a **LENS, not a projection**. `π` on its own is easy — walk a `Definition`, emit nodes and edges. The difficulty is that **`π` forgets**, so the way back cannot be a function of the graph alone: what an editor really is, is the pair `⟨get, put⟩` (Foster, Greenwald, Moore, Pierce & Schmitt 2007), and ★★ **`put` takes the old `D` as well as `G′` precisely so it can restore what `get` discarded** — a naive *regenerate `D` from `G′`* would destroy every detail the graph never carried. That asymmetry is the entire row. ★★ **STEP-0 corrected the framing before anything was built:** §IX names *Enzyme bodies* as the first casualty, and in this core `π` forgets them **for free** — `Definition.operators` is an allow-list of **names**, and the struct's own docstring already says *"operator bodies are not"* data here, since the bodies live in the `Registry`. A round-trip proof over Enzyme bodies would have been theatre. ★ What `π` forgets that is **real here** is two things, and they are what the preservation proof actually uses: the **invariant expression text** (the graph draws *that there is a rule called `floor` touching `finances`*, never `finances >= 500`) and the **fine type detail** (`Number { lo, hi }` draws as the coarse label `number`; `Record { fields }` as `record`). ★★ **Both laws are executed, not asserted.** **GetPut** holds in the stronger form the return type makes available — an untouched graph yields the **empty edit list**, not an edit that happens to be a no-op — and **PutGet** holds *literally*, `get(put(G′,D)) == G′`. ★★ **That literalness was a deliberate design decision worth recording:** the obvious move is an optional `expression` field on the invariant node, and it was rejected, because `π` would always leave it `None` while an editor's added node carried `Some`, so `get(put(G′,D))` would differ from `G′` in exactly that field and PutGet would have had to be weakened to *equal after clearing the fields π never fills* — a strictly weaker claim wearing the same name. Instead the detail travels **beside** the graph in `Supplied`, `G` stays purely structural, and it is therefore also exactly what a surface draws, with no phantom fields it must know to ignore. ★★★ **The preservation, proven rather than described:** remove an operator on the graph — a change the graph *can* express — put it back, and the invariant's expression text and the dimension's `lo`/`hi` bounds come back **byte-identical**, recovered from the old `D` the graph never carried them in. ★ And where the detail is genuinely absent — a **new** node — `put` **refuses and names what is missing** (`MissingDetail { node, needs }`) rather than inventing a plausible expression or default type; the same refusal-over-invention discipline as `Strategy::Refuse` and `TrustPolicy`'s missing `Default`. ★ The one addition needing nothing supplied is an **operator**, because there `π` forgot nothing — the asymmetry made concrete, and it falls out of the data rather than being declared. ★★ **A graph edit is a TYPED edit, all the way to the gate:** `put` returns `Vec<Edit>` and never a rebuilt definition, so §II's *an untyped "here is a new JSON document" edit has no kind* is honoured — proven end to end by a graph-added invariant becoming a typed `AddInv`, reaching `admit_edit`, and being **refused with `WouldStrand`** naming the instance and the rule, which is **EDIT-11's own check firing on a change made by dragging a node**; the same edit against a compliant instance is admitted. ★ A modified expression becomes **one `ModifyInv`** rather than a drop-and-add, which matters because `DropInv` cannot strand and `AddInv` can, so the drop-then-add spelling would slip a strand-capable change past `can_strand`'s asymmetry as two individually-safe-looking steps. ★★ **The counterweight is that the gap was narrow precisely because the surrounding work was done:** on the `put` side the typed taxonomy, `admit_edit`, authority, the version DAG, inverses, rollback, μ + EMC and EDIT-11's stranding decision were all already built, so a graph edit had a well-typed destination waiting; on the `get` side `Definition` was already the right three fields, `parse_predicate` existed, and ★ `goodhart::referenced_dimensions` already walked a predicate AST for root names — so the edges are **derived** rather than guessed, by **reusing** that walker instead of writing a second one that could drift from the first. ★ And the reference's own read-only `/definition` route is the correct shape for a surface to consume: additive, non-mutating, a passthrough rather than a re-derivation. ★ **One false positive worth naming, because it is the word a reviewer greps first:** `projection` in `admission.rs` is `Π_A`, the **clamp** projecting a value onto an admissible interval — real, load-bearing, and nothing to do with `π : D → G`. `Lens`/`lens` were grep-0. And `Edge` was a **collision**, not a false positive: `router::Edge` is the MoE routing edge, so the newcomer took `GraphEdge` — twenty-third collision, newcomer takes the longer name. **Residuals:** this is the **lens, not the editor** — the interactive Edit zone is the surface block's work; there are **no operator→dimension edges**, because that needs an `EffectSummary` and MON-1 already found **no shipped operator declares one**, so they would be uniformly absent and drawing them would be fabrication; an unparseable invariant appears as an **unattached node** rather than being hidden, since a rule the graph cannot explain is exactly the one an editor most needs to see; `put_apply` applies edits directly rather than through `admit_edit`, because the laws are about the lens and a gate refusal is a fact about the definition; the graph is **top-level-dimension granular**, matching how `referenced_dimensions` reports, so the two halves agree by construction; and `put` does not reorder, since `Edit` has no move or rename kind. ★★★ **M-EDIT IS COMPLETE — EVERY ROW.** EDIT-14/15/16 stay recorded as Python-side defects rather than Rust build items. ★ **And `G` is the surface block's input**, so UI-in-Rust now has a real graph to draw and a safe way back — the dependency is discharged before the block opens. **→ Phase 3 remaining:** the surface rows (UI-in-Rust UI-1…15, MON-7 preattentive encoder, CTL-8 persistent panel) and IMM-11/12 app-layer.*

★★★ **MON-7 OPENED THE SURFACE BLOCK** (`preattentive.rs`), taken first on purpose so the block's conventions — what a channel is, what gets encoded, what deliberately is **not** — are set before fifteen UI rows lean on them. ★★ **The *subject to* clause is the load-bearing half of §VII, and it is a TYPE.** The model maximises discriminability *subject to* the attributes coming only from Treisman's preattentive set, because an encoding that leaks onto a non-preattentive channel — a number to read, a label to parse — does not degrade gracefully, it **stops being a glance** and becomes a serial scan. So `Channel` has exactly seven variants and **no `Other(String)`**: a non-preattentive encoding cannot be written down, the same discipline as EVT-6's missing `close_at(τ)` and EVT-10's `SnapshotDim` with no `add`. ★★★ **Urgency is the household's, not the widget's, and three things make that structural rather than conventional.** `VisualSpec` has **no public constructor and no setters**, so a widget cannot build itself a bright spec because it cannot build a spec at all — the `Capability`/`ApprovalToken` unforgeability discipline applied to a surface. The channel map is **fixed, not a parameter**: there is no `EncodingSpec`, because a configurable map is a gaming surface where whoever chose it could route urgency onto a channel nobody looks at, or route its own metric onto brightness. And ★★★ **brightness is field-relative**, with `encode_field` the only entry point — preattentive processing is parallel *across the field*, so brightness only means anything relatively, and the consequence is the property worth having: **a widget can only get brighter by being WORSE.** Asserted in both directions: raise a sustain's own `w` and it brightens; leave it completely unchanged and let a **peer** move further from `V`, and it **dims**. Attention is zero-sum across the field and cannot be manufactured — there is no action a widget can take to become more salient except actually being in worse shape. ★★ **That discharges UI-13 in advance** — *salience is never rendered as salience* is now a property of the encoder rather than a rule the surface has to keep — and it is the **encoding-side twin of Flight 401**: where the Curated UI stops a loud-but-safe widget **winning a slot**, this stops it **looking urgent** once it has one. ★★ **Discriminability is RUN, not claimed:** §VII asks for the map that maximises it, and what is checkable is the necessary condition — distinct state-classes must encode distinctly — asserted over every class this encoder can see, with an identical peer held constant so the peer cannot be what makes them differ; `Hue` has three variants so *two near-identical hues for fine and critical* is unrepresentable rather than merely discouraged. ★ The **psychophysics is named as out of reach** rather than smuggled in: whether a particular amber is far enough from a particular red for a particular person on a particular screen is a rendering and measurement question, so discriminability here is **bounded below, not maximised**, and the difference is stated. ★ **Encoder, not renderer** — `Hue::Amber` never `#E8A020`, a `Fraction` never a pixel radius, `Motion::Pulse` never a keyframe — kept honest by asserting that no `#`, `px`, `rgb` or `hsl` reaches a spec, rather than merely intending it. ★ And **Tufte is honoured by leaving two channels empty**: saturation and enclosure are preattentive and deliberately **unassigned**, because there is no sixth or seventh data dimension and giving them a value would be decoration wearing an encoding's clothes; they stay in the type because the *set* is the set. ★ **It consumes the real engine** — a conformance case declares a real `Region`, runs a real `MonitorEngine` pass and encodes its `Ingested`, so the claim *this encodes `d(s,V)`* is checked against the thing that computes `d(s,V)` and would break rather than silently drift if the reading shape changed. ★★ **The counterweight belongs to the reference and it is the important one.** Slice 0 derived the single urgency signal from real data (`pct = spent/allocated`, a pocket's distance from its own declared limit), and ★★ **Slice 13's Curated UI reused that formula verbatim** rather than inventing a second — the Additions' *no second notion of important* honoured on purpose. So the reference already has **one** urgency and already feeds it to **selection** (the knapsack); this row adds the **other consumer**, the encoding. Two consumers, one signal, which is precisely why it cannot be gamed at either end. ★ And the reference's widget layer is real and useful (`visualize.pocket_ring`, `constraint_health`, `event_feed`, the `WidgetTypeRegistry`) — the gap was never *there are no widgets*, it was *nothing maps state to a preattentive channel*. ★★ **The contrast worth naming, because it is exactly what this row forbids:** `colour_rule` (`"amber_if_below_20pct"`) is a **widget-chosen** string hint declared per UI-schema field, stored, serialised by `ui_render.*`, sent to the client — and **evaluated by nothing on either side**, grep-0 in `apps/web/src`, so the string travels the whole way to the browser and no code reads it. That is R2 #25's *exists, unwired* verified precisely, and the same *a field, not a check* pattern as `min_privilege` and `Skin`. Even wired, it would let a widget decide when it turns amber; this row makes that shape unrepresentable. ★ **Greps:** `visual` returns 27 hits and **none is about visual encoding** (all `visualize.*`, the widget-operator namespace); `salience` returns 4 and all are **real and about selection**, which is the counterweight's own evidence. **Residuals:** the psychophysics is not checkable here; the map is deliberately non-configurable; **a one-item field carries no brightness and no size** (nothing to be brighter than, and `1.0` would be a fabricated maximum that makes a healthy lone sustain look maximally urgent); an all-zero field emits neither rather than `0/0`; **nothing calls this from the engine yet, and should not** — encoding has a surface's cadence, and a per-ingest encode would compute a field of one, which this deliberately refuses to give brightness; and trend is a **one-step** reading (`w` against its own EWMA level, with a dead-band that scales so *level* means level on a series near a million), since fitting a slope is MON-3's territory and would be the fabricated-model trap CTL-11 already declined. ★ **MON-3 (traces) is now the Monitor's only open row.** **→ Phase 3 remaining:** **UI-1 … UI-15** (★ **UI-2**, *the type-checker is not on the load path*, changes a real property rather than adding a surface and wants its own slice; ★★ **UI-13 is now discharged in advance**), **CTL-8** (persistent panel), and **IMM-11/12** app-layer.*

★★★ **UI-2 FOLLOWED, and STEP-0's crux changed the row from a port into a build** (`widget.rs`). Taken **first** of the surface rows on purpose: it changes a real property rather than adding a surface, so every later UI row lands on a load path that already type-checks — doing it later would have made it a retrofit across fourteen rows. ★★ **The crux: there is no widget code in this core at all.** `curated` is **grep-0**, and all twelve `widget`/`Widget` hits plus all three `salience` hits are **prose in doc comments** — `editing.rs` explaining why a `RebindWidget` edit kind is absent, `monitor.rs` naming `WidgetVisualEncoder` as MON-7's row, `region.rs` calling `d(s,V)` *THE salience quantity*, and MON-7's own header. No widget type, no binding table, no `compose(r)`, no knapsack, **no load path**. So porting `validate_widget_schema` here would have produced a checker **beside nothing** — which checks nothing and cannot have the property the row is about. The honest scope is the **typed load path with the judgment on it**, and that is also why the fix stayed cheap: there was no existing untyped path to retrofit, so the check is structural from the first line. ★★★ **The check is ON the path, structurally.** A `WidgetDecl` is what an author writes; a `LoadedWidget` is what a surface may draw, and it has **private fields and no public constructor** — the only way to obtain one is `WidgetSet::load`, which checks `inputs ⊆ dim(S) ∧ emits ⊆ T`. So *a widget that does not typecheck cannot be rendered* is a fact about the types rather than a rule someone has to remember, the same discipline as `Capability`, `ApprovalToken` and `VisualSpec`. A typo'd dimension (`finaces` for `finances`) produces **no widget at all** — not one that renders blank, not one that reads `null` at runtime. Off-the-load-path — *validate if someone calls it* — is precisely the R2 #25 defect, and it is the same shape `min_privilege`, `Skin` and `colour_rule` all had before their rows closed; naming it as a **pattern** is what makes it a class of defect rather than four accidents. ★★ **Two containments, nested, enforced at different times.** At **load**, `emits ⊆ T`: a widget may not even name an operator this definition forbids. At **emit**, the operator must be in **its own** declared list — proven by a widget refused for `budget.record_income`, which the definition *does* permit and which the widget simply never asked for. The same shape IMM-10 gives a learned rule with its ingress bound. ★ And `WidgetEmission` has **no public constructor** either, so an emission naming an operator no widget declared cannot be forged and handed to the gate. ★ **Three distinct load errors**, kept apart for the reason EVT-15 splits `Refused` from `NotPermitted`: *that is not a path*, *that path names nothing declared*, and *not permitted* versus *not registered* — each sends an author to a different repair, and the last pair is proven by a definition that permits an operator nothing registers, so only the registry error fires. ★★ **Every** error is reported rather than the first — fixing a surface one message at a time is the experience `editing::typecheck`'s *well-typedness is a property of the whole definition* already avoids — and **one bad widget refuses the whole set**, because a partial load is **silent degradation**: the widget simply is not there, which is indistinguishable from one that decided it had nothing to show. A surface that fails loudly can be fixed; one that quietly renders four cards instead of five cannot be debugged by the person looking at it. ★ **Reuse, not redefinition:** `dim(S)` and `T` are read from `Definition` — the same two EDIT-13's `π` walks — and inputs are parsed with `predicate::parse_state_path`, which exists *precisely so a second wildcard-path grammar does not appear inside this crate*, per its own docstring citing the Constraint article's record of the reference's two divergent evaluators. Wildcards work because `Schema::declares` already understands them. ★ **EDIT-13 pays off immediately:** a real typed `Edit` retiring a dimension or an operator makes a loaded set report **stale** (`is_current_for`), while adding an invariant correctly does **not** — the staleness key covers `dim(S)` and `T` only, because a wider key would report staleness that is not there and send a host reloading its whole surface every time a rule was tightened. ★★ **The counterweight belongs to the reference and it makes the gap small and precise:** `w = ⟨inputs, render, emits⟩` with `inputs ⊆ dim(S)` and `emits ⊆ T` is **the reference's own schema and its own check** — this core invented neither the shape nor the containments nor the idea of typing a widget against the definition — and the Curated UI module as a whole is **mostly built** there (typed widgets, β keyed on event class, `compose(r)` as a pure computed value, a genuine knapsack, deterministic effect-first capture). So the gap is **one word wide**: *on*. Not *the check does not exist*, but *nothing makes it run*. ★★★ **And STEP-0 caught this repository overstating itself.** MON-7's vector, written one slice earlier by this same process, recorded *urgency → selection* as **`AT PARITY — curated_ui.py's knapsack there, curated_ui.rs's equivalent here`**. **There is no `curated_ui.rs`.** The line was wrong and is **corrected in that vector** rather than quietly left standing — a vector that overstates parity is the single failure mode this whole discipline exists to catch, and it is worth recording that the discipline caught one of its own. **Residuals:** this is the **load path, not a surface** — β, `compose(r)`, the knapsack and the drawing are later rows; **`render` is an opaque string by design**, so a misspelled render tag is **not** caught here and that cost is stated rather than hidden (typing it would mean the core acquiring a card/ring/feed vocabulary, which is the line MON-7 already drew); **emission params are not type-checked**, because `OperatorMeta` carries no params schema in this core — the gate still catches a bad call, the point is it is caught **there**; a loaded set **can go stale and says so but does not self-heal**; and nothing wires a `WidgetEmission` to `execute`, the same core→host seam every other row draws. **→ Phase 3 remaining:** **UI-1, UI-3 … UI-15**, **CTL-8** (persistent panel), and **IMM-11/12** app-layer.*

★★★ **UI-1 FOLLOWED — the compose engine** (`curated.rs`), and it is the first row in a while with a **reference implementation to measure against** rather than only a spec: Slice 13 shipped β, `compose(r)` and the knapsack in Python. The algorithm is ported **at parity on purpose**; three things are sharper, and each closes a gaming surface rather than adding a feature. ★★ **STEP-0 settled β's key first, and the answer was symmetric:** there is **no `EventClass` in this core** — `EventClass`/`event_class` grep-0, and the Rust `Event` carries only `name: String` — ★ **and the reference has none either**: its `event_class` is a plain string, and `binding_key` returns that string or a `UNIT` sentinel, both used as `dict` keys. So there was no type to port, and inventing an event taxonomy would have been this core deciding how the reference ought to name its events. β keys on `Event.name` directly, with **one** sharpening kept: `BindingKey::{Unit, Event(String)}` makes `EventClass ∪ Unit` genuinely the **∪** the notation claims. ★ The sentinel collision that removes is **theoretical** — nobody names an event `__unit__`, and that is worth saying plainly rather than inflating — but the second effect is real: a widget with **no** binding becomes **unrepresentable**, which the reference has to reject at runtime (*"must declare either event_class or unit=true"*). ★★ **Event-first in behaviour, not only in shape:** an event-bound widget whose class is absent from the recent log is **never scored** — asserted through `candidates_considered` rather than through the selection, because *ranked low* and *never considered* are different things and only the second is what event-first means. That is what stops `compose` degrading into *scan everything and rank*. ★★★ **Urgency is the real `d(s,V)`, and this is where the port improves on its source.** The reference's urgency is an explicitly-**declared proxy**, `pct = spent/allocated`, and UI-7 states the reason in its own row: *"deliberately reusing the Monitor's Slice-0 signal because CUSUM/EWMA don't exist"*. In this core they do, and so does `region.rs` — so a widget's urgency is its **share of `d(s,V)`**: the per-dimension weighted excess over the dimensions it declares, as a fraction of the sustain's total. *The fraction of the household's trouble this card can show you.* Inside `V`, every urgency is 0, which is correct: a household inside its own viable region has nothing urgent, and a surface that manufactured urgency there would be training people to ignore it. ★★★ **Two gaming surfaces, both closed structurally.** (1) **A widget cannot set its own urgency** — its only lever is *which dimensions it declares*, and UI-2 already checked those against `dim(S)`; the distance itself is the household's. One signal, now three consumers: Monitor escalation, MON-7's encoding, and this selection. (2) ★★★ **A widget cannot declare itself cheap**, and this is the finding worth carrying: the reference's `knapsack_select` docstring cites *"Hick-Hyman's superlinear per-item cost — a widget with more inputs costs more attention chunks to parse, **see WidgetSchema.cost**"*, and following that pointer gives `cost: int = 1`, read straight off the spec. **The formula is written down and never computed.** That is a gaming surface of exactly the `colour_rule` kind — a widget reading eight dimensions can declare `cost: 1` and get a bargain, while an honest widget declaring `3` is penalised for its honesty. Here `attention_cost` is **derived** from `inputs.len()` and superlinear at `1 + n(n−1)/2`, the pairwise relating a reader actually has to do. ★★ **Together they close the one hole share-of-distance would otherwise open**: a widget *could* raise its urgency by declaring more dimensions — but the same declaration raises its cost **superlinearly**, so the attention budget prices the strategy out. A greedy all-dimensions widget captures the whole of `d(s,V)`, scores maximally, costs 4, and is **excluded** against a budget of 3 while two focused widgets costing 1 each are both selected. **The greedy move is self-defeating**, asserted rather than argued. ★★ **The knapsack is real, and proven twice.** Greedy-by-score takes the 0.90 item and stops at 0.90; the DP takes the 0.60 + 0.55 pair for 1.15 — the case that tells a genuine DP from a sort-and-take-N. And optimality is asserted **against brute force** over every feasible subset, because *every selected widget provably outscored what it displaced* is the whole reason a person would trust the surface, so it is worth proving over the algorithm rather than over one example. Every exclusion returns **with its score**, so *"N other things stayed quiet"* has something true to say — the OPV-29 discipline again. ★ **One behaviour found by testing and kept:** a widget scoring exactly 0 is **never selected**, because the DP takes an item only when it *strictly* improves the total. That is right rather than a gap — `K ≈ 4` is scarce and a card worth nothing is precisely the noise a curated surface exists to remove — and it is **at parity** with the reference, whose `dp[c-w] + v > dp[c]` behaves identically. Nothing is hidden: it was considered, it returns in `excluded` with its `0.0`, and `spent` is 0. ★ **Read-only by signature** — shared references in, a `View` out, no `Registry`, so nothing here could run an operator — and asserted anyway across two calls, which doubles as the proof the view is deterministic. ★★ **And UI-2's typed population is the ordering benefit realised**: `compose_view` takes a `WidgetSet`, so **a widget with a made-up input cannot enter the knapsack** — it never loaded. Doing UI-1 first would have meant a compose that ranks widgets whose inputs may not exist, with UI-2 arriving later as a retrofit through it. ★★ **The counterweight is that the algorithm is the reference's and it is good.** Its knapsack is a **genuine 0/1 DP**, not the sort-and-take-N that would have been easy to ship and hard to notice, with the backtrack, the integer scaling and the excluded-with-scores return all already there — which is what makes *"N stayed quiet"* a true statement on Bonnie's phone today. `α = 0.75` dominant is its choice and the right one. ★★ And the *no second notion of important* discipline is the reference's own, honoured under pressure: Slice 0 derived the urgency signal and Slice 13 **reused that formula verbatim** rather than inventing a selection-specific one, so this core inherits a signal that was already single and only substitutes a better measurement of the same thing. ★ Its proxy is **declared as a proxy** in its own row, with the reason — which is what makes replacing it an upgrade rather than a correction. ★ **A false positive worth naming because it is the first word a reviewer greps:** `compose` in Rust is **OP-5's checked composition** (`wp`, pathway chaining, illegal chains refused at write time) — real, load-bearing, and nothing to do with `compose(r)`; the newcomer is `curated::compose_view` for that reason (and `WidgetCandidate` for `ooda::Candidate`, twenty-seventh collision). **Residuals:** `render` stays **opaque**, so this ranks rather than renders; urgency-as-a-**share** is a modelling choice and is named as one (a widget's urgency is relative to what else is wrong); `relations_violated` contributes **no** urgency, inherited from `region.rs`'s deliberate refusal to score a boolean — the widget layer does not invent the number the region layer refused to; `device` is carried and **not** used to rank, since naming a device-specific budget would be inventing policy; relevance is token overlap, not meaning, and is the weaker term at `λ = 0.25` for exactly that reason; and nothing calls `compose_view` from an engine — a host assembles `r` at a surface's cadence, the same seam MON-7 draws. **→ Phase 3 remaining:** **UI-3 … UI-15** (★★ **UI-13 discharged in advance** by MON-7; ★ **UI-7 unblocked**, since `d(s,V)` now replaces the proxy its blind spot was about), **CTL-8** (persistent panel), and **IMM-11/12** app-layer.*

★★★ **UI-7 CLOSED, AND THE STEP-0 RECONCILE *IS* THE SLICE.** The row names a defect precisely, and the defect belongs to the **proxy**: `pct = spent/allocated` is undefined at zero allocation, so the reference returns `0.0` there — meaning **an unfunded pocket that has been spent from scores as perfectly calm**, which is backwards. ★★ Checked in code before touching anything: **`spent/allocated` does not exist in this core's urgency path.** Every `allocated` hit in `sustena-core/src` is `budget.rs`'s operator writing real state paths, a `compose.rs`/`inverse.rs` fixture, or the single docstring in `curated.rs` *describing* the reference's proxy; `urgency_of` calls `region.distance()` and nothing else. **There is no denominator, so there is no zero-denominator case to special-case wrongly** — the artifact is structurally impossible here rather than fixed, UI-1 having discharged it. ★ The honest move was to **say so and narrow**, not to re-derive a proxy that is gone. ★ The *'unmapped captures pinned at 1.1'* constant is likewise **grep-0** — a Python artifact of an ingest queue this core does not have, recorded as checked rather than assumed. ★★★ **CASE A proves the fix rather than asserting it:** the exact scenario the row worries about — a pocket with **zero allocation, spent from**, driving `balance` through a wall `V` declares — reads a **non-zero** urgency with a `Bounded` basis. The metric the proxy was standing in for catches precisely what the proxy called calm. ★★★ **CASE B is the genuine finding, and it is now a type.** Where `V` bounds **nothing** the widget reads, `d(s,V)` is honestly `0` — but that means *the household declared no wall here*, not *nothing is wrong*. ★★ **No wall is invented to force urgency**, because inventing one would be this core deciding what a household should care about — the same fabrication every prior slice has refused. Instead the basis is **reported**: `UrgencyBasis::{Bounded, Undeclared}`, with `describe()` saying *silence rather than safety*. Two widgets can read the same `0.0` and mean opposite things, and a surface can now tell them apart. ★ **The proxy MANUFACTURED a wrong zero; `d(s,V)` gives an honest one — and this makes which kind it is legible**, which is strictly more than the row asked for and is the part worth carrying. ★★ **The second residual was real, and it was the one the row names in its own title: the weights.** `pub const ALPHA_URGENCY: f64 = 0.75` was exactly *'a policy living in a module constant'*, and §IV is blunt about it — *"Nothing derives α = 0.75. It is a declared preference ... belongs in a declared constraint where it can be **argued with**, not a module constant."* `SaliencePolicy::declared(α, λ)` is now passed by the **household**, and a widget has nowhere to put one, so the weights are argued with by the household and by nobody else. ★ **It keeps a `Default`, and that is the honest call rather than a lapse** — `TrustPolicy` (IMM-10) has none because what a trust level may reach is a per-deployment decision with no safe answer, whereas here the article *states* a preference and it is a good one. The defect was never *there is a default*; it was *there is no way to hold a different one*. So the default survives, **named in the constant's own docstring as a declaration rather than a derivation**. ★★★ **Two things are refused as unarguable:** `α > λ`, because a policy letting a **search query outrank the household being in danger** would reintroduce at the weights exactly the gaming UI-1 closed at the inputs — and the **tie is refused too**, since *equal* is already a surface where a query can win; and `α + λ = 1`, so `score ∈ [0,1]` keeps meaning the same thing across sustains and the DP's integer scaling stays interpretable. **You may argue the ratio; you may not argue urgency into second place.** ★ And the precedent was already in this codebase: `Region::weights`'s own docstring reads *"A modelling choice, not a fact. Declared here so it can be argued with."* The salience weights had simply never been given that treatment. ★ **A false positive named because it is the grep a reviewer would run to check this row:** `spent`/`allocated` **do** return hits in Rust, and all of them are innocent — the operator's real state paths, test fixtures, one prose mention of the reference, and `View.spent`, which is the **attention** budget rather than money. Named so a non-zero count is not read as *the proxy is still here*. ★★ **A vector correction, kept on the record because it is the second one on the same line:** `preattentive.json`'s *urgency → SELECTION* entry first read AT PARITY citing a `curated_ui.rs` that did not exist, was corrected by UI-2's STEP-0 to **PYTHON-ONLY** (true that morning), and UI-1 then built β and the knapsack — making PYTHON-ONLY an **understatement**. It now reads at parity, with both corrections recorded, because **a vector that understates is as wrong as one that overstates**. **Residuals:** `Undeclared` is **reported, not penalised** — no floor, no boost, no warning severity, since ranking it up would fabricate the household's concern; the basis is **per-widget**, so a widget reading one bounded and one unbounded dimension reads `Undeclared` naming the unbounded one (the conservative direction, stated rather than inferred); and the sum-to-one rule constrains the *argument* without deriving the ratio — nothing here makes `0.75` correct, and the row's point stands: it is a preference, now held somewhere it can be disagreed with. **→ Phase 3 remaining:** **UI-3 … UI-6, UI-8 … UI-15** (★★ **UI-13 discharged in advance** by MON-7), **CTL-8** (persistent panel), and **IMM-11/12** app-layer.*

★★★ **UI-13 CLOSED — AND IT WAS NOT THE PROOF-ONLY SLICE IT WAS EXPECTED TO BE.** It was taken on the reading that MON-7 plus UI-1/UI-7 had already discharged *salience is never rendered as salience*, with the reconcile expected to confirm closure. ★★★ **The reconcile found a third lever instead.** The property has **three** surfaces a widget could stage its own prominence on. Two were genuinely shut: **brightness** by MON-7 (field-relative across the field, and `VisualSpec` has no public constructor, so a widget can only brighten by actually being further from `V`), and **score** by UI-1/UI-7 (urgency is the household's `d(s,V)`, cost is **derived** from the declared inputs rather than author-declared, and the weights are the household's `SaliencePolicy`, which refuses to let urgency be argued into second place). ★★★ **The third — POSITION — had been claimed by no row, and it was open.** The mechanism was two innocuous things meeting: `WidgetSet` is a **`BTreeMap` keyed by widget id**, `compose_view` additionally sorted candidates by `id`, and Rust's `sort_by` is **stable** — so wherever two widgets tied on score, the alphabetically-earlier **widget-authored** name took the higher slot. `aaa_spending` outranked `zzz_spending` at identical score. Small, real, and exactly the Goodhart move the row is named for; it survived three prior slices because each of them was looking at a different surface. ★★ **Closed the way the other two were — by removing the lever rather than policing it.** `rank_selection` orders on three keys a widget cannot set: **score** descending; **urgency** descending, so a search term cannot break a tie between two genuinely urgent cards (the `α > λ` principle applied one layer below the score); and **cost** ascending, which is derived from the declared inputs and is the right preference anyway — at equal value, spend less attention. ★★★ **Past those three, the tie is DECLARED rather than broken.** The two widgets are indistinguishable on everything the household can measure, so rather than fabricate a winner they share a `rank`: **position carries no information the score did not already give**, and where it would, the rank says so. That is the `Skew::Unknown` / `Soundness::Unavailable` / `UrgencyBasis::Undeclared` discipline applied to ordering — *do not invent a distinction you do not have* — and it is now the fourth place in this core where the honest third answer beat a plausible one. ★ Ranks are **competition-style** (`0, 0, 2`, not `0, 0, 1`), so a reader can see the third card is behind **two** widgets rather than behind one group; collapsing them would hide how many peers shared the top. ★★ **And the residual array order among peers is the HOUSEHOLD'S declaration order.** `WidgetSet` now keeps the order it was loaded in for exactly this reason — a `BTreeMap` alone would have handed the last word back to the widget's name. So **renaming a widget changes neither its rank nor its position**, while the household *can* still reorder its own peers. Both halves are asserted, and together they say the thing that matters: the lever moved **from the widget to the household**, which is the right owner, rather than disappearing. ★★★ **All three surfaces are now covered, and the property collapses to one sentence worth keeping:** a widget can raise its prominence in exactly one way, on every surface — **by the household genuinely being further from its viable region on a dimension the widget declared it reads.** ★ **Divergence:** the concern is the reference's own (§IV), and it is at parity as a *concern*; the reference's `knapsack_select` sorts by score with Python's `sort`, which is **also stable**, so its ties likewise fall through to insertion order — not score-derived either, and neither side reported a tie before this. **Residuals:** a surface **must render `rank`, not the array index** — the `Vec` necessarily has an order and among peers that order is the household's declaration rather than a ranking; the core reports the tie and cannot force a renderer to honour it, a boundary named rather than assumed. And the household **can** still order its peers, deliberately. **→ Phase 3 remaining:** **UI-3 … UI-6, UI-8 … UI-12, UI-14, UI-15**, **CTL-8** (persistent panel), and **IMM-11/12** app-layer.*

★★★ **CTL-8 SHIPPED AND M-CTL IS COMPLETE** (`panel.rs`, `curated.rs`) — the last non-UI row in Phase 3. ★★ **STEP-0 resolved the phasing question the row had been moved on: this is a CORE property, not host state, and the reason is the sharp one — it needs NO stored state.** `compose_view` resolves fresh and keeps nothing, which looked like a tension with a *persistent* panel; but a persistent panel is **not a remembered identity surviving a recomposition**, it is an **unconditional inclusion by declared policy**, re-derived on every call. Adding the tier required **zero storage**, which is exactly what makes it composable here rather than something a host has to hold. ★★★ **Two tiers, and the persistent one is OUTSIDE the attention budget:** `view = persistent(ALWAYS) ∪ knapsack(dynamic, K≈4)`. The reason is one sentence — **a panel that could be displaced by a busy day is not persistent, it is merely high-scoring** — so `View::spent` counts the dynamic tier alone. Proven three ways: against a budget of **1 fully occupied by a maximally-urgent widget** the panel is present anyway; composing with and without it leaves `spent` **and** the selection byte-identical, so it **cost nothing and displaced nobody**; and it shows when *nothing* is wrong, which is the whole point of `I(p)` being high for **all** states — *nothing is wrong* is itself the answer a console gives. ★★ It carries **no score and no rank**, because ranking it would put it back into the competition it is exempt from and a score would invite a surface to order it against widgets it never competed with. ★ **`WHEN_ACTIVE` needs no machinery of its own**: such a panel **is** a widget — eligible through β, merged into the same candidate pool, selected through the knapsack — so the policy is genuinely two-valued with a real behavioural difference rather than a label with one live branch. ★★ **`I(p)` is DECLARED, not computed, and that is the honest form.** §VIII gives `I(p) = H(state|absent) − H(state|present)`, and computing `H(state | ·)` over a real sustain's state space is **intractable** in exactly the way MON-2's Kalman filter and MON-1's observability matrix are — both **declined imports** in this core rather than approximations. So there is deliberately **no `information_value()` returning a number**; `PanelDecl::always` instead **requires a justification** and refuses without one (`UnjustifiedPersistence`). An exemption from the attention budget is the single claim that most needs writing down to be argued with, and the justification travels into the view so a surface can say *why* the panel is always there. Same treatment `Region::weights` already carries and `SaliencePolicy` gained in UI-7. ★★★ **Exemption is the FOURTH salience surface**, and it is closed the way UI-13's three were — by removing the lever rather than policing it. `WidgetDecl` has **no policy field**, so persistence is **unspellable from the widget side**: a `PanelDecl` is a wrapper only the household constructs, and the byte-identical widget declaration lands in whichever tier the **household** put it in. The salience family now reads: brightness (MON-7), score (UI-1/UI-7), position (UI-13), exemption (CTL-8) — and a widget can raise its prominence on none of them except by the household genuinely being further from its viable region. ★ **The divergence expectation was wrong, and it is recorded rather than quietly adjusted:** `PANEL_POLICY` and `WHEN_ACTIVE` are **grep-0 on BOTH sides**. The reference has `PANELS` in `shell.jsx` — a hardcoded **seven-entry frontend nav list** for the Mycelium cockpit, with no ALWAYS/WHEN_ACTIVE distinction and no relation to Orchie's curated view. The panel *list* exists as navigation; the *policy* exists only in the article, so there was no declared intent in code to credit. ★★ **A greppable false positive worth naming, because it is the grep for this row:** `persistent` returns **14 hits in Rust and none of them are this** — every one is CUSUM's sense (`detect.rs` exists to catch *small persistent changes* rather than spikes, and `Shift::Upward` documents *running persistently high*), plus `pincer.rs`'s *persistent condition* debounced by a refractory window, which is a third sense again. ★ And `panel`'s four hits are the **rollback panel** (Controller §II), a real and different concept, plus the one docstring line recording that §VIII was not built. **Residuals:** ★★ **flicker-stability is NOT this row** — keeping a *dynamic* widget in its slot across recompositions to avoid churn needs memory of the last view, which `compose_view` deliberately does not keep; genuinely different and harder, named rather than smuggled in. ★ The canonical panel list (console / health / IoT / sessions) is the **household's**, not hardcoded here, because a core shipping its own would be deciding what every household must always see — the same fabrication the whole salience family refuses. And a `WHEN_ACTIVE` panel is indistinguishable from a widget once composed, deliberately, so a surface cannot style it differently. ★★★ **M-CTL is complete; Phase 3's non-UI work is done.** **→ Phase 3 remaining:** **UI-3 … UI-6, UI-8 … UI-12, UI-14, UI-15**, and **IMM-11/12**, both app-layer.*

★★★ **SURFACE-BLOCK RECONCILE — every M-UI row is now honest, and the sweep found one real residual.** Five rows had been **discharged by UI-1 and never flipped**, and are flipped here with code evidence rather than rebuilt: **UI-3** (β — `BindingTable` keyed on an event class, with the ∪ as a sum type and an operator name unable to occupy a key at all), **UI-4** (`compose(r)` — fresh per call, read-only by signature since there is no `Registry` in it), **UI-6** (salience — and sharper, since urgency is the real `d(s,V)` and the weights are household-declared policy with `α > λ` refused as unarguable), **UI-8** (the budget — and Hick–Hyman **computed** rather than cited, plus CTL-8's persistent tier giving the pool its other half), **UI-9** (the knapsack — the reference's genuine DP, ported, and proven both against a greedy counter-example and against **brute force**). ★★★ **UI-5 was NOT discharged, and that is the finding.** UI-1's event-first eligibility withdraws an *event-bound* widget whose class did not fire — but a **Unit-bound** widget is always eligible, so one with nothing to show still entered the ranking, scored `α·0 + λ·0.5 = 0.125`, and **was selected**, because the DP takes anything strictly above zero. That is §III's *unexplained `0`* exactly, and it had survived four surface slices because each was looking at scoring rather than at eligibility. ★★ Closed with a **grounding condition enforced before any ranking**: a widget is grounded when at least one declared input resolves to something real — present, non-null, and **not an empty container**, since an empty map or list is the *nothing to say* case. Ungrounded widgets **withdraw**: not scored, not excluded-with-a-score, and **not counted in `candidates_considered`**. ★ **Derived, not declared**, from the same inputs UI-2 already checked against `dim(S)`, so a widget can no more declare itself grounded than cheap or persistent — and the withdrawal is **reported** (`View::withdrawn`), because *never had anything to say* and *considered and outranked* are different facts: an exclusion carries a **score**, a withdrawal carries a **reason**. ★ Two honest limits: grounding is **any-of, not all-of** (one resolving input grounds a widget reading five — the permissive direction, since a card showing four of five still says something), and it **does not apply to the persistent tier**, because `ALWAYS` means always. ★★ **Two rows got a real core-vs-app-layer decision rather than a shrug.** **UI-10 is APP-LAYER**: its FSM state is a capture *conversation's* accumulated `known`, held between round-trips, and this core is deliberately stateless across calls — a δ whose purpose is to accumulate would be the first thing here that remembers anything between requests. Its own critique (*hand-rolled per flow*) is an app-layer improvement and is not made better by moving it into a core that would have to grow session state to hold it. **UI-11 is APP-LAYER FOR NOW WITH A NAMED CORE SLICE**: the inference `ℐ : ε → (o, θ)` is genuinely portable — deterministic, no LLM, no `eval`, `θ` from the operator's real signature, pockets matched against live state, every input something this core already holds and nothing needing I/O, a clock or randomness — **but** it is fed by the **transducer** and by **capture history**, both app-layer, so a core `ℐ` today would be a signature-introspection helper with no caller, which is precisely the *port a checker to sit beside nothing* mistake UI-2's STEP-0 refused. Queued for when a Rust-side caller exists; the natural one is an Enzyme-call proposer over `Definition` + `Registry`, which is also what **EVT-15's un-wired `EnzymeCall` residual** would consume. The blocker is a missing consumer, not a missing capability. ★ **UI-12 is deferred with the LLM/economy work**, and the **disposing** half is already built: `WidgetSet::load` refuses any widget whose `inputs ⊄ dim(S)` or `emits ⊄ T`, and `LoadedWidget` has no public constructor — so **a generated view faces exactly the gate a hand-written one does**, with no generation-specific path to widen. What is deferred is the proposer, not the constraint. ★ **UI-14 and UI-15 are honest notes, confirmed as such.** UI-14's *two widget notions* divergence is **Python-side** — this core has one typed notion, so the divergence does not exist here and cannot be closed here (backlog #36 stays app-layer); and its *a budget bounds what is shown, not whether the state is understood* is a true observation, now sharper since UI-5 makes *nothing to say* visible rather than letting a card imply understanding it lacks. UI-15 is a **coverage** fact rather than a defect, and also sharper: an honest **empty** view (`candidates_considered = 0`) is now distinguishable from one where cards **withdrew**. ★★★ **The block's true state: UI-1 … UI-9 and UI-13 ✅ in Rust; UI-10/UI-11 app-layer (UI-11 with a queued core slice); UI-12 deferred; UI-14/UI-15 notes. No unbuilt Rust-core mechanism remains in the surface block, and with M-CTL closed, PHASE 3'S RUST-CORE WORK IS DONE.** **→ Next front: the deep bundle, OPV-4 `Π` first.**

★★★ **THE DEEP BUNDLE OPENED ON OPV-4** (`strategy.rs`), and it is the bundle's **one row with a parity half** — `operative_graph.py` is Π in production, so the walk was **measured** rather than invented, and the counterweight this session's greps have twice caught overstatements with was available for once more. ★★ **STEP-0 corrected the brief's premise, and in OPV-1's favour**: the GO expected `ω` to carry a declared `Π` slot to fill. It does not — and `operative.rs` says why in its own docstring: *'follow-ons ... **have no placeholder here, because a declared-but-inert field reads as built**'*. So this row **adds** the mechanism, and OPV-1's restraint is **vindicated rather than corrected**: had the slot existed, `ω` would have spent five days looking like it had a strategy. ★★★ **But the mechanism Proposition 3 needs was already there, and that is the reconcile's real payoff.** OPV-1 made Proposition 1 structural by minting `LegalMove` **only** through `Shared::legal_move`, so a proposal outside `T` is unconstructible. Prop 3 — *a strategy adds structure, not capability* — is the same claim one level up, so **`StrategyNode` holds a `LegalMove`, not a name**: a node naming something outside `T` is **unconstructible**, refused at build (`MoveNotInT`), where the reference stores `operator_name: str` and raises `ValueError` **mid-walk**. *A strategy that cannot legally be run is a defect in the strategy, not an event during it.* ★★★ **And the bound is proven, strictly, not claimed**: a world reaching `s2` via `m1` then `m2`; a strategy naming only `m1`; `reachable_under(Π) ⊂ Shared::reachable`, with `s2` outside its reach. A strategy **re-sequences and conditions** moves that were already legal and **can only narrow** — computed from `Shared`'s **own** transition table via the one small read this row added (`next_state`), because a bound derived from a copy would prove nothing. ★★ **Every node goes through the real gate.** The walker calls `execute_admitted`, so `Π` inherits guard, `V`, `D`, closure, `F` and the authorization/token conjuncts with **no strategy bypass**; a refused node stops the walk carrying **the gate's own reason**, with earlier commits intact and state byte-identical when nothing committed. ★ Two containments stay live and both are tested: `T` at **build**, the sustain's allow-list at the **gate** — a move can be in `T` and still not permitted here. ★ **Parity, verified rather than assumed**: the conditional walk with first-matching-edge, `$dot.path` **resolved never evaluated** — including leaving an unresolvable reference as the **literal string** rather than substituting `null`, checked in `_resolve_dynamic_kwargs` rather than taken on trust — `{{token}}` build-time calibration refusing an unsupplied token, the six comparison operators with absent-field → false and type-mismatch → false, and the `max(2·|N|, 10)` guard. ★ Two passes at two times, and a calibration pass leaves a `$` reference **untouched**, asserted because that is precisely where one resolver eats the other's syntax. ★★★ **The finding: the reference's graph calls operators OUTSIDE the gate.** `run` calls **`op_meta.fn(context, **kwargs)`** — the operator *function* — not `SustainEngine.execute_operator`, which is where the enforcement gate, the fold-append and the parent-binding check live. So a strategy there executes operator bodies on a path the gate never sees. ★★ **The counterweight is substantial and is why this is a finding rather than an alarm:** the graph's output is an `OperativeProposal` (`_build_proposal` is its return), and Slice 10's whole discipline is that **operatives advise, the human decides**, with `accept_suggestion` going through `execute_operator` for the actual commit — so the operators a graph calls are overwhelmingly evaluative (`mentor.evaluate_budget` and its kin). A **shape** worth naming, not a live hole. ★ **The cycle guard reports rather than truncating**: both sides bound the walk identically, but the reference then falls out of the loop and builds a proposal from whatever the last result happened to be, so **a cyclic graph and a completed one look alike to the caller**. `WalkOutcome::StepsExhausted` makes them different, and `finished()` is false — the `Skew::Unknown` third-answer discipline again. ★ **One small real difference recorded rather than rounded away**: the reference's dynamic resolver recurses into nested **dicts but not lists**, so a `$ref` inside a list literal is left unresolved there; this resolves both. Small — and a vector that rounds small differences to *identical* is how a large one eventually slips through. ★ **Naming**: `StrategyGraph`, not `Strategy` — `admission::Strategy` is the constraint verdict policy (refuse / clamp / defer), and confusing *what a rule does when it is broken* with *how an operative reasons* would genuinely mislead. Twenty-eighth collision. **Residuals:** the walk **does not build a proposal** (presentation, and OPV-1's `Proposal` already exists); ★★ authorization and effects are passed **`Unchecked`**, so a caller wanting the principal or approval conjuncts on a strategy's nodes must thread them and there is no API here yet — **the honest residual, because a strategy is exactly the deputy IMM-7's capability was built for**; `typecheck` validates shape rather than acyclicity, with the step limit named as the substitute; first-matching-edge means **edge order carries meaning** rather than specificity; and state threads, so a mid-walk refusal leaves earlier commits in place. **→ The bundle's remaining rows have NO reference implementation** — OPV-6 (learning), OPV-7 (attention), OPV-11 (`M_self`/`M_world`) are R2-spec-only, so their STEP-0s must lean on internal consistency (`Π` now, `mixture.rs`'s declared-never-learned weights, `region.rs`'s `V`) rather than on a cross-implementation grep.*

★★★ **OPV-7 FOLLOWED — the bundle's first genuinely reference-less row** (`attention.rs`), and the substitute for the missing parity half worked. ★★★ **STEP-0's constraint was the one that mattered: narrow must NOT become a fifth notion of *important*.** The salience family has closed rival-importance on four surfaces — brightness (MON-7), score (UI-1/UI-7), position (UI-13), exemption (CTL-8) — and a **scoring** attention is precisely where a fifth could open. So `Score` has **no public constructor and no public fields**; the only way to obtain one is `narrow_scan`, which computes it from `Region::distance` — the household's own `d(s,V)` — and nowhere else. An operative cannot mint a score, cannot raise its own, and has **no field anywhere** in which to declare a rival metric; the same mechanism as `VisualSpec` and `Capability`, applied to the fifth surface. ★ The absence is checked too: a sustain whose state cannot be read yields **no score at all**, not a fabricated zero. ★ And the same discipline one size down — `Aperture::stance()` reads narrow-versus-broad **off `⟨b,d,ρ⟩` itself**, so an operative cannot call a broad aperture *narrow* by declaring it so, which matters because *both non-empty* is enforced by comparing stances and a declarable label would make that refusal trivially evadable. ★★★ **Both attentions are structural**: two required fields, no `Option`, no single-attention constructor — so *a council of one-attention agents is a council of half-minds* is unrepresentable rather than discouraged — and a pair landing in the **same region** is **refused**, because declaring one aperture under two names is the partition arriving through the back door, and §V is explicit that both belong to **one** mind. ★★ **Two output types, not two scores.** Narrow returns a `Score`; broad returns a `Reframing` — *the frame you were scoring in has changed* — carrying **no magnitude**, because a number there would invite a caller to weigh it against a narrow score, and that collapse is exactly what makes an agent a maximiser inside a frame it cannot notice is wrong (Ashby's remainder; §XIV's Goodhart drift arriving through the attention layer rather than the utility one). ★ Broad also **ignores what the task was already looking at**, so it reports what narrow is *not* covering — without which the two attentions collapse into one louder one. ★★★ **A path with fringes, and the subtree is unspellable.** Each level samples up to `b` siblings as fringes and descends into exactly **one** child, so the walk is linear in `d`. There is **no `walk_subtree`** — the geometric traversal is not merely discouraged, it is not expressible. `subtree_visits()` exists **only to measure the gap** and nothing consumes it: at `b=8, d=6` the subtree is **over thirty thousand** visits against a path of **48**. ★★ **The closed trade, as arithmetic:** `κ·b·d·ρ`, so `⟨1,4,3⟩` and `⟨12,1,1⟩` both cost 12 — deep-and-narrow and broad-and-shallow are the **same spend**, so neither is the cheap one and choosing between them is a choice about what to see rather than what to afford. Doubling depth breaks a budget until resolution pays for it: **no free axis**, which is why the meter is a product and not a sum. The bound applies to the **pair**, since an operative attends with both. ★★ **Depth walks the real ⊕ tree** — `MonitorEngine`'s parent relation, the same structure `holarchy::escalate` climbs; escalation goes up, attention goes down, and **no parallel tree was introduced** (children are derived from the existing parent chain rather than stored twice). ★ **And the footprint reconciles with UI-1 in one sentence**: pawa bounds the **scan**, `K ≈ 4` bounds the **hold** — *anything can scan widely and hold four; nothing can hold widely.* ★★ **The counterweight is the reference's and it is real:** the cheap-scan/costly-look split is **its own working discipline**, documented in `operatives/base.py` — *"`should_evaluate()` is ALWAYS cheap: threshold check only, zero LLM calls"* — and honoured across 27 hits. That is §V's economy, and it is why the reference's operative layer is affordable at all. What was missing is the **declaration**: the discipline is a comment and a naming convention, so nothing stops a future operative doing expensive work in the cheap half and nothing says how wide or deep a scan may go. **This row made the shape a type and the width a bounded quantity; it did not discover the shape.** ★★★ **The greps are worth carrying, because one is the sharpest false positive in the whole set by count:** `attention` returns **51 hits in the reference and zero about this** — every one is **needs-attention**, the Monitor's queue, which is the *object* of attention rather than the faculty; `narrow` and `broad` return **42 between them, all English adjectives**; `resolution` returns 15, none the `ρ` axis. ★ Named alongside them is the one that is **real** — `should_evaluate` — so it is not overlooked while the noisy zeros are dismissed. **Residuals:** ★★ `B_att` is **a number, not a balance** — the cost function and bound are real, `κ` is **declared and uncalibrated** (the `pawa_meter` position, in the same words), and wiring it to a juul balance is parked: **the cost discipline is this slice, the money is not**. ★★ The stance boundary `b > d·ρ` is a **declared heuristic**, named as one. ★ The path descends into the **first** child, so a most-urgent-child descent is a real follow-on. ★ Broad reports the **first** frame change rather than enumerating all of them. ★★ And **nothing calls attention from an operative yet** — `ω` still has no `attention` field, for exactly the reason it had no `Π` slot: *a declared-but-inert field reads as built*. **→ Two bundle rows left, both reference-less: OPV-6 (learning) and OPV-11 (`M_self`/`M_world`).**

★★★ **OPV-11 LANDS — and STEP-0 changed what the row WAS** (`models.rs`). ★★★ **`M_world`'s engine already existed here, in two pieces**: `tenet::TransitionModel` is `S × T → Δ(S)`, and — the one that matters — **`ensemble::ModelTemplate` + `Scenario` ARE already §VII's declared-structure / learned-residual split**: the template declares *which edges exist*, the scenario binds *the probabilities on them*, `instantiate` combines. So `WorldModel` is a **framing** that adds exactly two things, a **fidelity claim** and an **identity** — and the identity is the load-bearing one, because it is what makes the correlation below **detectable at all**. This row did not build a second engine and does not claim to; saying which parts are new **is** its substance. ★★★ **The sharp question — how does an operative model a world it structurally cannot HOLD — resolved structurally rather than answered rhetorically.** `M_world` is `Σ̂`, an **approximate copy**: `Ŝ`/`V̂`/`T̂` are **authored** and merely *consistent with* `Shared`, never a handle on it, so the operative never holds the world it models. `agrees_with` is therefore a **report** — `imagined` (moves the model believes in that `T` does not have) and `unmodelled` (moves `T` has that the model is blind to) — and deliberately **not a constructor guard**: a model is *allowed* to be wrong, because §VII's whole claim is that it is wrong *in ways it cannot notice*, and a guard would hide precisely that by refusing to build it. ★ `blind_to(schema)` names the dimensions outside `Ŝ` as **unregulated** — Ashby's remainder, stated rather than implied. ★★ **`M_self` composes `ω`'s own parts rather than forking them**: `SelfModel::of(&Operative, &StrategyGraph, &Attention, boundary)` reads objectives off `u`, moves off `Π`, budget off `⟨narrow, broad⟩` — nothing copied that could drift, so *consistent with `ω`* is a property of the construction and not a test that could rot. ★★★ **And CELL's recursion is proven, not asserted**: `moves_within(&Shared)` holds because `Π`'s nodes were minted from `Shared` in the first place (OPV-4's `LegalMove`), so the inner Sustain is bounded by the outer world **by construction** — the same Prop-1 mechanism one level further in. ★★★ **THE KEYSTONE — the shared-model correlation, made honest.** `EffectiveN::{Independent(n), BoundedAbove{headcount, shared_model}}`, with `Agreement::independent(n)` **taking no model** and `Agreement::via_shared_model(n, &m)` **structurally unable to yield `Independent`**. So *five councillors who forked one `M_world` agreeing* and *five independent observers agreeing* are **different values of different shapes**: the masquerade is **unspellable** rather than discouraged, and the same headcount reads differently **by provenance alone** — the `Capability`/`VisualSpec`/`Score` mechanism applied to a claim about evidence. ★★★ **And no number is fabricated.** `BoundedAbove` carries the headcount and the model's id and **no estimate**, because the shortfall depends on *how wrong the shared model is*, which **nobody inside it can measure** — the `Skew::Unknown` / `Soundness::Unavailable` / `UrgencyBasis::Undeclared` third-answer discipline, arriving now at the arithmetic of agreement. Forking gives independence of **sampling**, not of **assumptions**, and the correlation **does not dilute with headcount**: 2, 20 or 200 councillors are all still one model, and the test asserts exactly that. ★★ **The counterweight is real and the finding is too.** `council.py` aggregates as **`weighted_yes = Σ confidence for YES votes`** — summing confidences across councillors is precisely the arithmetic §VII names as the masquerade. But the reference **forks per councillor first** (Slice 7.3, unique fork ids, none shared): it has the *sampling* independence, and it built that **before** anyone framed the problem; and it **never claims** assumption independence anywhere, so the shortfall there is **unnamed rather than misstated** — a shape worth naming, not an alarm. ★★ **Greps carried so nobody re-derives them:** `M_self`, `M_world`, `conant` and `ashby` are all **grep-0**; `fidelity` returns **6 hits, every one unrelated English**; `effective` returns **10, likewise**; and `model` returns very many real hits, **none of them about this** — they are ML, ORM and data-model uses, which makes it the row's sharpest false positive by count. **Residuals, each named rather than left to be discovered:** ★★ `structure_is_declared()` **is always `true`** — that is the row's honesty, not a stub: structure is *never* revised by data here, and a method that could return `false` would imply a learning path that does not exist. ★★ **The fidelity claim is declared, never measured** — `Fidelity::claimed` demands a **non-empty basis** and a value in `[0,1]`, and **the gate never reads it**: `execute` takes no model, so a claim cannot reach an admission, structurally rather than by promise. ★ `Projection` has **no value-only accessor** — `reported() -> (f64, usize)` hands back the horizon *with* the number, so a projection cannot be quoted without the horizon it is only valid over. ★★ **Nothing wires either model onto `ω` yet**, the same restraint as `Π` and `attention`: *a declared-but-inert field reads as built*. ★ **`⊕̂` is not modelled** — a model of composition is a further row. **→ One bundle row left, and it is reference-less too: OPV-6 (learning).**

★★★ **OPV-6 CLOSES THE DEEP BUNDLE — AND THE FOUR-PHASE PLAN** (`learning.rs`). ★★★ **STEP-0's sharp question was the whole scoping decision, and it came straight off the previous row**: OPV-11 shipped `WorldModel::structure_is_declared()` returning `true` for every value **because no learning path existed**. OPV-6 builds the learning path — and it had to be established that doing so does not falsify the previous row. It does not, because the two are about **different objects**: a world model's **causal structure stays declared** (only the residual — a `Scenario`'s probabilities — is bound from data, and rewriting `T̂` from observations is a far larger claim than §VII makes), while the **strategy library** — the memes, each of which is a `Π` from OPV-4 — is exactly what §IV's vary/select/retain moves. **Learning changes the operative, not the world it believes in**, and `structure_is_declared` is still `true` after this row. ★★★ **Proposition 3 survives variation, by inheritance rather than a second check written beside it**: `vary` rebuilds a strategy through the **same public constructors**, so every node still holds a `LegalMove` minted from `Shared` and there is no path anywhere in the module that builds a node from a bare string. An `Extend` naming a move outside `T` comes back in `Varied::refused` as `MoveNotInT` — **reported, not dropped**, which is how *variation cannot widen `T`* is observable rather than merely true — and the positive half asserts the variant's move set is a subset of `Shared::moves()` **itself**, not of a copy. ★★ **`select` returns a FRONTIER, never a winner.** `u_i` is a vector and OPV-3 deliberately gave `Utility` no scalar method — `scalarise` is a free function that exists to demonstrate what collapsing deletes. So selection returns `pareto_frontier` over the viable variants. OPV-29 calls showing only the top-ranked option *a presentation bug with the consequences of an architecture bug*; here it would be an **architecture bug outright**, because the survivor is what the library **keeps**. ★★ **The venue is the real sandbox** — `run_with_effect(..., EffectClass::Sandbox)`, the full §4E gate, §X's fork-and-replay under the *same* rules, added as a small delegating variant so every prior caller of `run` behaves byte-for-byte as before. A variant that would breach the household's own `liquid_non_negative` comes back `Fitness::Refused` and never reaches the frontier: **a bad score, not an error the caller has to handle**. ★ A run that finishes but leaves a dimension `u_i` reads absent is `Fitness::Unscorable` — **no score, not zero** — matching `Utility::at`'s own refusal to substitute a number nobody supplied, and the `Skew::Unknown` / `Soundness::Unavailable` / `UrgencyBasis::Undeclared` / `EffectiveN::BoundedAbove` family one more time. ★ **Isolation is by construction, not by mechanism**: this core is pure, the walk gets a clone, the result is read and dropped — nothing was protected from a write because there was no write to protect from, and that is said plainly rather than claimed as a sandbox feature. ★★ **Steps happen on RESULTS, not on a clock, and structurally**: `Feedback` has exactly §IV's three variants and **no `Scheduled`**; `learn` takes one by value with no overload that omits it, so a host with a scheduler still has to **name the result it is reacting to**. ADR-0001 makes it doubly true — the core has no clock to trigger a round with. ★ And `Feedback::from_projection` returns **`None` when the projection and the observation agree**: *a learning step with no triggering result is a no-op*, said by the constructor rather than left to discipline. A divergence carries the **horizon**, because OPV-11's `Projection` cannot be quoted without one and a divergence quoted without its horizon is not a fact. ★★★ **"Operatives are attractors" is INSTRUMENTED, NEVER ASSERTED.** §4G.1 reads operatives as attractors of the iteration; there is no theorem here that it converges, that its fixed points are unique, or that reaching one is good — **the evolutionary literature supplies the mechanism, Sustena supplies the scoring, and neither supplies a guarantee**. So `Stabilisation::proves_convergence()` returns `false` for **every** value, present so the claim can be asserted **absent** rather than merely left unwritten — the same device as `structure_is_declared` always returning `true`, one row earlier. What *is* built is the instrument: `LearningTrace` records the rounds, `stabilisation()` reports trailing rounds that changed nothing (with **`Unobserved`** as the third answer — *nothing has been observed* is not *nothing changed*), `retained_fitness()` answers whether the survivors actually scored better, and `triggers()` reads the results-not-a-clock rule back off the record. ★★★ **No Free Lunch, made structural rather than warned about.** Averaged over all objectives every search performs identically (Wolpert–Macready), so a library is worth exactly **this** household's non-uniformity. `MemeLibrary` is scoped to one `(sustain, operative)` pair; `LibraryScope` has **no `Default`**, because a scopeless library would be the global shared pool NFL says cannot exist; `Meme` has **no public constructor**; and `MemeProvenance` is **minted only by the library**, so `import` is the only door for a foreign strategy and it **always** stamps `Imported { origin }`. **A stranger's meme cannot be written down as `Authored`** — the `Capability` / `Score` / `VisualSpec` mechanism applied to a claim about where a strategy came from. ★ Importing from your own scope is refused (`SameScope`: nothing crossed a boundary, so calling it an import would be a lie about provenance), and an imported meme **drops the donor's `Fitness`** — a score earned against another household's problem distribution is not a score here, so a borrowed meme arrives **unproven** and has to earn its place in this library's own rounds. ★★ **The counterweight is real, and the nil-counterweight reading would have been wrong: the reference RUNS this loop one layer down.** `parse_rule_learn.py` + `parse_rule_proposer.py` + `add_parse_rule`/`modify_parse_rule`/`retire_parse_rule` are a genuine vary/verify/retain over **ParseRules** — `synthesize_rule_from_correction` is deterministic template synthesis, **explicitly not an LLM**, the same discipline this row adopted for its own (ADR-0001) reason; `verify_proposed_rule` is a real two-condition `V(r)` with **both** required; and `parse_rule_edits` (`active`/`superseded`/`retired`) is **append-only versioned** — a correction is a new version, never a silent overwrite. ★★ And it is genuinely **feedback-triggered**: the round fires from a human's confirmed capture correction, never from a schedule. *The results-not-a-clock discipline is the reference's own practice; this row declared it rather than discovering it.* ★★★ **The finding is what it has no analogue of, and it is the two things the article is sharpest about**: (1) **selection there is a corpus regression check, not a fitness** — does the candidate match its own example, does it steal another rule's — so nothing runs a candidate **under the gate** and asks what state it produced, and `select_{u,Viab}` has no counterpart; the venue this row uses exists in the reference for proposals and is simply not pointed at learning. (2) **There is no NFL scope**: `parse_rule_edits` is not per-Sustain, so a rule learned from one household's SMS enters a shared rule set, and `ParseRule.provenance` is a plain **string** field a caller can set to anything — including to whatever makes an imported rule read as native. ★ The reference is also honest about its own gap: `propose_and_verify(generator=None)` ships with **no generator wired** — the mechanism-exists-search-does-not position, disclosed rather than papered over, which is the same posture this row takes toward convergence. ★★ **Greps carried, and one is the sharpest false positive of the whole session by count**: `vary`, `meme`, `evolutionary`, `holland`, `wolpert`, `attractor`, `fitness` and *no free lunch* are all **grep-0** — but **`variant` returns 135 hits and every single one is the substring inside `invariant`** (`InvariantSpec`, `invariants`, the binding/advisory authority flag). `library` (32) is the Arena package-marketplace panel; `provenance` (45) is `ParseRule.provenance` and `/orchie/balance-provenance`; `select_` (7) is SQLAlchemy's `select_from`; `retain` (1) is an ingest docstring about storage. ★ **A real defect caught by a test rather than by inspection**: a variant's id was the parent plus the op's *label*, so two `Optimise` ops on the same node and parameter minted the **same id** — two genuinely different variants silently conflated, one scored and the other reported *already in the library*. Fixed at the root: the id is `parent/index:label`, unique **by construction**, and the label now carries the **value**, because *retuned `amount`* and *retuned `amount` to 250* are different facts and only the second says what was learned. **An identity and a description are different jobs**, and this is what happens when one value does both. ★ **Naming**: `LearningRound`, not `Round` — `consensus::Round` is a Paxos ballot, a genuinely different thing to confuse a learning step with. **Thirty-first collision**; the newcomer takes the longer name. And `vary`/`select`/`retain`/`learn` are deliberately **not re-exported at the crate root**: four of the most generic verbs in the language, and `retain` is already `Vec::retain` to every Rust reader — `learning::vary(...)` says which vary it is. **Residuals, each named rather than left to be found:** ★ the three vary operators are the article's three and there is **no crossover** between two memes, a real evolutionary operator this row does not implement; ★ `retain` is **append-only** — nothing is ever removed from a library, so a meme that stopped winning is still there to be read, and **forgetting is a separate decision from learning** which this row does not make; ★★ **extending at the exit moves the exit**, a chosen semantic rather than a derived one — the alternative left the new node unreachable, growing `moves()` without changing any behaviour, which is the inert-addition failure this build refuses everywhere else; ★ selection scores the **end state only**, so two variants reaching the same state score identically regardless of the path; ★★ **nothing wires a meme library onto `ω` yet** — the fourth deliberate omission in this bundle for the same stated reason, *a declared-but-inert field reads as built*; and ★ **no cost is charged for a round**, though `select` runs every variant through the sandbox, which OPV-7's `B_att` and the pawa meter would both have opinions about — parked with `κ` and the juul balance. **→ THE DEEP BUNDLE IS CLOSED, M-OPV HAS NO UNSTARTED ROWS, AND THE FOUR-PHASE PLAN IS DONE.** What remains across the build is **declared residual and app-layer work**, all of it already named: the UI-11 `ℐ` core slice and EVT-15's un-wired `EnzymeCall`, which want the **same missing Rust-side consumer**; threading `Authorization`/`EffectClass` off `Unchecked` onto strategy nodes, for which **IMM-7's capability is the natural fit**; wiring `Π` / `attention` / `M_self` / `M_world` / the meme library onto `ω`, now **one honest follow-on rather than five**; the economy (`κ`, `B_att`, juul spending, UI-12) parked as one block; and the app-layer rows IMM-11/12, UI-10/UI-11, UI-14/UI-15.

★★★ **FOLLOW-ON WIRING 1 — `ω` IS ASSEMBLED** (`agent.rs`, 2026-08-18). The first of the three named follow-ons, and the biggest. ★★★ **STEP-0's whole point is that the discipline now runs in REVERSE.** Four slices left a faculty off `Operative` with the identical comment — *'follow-ons ... have no placeholder here, because **a declared-but-inert field reads as built**'* — and all four now exist, so the omission's reason has expired. But that same sentence governs the wiring: a field `ω` merely stores, that no method reads, would be **the exact defect the omissions were dodging, committed instead of dodged**. So the acceptance test is not *is the field there* but *is it read on a real path*. ★★★ **Which is why the faculties are REQUIRED, not OPTIONAL.** Adding `attention: Option<Attention>` and `library: Option<MemeLibrary>` to `Operative` would have made every method that needs them fail at runtime on a missing field — a guard, and a field that may or may not be built, which is the inert shape in a different costume. Instead `Agent` composes `⟨Operative, Attention, MemeLibrary⟩` with **no `Option`, no `Default`, no partially-assembled constructor**: an agent without attention would be OPV-7's half-mind and is unrepresentable, and `Operative` stays exactly the boundary OPV-1 built. ★★★ **`Agent` holds NO `Shared`, so Prop 1 and Prop 2 are untouched** — the world is an argument to every method that needs it, never a field. Proven **behaviourally rather than by inspection**: the *same* agent handed a **narrower** `Shared` refuses a variation naming a move that world lacks, and then admits it when handed the wider one — which could not happen if the agent carried a world of its own. ★★ **STEP-0 also settled the dispatch reconcile: there was no operative-level `decide`/`evaluate`/`should_evaluate` in the Rust core to wire into.** What exists is `mixture::Mixture::route` → `Dispatch::run(f)`, where the router picks *which* operative acts and `f` is applied to the selected tickets and to nothing else — and what `f` DID was entirely the caller's business. So `Agent::act` **is** what such an `f` calls, and `Agent::operative()` hands back the `&Operative` a `Contender` borrows. Two halves of one path, not a parallel loop; nothing in `agent.rs` routes, for the reason `strategy`'s own header gives — *a strategy that also routed would be choosing its own author*. ★★ **ONE PATH, THREE FACULTIES, IN ORDER.** `Agent::act` (1) scans the real `⊕` tree with the **narrow** aperture and ranks what it reached by `d(s,V)` via `narrow_scan` — `Score` still has no public constructor, so an agent cannot mint one and the urgency stays the household's; (2) runs **broad** attention outside the task's branch, and ★ **a fired `Reframing` ENDS the turn before `Π` runs**; (3) writes the top-scoring focus into the strategy's own `trigger_event` and runs the meme through `strategy::run`, node by node through the real gate. So attention **filters and parameterises** what `Π` sees rather than being computed beside it. ★ **The stop-on-reframe is a chosen semantic and is named as one**: the alternative was to compute a `Reframing` and drop it on the floor, which would make broad attention a faculty that runs and changes nothing — precisely the inert shape this slice exists to avoid — and reasoning inside a frame you have just been told is wrong is the failure mode OPV-7's two output types exist to prevent. ★★ **`act` adds no bypass**: with the household's own `liquid_non_negative` armed, a meme that would take the balance negative is **refused mid-walk** and the turn reports it. The agent chooses *whether* and *about what* to run, never *whether the gate applies*. ★★★ **Learning and reasoning are the SAME object.** `Agent::learn_from` runs a full vary/select/retain round over the agent's own scoped library, and the meme a round retained is runnable by the very next `act` — **no reload, no re-registration, no sync step that could be forgotten**. That is what makes the wiring real rather than two features that happen to sit on one struct. ★★ **`M_self` is a DERIVATION, never a field** — `Agent::self_model` calls `SelfModel::of` on the agent's own parts, so it cannot drift from the library it describes, and it returns `None` for a meme the agent does not hold, because *a self-model of a strategy you do not have is not a model of yourself*. ★★★ And the chain survives assembly: a **learned** meme's `M_self` still satisfies `moves_within(&Shared)` — Prop 1 (minting) → Prop 3 (nodes hold `LegalMove`) → variation (rebuilt through the same constructors) → `M_self`, unbroken, now through the assembled agent. ★★★ **`M_world` went to `Omega`, not to the agent** — §VII is explicit that there is **one per household**, forked per councillor, so it hangs on the population that already *owns the one world* and `Agent` has **no `WorldModel` field at all**. That is the claim, not a gap: a private world-model per operative would contradict the article outright *and* would make the shared-model correlation undetectable, since the correlation is precisely what happens when several operatives reason through the **same** model. ★ The `Option` there is honest and is **read, not merely stored**: `Omega::agreement_over(headcount)` returns `EffectiveN::BoundedAbove` when a model is declared and `EffectiveN::Independent` when one genuinely is not — so **OPV-11's keystone now fires off real population state** rather than a hand-built `Agreement`. ★ **Naming**: `Reasoning`, not `Act` — `ooda::OodaPhase::Act` is the *do it* phase after a human authorised, a genuinely different thing to confuse a reasoning turn with. **Thirty-second collision**; the newcomer takes the different name. ★★ **Divergence: none to grep.** This slice adds no new law, it connects four already-built ones, so there is no article mechanism and no Python parity — and the counterweight is the **omission comments themselves**, which named in advance the exact defect this slice could commit. A harder standard than a parity grep, because it is specific. **Residuals, named:** ★★ `Agent` is a **separate type** from `Operative` rather than a widened one — chosen so the faculties can be required rather than optional, at the cost of two names for what the article calls one thing; ★ the router still takes `&Operative`, so a caller wires `Agent::operative()` into a `Contender` and calls `Agent::act` from the `Dispatch::run` closure — **no `route_agents` convenience exists, deliberately**, because inventing one before a caller needs it is how a parallel path starts; ★★ attention's scan needs a `MonitorEngine`, which is **host state**, so `consider`/`act` take it as an argument for the same reason the agent holds no `Shared`; ★ the focus threaded into `Π` is the **single** worst-ranked sustain, where UI-1's `K ≈ 4` bounds a **held set** — threading the set is a real follow-on; ★ `learn_from` **does not consult `M_world`** — `Feedback::PredictionDiverged` exists and an `Omega`-level helper that builds one from a household projection is the natural next connection, and nothing here fabricates it; ★★ and **no agent is ever constructed by the core** — assembling one is the host's job, exactly as declaring a `WorldModel` is. **→ Two follow-ons left: (2) threading `Authorization`/`EffectClass` off `Unchecked` onto strategy nodes — IMM-7's capability, and *a strategy IS the deputy it was built for*; and (3) UI-11's `ℐ` + EVT-15's un-wired `EnzymeCall`, which are one Rust-side consumer.**

★★★ **FOLLOW-ON WIRING 2 — A STRATEGY NOW RUNS UNDER AN ATTENUATED CAPABILITY, AND OPV-4'S `Unchecked` RESIDUAL IS DISCHARGED** (`agent.rs` + `strategy::run_under`, 2026-08-18). The second of the three named follow-ons, and a direct consequence of the first: wiring 1 made the gap *visible* by giving `ω` a reasoning path at all. ★★★ **STEP-0 confirmed the gap in code before anything was designed**: `strategy::run_with_effect` passed `&Authorization::Unchecked` to `execute_admitted` on **every** node, and `Agent::act` called it — so an agent reasoned **outside the very authorization conjunct IMM-7 was built for**. OPV-4 had named this exact residual when it shipped: *"authorization and effects are passed `Unchecked` ... the honest residual to name, since **a strategy is exactly the deputy IMM-7's capability was built for**."* This slice is that sentence, built. ★★★ **And it is the confused deputy in its designed place.** A strategy acts on a household's authority over inputs it did not choose — a trigger, a meme, a human's correction — which is Hardy's compiler-billing-the-wrong-account exactly. `Authorization::Capability` is the variant built for it and carries **no `&Memberships`**: the ambient set is not merely unconsulted, it is **absent**, so a strategy cannot fall back on the issuer's wider authority even by mistake. Miller–Yee–Shapiro's *no ambient authority*, as a property of the type rather than a rule somebody keeps. ★★★ **`Agent::act` takes the `Warrant` as a REQUIRED parameter**, so *there is no route to `Unchecked` through an `Agent` at all* — and the compatibility half is asserted rather than assumed: `run` and `run_with_effect` delegate to the new `run_under` with `Unchecked`, so every caller written before this behaves **byte-for-byte** as it did and the R1 parity vectors are untouched. The structural claim is precisely scoped, and a test says so. ★★★ **LEAST PRIVILEGE BY PROVENANCE — where OPV-6 meets IMM-7.** `Agent::authority_for` attenuates the household's capability for **one meme**, two narrowings, neither optional: `Rights::Only(that meme's own moves)` — least privilege even for an authored strategy, since a strategy gets what its own nodes name and **never the household's ambient rights** — plus a tier ceiling from `MemeTrustPolicy`, keyed on the meme's `MemeProvenance`. So an **imported** meme runs under `TIER_CONTRIBUTOR` where an authored one holds `TIER_OWNER`, and since `budget.*` needs `TIER_MEMBER` it is **refused on tier alone** while the authored copy commits. That is IMM-10's discipline for a learned parse rule, applied to a learned strategy — and NFL's own reasoning read as an authority rule: a meme built against somebody else's problem distribution should not act on yours with full force. ★ `MemeTrustPolicy` is a **deliberate mirror** of `learned::TrustPolicy` — declare all three, **no `Default`**, because a policy with a hole in it fails open somewhere — and is named separately rather than reusing it, because a meme is not a rule and `RuleTrust`'s three levels answer a different question. ★★ **NOTHING WAS REBUILT**: `Capability`, `Attenuation` and `Rights` are IMM-7's, unchanged; a strategy is simply a new **holder**, exactly as a learned parse rule is. ★★★ **TWO REAL REFUSALS, and the stronger one is first.** At **attenuation**: a strategy naming a move the household's own capability does not carry is `Amplification::Rights` — `attenuate` refuses to widen, so **no warrant is issued at all** and the strategy cannot run, which is strictly stronger than handing it authority and stopping it node by node. At the **gate**: a warrant cut for meme *A* running meme *B* refuses with `capability_carries`, and a warrant aimed at another household refuses with `NotDesignated` — the redesignation refusal, the deputy's own move, which `Attenuation` also makes **unspellable** by carrying no sustain at all. ★ Both gate refusals are tested with the out-of-rights node **first**, so `state` is asserted **byte-identical** to before the turn: refused, not rolled back. ★★ **THE `EffectClass` RECONCILE.** `select` (OPV-6) already ran `Sandbox` — confirmed, unchanged. `Agent::act` runs `Sandbox` too, and that is a **structural claim rather than a shortcut**: a live effect is admitted only by an `ApprovalToken`, which is bound to **one** `(operator, params)` `Binding` and is single-use via the nonce ledger — and a strategy is **many** nodes. One token cannot admit a walk, `act` takes none, and there is no way to hand it one, so **an agent structurally cannot fire a live effect**. Making a strategy's outcome real stays the human's approve-and-execute path (OPV-30/31), one act at a time — the same discipline EVT-15 keeps for replay (*do not ship a replay that could re-fire a payment*). The two paths now differ in **authority**, not in effect class: scoring a candidate is done on the caller's own terms, while an agent reasoning for real is answerable to a warrant. ★ **PROP 3 AND THE TWO CONTAINMENTS HOLD**, and they nest rather than compete: a node is still a `LegalMove` in `T` at **build** (unchanged), and now also within the warrant's rights at the **gate** — the same nested pair UI-2 and IMM-10 already use. `T` says what could ever be legal here; the warrant says what *this* strategy may do on *this* authority. ★★ **Divergence: internal wiring, and the counterweight is threefold and specific** — OPV-4's residual **predicted this slice and named the fix**; IMM-7's `Capability` was **built for it** (unforgeable, no `Deserialize`, proven by a `compile_fail` doctest, attenuation-only, redesignation unspellable); and the confused-deputy citation is the article's own. The gap was precisely that the strategy path passed `Unchecked`, and nothing else. ★ **Explicitly NOT in scope**: this threads a capability **through** the strategy run; it does **not** issue capabilities to operatives from a central authority — that is a policy layer, and inventing one here would be unrequested machinery. Where a warrant comes from is named as the boundary: the household grants it via `Capability::issue`, which consults a **real membership edge** and therefore *transfers* authority rather than minting it, and `authority_for` only ever narrows what it was handed. ★ **Naming**: `WarrantError`, not `AuthorityError` — `editing::AuthorityError` answers whether a *definition edit* was authorised, a different question. **Thirty-third collision**; the newcomer takes the different name. **Residuals, named:** ★★ `run`/`run_with_effect` **keep** `Unchecked` for parity, so the claim is *no route through an `Agent`*, not *no `Unchecked` anywhere*; ★★ the warrant is cut **per meme, not per turn**, so a host must **re-cut after a learning round** — a warrant cut for the parent correctly refuses the varied child that added a move, which is the right default but is work the caller owns; ★ `MemeTrustPolicy` has three levels because `MemeProvenance` has three variants, so it does **not** distinguish *which* household a meme was imported from; ★★ **the core issues no capability** — `Capability::issue` needs host-held `Memberships`, exactly as assembling an `Agent` and declaring a `WorldModel` are host jobs; and ★ `learn_from` still runs under `select`'s own terms rather than a warrant, because threading one into `select` would make a variant's **fitness** depend on the authority of whoever asked — a different and much larger claim. **→ One follow-on left: UI-11's `ℐ` + EVT-15's un-wired `EnzymeCall`, which want the same missing Rust-side consumer and are therefore one slice.**

★★★ **FOLLOW-ON WIRING 3 — THE DETERMINISTIC ENZYME-CALL PROPOSER, AND THE FOLLOW-ON SET IS COMPLETE** (`enzyme.rs`, 2026-08-18). ★★★ **STEP-0's question was whether two residuals were really one consumer, and UI-11's own row answers it**: *"queue it as a real core slice **when a Rust-side caller exists** — the natural one is an Enzyme-call proposer over `Definition` + `Registry`, **which is also what EVT-15's `EnzymeCall` residual would consume**. Until then it stays app-layer, and the reason is a missing consumer rather than a missing capability."* And the EVT-15 side checks out by grep: `EnzymeCall` had **no producer at all** — every one in the crate was hand-built in a test module. One piece, wanted twice, built once. ★★★ **But the discharge is scoped honestly, because EVT-15 asked for a DIRECTION this does not build.** Its residual is worded *"not wired to `execute` (nothing yet **records** an `EnzymeCall` when an operator runs)"* — a **recorder** over the host's call log. This slice builds the **proposer**: description → candidate call. That gives `EnzymeCall` a real producer and gives `replay_under` something it was not handed by hand, which is the half that was blocking UI-11; it does **not** make `execute` write a call log, because that log is **host state**, exactly like the capture history UI-11 itself named as app-layer. **Producer ✅, recorder ⬜**, and both are written into the row rather than one being claimed for the other. ★★★ **`θ` IS DECLARED HERE WHERE THE REFERENCE INTROSPECTS — a real difference, named rather than smoothed over.** UI-11's row says *θ from the Enzyme's real `inspect.signature` so never more fields than it declares*. A Rust operator is an `OperatorFn` reading a `Map<String, Value>` **by name** — there is **no signature to introspect** — so the honest analogue is a declaration, and `OperatorMeta` gained `params: Vec<ParamDecl>` (filled for the four real `budget.*` operators; every other registration declares none). The trade goes both ways and both directions are stated: a declaration **can** disagree with the body, which introspection cannot; but a declaration **is** the contract, where an introspected signature is only an accident of how the body happened to be written. ★★ **And the property the row actually cares about survives either way**: a proposal can never carry a field the operator does not declare, because the builder only ever writes names it read off the declaration — asserted directly against the registry's own list. ★ An operator declaring **no** params is not a lie but a statement, and the proposer answers `CannotInfer` naming exactly that rather than guessing a shape from a body it cannot see. ★★★ **POCKET MATCHING GENERALISED RATHER THAN PORTED.** The reference hardcodes matching against pockets. Here a parameter declares `names_within: Some("finances.pockets")` and the proposer matches candidate values against **the keys actually present at that path in this state** — so the core never learns what a pocket is, and any sustain that names a collection gets the same behaviour for free. Same declared-rather-than-baked-in move as `Region`'s weights or an invariant's authority. ★★ **ALL THREE OF UI-11's RULES ARE KEPT, AND EACH IS A DISTINCT OUTCOME.** *A hint that eliminates every candidate is treated as **no hint*** — 'something happened' narrows to zero, so the proposer falls back to the **full** candidate set and **asks**, rather than reporting that nothing matches. *History may pre-fill but **never silently*** — a value from `known` is listed in `from_history` and an inferred one is not, so a caller can always say which. *Never skips the confirm* — the output is a **candidate**: `propose_call` runs no operator, touches no state (asserted byte-identical), and turning it real stays the human-gated approval path follow-on wiring 2 made structural. ★ **And pre-fill is not a bypass**: a remembered pocket that has since been renamed away is **rejected**, not proposed. ★★ **Ambiguous versus missing are different answers**: an unnamed pocket returns `NeedsDisambiguation` whose options are **this sustain's own** pocket names — values a human can actually tap — while a missing amount returns the same variant with **empty** options, because manufacturing a tap-list of plausible numbers would be exactly the invention this refuses. ★★ **A REAL DESIGN FLAW A TEST CAUGHT, NOT INSPECTION.** The first version narrowed operator candidates using both the operator's **name segments** and words from its own **description**. *'spend 50 from food'* then matched `budget.allocate` as well as `budget.spend` — because `budget.allocate`'s description contains the word **"from"**. A description is prose written for a human; using it as a matching vocabulary turns ordinary English into operator hints, which is precisely the accidental inference this module exists to refuse. Fixed at the root — **the name is the contract, the prose is not** — and a regression test asserts the trap is still present in the prose so the test keeps meaning something. ★★ **Reuse, not reinvention, in every direction**: the output is EVT-15's own `EnzymeCall` (not a parallel type); the operator set is `Definition::operators ∩ Registry` (so an operator outside the definition is **rejected**, even when stated as a known fact); the parameter shape is `OperatorMeta::params`; and live values come from `State::get`'s own dot-path walk, the same reader `Region` uses. ★ **Naming**: `propose_call`, not `propose` — `consensus::propose` is a Paxos ballot, **thirty-fourth collision**; and `CallProposal`, not `Proposal` — `operative::Proposal` is what an operative puts forward, **thirty-fifth**. ★★ **Divergence: reference-less, and the counterweight is the two residuals themselves.** The reference's `ℐ` is app-layer (`effect_capture.py`) by the ING rows' own decision, so there is no parity half to grep. But the residuals are a **stronger** counterweight than a grep would have been: they were written **in advance**, by the slices that declined to build this early, and between them they named the caller, its inputs (*a `Definition`, a `Registry` and a state value, all of which this core already holds*) and the rules it had to keep. **Residuals, named:** ★★ the app-layer **feed** stays app-layer — the transducer and the capture history arrive as an `Effect`'s `text` and `known`, two plain values, so what ported is the **inference**, not the plumbing, and UI-10 is untouched; ★★ EVT-15's **recorder half is not built**; ★ number extraction is deliberately dull (the first token that parses as `f64`, no currency grammar — that belongs to the transducer); ★★ operator narrowing is **exact-token, not fuzzy** — no stemming, synonyms or edit distance, so a description without a name segment falls through and asks, which is honest but means a real household needs the app layer's richer matching in front of it; ★ **one question at a time** — the first unresolved field in declaration order, matching the reference's own round-trip shape; and ★★ **nothing here commits**. **→ THE FOLLOW-ON WIRING SET IS COMPLETE.** All three shipped in the order they were named: `ω` assembled (1), reasoning under a warrant (2), and the Enzyme-call proposer (3). What the whole build has left is **the economy, parked as one block** (`κ`, `B_att`, juul spending, UI-12) and **app-layer rows** (IMM-11/12, UI-10, UI-14/UI-15) — plus EVT-15's recorder half and the smaller residuals each slice named in its own row. There is no unstarted core mechanism left in the plan.

★★★ **THE ECONOMY LAYER OPENS — PAWA-1, THE PAWA METER, IS BUILT IN RUST** (`pawa.rs`, 2026-08-18). ★★★ **The hard boundary first, because every slice in this layer is governed by it and this is the easiest place to keep it (ADR-0001 D5; PAWA-13 states it):** the **software** — meter, ledger, gating, royalty settlement, treasury accounting, governance — is buildable now on a single host as **INTERNAL ACCOUNTING**. **Issuing a real, public, transferable token is a distinct, later, human-authorised act and is OUT OF SCOPE.** Juul is a utility unit tracked internally: never real money, never a real transfer, never a payment rail, and the egress hard boundary holds alongside it. **`pawa.rs` keeps that promise structurally rather than by intention: it has NO BALANCE AT ALL** — no debit, no credit, no ledger — and a test asserts that recording a reading changes nothing anywhere. This is the **odometer, not the gas pump**. ★★★ **STEP-0, part 1 — parity with `pawa_meter.py`, term by term.** The Python meter is real and shipped (PAWA-1 ✅ app-layer, Slice 15), so this is an **R1-style parity port**, not an R2 spec row. Ported exactly: `pawa = κ_c·compute + κ_s·storage`; `compute = mutation_count + event_count + constraint_eval_count`; `storage` = **real bytes durably added**, the serialized event payloads plus the serialized mutations — which is precisely the reference's `len(payload_json) + len(mutations_json)`, and the test asserts it equals that sum computed independently so it cannot drift into an approximation; `κ_c = 1.0`, `κ_s = 0.01`; and **wall-clock deliberately excluded** from the formula. ★ The `constraint_eval_count` distinction ports intact and it is a **measurement** question rather than bookkeeping: an operator's own declared pre- and post-conditions **always** count (they are always evaluated), while the sustain's invariants count **only when the enforcement gate actually ran** — a sustain that has not armed enforcement genuinely had **zero** invariant checks performed against this call, and counting them would be *measuring a check that never happened*. Asserted both ways, and the armed case is shown to raise a real run's compute by exactly the two invariants checked. ★★★ **STEP-0, part 2 — the DECLARED cost and the MEASURED one are different things and they stay apart.** `compose::Enzyme::pawa_cost: u32` (and `OperatorMeta::pawa_cost`) is the **author's declared estimate**: static, known before the run, and it **composes by summation** so a pipeline's declared cost is the sum of its parts'. The meter is the **measured reality**: dynamic, known only after a real run, different whenever the work is different. The reference keeps exactly this split and neither replaces the other — so rather than pick one, `PawaReading::declared()` carries the author's number **beside** the measurement, making the gap a **readable finding** rather than a discrepancy the meter silently resolved. The test asserts a real case where the declared `0` was an underestimate. ★★★ **AND THE RUST VERSION IS STRUCTURALLY STRONGER THAN THE REFERENCE.** There, metering sits inside an `if result.succeeded:` branch — correct, and a **discipline somebody has to keep**. Here `PawaReading` has **NO PUBLIC CONSTRUCTOR**, and `meter()` is the only way to obtain one and returns `None` for anything that did not commit. So *a refusal meters nothing* is a **property of the type**: a reading for a run that did no work is **unconstructible**, not merely unreached — the `Capability` / `Score` / `VisualSpec` mechanism, applied to a claim about cost. Asserted twice: `meter()` on a gate-refused run is `None`, and a `Meter` is **byte-identical** before and after an attempted refused run. ★★ **REPRODUCIBILITY, which is the whole reason wall-clock is excluded.** An identical run meters an identical pawa — compute, storage and price all equal — so a reading is comparable across runs, across machines and across time. And the exclusion is made **observable** rather than merely intended: two readings differing **only** in `elapsed_ms` price identically, and a reading with no host-supplied elapsed reports `None` rather than inventing a zero. ★ ADR-0001 makes it doubly true: this core has **no clock**, so `at` is host-supplied for the same reason `EffectClass::Live` carries a `now` — the core cannot mint a time and therefore cannot price one. ★★ **κ IS DECLARED AND EXPLICITLY UNCALIBRATED.** `KAPPA_COMPUTE = 1.0` and `KAPPA_STORAGE = 0.01` have **not been fitted** to any real infrastructure cost model. Storage being priced far more granularly than a compute unit is a **shape** argument — it matches how real systems meter storage against operation count — and a shape argument is not a calibration. **No calibration was invented to make the row look finished**: the same declined-import honesty as OPV-7's `κ`, `Fidelity`'s declared-never-measured claim, and the Kalman decline. ★ **Honest absence, one more time:** `Meter::stats_for` returns `None` for an operator that has never run, and an empty meter returns `None` for everything — because **zero is a measurement** (a read-only operator can genuinely cost little) and *never measured* is not. A per-sustain total of `0.0` for an unmetered sustain is fine and different in kind: that is a **sum over an empty set**, not a claim about an unmeasured operator. ★ **What is Rust wiring rather than parity:** the reference meters inside `SustainEngine.execute_operator` off its own engine internals; this core has no engine and no events table, so `meter()` takes the `Execution` value `execute_admitted` already returns, plus the operator's `OperatorMeta` and the call's `Enforcement`. ★ **Naming**: `PawaReading`, not `Reading` — `detect::Reading` is a monitor's observation of a dimension, a genuinely different thing to confuse a cost measurement with. **Thirty-sixth collision**; the newcomer takes the longer name. **Residuals, named:** ★★ **nothing here touches a balance** — PAWA-2's ledger is the next slice and PAWA-3's gate clause the one after, and **both are still internal accounting**; ★ the compute proxy is a **count, not a cost model** — one mutation and one constraint evaluation price identically, which is almost certainly wrong in reality and is exactly what a calibration would fix; ★★ `Meter` is **in-memory**, because durable metering storage is the host's exactly as the event log is; and ★★ **nothing calls `meter()` from `execute_admitted` yet**, deliberately — wiring it into the gate would make every existing call site meter whether its host wanted a meter or not, and the host owns durable recording anyway. **→ Next: PAWA-2, the juul ledger — `balance ← balance − pawa`, and still internal accounting.**

★★★ **PAWA-2 — THE JUUL LEDGER** (`juul.rs`, 2026-08-18). The balance PAWA-1's readings are charged against, and the first module in the build that holds a balance at all. ★★★ **So the hard boundary is stated first and kept STRUCTURALLY, not promised.** A juul balance is an **internal accounting fact on a single host** — never real money, never a **transferable asset**, never a payment rail; issuing a real, public, transferable token is a distinct, later, human-authorised act (ADR-0001 D5, PAWA-13). This module has **NO TRANSFER** — there is no method that moves juul between principals, and a `charge` has **no counterparty at all**; **NO MINT** — nothing creates juul except `credit`, which *declares* an opening balance and is deliberately named apart from a debit, with genesis at PAWA-7 and issuance at PAWA-8; and **NO RAIL** — no I/O anywhere, by ADR-0001. Asserted rather than asserted-in-prose: charging one principal leaves every other balance **untouched**, and total circulation **falls by exactly the pawa spent** rather than the juul turning up somewhere else. ★★★ **A DEBIT CANNOT EXIST WITHOUT A MEASUREMENT BEHIND IT — the structural question the PAWA-1 report left open, answered by the signature rather than by a check.** `charge` takes a **`PawaReading`**, not a number. `PawaReading` has no public constructor and its only source is `pawa::meter` over an `Execution` that **committed** — so a debit is always backed by a real, measured, genuinely-committed run. **There is no `debit(amount)` in the module**, so a charge cannot be invented; the principal charged comes **off the reading**, so a caller cannot redirect the bill; and every debit carries the operator, sustain and timestamp of the run that incurred it, so it is traceable back to its cause. ★★ **The affordability rule, and it is the LEDGER'S rather than the gate's.** `balance ← balance − pawa`, admitted only if `balance ≥ pawa` — `≥` and not `>`, so a balance exactly covering a run spends to zero, which is a real boundary and is pinned by its own test rather than left to the reader. `Insufficient` is an **honest refusal, not an error**: it carries both numbers so a caller can say *how much short*, and it appends **nothing** — the whole ledger is asserted **byte-identical** afterwards, the same refusal-changes-nothing discipline every prior slice keeps. ★ And it **never clamps to zero**: clamping would silently lose the overspend *and* report a debit that did not happen. An unfunded principal reads `0.0` — correctly, because that is a **sum over an empty set** rather than a claim about anything unmeasured — and therefore cannot afford a non-zero run. ★★ **THE BALANCE IS A FOLD, NEVER A MUTABLE NUMBER.** Exactly the `state = fold(events)` discipline one layer up: an **append-only** `Entry` list, summed on read, with **no stored balance field that could drift**. So `rebuild() == balance_of()` is true **by construction** rather than kept true by care, and a host's durable copy reloaded through `with_entries` is asserted **equal** to the original — the same core-holds-the-decision / host-holds-the-durability split `NonceLedger::with_spent` uses. ★ `Credit` and `Debit` are **distinct variants** rather than a signed delta column, so *where did this juul come from* stays answerable, and `debits_of` never counts a credit as one. ★★★ **A FINDING THE BRIEF DID NOT HAVE, AND THE STEP-0 RECONCILE TURNED IT UP.** The brief said `PawaLedger.charge()` is scaffolded with zero callers — **exactly right**, grep-confirmed, its only other mention in the entire reference is a docstring in `pawa_meter.py`. **But `deduct()` — the method `charge()` itself calls — HAS FOUR REAL CALLERS**: `budget.py`, `calendar.py`, `procurement.py` and `tasks.py` each call `ctx.pawa.deduct(...)` on every operator run. So the reference **does** have a wired debit path. ★★★ What it charges is **`meta.pawa_cost` — the author's static declared estimate** — and every one of those operators declares `pawa_cost = 0`, so the live path debits **nothing** on every call; `budget.py`'s own docstring says so outright (*'For budget operators (pawa_cost=0) this is always a no-op ... the pattern is established for paid operators in later epics'*). **That is PAWA-1's static-versus-measured split showing up as a real consequence: the reference's live debit path is wired to the ESTIMATE, not to the MEASUREMENT, and the meter's real, non-zero readings reach no ledger at all.** Named because it is precisely the wire this slice's `charge(&PawaReading)` signature makes impossible to get wrong. ★ **Parity credited honestly where it is due, rather than claimed as a sharpening:** the reference's `get_balance` is **already a fold** — `select(func.sum(pawa_ledger.c.delta))` over an append-only delta table — so the fold-based balance is **parity, not this row's invention**, and it is recorded as such. Also at parity: a per-principal balance, an insufficient-balance refusal that **returns rather than raises**, and zero-cost calls succeeding trivially. The genuine sharpenings are the **reading-backed debit** and the **credit/debit variant split**. ★ **Deliberately NOT ported:** `charge()`'s **70/20/5/5 royalty split**. That is PAWA-5/MYC-5's concern; it distributes to a contributor, a treasury, a referrer and a validator — **every share of which is a transfer** — and porting it would have breached the hard boundary in the very first module that touches a balance. ★ **Naming**: `JuulLedger`, not `Ledger` — `consensus::Ledger` is a Paxos acceptor set and `approval::NonceLedger` records spent approval nonces. Three ledgers, three genuinely different jobs. **Thirty-seventh collision**; the newcomer takes the longer name. **Residuals, named:** ★★ **nothing is wired into the gate** — `execute_admitted` neither meters nor charges, and PAWA-3's `balance ≥ pawa` conjunct on `admit` is the next row; ★★ `credit` is an **ungated declaration** — any caller can declare any opening balance, because *who may* is PAWA-7's and PAWA-8's question, and naming it apart from a debit is exactly what keeps that gap visible rather than buried inside a signed number; ★ the ledger is **in-memory**, durability being the host's as the event log and the meter are; ★ balances are **`f64`**, matching the meter's real-valued pawa — a fixed-point or integer-minor-unit representation is a real decision that belongs with the calibration of κ, not before it; and ★★ **there is no revocation or reversal** — the ledger is append-only, so a correction would be a compensating entry, which is a row nobody has written yet. **→ Next: PAWA-3, the out-of-pawa gate clause — `admit = guard ∧ constraints ∧ token_ok ∧ balance ≥ pawa`, one more conjunct on the existing seam.**

★★★ **PAWA-3 — THE OUT-OF-PAWA GATE CLAUSE, AND THE GAS LOOP CLOSES** (`execute_afforded` + `juul::Affordability`, 2026-08-18). `admit = guard ∧ permitted ∧ result∈A ∧ D ∧ F ∧ closure ∧ token ∧ **balance ≥ pawa**` — one more conjunct on the existing seam. ★★★ **STEP-0 settled the design fork by reading the gate rather than arguing about it.** `execute_admitted` applies the effect to a COPY (`let mut working = State::new(state.clone())`), takes `let candidate = working.snapshot()`, and checks closure, the invariants, the post-conditions, `D` and `F` against it — so **by the time the commit block is reached, the candidate's mutations and events already exist**. They had to: `D` is evaluated over the (before, after) PAIR and `F` over the declared movements. **The run's real cost is therefore knowable at gate time at no extra work**, and `candidate_pawa` returns **exactly the number the committed `PawaReading` will carry** — asserted equal rather than assumed, because admitting on one price and charging another would be a real defect. To make that equality structural rather than coincidental, `storage_bytes` was refactored to take the two collections instead of an `Execution`, so the candidate and the reading price through the **identical code**; two copies of that arithmetic would be two numbers that could disagree. ★★★ **PRICED AGAINST THE MEASUREMENT, NEVER THE ESTIMATE — and PAWA-2's own finding is why that matters.** `meta.pawa_cost` is not consulted by the clause. Pricing against it is the reference's wrong path: PAWA-2's STEP-0 found `deduct()` charging exactly that, and it is `0` for every operator that calls it, so **an estimate-priced clause never refuses**. A dedicated test asserts both halves of the trap — `budget.record_income` really does declare `pawa_cost = 0`, and its real metered cost really is greater than zero — then funds a principal with **half the real cost** and watches the gate refuse. An estimate-priced gate would have admitted it. ★★ **UNAFFORDABLE ⇒ REFUSE AND DISCARD.** No commit, no state change, and **no juul spent**: the ledger is asserted **byte-identical** after a refused attempt, with mutations and events empty and state untouched. *Refusal charges nothing* now joins *refusal meters nothing* rather than contradicting it — the same rule the advisory, capture, egress and metering slices each keep. The refusal carries its own reason, **`insufficient_pawa`**, distinct from guard, invariant, post-condition, `D`, `F`, closure and token refusals, because collapsing them would tell whoever reads the log to fix the wrong thing. ★ **Placed before the token redemption**, deliberately: being turned back on cost must leave an approval **unspent**, which is the courtesy `execute_admitted`'s own comment already grants an invariant refusal (*'being turned back by an invariant should leave the person free to fix the state and use the same approval, not burn it'*). ★★ **AFFORDABLE ⇒ COMMIT, THEN CHARGE, EXACTLY ONCE** — against the real committed `PawaReading` rather than the pre-commit number. They are equal by construction, but the **ledger entry is the committed one**, so every debit traces to a run that actually happened. Asserted: **one** debit (not zero, not two), the balance is opening minus the real cost, and the ledger's `rebuild() == balance_of()` still holds afterwards, so the debit went through the same append-only fold every other entry does. ★ And there are **two independent reasons a refused run cannot be charged**: the clause refuses before commit, *and* `meter()` returns `None` for anything uncommitted — a test covers a run turned back by an earlier conjunct (`params.amount > 0`) and finds the ledger byte-identical there too. ★★ **ADDITIVE AND OPT-IN-SAFE, BY THE DEVICE THIS BUILD HAS USED TWICE ALREADY.** `execute_admitted` and `execute` delegate to the new `execute_afforded` with **`Affordability::Unmetered`**, so every caller written before PAWA-3 and every R1 parity vector behaves **byte-for-byte** as it did — asserted directly by running the same call through both paths and comparing state and mutations. The same delegating shape as `run → run_with_effect → run_under` for the strategy walker. ★ And `Unmetered` is a **vacuous** clause, not a fail-closed one: **a host that has not opted into an economy has not said everyone has zero juul, it has said there is no economy to price against** — the same reading `Enforcement::enabled` gets for the invariant gate, and the same naming logic as `Authorization::Unchecked` and `EffectClass::Unchecked` (*a bypass that reads as ordinary is the problem; one that has to be spelled out is not*). ★★ **Divergence: the seam is at parity, the correctness is rust-ahead.** The reference's `_charge_pawa` already runs on the write path and returns an `OperatorResult.fail('Insufficient pawa: ...')` when `deduct` reports False — so the **shape** of an affordability refusal exists there and this row did not invent it. What it charges is `meta.pawa_cost`, `0` for every caller, so `deduct` returns True immediately and **the clause never fires**; `budget.py`'s own docstring says so. Naming both halves rather than claiming the whole thing as new. **Residuals, named:** ★★ the commit-time `charge` **cannot be `Insufficient` by construction** — the identical cost was checked against the identical balance a few lines above and nothing interleaves — so its return value is deliberately not re-handled, and the candidate/reading equality test is what would break first if that ever stopped being true; ★★ `Affordability::Metered` borrows the ledger **mutably for the call**, so one call charges one ledger, and concurrent or interleaved charging is a host concern (this core has no threads, by ADR-0001); ★ the clause prices **the whole run, not per node** — a strategy walk charges each node separately because each is its own `execute` call, which is right, but means a walk cannot be refused up-front on its **total** cost, and a budget over a whole walk would be a further row; and ★★ **nothing credits anyone** — the debit still has no counterparty, so PAWA-3 changes nothing about the hard boundary: **the clause refuses a run, it moves no value**. **→ Next: PAWA-5 + MYC-5, the royalty flow — the first slice in the layer with a TRANSFER in it, and therefore the first where the hard boundary needs restating rather than merely holding.**

★★★ **PAWA-5 + MYC-5 — CONTRIBUTION AS A PROGRAMMABLE FLOW, WITH THE RATIFIED TWO-REVENUE-TYPE SCHEDULE** (`royalty.rs`, 2026-08-18). ★★★ **The first slice in the economy layer with a TRANSFER in it — so the hard boundary had to be restated structurally rather than inherited.** Every prior module kept it by having **no transfer at all**; that is no longer available, so the distinction between an internal reallocation and a real payment is made three ways, each a property of the code. **(1) A transfer only ever moves juul between `JuulLedger` balances** — internal accounting, never a settlement instruction to anything outside this process. **(2) `ΣΔ = 0`, by the shape of the value**: a royalty is a set of `Entry::Transfer`s, and that variant's `circulation_delta` is `0.0` **by construction**, because it is **ONE ENTRY CARRYING BOTH ENDS** — a `TransferOut`/`TransferIn` pair would be two records a caller could write only one of, and `JuulLedger::with_entries` accepts arbitrary entries, so the pair would have been a real hole. Contrast a **mint**, the only thing that would raise the total: it is a **different variant that does not exist yet** (PAWA-7 genesis, PAWA-8 issuance). **(3) ★★★ The no-rail property stays**: there is **no code path from a transfer to real value** — no cash-out, no redemption, no external counterparty, no juul→anything bridge. **Juul is not redeemable, and that absence IS the distinction** — an internal reallocation and a real payment differ here by the **total non-existence of the second thing**. Proven the way the egress slice proved its own boundary: by **reading the source** of `royalty.rs`, `juul.rs` and `pawa.rs` for cash-out and payment-rail vocabulary. ★ Scanned over **production code only** — the test half split off, comment lines stripped — because *documenting* the boundary (**juul is not redeemable**) is not a breach of it, and a search that trips on its own prose proves nothing. ★★★ **STEP-0: the reference's four-way split is SUPERSEDED, not a port target.** `PawaLedger.charge()` runs the earlier `70/20/5/5` across contributor / treasury / referrer / validator and has **zero callers** — confirmed in PAWA-2's own STEP-0, where its only other mention in the whole reference was a docstring. MYC-5's row says the articles now all carry the ratified schedule and that migrating the code is an explicit **Track-3 migration**. So the five-way is built fresh and the old one is **recorded as superseded** rather than quietly replaced. ★★★ **THE RATIFIED SCHEDULE, term by term against the row.** Five recipients in the row's own order — **contributor / treasury / validator / proposer / referrer** — and **two revenue types**, because paying to **run** something is not paying to **have** it: a **pawa charge (usage)** splits `70/15/5/5/5`, a **licence sale (access)** splits `80/10/3/2/5`. The schedule adds the **proposer** share and **separates the revenue types**, neither of which the earlier single four-way could express. Read the contributor's 70 % as what it is economically: **the price the commons pays for variation** — selection can only act on variation that exists. ★★★ **CONSERVATION IS A THEOREM, NOT A CHECK BOLTED ON.** Each share is an integer floor of `amount · rate`, and **the remainder is assigned to the validator share** — so `Σ shares == amount` **exactly, for every amount**, because the leftover is **not discarded, it is where the last share comes from**. Tested adversarially, where an integer split leaks if the remainder is dropped: 1, 2, 3, 7, 11, 13, 17, 19, 23, 97, 101, 997, 1009, 7919, 65537, 999983 — under **both** schedules. ★ A rounding leak here would be a **conservation-invariant violation**, not a cosmetic bug, which is exactly why the arithmetic is **integral**: exact conservation over floats is not achievable and claiming it would be false. So the split is computed in `u64` — where the theorem is true — and the integer-valued shares enter the `f64` ledger; PAWA-2's residual already named `f64` balances as a decision belonging with κ's calibration, and this row confines the exactness claim to the domain where it holds rather than pre-empting that. ★★★ **AND THE CASE THE REFERENCE HAS NO ANSWER FOR IS HANDLED.** A single-process run genuinely has **no validator** — there is no second node to validate anything — and a contribution often has no proposer and no referrer. Dropping those shares would break conservation **silently**, so they are not dropped: **an absent role's share is folded into the TREASURY**, by a declared rule. The treasury is the right destination because it **is** the commons — a share nobody is there to receive returns to the pool everyone draws on. ★ The redirection happens **after** the remainder is assigned, so it is a reallocation among the same five buckets and the sum is untouched by construction; a dedicated test runs with **every** optional role absent, across both schedules, and still asserts exactness. ★★ **COST AND ROYALTY ARE TWO DIFFERENT FLOWS AND STAY APART.** `pawa(o)` — PAWA-1's measurement, charged by PAWA-3's gate clause — is a **cost**: no counterparty, and it **leaves circulation**. A royalty is a **transfer**: five counterparties, and circulation **unchanged**, asserted directly and cross-checked against the independent `rebuild()` fold so it is not an artefact of how the total is computed. `settle` does the second only; the gate's charge is untouched by this row. ★ **Free vs Royalty**: a `Free` licence transfers **nothing** and the ledger is asserted **byte-identical** — *free means free of royalty, never free of the work it causes*, since the run still pays its own cost through the gate. ★ And an unaffordable royalty is refused **wholesale**: paying the first recipients and running out would leave the split **unconserved against its own schedule**, so affordability is checked before anything moves. ★★ **What is at parity and what is the sharpening**, kept apart: **at parity** is the *concept* — that a run of a contributed thing pays its contributor, with a treasury slice bridging the private economy and the public commons; the reference expresses that and this row did not invent it. **The sharpenings** are the ratified schedule itself (five recipients, two revenue types), conservation **stated, tested adversarially and extended to the absent-role case**, and the conserved transfer **as a type** rather than as a sequence of separate `deduct`/`_write` calls that could diverge. ★ **Naming**: `RoyaltyRole`, not `Role` — `division::Role` is a division-of-labour role, a genuinely different question from who is owed a share. **Thirty-eighth collision**. **Residuals, named:** ★★ **the treasury is a named principal, not yet a Sustain** — it holds a balance like anyone else; `Σ_T` with its own viable region, reserve floor and category caps is **PAWA-6**, and the boundary is named rather than pre-empted; ★★ **`settle` is not wired into the gate** — PAWA-3 charges the cost on commit, and nothing yet calls `settle` from `execute_afforded`, because *which contribution a run is attributable to* is a question this core cannot answer alone (a `Licence` hangs on a contribution, and attributing a run to one is the Arena's registry, not the operator's); ★ **the royalty base is supplied by the caller** — deriving it from the run's own pawa is the natural wiring and is left to whoever does it; ★ **no rate ceiling is enforced** — a `per_mille` above 1000 computes a royalty larger than the amount, which the affordability check then refuses; a declared ceiling is a **governance** question (PAWA-9/10), not an arithmetic one; and ★★ **nothing here mints** — the only variant that raises circulation does not exist, deliberately. **→ Next: PAWA-6, the treasury as a Sustain `Σ_T` — which turns this row's named treasury principal into a real Sustain with `allocate`/`grant`/`disburse` and a reserve floor in its own `V_T`.**

★★★ **PAWA-6 — THE TREASURY IS ITSELF A SUSTAIN, `Σ_T`** (`treasury.rs`, 2026-08-18). ★★★ **The row's whole point is that it needed NO NEW MACHINERY, and that claim is the deliverable.** A treasury is a `Definition` (schema + `V_T` + allowed operators) plus a state, exactly as a household is. Its `allocate` / `grant` / `disburse` are ordinary `OperatorMeta` values registered on the ordinary `Registry`, with declared `params` shapes like any other operator; **every one goes through the same `execute_admitted`**; `V_T` is checked by **the same enforcement gate**; a disbursement needs **the same `ApprovalToken`**. There is **no treasury engine, no treasury branch inside the gate, and nothing anywhere in `execute_admitted` that knows a treasury exists**. ★★ And the recursion is **demonstrated rather than argued**: a treasury call replays through EVT-15's `replay_under`, which takes a `Definition` and a `Registry` and has no idea what a treasury is — so *a treasury can be simulated and governed exactly as a household can* is a fact about the code rather than a sentence about it. ★★ **THE RESERVE FLOOR IS AN ORDINARY INVARIANT.** Because `V_T` is a set of predicates, the floor is literally `treasury.balance >= <floor>` in the definition's own invariant list — and a payout that would breach it is refused with **`enforcement_gate`**, *the same reason and the same shape a household invariant breach produces*. **No floor-checking code was written**, which is the evidence that no bespoke mechanism exists. Category caps are the same device (`treasury.allocations.x <= n`), and a test asserts both refuse identically. ★★★ **ONE TRUTH FOR THE JUUL, and this was the sharpest design question of the row.** The `JuulLedger` is authoritative. The Sustain's state carries `treasury.balance`, but it is **never independently mutated**: every outflow operator **requires** a `ledger_balance` parameter and writes `ledger_balance − amount`, so the state figure is a **same-call projection** of the ledger and cannot drift from it across calls — asserted directly, by checking the candidate state's balance equals `ledger.balance_of(treasury)` after a real payout. ★★ And the two halves move together or not at all: **`pay_out` runs the gate AND performs the transfer, and performs the transfer only if the gate admitted.** A refusal leaves both untouched, so **there is no rollback path to get wrong** — because there is only one function that moves either. ★★★ **OUTFLOWS ARE CONSERVED TRANSFERS, NEVER MINTS.** A grant is a `JuulLedger::transfer` from the treasury's balance to a recipient's: `ΣΔ = 0`, circulation asserted unchanged, and treasury-plus-recipient equal to the original total. The treasury **cannot pay out more than it holds** — refused by its own guard with `treasury_solvent` before the ledger is ever asked, *and* the ledger would refuse it too. **Two independent guards, neither redundant**: the operator's keeps the STATE honest, the ledger's keeps the BALANCE so. ★ **A boundary named rather than pre-empted:** MYC-6 draws a later, different distinction in which a **grant is a mint**. That is deliberately not this row — a minting grant would need an issuance policy nobody has declared, and genesis (PAWA-7) and issuance (PAWA-8) do not exist, so building one now would be inventing governance. **Nothing in this module raises the total.** ★★ **EVERY DISBURSEMENT IS HUMAN-GATED.** An outflow runs under `EffectClass::Live`, so it needs an `ApprovalToken` **bound to that one act and single-use**. A token bound to a *different* act — which is the same thing as having no token for this one — is refused with `approval_token`, the transfer is not attempted, and the ledger is asserted **byte-identical**. And a **replayed** token is refused with the ledger asserted identical to after the first, legitimate payout: *at-least-once delivery must not become pay-twice*. An agent or a strategy cannot disburse from the treasury on its own, for exactly the reason `Agent::act` cannot fire a live effect and egress cannot send without a confirm: **a real outflow stays the human's explicit act**. ★★★ **A REAL FINDING A TEST CAUGHT, NOT INSPECTION — and the fix was at the root.** A declared category cap over a category the state does not hold **refuses everything**. The cap is `treasury.allocations.grants <= 500`; with `allocations` empty the predicate compares `None` to a number, which is a **type error**, and the gate then turns back **every** operation on the treasury — including a grant that has nothing to do with that category. **Four tests failed on it at once.** Fixed by making the definition and the opening state agree: **a declared cap implies the category exists**, so `opening_state` takes the same cap list the definition declared and seeds each at zero. ★★ **Making the gate lenient would have been the wrong fix**, and that is worth stating: if an absent dimension were tolerated, a cap over a *typo'd* category would **silently never fire** — a rule that looks enforced and is not. This is the same class of problem UI-2's load-time typecheck exists for (a rule referencing a dimension the state does not hold), which is also why the residual below names that as the real remedy. ★★ **Divergence: the reference has an ID and nothing else.** `NETWORK_TREASURY_ID = "sustena.network_treasury"` is a string constant naming a principal that royalty shares are credited to. **At parity**: that a share of every charge accrues to a network treasury under a stable id — PAWA-5's `Recipients::treasury` is its counterpart. **The sharpenings**: the treasury as a real, gated **Sustain** with state, `V_T` and operators (the reference's has none, and nothing can leave it because there is no disbursement path at all); a **reserve floor that actually refuses**; and **every outflow human-gated**. ★ **Residuals, named:** ★★ **nothing checks that the definition and the opening state agree** — `opening_state` takes the caps by convention, and a caller passing a different list gets the refuse-everything behaviour above; a `typecheck` over `V_T`'s referenced paths against the schema is the real fix, and it is UI-2's own device applied to a definition rather than a widget; ★★ **the periodic budget is a declared number, not a mechanism** — nothing recurs, because ADR-0001 gives this core no clock, and EVT-11's `Recurrence` is the primitive a host would drive it from rather than a scheduler fabricated here; ★ **`φ_p` is declared and unconsumed** — no code reads the protocol fee fraction, because what charges it is the Arena's settlement rather than the treasury's own operators; ★ **the treasury is not yet composed** — `⊕` with a household is available in principle, since it is a Sustain like any other, and is not exercised here; and ★★ **`pay_out` takes the token as an argument** — the core cannot mint one (only the simulate→vote→approve chain can), so *who approves a disbursement* is the host's question, exactly as who approves any live effect is. **→ Next: PAWA-7 genesis + PAWA-11 governance — the first mint, and the rules for who may change the rules. Note that the layer currently has NO mint at all, which is why every conservation claim above holds trivially; PAWA-7 is where that stops being free.**

★★★ **PAWA-7 GENESIS + MYC-6 GRANTS-ARE-MINTS — THE LAYER'S FIRST MINT, AND MINTING MADE VISIBLE** (`juul.rs`, 2026-08-18). ★★★ **STEP-0 found the defect in OUR OWN code, not only the reference's, and that is the row.** PAWA-2 shipped `JuulLedger::credit(principal, amount, reason)` and named it an *ungated declaration* in its own residual. Read against MYC-6 that is **worse than ungated**: a credit **raised total circulation** — `Entry::Credit`'s `circulation_delta` was `+amount` — so **it was a mint, and an undeclared one**. MYC-6's whole point is that a **transfer is bounded by the payer and self-limiting** while a **mint is unbounded**: it inflates the unit and makes Sybil registration directly profitable. *Both are legitimate designs; only one of them is legitimate **undeclared**.* ★★★ **So `credit` is gone.** `Entry::Credit` became **`Entry::Mint`**, which **names the declaration that authorised it**, and the only way to produce one is `JuulLedger::from_genesis`. **Renaming it to what it actually was is the resolution** — there is now **no path anywhere in the core that raises circulation silently**, and every `credit` call site across `juul.rs`, `royalty.rs`, `treasury.rs` and three conformance binaries was migrated to a declared genesis rather than the finding being noted and left. ★★★ **ONE-TIME BY SHAPE, which is why genesis needed no governance and could ship before PAWA-11.** `from_genesis` is a **constructor**, and there is **no `mint` method at all**: you cannot mint into a ledger that already exists, so declaring a second genesis makes a **second ledger** and the first's circulation is asserted unchanged. Its answer to *who may mint* is therefore structural — **nobody, afterwards** — which is exactly the property an ongoing issuance schedule does **not** have and why PAWA-8 genuinely needs PAWA-11's governance rather than merely benefiting from it. ★ `JuulLedger::new()` remains and gives a ledger with **no juul at all**: the honest empty case, not a shortcut around genesis. ★★ **`GenesisId` has no public constructor**, so an `Entry::Mint` cannot be written down without a real declared `Genesis` behind it — the `Capability` / `Score` / `VisualSpec` / `PawaReading` mechanism, applied to *the authority to create money*. ★★ **GENESIS IS THE LEDGER'S BASE AND EVERYTHING SINCE IS THE FOLD.** For every principal, `balance == g.allocation_for(p) + Σ(non-mint entries affecting p)` — asserted **exactly**, so **no juul is unexplained**: genesis is the base, everything after it is the fold, and `rebuild()` still agrees with `balance_of` so the two ways of asking give one answer. ★★ **Auditable forever, and it NAMES what it finds.** `Genesis::audit` checks a ledger against the declaration and returns `foreign` / `mismatched` / `undeclared` rather than a boolean — because those three send a reader somewhere different: *this ledger was merged with another's*, *an allocation was not honoured*, *somebody minted outside the rules*. A clean audit means the money supply is **exactly** the declaration, asserted after real trading and real spending. ★★ **The accounting, stated exactly rather than implied**: `circulation = Σ(mints) − Σ(costs debited)`, with **transfers contributing zero**. Asserted end to end — a transfer leaves the total untouched, a charge lowers it, and the sum of every mint equals the genesis total, so *every juul in existence came from a declared mint*. ★ There is **no burn**; if one is ever added it gets **its own variant**, for exactly the reason a mint does. ★★★ **AND THE HARD BOUNDARY IS RESTATED WHERE THE WORD MOST INVITES CONFUSION: minting the INTERNAL unit is not issuing a real coin.** Genesis brings the internal accounting unit into existence inside one host's ledger. It creates **no real value and no redeemable claim**: juul is still not redeemable, there is still no cash-out and no rail, and issuing a real, public, transferable token remains a **distinct, later, human-authorised act** that is out of scope (ADR-0001 D5, PAWA-13). **Minting juul ≠ issuing a coin**, and a slice that introduces the word *mint* is exactly where that needs saying rather than assuming. ★★ **Divergence: the reference has a grant whose KIND IS UNDECLARED.** `ONBOARDING_GRANT = 100` — *'Pawa granted to new users on signup'* — is a declared **constant** with, grep-confirmed, **zero callers**. And its **kind is unstated**: nothing says whether granting 100 pawa to each new user **mints** them (raising supply per signup, precisely the Sybil-profitable case MYC-6 names) or **transfers** them from a treasury (bounded, self-limiting). MYC-6's own row says it: *the Onboarding Grant's kind must be declared before it ships*. **At parity**: the concept of a starting distribution. **The sharpening**: `Entry::Mint` as a distinct, visible variant naming its declaration — so **the ambiguity is not expressible here**, because a mint cannot be written as a credit. **Residuals, named:** ★★ `GenesisId` has no public constructor, but a holder of a `Genesis` could clone its id and forge a `Mint` through `with_entries` — **closed by detection rather than by construction**, which is exactly why `audit` exists and why it names what it finds; said plainly rather than overclaimed as structural; ★★ **there is no burn**, and one would be its own variant; ★ a **zero allocation is allowed** and produces a real `Mint` of `0.0`, because declaring somebody with nothing is a meaningful statement and suppressing it would lose them from the audit; and ★★ **nothing ties a genesis to a Sustain** — `Genesis` is a free-standing declaration rather than a spec field, and wiring it into a `Definition` is the natural follow-on, not fabricated here. **→ Next: PAWA-11 governance + PAWA-8 issuance, the ongoing-mint-under-governance pair — where *who may mint* stops being answerable by 'nobody, afterwards' and needs a declared, revisable schedule instead.**

★★★ **PAWA-11 — THE PARAMETERS ARE A DECLARED SUSTAIN; A PARAMETER CHANGE IS AN ENZYME** (`governance.rs`, 2026-08-18). Built before PAWA-8 because **issuance's whole safety property is that its schedule is changeable only through governance** — the machinery has to exist before the thing that depends on it. ★★★ **STEP-0, part 1: PAWA-6's treasury is the template, and it is reused rather than re-derived.** A governance Sustain is a `Definition` plus a state; `governance.set_parameter` is an ordinary `OperatorMeta` registered on the ordinary `Registry`; `V_gov`'s bounds are **ordinary invariants**; a change needs the **same** `ApprovalToken`; `enforcement_of` is the same function the treasury reuses; and a change **replays through `replay_under`**, which takes a `Definition` and knows nothing about governance. **No bespoke governance engine exists** — the second time this build has made that claim, and the second time it is checkable by looking at what was *not* written. ★★★ **STEP-0, part 2 — THE CONST RECONCILE, AND IT FORCED A DELETION.** `κ_c` and `κ_s` were module constants in `pawa.rs`: **the** value, read directly by `compute_pawa`, which `meter`, `candidate_pawa` and PAWA-3's affordability conjunct all called. A governed parameter **cannot also be a hardcoded const that silently overrides it** — that is precisely the two-sources-of-truth problem PAWA-6 solved for the treasury's balance, arriving one layer up. So the constants were **demoted** to `Parameters::GENESIS_KAPPA_COMPUTE` / `GENESIS_KAPPA_STORAGE` — *the value κ is **declared with** at genesis*, not *the value κ **has*** — and ★★★ **`pawa::compute_pawa` was REMOVED OUTRIGHT**: not deprecated, not kept for compatibility, not left as a convenience. Pricing now goes through `Parameters::price`, and `meter`, `candidate_pawa` and `Affordability::Metered` all carry the `&Parameters` in force. **There is no path left in the crate that prices from a constant** — one truth per parameter, enforced by deletion rather than by discipline, because a convenience function that still worked would be exactly the shadow this row exists to remove. ★ And the κ honesty from PAWA-1 is carried verbatim: **making a number revisable does not make it right**. `κ` is still uncalibrated; governance is the mechanism for fixing that once real infrastructure cost is known, not a substitute for knowing it. ★★ **A CHANGE IS GATED, RECORDED AND REPLAYABLE — all three, and each is tested.** *Gated*: an untokened change — a token bound to a **different** change, which is the same as having none for this one — is refused with `approval_token` and the state is asserted unchanged, because **a parameter change is a deliberate act**, not something an agent does alone (the same discipline that stops `Agent::act` firing a live effect and stops a strategy disbursing from the treasury). *Recorded*: a valid change appends exactly one `event.governance.parameter_changed` carrying **both the old and the new value**, so the event alone says what moved without needing the state before it. *Replayable*: three changes replayed through `replay_under` reconstruct the parameter's whole history and land on the last value — **a parameter's current value is genesis plus its changes, nothing else**. ★★ **BOUNDS ARE ORDINARY INVARIANTS.** Each `ParameterSpec`'s range becomes two invariants on the definition, so an out-of-range change is refused with **`enforcement_gate`** — the same reason a household breach gives, which is the evidence no bound-checking code was written. Both ends are tested: a **negative** coefficient would pay people to do work, and an **unbounded** one would let a single change price every future run out of reach. ★ A parameter **nobody declared cannot be invented** (`parameter_declared`), because bounds exist only for declared names — a change that *created* a name would be creating an ungoverned parameter, sidestepping `V_gov` entirely. ★ And a spec whose **genesis sits outside its own range** is refused at declaration time, since it would open in breach and refuse every subsequent change. ★★ **COVERAGE, STATED RATHER THAN IMPLIED — which is why this row is 🔨 and not ✅.** **Governed now, end to end**: `κ_c` and `κ_s` — declared with bounds, changed only through the gated Enzyme, and the value in force is what the meter and the gate actually price with. **Named as a follow-on, with its reason**: the ratified royalty schedule stays a constant in `royalty.rs`, because governing it needs a bound this row does not have — **the five shares must sum to 100** — and MYC-5's conservation theorem depends on exactly that. `ParameterSpec` bounds **one scalar between a min and a max**, so a five-share schedule with a *joint* constraint genuinely does not fit the shape built here: that makes it a follow-on rather than an oversight, and designing that invariant deliberately is better than half-wiring the schedule now. ★ The treasury's `φ_p` and `period_budget` were already declared in `Σ_T`'s own state (PAWA-6) rather than being constants, so they are governed by the treasury's own definition and need nothing here. ★★ **Divergence: the reference's economic parameters are module constants, ungoverned.** `KAPPA_COMPUTE = 1.0` and `KAPPA_STORAGE = 0.01` in `pawa_meter.py`; the royalty percentages hardcoded inside `PawaLedger.charge`. Nothing declares them as governed, nothing bounds them, nothing records a change — because **changing one means editing and redeploying the module**: unbounded, unrecorded, unattributable, invisible to any audit. **At parity**: the parameters exist, are declared at module level, and carry an honest docstring saying they are *not calibrated against any real infrastructure cost model* — that honesty is the reference's own and is kept verbatim here. **The sharpenings**: a change as a **gated Enzyme** rather than a redeploy; **bounds as ordinary invariants**; and **one truth enforced by deletion**. **Residuals, named:** ★★ **only two parameters are governed** — the mechanism is complete, its coverage is two; ★★ **a parameter is a scalar**, which is the same fact that makes the schedule a follow-on; ★ **nothing connects a change to a council** — the approval token is required and *who may approve one* is the host's question, exactly as it is for a treasury disbursement, with PAWA-9/10's voting rules their own rows; ★ **no change takes effect at a future time** — a committed change is in force immediately, because this core has no clock, and a scheduled change would lean on EVT-11's `Recurrence` rather than a fabricated scheduler; and ★★ **`Parameters::read` falls back to genesis** for a value the state does not carry, which is deliberate — an ungoverned deployment is running the **declared defaults**, not running nothing — but it does mean a **typo'd parameter name reads as the default rather than failing loudly**. The `parameter_declared` refusal covers the write path; the read path is the softer half, and it is named rather than glossed. **→ Next: PAWA-8, ongoing issuance — the governed mint, where *who may mint* stops being answerable by 'nobody, afterwards' and becomes a declared, revisable schedule that this row's machinery is what makes safe.**

★★★ **PAWA-8 — ONGOING ISSUANCE, THE GOVERNED MINT** (`issuance.rs`, 2026-08-18). The row PAWA-11 was built to make safe, and it shows: this slice wrote very little, because three earlier rows had already done the work. ★★★ **STEP-0, part 1: TWO MINTING PATHS, AND THEY STAY DISTINCT.** PAWA-7's `from_genesis` is a **constructor** with no `mint` method beside it — genesis is one-time *by shape*, and its answer to *who may mint* is **nobody, afterwards**. PAWA-8 is the **other** path: `JuulLedger::issue`, a method on a ledger that already exists, and deliberately **not a second genesis** — a genesis is a fixed value you read once and audit forever, whereas issuance is a **rule that keeps applying**, so it needs exactly the revision machinery a once-and-forever declaration does not want. The distinction is carried **in the entry itself**: `Entry::Mint` is still the one visible circulation-raiser (MYC-6's point stands), but it now names a **`MintAuthority`** — `Genesis(GenesisId)` or `Issued(IssuanceId)` — and **neither id has a public constructor**, so neither answer to *where did this juul come from* can be written down without a real declaration behind it. ★★ **And the audit had to move with it, which is a real consequence rather than a cosmetic one.** `Genesis::audit` previously treated any mint not naming *this* genesis as **foreign**. An issuance mint is not foreign — it is legitimate and governed — but **silently skipping it would leave juul unaccounted for** and break the audit's whole promise, while calling it foreign would report a governed mint as a defect. So `Audit` gained an **`issued`** list, and the honest statement became *exactly this genesis, **plus** this much issued under these declarations* rather than *exactly this genesis*. Both halves are asserted: the audit is still **clean** after a real issuance, **and** the issued total is **named**. ★★★ **STEP-0, part 2: THE SCHEDULE SHAPE — what is governed versus declared at spec.** PAWA-11's `ParameterSpec` bounds **one scalar between a min and a max**, which is precisely why the royalty schedule stayed a const one row ago. **The rate fits that shape exactly**: `issuance_rate` (juul per pawa served) is declared, bounded, and revisable **only** through the gated Enzyme — and `Schedule::Fixed` carries **no rate of its own**, so an issuance can only be priced from `Parameters`, the same one-truth discipline PAWA-11 enforced by deleting `compute_pawa`. **The schedule TYPE does not fit** and is declared at the spec instead: a **decaying** schedule needs an epoch or elapsed time to decay *against*, and this core has **no clock** (ADR-0001); a **target-rate** schedule needs the rate **derived** from current circulation against an inflation target, so the governed value would no longer be the rate at all — it would be a target, with the rate a function of ledger state that a static min/max on one path cannot bound. ★ So `Schedule` has **one variant, deliberately**, rather than three with two unimplemented: a declared-but-inert variant **reads as built**, which is the exact defect this codebase keeps naming. The fixed-rate governed case ships **whole**; decaying and target-rate are named follow-ons **with their reason**. ★★★ **DENOMINATED IN WORK, AND STRUCTURALLY SO.** `issue` takes a **`PawaReading`**, not an amount — the exact mirror of PAWA-2's `charge(&PawaReading)`, turned the other way round, and for the same reason: `PawaReading` has **no public constructor**, so the only way to obtain one is metering an `Execution` that **committed**. **There is no `mint(amount)` on the ledger**, so **there is no issuance without work behind it** — the issued amount is always `rate × pawa_served` over work that genuinely happened. ★ And the **caller pays while the server earns**: the reading's own principal is *who ran the operator and is charged for it*, the server is *whose machine did the compute*, so an issuance is **not a refund** — asserted as two different numbers landing on two different balances after a real charge and a real issuance. ★★ Which machine served a run is a fact the core **cannot know** (no I/O, by ADR-0001), so `server` is host-supplied exactly as `at` is; **on a single host there is one server** and it earns the whole issuance, with multi-host distribution named as PAWA-9 rather than gestured at. ★★★ **THE RATE IS GOVERNED, AND THAT IS THE WHOLE SAFETY PROPERTY.** Proven end to end rather than by inspection: the rate is switched on by a **real** `governance.set_parameter` call through the **real** gate under a **real** approval token; the parameters are then **read back out of the resulting state** — not from a const, not from the value that was passed in — and an issuance priced under them mints at that value, asserted as an **inequality** against the genesis pricing so nothing could be silently shadowing it. An **untokened** change is refused with `approval_token` and the refusal is asserted **through to the issued amount**, not merely at the gate; a rate above its declared ceiling is refused with `enforcement_gate`, the same reason a household breach gives. A change appends the ordinary `event.governance.parameter_changed` and **replays through `replay_under`**, which knows nothing about issuance either. ★ The ceiling itself is expressed **against a meaning rather than picked**: a rate of **1.0 is break-even**, where an issuance exactly replaces what the run burned, so the bound is *ten times break-even* — and an **unbounded** rate is precisely the Sybil-profitable inflation MYC-6 named. ★★ **ISSUANCE IS OFF AT GENESIS** (`rate = 0.0`), which is a statement and not a placeholder: an economy should not begin inflating because nobody chose a rate. A zero rate issues nothing **and appends nothing** — a zero mint would be a line in an append-only log that raised circulation by nothing and told a later reader nothing — so switching issuance on takes a deliberate, gated, recorded act. ★★ **THE EXTENDED CONSERVATION ACCOUNTING**, asserted as an equation over a real genesis, a real issuance, a real charge and a real royalty transfer: `circulation = Σ(genesis mints) + Σ(issuance mints) − Σ(costs debited)`, with transfers contributing **zero**. And `minted_total − issued_total == genesis.total()`, so the two mint terms are **separable** rather than merely summing correctly — issuance is the **second, and only other, circulation-raiser**. ★ Issued juul is **ordinary juul** once it exists: a PAWA-5 transfer of it moves the total by nothing. ★★★ **AND THE HARD BOUNDARY, RESTATED WHERE THE WORD MOST INVITES CONFUSION.** Issuance mints the **internal accounting unit** inside one host's ledger: **no real value, no redeemable claim, no cash-out, no rail** (ADR-0001 D5, PAWA-13). Re-proven by a source grep over `issuance.rs` and `juul.rs` **production code** — tests split off and comment lines stripped, and **said so in the test**, because a boundary check that trips on its own prose proves nothing and one quietly narrowed to pass would prove less. A slice about creating money is exactly where that line has to be said again rather than assumed. ★★ **DIVERGENCE: grep-0, rust-ahead.** `issuance|monetary|inflation|mint|emission` across the reference's `sustena/` returns **exactly one hit**, and it is a **named false positive** — `pawa.py`'s docstring says *"a bulk transaction issuance from the SQLite log"*, meaning batch writes, not money creation. There is no issuance mechanism, no monetary policy, no schedule and no rate anywhere. ★★ **What the reference does have is the cautionary case**, and both halves of it matter: `ONBOARDING_GRANT = 100` is **declared and read by nothing** — the only gesture at ongoing issuance does not actually issue — and **had it been wired** it would go through `PawaLedger.credit`, which raises a balance with no counterparty and is therefore an **undeclared mint**, structurally indistinguishable from a royalty credit in the same ledger, unbounded, and changeable only by editing the module. That is exactly the Sybil-profitable inflation MYC-6 warned of, and exactly why this row was sequenced **after** PAWA-11 rather than before. ★★★ **Counterweight — what issuance composes rather than invents**: **PAWA-1's meter** supplies the `PawaReading` (the measured work that earns it, and the reason there is no amount to invent); **PAWA-7's `Entry::Mint`** supplies the visible circulation-raiser (so an issuance is a mint and says so); **PAWA-11's governance** supplies the rate's revision path (gated, bounded, recorded, replayable). Adding a governed parameter was **one const, one accessor and one line** in `declared_parameters()` — no new Enzyme, no new gate, no bound-checking code — which is the strongest evidence either row can offer, for both at once. **Residuals, named:** ★★ **one server**; ★★ **one schedule shape**; ★ **nothing calls `issue` automatically** — the affordability conjunct is about the **caller's** balance and issuance is about the **server's**, and wiring the two together is a host-side policy decision this core should not make, so a host that never calls `issue` has an economy that **only ever shrinks**, which is the honest current default; ★ **no burn** (if one is added it gets its own variant, for the same reason a mint does); and ★★ **`Issuance` itself is ungoverned** — the *rate* is governed, but *which issuance declarations exist* is not, since a host holding the value decides that. **Closed by audit rather than by construction**, exactly as PAWA-7's forgeable-`Mint` residual is, and named the same way rather than overclaimed. **→ Next: B_att wiring — OPV-7's residual, tying the attention cost to a real budget; the last economy piece.**

★★★ **B_att — THE ATTENTION COST GETS A REAL BUDGET, AND TWO OPV-7 RESIDUALS CLOSE** (`attention.rs`, `agent.rs`, 2026-08-18). ★★★ **STEP-0 corrected the brief, which is why it runs first.** The framing offered was *the attention cost is a declared number with no budget behind it*. That was **half stale**: `Attention::declared` has always taken a budget and **refused** a pair whose combined `κ·b·d·ρ` exceeded it, so the cost function and a **declaration-time** bound were already real and enforced. What OPV-7's own recorded residual said — and it was precise — is that **`B_att` is a number, not a balance**: nothing was ever *spent*, so a second scan cost exactly what the first did and an operative could attend without limit. ★ A second recorded residual, *nothing calls attention from an operative yet*, had **already been closed** by the agent-wiring slice and was still sitting in the tracker as open. Both are corrected here rather than quietly dropped, because **a stale limit is worse than a missing one**. ★★★ **THE IDENTITY HOLDS BY CONSTRUCTION, NOT BY CHECK.** `spent` is the **fold** of an append-only spend list and `remaining` is `allocated − spent`, so `spent + remaining == allocated` is true **by the shape of the type** — there is no stored total that could drift from the spends that produced it, the same discipline as `state = fold(events)` and `JuulLedger`'s balance, one layer up. It is asserted as an **equation at every step** of a real sequence of spends and refusals, not spot-checked at the end, because an identity that only holds at the end is a coincidence. ★★★ **REFUSED, NEVER CLAMPED — AND THE REASON IS STRUCTURAL, NOT TASTE.** The obvious alternative is to shrink an axis until the aperture fits. It is rejected because `Aperture::stance` classifies by `b > d·ρ`, so **lowering `ρ` or `d` can flip a Narrow aperture to Broad** — and `Attention::declared` exists to refuse a pair that is **one region twice**, since *declaring the same aperture under two names is the partition of the council arriving through the back door*. A clamp could therefore **silently produce exactly the attention this module already forbids anyone from declaring**: unsound here, not merely unspecified. Proven by a case that takes a real two-region pair, lowers `ρ` by one, watches the stance **flip**, and confirms the resulting pair is refused as `NotTwoRegions` — a demonstration, not an argument. ★ And the operative is **told**: an unaffordable turn returns `Reasoning::AttentionUnaffordable` carrying both numbers — the **fifth honest outcome**, deliberately distinct from `NothingInView`, because an exhausted budget and an empty household are different facts about a turn and collapsing them would make them indistinguishable. *Silently over-served* and *silently under-served* are the same defect wearing different signs. ★★ **NO `spend(amount)`.** `AttentionBudget::spend` takes an **`Attention`**, which cannot be constructed except through `Attention::declared` — which already refuses a one-region pair and an unaffordable one — so every spend is backed by a real, well-formed attention and **a cost cannot be invented**. The exact mirror of `charge(&PawaReading)` and `issue(&PawaReading)` one layer down, and each record **names the attention it paid for**, so *what was this spent on* stays answerable. ★★ **CHARGED ONCE FOR THE PAIR, NOT ONCE PER APERTURE.** The two attentions are one mind's; billing them separately would let a turn afford narrow, spend it, and then discover it could not afford broad — leaving the operative with exactly the **half-mind** OPV-7's *two required fields, no `Option`* rule exists to prevent. Asserted as an exact figure: one turn, one spend, `narrow + broad`. ★★ **THE WIRING, NOT JUST THE TYPE.** `Agent::act_within` charges `B_att` **before either scan runs** — the order is deliberate, ahead of broad attention's word, because a turn that could not afford to look has not discovered that the frame holds, it has discovered **nothing**. `Agent::act` delegates with `AttentionSpending::Unbudgeted`, the **fourth** use of the delegating-variant compatibility pattern (`run`/`run_with_effect`/`run_under`, `execute_admitted`/`execute_afforded`, now `act`/`act_within`), so every prior caller and every R1 vector is byte-identical — and the refusal branch is asserted **unreachable** under `Unbudgeted` rather than assumed. `Unbudgeted` is named for the same reason `Unmetered` and `Unchecked` are: *a bypass that reads as ordinary is the problem; one that has to be spelled out is not*, and an absent budget makes the clause **vacuous** rather than failing closed to *attend to nothing*. ★★★ **AND `κ_att` IS NOW GOVERNED, WITH THE CONSTANT DELETED.** `attention::KAPPA` was a module constant read by `Aperture::cost()`. One row after PAWA-11 deleted `pawa::compute_pawa` and both its constants for exactly this reason, leaving a third in place would have been an inconsistency to explain away — so it was **demoted** to `Parameters::GENESIS_ATTENTION_KAPPA` and **`Aperture::cost()` removed outright** in favour of `cost_under(&Parameters)`. **No path prices an aperture from a constant.** The genesis value is `1.0`, *exactly* the constant it replaced, so every already-declared aperture costs what it always did and the entire pre-existing suite passes unchanged — which **is** the parity evidence. Proven end to end besides: a **real** gated `governance.set_parameter` doubles `κ_att`, the parameters are **read back out of the resulting state**, the same aperture costs twice as much (asserted as an inequality against the genesis pricing), and — the part that matters — **a budget that afforded an attention at genesis `κ` refuses it at the governed one**, so the coefficient reaches the *enforcement* and not merely the arithmetic. ★ **Two consequent deletions, the same one-truth move one level down.** `Scan.cost: usize` became `Scan.aperture` plus `cost_under`, because a stored cost would be the shadow constant this row exists to remove. And `SelfModel`'s `attention_budget: usize` field — which stored the **cost** and called it the **budget**, a naming defect that only became visible once a real budget existed — was **removed rather than renamed**, replaced by the attention itself plus `attention_cost_under`. ★ **WHAT IS DELIBERATELY NOT GOVERNED: THE ALLOCATION.** `κ_att` is governed because it is a coefficient of the cost *model* and one number is the right number. How much attention *this* operative gets *this* period is a **policy that legitimately differs between operatives**, so a single governed scalar would be the wrong shape — the same judgement PAWA-8 made about its `Schedule` type — and it is host-supplied, said rather than defaulted into a global number nobody chose. **Divergence: unchanged and reference-less.** The two-attention shape exists in the reference only as a **convention** (`should_evaluate` cheap versus `evaluate` costly, honoured by discipline); there is no aperture, no `⟨b,d,ρ⟩`, no cost function and no budget, so there is nothing new to diverge from and the counterweight remains **internal consistency** — `d(s,V)`, the `⊕` tree, and now the fold-and-refuse discipline the juul ledger already holds. **Residuals still standing, named:** ★★ `κ_att` is **still declared and uncalibrated**, and **`B_att` is still not denominated in juul** — bridging them needs `κ_att` and `κ_c` calibrated *against each other*, which neither is, so an exchange rate today would be a **made-up number between two declared ones**; parked with the calibration rather than half-wired. ★★ `ParameterSpec` expresses **bounded, not positive**, so a governed `κ_att` of **zero** makes every scan free and the budget vacuous — and note the asymmetry with `issuance_rate`, where zero is a *safe* policy (create no money) while here it *disables an enforcement mechanism*; what can still be said is that it would be a **visible** change, gated and recorded and replayable, rather than a source edit. ★ **No period**: the core has no clock, so `renew` is the **host** saying one turned. ★ And the scan's footprint **still does not feed** `compose_view`'s candidate pool — real, named in the original limit, still unbuilt. **→ Next, sequenced not dropped: the ISSUANCE-SIDE SEAM** — joining a committed run's charge to the serving host's issuance, closing PAWA-8's *nothing calls `issue` automatically*. Deliberately its own slice: it belongs to `Affordability`/`execute_afforded` and has nothing to do with attention, and folding two unrelated seams into one commit would muddy both tracker rows.

★★★ **THE ISSUANCE SEAM — a committed run now pays its server** (`juul.rs`, `operator/mod.rs`, 2026-08-18). The follow-on B_att's paragraph named as *sequenced not dropped*, landed immediately after it, and the last named piece of the economy layer. ★★★ **STEP-0 confirmed option (a) fits the real signatures, which is why it stayed option (a).** The debit block in `execute_afforded` **already holds the very `PawaReading` issuance needs** — it is the line that meters a committed execution and charges it — so the seam is **one `if let` inside a block that already existed**, not a new stage in the pipeline. `Affordability::Metered` gained a single field, `serving: Option<(&Issuance, &str)>`, and exactly five construction sites across the crate needed `serving: None` added. Nothing had to be forced. ★★★ **ONE READING, TWO FLOWS — and that is what makes it not a refund.** The caller is **charged** `pawa` and the server is **issued** `rate × pawa_served`, both derived from the **same** `PawaReading`, so the work that cost the one is exactly the work that earned the other. Asserted as two genuinely different amounts landing on two genuinely different balances, with the cost read off the **real debit entry** rather than reconstructed by subtracting balances — the ledger records what was charged, and asking it is both exact and closer to the claim being made. (The first draft *did* reconstruct by subtraction and failed on float error; fixing it by asking the ledger is better than widening a tolerance.) ★★ **OPT-IN, AND VISIBLE AT THE CALL SITE.** `serving` is an `Option` a caller must **spell out**, deliberately not a defaulted constructor helper: **opting in creates money**, so it should be visible exactly where it happens, and a helper that filled it in would make the **opt-out** the thing you had to notice — the same reasoning that keeps `Unmetered` and `Unchecked` spelled out rather than defaulted. ★ `Affordability::serving()` is kept **distinct from `metered()`** on purpose: metering makes a run **cost** something, serving makes it **earn** something, and they are opted into separately because they are different decisions about different principals. ★★ **BACKWARD COMPATIBILITY MEASURED, NOT STATED.** With `serving: None` — what every pre-seam caller passes — a committed metered run appends **exactly one** entry, the debit, and `issued_total()` stays at zero; the circulation figure is checked against the genesis total minus the charge, so *byte-identical to before* is a measurement rather than an assurance. ★ A **refused** run reaches neither flow, and that reuses PAWA-2's own property rather than re-arguing it: `meter` returns `None` for anything uncommitted, so a gate refusal cannot reach a charge and therefore cannot reach an issuance — proven with a real allow-list refusal under a *serving* `Affordability`, ledger compared byte-for-byte. ★ And a **zero governed rate serves nothing even when opted in**, which is the genesis posture reaching the seam: opting in is **permission, not creation**, and the amount is still the governed rate times the measured work. ★ **DEFENCE IN DEPTH, NAMED AS SUCH.** The issuance is guarded on the charge having actually landed. It always does at that line — the identical cost was checked against the identical balance a few lines above and nothing interleaves — so the guard protects against a **later refactor**, and it is said plainly rather than left to read as a real branch a reader should reason about. ★★★ **CONSERVATION RE-ASSERTED OVER A LIVE PATH**, not hand-built entries: after three real gated, metered, **serving** executions plus a royalty transfer, `circulation == genesis + issued − debited` holds and `minted_total − issued_total == genesis.total()` still separates the two mint terms. ★ Compared within a stated tolerance, **and said so**: the identity is exact in the reals, but `f64` summation over many entries is not associative, so the slack is about the **representation** and not about the claim — a bit-exact assertion there would be testing IEEE-754, not the accounting. **The hard boundary is untouched** (ADR-0001 D5 / PAWA-13): the seam moves the *internal* accounting unit between *internal* balances, and mints the internal unit; no real value, no redeemable claim, no rail. **Divergence: unchanged.** The reference has no issuance at all (grep-0, with its one named false positive), so there is no seam to diverge from; the counterweight remains what PAWA-8's own row records — this composes PAWA-1's meter, PAWA-7's visible mint and PAWA-11's governance, and now the gate's own already-existing debit block, rather than inventing anything. **Residuals:** ★★ **one server**, and a host naming itself is **trusted rather than verified** — verifying a serving claim, and splitting a reward across several servers, are both **PAWA-9** and are not built here; ★★ **one schedule shape**; ★ **the seam is opt-in**, so a host passing `serving: None` still has an economy that only shrinks — but that is now a **host's choice** rather than a missing mechanism, which is precisely the difference this closes; ★ **no burn**; and ★★ **`Issuance` itself is still ungoverned** — the rate is governed, which declarations exist is not. **→ The economy layer's named pieces are complete.** Next is the app-level set (IMM-11/12, UI-10, UI-14/15), where STEP-0 will be heavy: most are already ✅ in the Python app, so the first job there is reconciling **genuinely undone** against **app-layer-only**, before anything is built.

★ **A RUNNABLE SURFACE — `cargo run --example household_run`** (`sustena-core/examples/household_run.rs`, 2026-08-18). Not a spec row and not a mechanism: the crate was **lib-only**, so there was no way to *watch* the foundation work. This walks one household Sustain end to end — declare `Σ`, three real transitions through the real gate, `state = fold(events)` rebuilt from the log and compared to live state, **two** refusals (the operator's own guard, then the enforcement gate itself, via the registry's deliberately-unguarded `test.force_negative`), a real `PawaReading`, and a composed curated view. Every printed number comes out of the library; none is a literal. ★★ **One STEP-0 finding, fixed rather than worked around**: `semantic::enforcement_of` — the one function turning a `Definition` into its gate — was `pub(crate)`, while `treasury::enforcement` and `governance::enforcement` were already public one-line wrappers over exactly it. A holder of a `Definition` with no way to arm it would make every non-treasury, non-governance Sustain second-class, so it is now `pub` and re-exported. ★★ **And the demo reports two honest things rather than dressing them up**: the everyday overspend is caught by the operator's **own guard** (`pocket_balance_sufficient`) before the invariant ever gets a turn, which is defence in depth and is labelled as such rather than passed off as the gate; and every widget urgency reads `0.000` with basis `Undeclared`, because `V` here bounds **nested paths** while the curated layer keys urgency on **root dimensions** — so the view prints the library's own *"a zero here is silence rather than safety"* instead of a calm green. **Worth its own row later**: whether `basis_for`'s root-splitting should understand nested dimensions, or whether `V` is meant to be flat. Flagged, not fixed — a demo is the wrong place to change a semantics.

★★★ **THE UI WORKSTREAM OPENS — `apps/mycelium`, a WALKING SKELETON** (Tauri v2 + SolidJS + specta, 2026-08-19). New engine, new UI: built fresh on `sustena-core`, with `apps/web` left **frozen** as reference. One thin vertical slice end to end rather than a horizontal layer — the Console gate-result screen, chosen over the Monitor because it is the only one that has to **fail honestly**. ★★ **STEP-0 found one real fork and it changed the design**: deriving `specta::Type` on the engine's own `Execution`/`OperatorResult` would have added a codegen crate to `sustena-core`, which **ADR-0001 forbids** (deps fixed at serde/serde_json/thiserror so it compiles unchanged to WASM/Android/iOS). So the wire contract is a **DTO layer in the host** (`src-tauri/src/dto.rs`) — and on inspection that is the better layering anyway, since the core has no I/O and no opinion about a wire format, which is exactly what a DTO is. The typed-boundary property still holds, one layer out: TS is generated from Rust, and it **caught a real type error in the hand-written wrapper on its first run**. ★ `semantic::enforcement_of` was made `pub` for the demo example one commit earlier and is what lets the host arm an arbitrary `Definition`. **Named as stubbed, not built:** no persistence (state is a `Mutex` in the host), no event-log/fold view, no simulation, economy, governance, council or definition editor, one hard-coded Sustain, no live telemetry channel, no Orchie, desktop only. **Verified:** the real window ran, invoked `get_sustain` over real IPC, and the host answered from the real engine (`[ipc] get_sustain -> gate armed=true operators=4`); `cargo run --bin smoke` prints the exact `GateResult` JSON for three admitted calls and one **refused** one carrying the engine's own reason with `mutations: 0`; and outside Tauri the UI renders its honest failure state rather than a fake result.

★★ **MYCELIUM V1.1 — PERSISTENT, MULTI-SUSTAIN** (2026-08-19). The cockpit stops forgetting. ★★★ **The EVENT LOG is what persists, not a state snapshot** — the engine's central property is `state = fold(events)`, so storing a snapshot would make the file the source of truth and the log a story about it, unresolvable the first time they disagreed. `Store::load_state` folds the log with the engine's own `fold_events` and is the **only** way to obtain state, so the property cannot quietly stop being true. Append-only JSONL per Sustain, `sync_all()` per append, registry written temp-then-rename. ★★ **The composition tree is validated BY THE ENGINE**: `MonitorEngine::flatten_holarchy` refuses a duplicate id, an unknown parent and a cycle, and the UI shows its verdict — the parent/child links are engine-checked rather than trusted because the host wrote them. ★★★ **Roll-up ρ renders as an HONEST UNAVAILABLE STATE**: it does not exist in `sustena-core` (the Python engine has it), and summing the six habitats in the host would be host arithmetic wearing an engine's name. Nothing is summed; the card says why. **Proven:** seeded 7 Sustains (Homestead + Bonnie/Cira/Epha/Mum/Kui/Frankie), ran real operators, dropped the process, reopened from disk alone — `cached state == fold(persisted log)` for all seven, and the app's second launch logged *loaded from log*, not *seeded*. A refused overspend appended **nothing** (homestead stayed at 3 log lines). **Stubbed, named:** no event-log view, no simulation/economy/governance/council/ingest/Define, templates declared in Rust not authored in-app, no push channel yet (V1.2), no undo/delete, no Orchie, desktop only.

★★ **MYCELIUM V1.2 — THE PUSH CHANNEL + THE MONITOR SCREEN** (2026-08-19). ★★★ **The event IS the message.** The host emits one typed `Committed` per committed operator call — carrying the new state, the events published, and `V` re-evaluated — and **a refusal emits nothing**, because a message announcing no change would be a change that did not happen. No tick, no sampler: the core has no clock, and sampling would manufacture events nothing caused. ★★ **The UI subscribes ONCE** (`lib/live.ts`) into a Solid `createStore`, merged with `produce`/`reconcile` so only the paths that moved repaint. `Console.execute` deliberately does **not** re-fetch after a commit — asking again would hide a broken channel behind a re-read — and a grep confirms there is no `setInterval` and no timed refetch anywhere in `src/`. ★★ **`V` is evaluated by the ENGINE**, not by the app: each invariant goes through `sustena_core::predicate::check`, the same evaluator the gate uses, and an unparseable rule reports as **not holding** rather than as satisfied. ★ **One threshold is the app's and is labelled so** — the ≥80% pocket attention line; the engine's real urgency is `d(s,V)` over a declared `Region`, and these templates declare invariants rather than intervals, so borrowing a Region's authority would mean declaring thresholds nobody chose. The **event-log view** now renders the log V1.1 persisted, read from disk. Roll-up ρ keeps its honest unavailable card. **Stubbed:** no Constellation/app shell (V1.3–1.4), no Simulate/Define/Composition/Economy/Council/Ingest, templates still declared in Rust, no undo or delete, no Orchie, desktop only.

★★ **MYCELIUM V1.3 — THE CONSTELLATION HOME + THE APP SHELL** (2026-08-19). The persistent frame (left nav grouped watch/act/system · topbar with brand, Sustain selector, push badges, identity and a **UI** clock · status belt) with panels swapping inside it. ★★★ **A second typed event, `Refused`, carrying NO STATE FIELD AT ALL.** The V1.2 rule stands — a refusal still emits no state delta — but a gate-stream ticker asks a different question from a state channel, so the honest answer was a second type rather than a weakened first one: a consumer cannot treat a refusal as a change by forgetting to branch, because there is nothing to read. ★★ **The Constellation is read from the world summaries**, never hardcoded — proven by grepping the shipped bundle for the six member names (**zero hits**; they live only in the host seed). Summaries now carry pockets and `V` as the engine evaluates it, so a broken rule anywhere shows without hydrating a single state document (fork (a), as approved). ★★ **The gate stream is fed ONLY by the channel** — an entry appears because the engine decided something, never because a screen asked. ★★★ **Household liquid is NOT summed** — the telemetry cell and the status belt both read `—`, because roll-up ρ is not in `sustena-core` and adding six numbers in the host would be host arithmetic wearing an engine's name. Every other figure (sustains, logged events, rules watched, rules broken) is a real count over the persisted world. ★ `Committed::of`/`Refused::of` moved into the DTO layer so the headless proof and the running app build byte-identical messages. **Stubbed and shown as such:** nine nav panels disabled and marked `soon`; `bg.myc` is declared, not authenticated (still `Authorization::Unchecked`); the clock is the UI's, not the engine's; peers and pawa are honest dashes.

★★ **MYCELIUM V1.4 — THE ACTION SCREENS: Console (full), Simulate, Composition, Economy** (2026-08-19). ★★★ **The economy is genuinely wired into the gate**: every committed call runs `execute_afforded` with `Affordability::Metered`, so `balance >= pawa` is a real clause priced against the candidate's actual cost, and the `serving` seam is opted in so the host earns `rate x pawa_served` at the **governed** rate. `pawa::meter` produces every reading (no public constructor -> a debit cannot be invented), `JuulLedger::charge` takes the reading rather than a number, and `Parameters` are read from a governance Sustain's state, never a constant. **Proven:** balance 999,971.39 after three real runs with per-operator debits naming the operator; `genesis audit` clean; `set issuance_rate 1.5` **Admitted** and read back in force; `9999` **Refused (enforcement_gate)** with the old value still in force; `kappa_mystery` **Refused (parameter_declared)**; and with the rate governed on, a real run **issued 14.01** and the balance rose. ★★★ **Simulate is a fork, not a simulator** — STEP-0 confirmed no scenario-fork API exists in `sustena-core` (`ensemble::Scenario` is model ensembles), so a branch is `state.clone()` plus the **same `execute_admitted`**; proven that live state, the log AND the ledger are all unchanged after a branch, and that a step refused in the fork carries the engine's own reason. ★★ **Console's measured pawa is a measurement**: `stats_for` returns `None` for an operator the meter has never seen, and the UI shows *not measured* — never a zero. ★★★ **`holon.transfer` is NOT approximated**: an atomic conserved cross-Sustain move is not in the core, and the two-call host workaround would lose money on a refused second leg — the screen says exactly that rather than shipping it. Roll-up ρ keeps its honest unavailable card in three places. **Stubbed:** Define, Council, Ingest, Network, Library, Profile still `soon`; the identity is declared not authenticated; the approval-token clause is vacuous (`Authorization::Unchecked` everywhere), so parameter *bounds* are enforced while *authority* is not.

★★ **MYCELIUM V1.5 — THE REMAINING PANELS** (2026-08-19). Twelve panels, none disabled. ★★★ **STEP-0 corrected the brief twice.** COUNCIL is **not** Python-only: `council::resolve` and `aggregate_delegated_votes` are real in Rust, and the rules they encode are wired — a person's vote overrides the council, an **abstaining** person leaves it *in voting* rather than deciding, and collected votes with nobody in favour fail. PROFILE's capability model is real too: `effective_privilege` walks the membership path with the **weakest-link** rule and `permitted` compares it against each operator's declared `min_privilege` — proven live (tier 0 owner over 7 membership edges). ★★★ **DEFINE authors a real `Σ` and the ENGINE decides whether it lands**: `editing::typecheck` binds every invariant against the schema and `editing::safe` checks it against every live instance; a definition failing either is **never persisted**. Proven: well-typed → Accepted; `rainfall >= 0` over an undeclared dimension → `NotWellTyped`; and after instantiating from the authored definition, adding `soil >= 90` → `WouldStrand ["garden-1 · soil_rich (soil (40.0) >= 90 (90) failed)"]`. An authored definition instantiates through **exactly** the built-in path. ★★ **Three designed absences, not blanks** — Ingest, Network and Library each state what the core *does* have, what it does not, and where the capability lives. Ingest additionally refuses to ship a host-side parser, because the Python transducer's own history is speculative patterns that matched no real message until real samples arrived. Network refuses to show `0 peers`, because that would imply a network that found nobody. ★ **A real engine finding, reported not smoothed:** an unparseable predicate (`soil >>= ??`) returns *"undeclared dimension >="* rather than a syntax error — the parser partially consumes it. Still a correct refusal; the message misleads, and the UI shows the engine's words verbatim rather than inventing a friendlier lie. **Stubbed:** every call still runs `Authorization::Unchecked`, so the capability model is displayed and not enforced; Council has resolution but no proposal lifecycle (no persisted proposal, no deadline — the core compares nothing it cannot replay, so `expired` is the host's question and this app has none).

★★ **MYCELIUM V1.6 — THE CLOSING POLISH PASS · v1 IS COMPLETE** (2026-08-19). The cockpit stops being twelve screens and becomes one product. ★★★ **`src/ui` is the design system as code**: `app.css.ts`'s 98 ad-hoc exports — grown a section per slice, with the same card / verdict / empty-state re-spelled on every screen — are replaced by tokens, layout primitives and **one definition each** of Card, Verdict, Unavailable/Absent, Hypothetical, Boundary, Empty, ErrorState, Row/Readout/NoteRow/Meter/TelemetryCell, Badge/Dot. The two that most needed it are the honesty components: a gate refusal that rendered differently in two places is one a person learns to read twice, and the designed absence is how the app tells the truth about itself. ★★★ **Responsive as PRIMITIVES, not per-screen media queries — and this is Orchie's groundwork.** Breakpoints are tokens (`bp.md` 900, `bp.sm` 560) consumed only inside `src/ui`; **grep confirms zero media queries in any screen file**. `Split`/`Column`/`Stack`/`Cluster` collapse to one column below `md`, the nav becomes a horizontal rail that scrolls **inside itself**, and every primitive sets `min-width: 0` — the one rule that stops a grid child refusing to shrink and pushing the page sideways. **Proven at 375px across all twelve panels**: page `scrollWidth == 375`, forcing `scrollLeft` moves nothing, and **zero elements overflow inside any panel**; the only two sideways scrollers are the nav rail and the status belt, both by design. At 1440px the same primitives give `212px + 1228px`, vertical nav, group titles and footer back. ★★ **Behaviour unchanged, proven by rendering all twelve** — Constellation's real graph and honest un-summed household cell, Monitor's meters and the `—` for an unmeasurable pocket, Console's *not measured*, Simulate's `◆ HYPOTHETICAL`, Economy's boundary and genesis audit, Define's authored list, Profile's *not enforced* badge, and the three designed absences. ★ **A real bug the extraction surfaced and fixed:** `text-transform: uppercase` turns `ρ` into `Ρ` — a different letter — so the roll-up absence read *ROLL-UP Ρ*. `Sym` holds a symbol out of the transform, because the cockpit's labels are full of Greek that carries meaning. ★ Inline `style={{…}}` attributes fell 75 → 12, and the twelve that remain are genuinely value-dependent (a ledger entry's colour, an input's width). Engine untouched and green (2090 tests). **v1 is complete; what remains is named in the README's *Not built* list** — roll-up ρ, `holon.transfer`, the transducer, peer transport, the arena, the council's proposal lifecycle, authority enforced rather than displayed, and Orchie.

★★ **ROLL-UP ρ — THE FIRST HONEST ABSENCE CLOSED** (2026-08-19 · Composition §VIII · parity with the reference's `compute_rollup`). `sustena-core::rollup` folds a parent's own state and every linked child's into its declared aggregates. ★★★ **Three refusals to fabricate, each structural rather than conventional.** (1) **Fresh every call, never persisted** — a household figure that could be stale would be a number with no way to say when it stopped being true, the same reason the fold recomputes from the log; a second call with unchanged inputs reproduces itself, asserted. (2) **An unreadable member is EXCLUDED AND NAMED, never zeroed** — a silent drop is arithmetically indistinguishable from a member who genuinely holds nothing, so `included`/`excluded` travel with every value and the exclusion carries the engine's own reason; proven live against the real household by adding one unreadable member and confirming the total **did not move**. (3) **`AggregateReading` has no public constructor** — a household total is exactly what a surface would be tempted to assemble itself, and one assembled outside `compute` would carry no exclusions; the same structural guarantee `PawaReading` gives a debit. ★★★ **STEP-0 found the reducer and the resolver ALREADY IN THE CORE, so ρ declares neither.** `predicate::AggFunc` already had the same five ops with — as it happens — the reference roll-up's exact empty-set answers (`sum`→0, `avg`→0, `min`/`max`→null); the arithmetic was inline in `eval.rs`, so it was **extracted to `AggFunc::reduce` and both callers now reach one definition** (the same one-truth-by-deletion move PAWA-11 made). The path grammar is `parse_state_path` and the resolver is `resolve_path` — the two `V` uses — so a wildcard means the same thing to a rule and to a household total. Name collision #40: `Operand::Aggregate` was taken, so the newcomer took the longer names (`AggregateDecl`/`AggregateReading`). ★★ **Aggregates live in `Σ`** (`editing::Definition::aggregates`), not in the host — a household total is a property of what the household IS, not of which product displays it. **Three divergences recorded, all narrow:** whole floats render as integers (the reference seeds the wildcard sum with `0.0`; values equal, compared numerically); an **array** container is readable under a wildcard where Python accepts only a dict (leaning Rust — refusing it in ρ alone would make one wildcard mean two things, the exact two-evaluator divergence this core deleted); and a **scalar** container is excluded in both, which took an explicit guard because resolving a wildcard against a scalar yields an empty list that would have summed to a confident `0`. ★ **A defect this port found in itself:** Rust's `Sum for f64` folds from the additive identity **`-0.0`**, so an empty pocket container contributed a *negative* zero — the same number, formatting as `-0.00`, reading on a household screen as a debt; normalised at the source with a test that asserts the sign bit. **Wired end to end:** a `get_rollup` command, a **third typed push event `RolledUp`** (a member's commit moves its HOUSEHOLD's total — a different Sustain's derived reading, which is why it is its own message and not a field on `Committed`; a refusal emits none, because ρ is a function of state), and the Monitor, Constellation, Composition and status-belt absences all replaced by the real figure through **one shared `AggregateReadout`**. The `WorldDto::rollup_available` flag — which only ever said `false` — is deleted. **Proven headlessly on the real seeded household:** `26,800 = 13,200 (the household's own) + 3,600 + 10,000 + 0 + 0 + 0 + 0`, matched against a hand sum computed independently; recompute reproduces itself; a ghost member excluded, named, and the total unchanged. **2,109 tests** (1,162 unit · 944 conformance · 3 doc), clippy strict-clean on both crates.

★★ **`holon::transfer` — THE ATOMIC, CONSERVED CROSS-SUSTAIN MOVE** (2026-08-19 · Composition §4E.6 · parity with the reference). The absence V1.4 deliberately refused to approximate — *spend here, record income there is two gated calls, and if the second refuses the money has left one household and arrived nowhere* — is now the real thing. ★★★ **Atomicity is STRUCTURAL.** The only way to hold a leg is to hold a `TransferSettlement`, and a settlement always holds both: there is no `Leg::new`, no constructor, and no function returning one leg — so *apply the debit, then decide about the credit* is not a sequence a caller can write. A refusal produces **nothing at all** (`Transfer::Refused` has no leg field), so a partial anything is unrepresentable rather than merely avoided. ★★★ **Conservation is CHECKED, by the core own law** — `TransitionRule::Conservation` evaluated over the **combined pair** (`{from, to}`, the quantity summed across both paths), the same `D(s,s')` machinery a single Sustain rules run through. Third one-truth reuse in two slices, after `AggFunc::reduce` and `resolve_path`. ★★ And it is **stricter than the reference**: `Tolerance::Exact` conserves in integer minor units where the reference rounds to six decimal places, so a fractional amount is refused rather than moved — legal only under an explicitly named `Moving::real(path, tol)`. ★★ **The link check moved from a branch to a type**: `Linked::between` returns an `Option` and `transfer` takes the token, so a transfer between unrelated Sustains is unspellable. ★★ **Domain-free** — the moving dimension is a parameter; `finances.liquid.balance` appears nowhere in the core outside doc examples, and the host names it. ★★★ **Crash-safety, in the host, PRODUCED rather than imagined.** Two `.jsonl` files cannot be appended atomically, so `Store::commit_transfer` writes a **write-ahead intent journal** carrying both legs fully materialised, appends leg A, appends leg B, then deletes the journal; `recover_transfers` finishes any interrupted one on the next open, **before any state is folded**, and is idempotent by `seq` so replaying cannot double-apply money. The smoke bin uses a `crash_after_first_leg` seam to produce the real half-written state and proves the recovery: mid-crash `bonnie log 10 -> 11, homestead log 16 -> 16` (one leg on disk, one missing), then on reopen `bonnie 11, homestead 17`, both balances healed exactly once, `Σ = 25,200` unchanged throughout, reopening again changes nothing, and `state == fold(log)` still holds for both sides. **Wired:** a `transfer` command that pushes `Committed` for **both** Sustains plus `RolledUp` for whoever total moved, and Composition honest-unavailable card replaced by a real instrument that quotes the engine conservation line rather than adding the balances up itself. **A defect the typed boundary caught:** an enum-level `rename_all` renames the VARIANTS and leaves struct-variant fields snake_case — `tsc` failed on `totalBefore` the first time the UI read it. **Real proof:** `4,500 + 16,500 = 21,000` → `4,250 + 16,750 = 21,000`; an over-balance transfer refused with both sides byte-identical. **2,128 tests** (1,178 unit · 947 conformance · 3 doc), clippy strict-clean on both crates.

★★ **AUTH ENFORCEMENT — `Authorization::Unchecked` IS RETIRED, AND `bg.myc` IS A KEY** (2026-08-19 · Immune §IV · Sustain §4.5). Two halves, and STEP-0 reshaped both. ★★★ **The enforce half needed NO engine work at all.** The authorization conjunct is already fully built in `sustena-core`: `permitted_with` walks the containing path with the weakest-link rule, `execute_afforded` checks it BEFORE the guard, and it already refuses with **six distinct names** (`not_a_member`, `insufficient_privilege`, `skin_resolves`, `capability_designates`, `capability_carries`). The only thing missing was that the host passed `Authorization::Unchecked`. So this slice is host wiring plus vectors — reported as such rather than dressed up as an engine port. ★★ **And the reference has none of it**: grep-confirmed, `min_privilege` is one dataclass field, one keyword argument, **zero readers**, with no `permitted`/`effective_privilege`/`privilege.check` anywhere — so the vectors are SPEC (R2), not parity. ★★★ **The household model is not uniform, on purpose.** Ownership is now DECLARED on each `SustainRecord` (`owner`), not inferred: the person owns the household and their own habitat and is an OBSERVER on every other member habitat. A uniform owner-everywhere graph would make the enforcement true and pointless — a check that can never fire proves nothing. **Live proof:** `budget.record_income` on `habitat-bonnie` (path `[homestead, habitat-bonnie]`, tier 0) **admits**; the same call on `habitat-cira` (path `[homestead, habitat-cira]`, weakest link tier 3) is **refused** with `insufficient_privilege`, the reason names *holds tier 3 ... requires tier 1 or stronger*, and their state is byte-unchanged. ★ An earlier pass matched `habitat-<x>` against the principal handle and got it wrong the moment `bg.myc` and `bonnie` were not the same word — fixed by making a Sustain say who it belongs to rather than the host inferring it from spelling. ★★★ **The login half is a REAL KEYPAIR, not a password check.** `identity.rs`: an ed25519 signing key sealed under XChaCha20-Poly1305 with a key derived by PBKDF2-HMAC-SHA256 at 600,000 iterations (the reference uses the same KDF at 260,000). **Nothing is compared against a stored answer** — a wrong passphrase derives a wrong key and the AEAD tag fails, so failing closed is the only thing that can happen; the recovered key is then made to **prove itself** against the public half and a stored signature. A wrong passphrase, a tampered ciphertext and a swapped public key all return the **same** message, because telling them apart would tell an attacker which half they got right. `Debug` never prints key material, enrolling twice refuses rather than overwriting a private key, and a sub-12-character passphrase is refused with nothing written. ★★ **The lock screen is a door, not a curtain**: a locked host has no principal, so `World::call` and `World::transfer` both refuse with `not_authenticated` whatever the UI renders — proven by calling an operator on a locked world before unlocking. `holon::transfer` gets its own authorization check on **both** sides, since it is not a registry operator and would otherwise be the hole an operator-only check leaves. Profile's *declared, not authenticated* and *displayed, not enforced* notes are **retired because they stopped being true**, replaced by the real key fingerprint, the KDF in force, and an `enforced` badge. **An authored vector was wrong and running it said so:** the composite conjunct case used a negative amount, which `budget.record_income`'s own guard caught first — corrected to a ceiling the operator cannot pre-empt, and recorded rather than quietly fixed. **2,132 core tests** (1,178 unit · 951 conformance · 3 doc) + **9 host tests**, clippy strict-clean on both crates.

★★ **INGEST WIRED — THE QUEUE, AND THE ONE THING THAT IS NEVER STORED** (2026-08-19 · 4c). `Ingested` is the capture queue beside the logs: append-only messages, declared sources, learned rules, and the classification history that pre-fills a repeat merchant. ★★★ **The secret check runs before the host opens a file.** Not a `match` after the row is built — an early return whose condition is `Transduction::storable()`, the engine own answer. A rejection leaves **no row, no payload, not even a dedup key**, because the key is a hash of the text and a hash of an OTP is still a record that the OTP existed; it does not even stamp the source as seen, which would record that a secret arrived at a particular moment. **Proven live**: queue rows `1 -> 1` across a real OTP capture, and a filesystem scan of the whole ingest directory finds neither the code nor the word. ★★ **A mapped message applies through `World::call`** — the same gated, priced, logged, pushed path the Console uses, as the unlocked principal; there is no ingest-specific write path. Proven live: a real M-Pesa message moved the balance `3,650 -> 5,150`, and a replay moved nothing. ★★ **Source-strict end to end**: the same M-Pesa text tagged `kcb` comes back `unparsed` and queues for a person. ★ **Correction-learning proven end to end**: an unrecognised shape captured as `unparsed`, a correction synthesised and verified, and the same shape with a **different amount and reference** then captured as `mapped` via `learned_smoke` and applied. ★ Ingest honest absence is retired from the README because it stopped being true. `Registry` now implements both `OperatorUniverse` and `OperatorParams`, so a rule typecheck and a capture inference ask the same source of truth the gate will. `Capture` `Debug` is hand-written: a derived one would print a stored message whole raw payload into any log that formatted it. **20 host tests** (9 identity · 11 ingest), core at 2,201, clippy strict-clean on both crates.

★★ **ORCHIE — THE PHONE-FIRST CURATED FACE, AND UI-10 GETS ITS CONSUMER** (2026-08-19 · Curated UI §§III–VIII · host + one core fix). One app, two faces: Mycelium is the cockpit, **Orchie is the surface a person lives on**, on the same V1.6 primitives (`orchieColumn` is `width:100%` with a `440px` cap, so 375px is a narrowing rather than a layout). ★★★ **STEP-0 found that per-card urgency was structurally impossible on a household's real shape, and the fix is the slice.** `roots_read` splits a widget's declared input at the first `.`, so a card reading `finances.pockets.food.spent` attributes to the root **`finances`** — while `Region`'s intervals are full paths. The two never match, so `urgency_of`'s filter selects nothing and **every card reads `0.000`**, forever, on any nested money shape. ★★ Not closed by changing `roots_read` — it is conformance-recorded and correct for flat readings — but by composing over a **projection**: `reading_of(state, unclassified)` derives top-level numbers (`liquid`, `worst_spent`, `worst_allocated`, `worst_pocket`, `unclassified`) from the household's **own** state, and `region_from(reading)` builds `V` **from the household's own allocations** — reading their ceiling is not declaring a threshold. ★ **Found by writing the test and watching it fail**, which is the only reason it is not shipping as four calm cards on a household in trouble. ★★★ **One real core fix, and it is the negative-zero hazard again.** `Sum for f64` folds from the additive identity **`-0.0`**, so a card reading none of the currently-out-of-region dimensions summed to negative zero, and `clamp` **kept** it (it is in bounds) — reaching a screen as **`-0.0%` urgent**, a claim where the truth is calm. Same number, different claim — `rollup.rs`'s own hazard, one module over. `unsign_zero` is now `pub(crate)` and applied in `curated.rs`; **caught by a real feed printing `urgency -0.000`, not by reading the function**, and pinned by a test asserting the sign bit rather than the value. ★★ **A second honesty fix in the projection: `unclassified` is OMITTED when it is zero**, matching the `worst_*` reading's discipline. A zero would be a *reading*, which the card would then rank at zero and still render as an empty box; an **absent dimension** is the honest shape of *there is nothing to say*, and `grounded()` turns it into UI-5's **withdrawal with a reason**. So the two kinds of quiet stay distinguishable end to end: **withdrew** carries a reason, **outranked** carries a score, and the surface renders them as different sentences — with the engine's own reason under the plain line rather than instead of it. ★ ρ's **exclusions are never silent** on the summary card either: a member that could not be read is named, not counted as zero. ★★ **The capture flow is UI-10's δ, and the row's own condition is now met** — it was left app-layer *because this core is deliberately stateless across calls*, and here the accumulated `known` map lives where it belongs: in the conversation, re-sent each round, one question at a time, options that are the household's **own** pocket names, and a confirm every time. **`learn_rule` now receives the params a person actually confirmed** (it was sending `{}`, which synthesises nothing and would have reported *learned*); money-safety asymmetry is unchanged — a learned income rule maps, a learned spend still asks. ★★★ **Every write goes through the real gate as the authenticated `bg.myc`**, with a refusal rendered as a refusal: the smoke drives a real narration (*'spent 40 on food'*) → `infer` → `budget.spend(amount=40, pocket_name=food)` → **committed**, `food.spent 0.00 → 40.00`; then an over-spend refused with the operator's own reason and the household **byte-unchanged**. ★ And β is shown deciding rather than an `if`: `recent_spend` is not a candidate until an `event.finances.pocket_spent` has actually happened. **Residual, named:** the physical render of the Orchie face at 375px is the standing headless item — layout is verified structurally (no fixed widths, `min-width:0` on every primitive, no horizontal overflow at 375px on the running shell) and the app launches and loads 7 real sustains from the log, but the on-screen tap is Bonnie's.
