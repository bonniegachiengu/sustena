# SUSTENA — RUST BUILD · WBD + TRACKER

*The single source of truth for building **all** Sustena features (every technical article) in Rust. This is both the Work-Breakdown Document (what to build) and the live tracker (what's done / pending). It supersedes the earlier "upgrade the Python app in place" framing and folds in the scattered trackers (R2_BACKLOG, ledgers).*

**Angle:** the engine is being rebuilt as a portable **Rust core** (`sustena-core`), verified against conformance vectors. **R1** re-laid the existing Python behaviour in Rust at parity; **R2** builds the article specs the Python code never had. This document is the full remaining breakdown, article by article, through to the whole system.

> ★ Direction confirmed by Bonnie, 2026-08-12: *"continue with rust build for all sustena features in the technical articles."* The original spec's §0 First Principle (upgrade Python in place, stay in WSL, no rebuild) is **superseded** — recorded for history, not deleted. The Python app stays **live and untouched** as the reference until the Rust core ships.

---

## Status legend

- ✅ **done** — built in Rust and passing conformance vectors (R1 parity or R2 spec)
- 🔨 **in progress**
- ⬜ **pending** — on the plan, not started
- ⏸️ **deferred** — held with the economy layer (ADR-0001 D5, pending legal) or genuinely-later on engineering grounds
- **✅ app-layer** — built and live in the Python reference, not yet in the Rust core (real, but not portable yet)

*Build note: **all 18 technical articles have now been fully read and extracted** — the completeness checklist at the end is all ✓ and this WBD is feature-complete as a breakdown. What remains is the building.*

> **★ Standing rule — read to the last line of the article.** Many articles carry a trailing **`## Additions — ratified 2026-08-05`** section and/or **corrections inside the Sustena Note** (*"that is now superseded"*, *"four corrections to that audit"*). These are **canon**, they routinely **flip a status from not-built to built**, and they are easy to miss by stopping at the references. Rows sourced from them are marked `Add.` or `Note` in the § column.
>
> **Never label anything "not article-derived" without reading the article first.** If it appears in an article, cite the §. Two instances of this error were found and corrected on 2026-08-12, and they had **different failure modes** — worth knowing both:
>
> - **Stopping at the references.** M-PAWA's KES peg, royalty schedule and settlement stack were folded from a retired tracker and labelled "not article-derived"; they are in Pawa's own trailing *Additions* section.
> - **Reading a section but not exhaustively.** ADD-2b's governance model was recorded as "in no article body" on the strength of a `grep` for `RFC` — which only matched *RFC 1034*, the DNS citation. Mycelium **§X·§XI** already carried most of it under different words (Foundation-with-zero-authority, `CouncilSession`/`StakeholderSession`, per-edge privileges, skins, quorum, the FLP/Paxos/BFT bounds). **A keyword miss is not an absence.** Read the section.

---

## Status snapshot (the tracker headline)

| Layer | Module | Article | Rust status |
|---|---|---|---|
| Substrate | **Events & Time** | Events and Time · RECORD/GAIA | ✅ mostly (fold, typed events, event-time order, Lamport tie-break, dedupe, **vector clocks + genuine concurrency detection** — 2026-08-16, derived from the `causes` graph with no wire-format change; **two clocks + skew, with negative skew surfaced and structurally unclampable** — 2026-08-17, `t_ingest`/`source` additive on the record so a pre-slice log still folds byte-identically; **watermarks + windows that close only on them + the mandatory lateness policy** — 2026-08-17, with `W(τ)=τ` available only by declaring a zero out-of-orderness bound out loud; **periods — `RRULE` recurrence + zone-id anchoring, store-the-rule, partition structural** — 2026-08-17, tz data injected rather than bundled, no new dependency; **replay + checkpoints `(s_k,k)`, discardable and derived-never-authored, with replay unable to reach an effect channel** — 2026-08-17; **declared dimension kind — LWW attaches from the declaration, and a delta on a snapshot dimension does not compile** — 2026-08-17; **semantic replay under `D′` — a logged call admitted under `D` can be found refused under `D′`, re-run through the real gate, and replay is structurally unable to re-fire an external effect** — 2026-08-17) · ⬜ the rest of the RRULE grammar, the DSL authoring surface, effect journaling, `payload` on the durable record |
| Definition | **Sustain design (S)** | Sustain · CELL | ✅ typed state, organisational closure, composition ⊕/ρ, **V as a region + `d(s,V)`**, **viability kernel + runway** (2026-08-12) · **boundary `B = ⟨scope, μ⟩` + the autopoietic closure law ✅ (2026-08-17)** — an ordinary operator that moves μ is refused at the gate, and boundary change is a separate higher gate · the firewall `F` over what crosses it **✅ (2026-08-17, = M-CON)** `F` |
| Definition | **Operator (T)** | Operator · ENZYME | ✅ guards, post-conditions, derived inverse, privilege, checked composition · **the author-time `wp` obligation ✅ (2026-08-17)** — flags a postcondition the guard cannot guarantee, without blocking · ⏸️ metering only (economy-deferred) |
| Definition | **Constraint engine (V)** | Constraint · LAW/TOLERANCE | ✅ one evaluator, refuse/clamp/defer, gate refuses on unparseable · **the firewall `F` ✅ (2026-08-17)** — all three predicate families built, irreducibility proven, and the clamp trap honoured by construction (no clamp strategy exists to declare) |
| Definition | **DSL** | DSL · GENOME | ⬜ mostly pending (arithmetic, bracket indexing, full authoring surface) |
| Definition | **Immune / security** | Immune · TOLERANCE | 🔨 the gate half ✅ (no-bypass, fail-closed) · **authority ✅** (per-membership-edge privilege + `min_privilege` enforced — 2026-08-12; **`skins` resolved, fail-closed on an unresolvable bundle** — 2026-08-17; **capabilities — authority that travels WITH the request, so a deputy on untrusted input cannot reach an object the request never designated; redesignation, widening and forging all unrepresentable** — 2026-08-17) · **learned tier bounded ✅** (`R_fixed` a structural fixed point of every edit kind and holding no capability at all; **trust a real input to precedence AND privilege**; and the untrusted tier holding a capability **attenuated to a declared ingress bound** — refused at the gate for anything outside it, with no trust level able to lift the ceiling — 2026-08-17) · ⬜ self/non-self, memory, tolerance, capability revocation; IMM-11/12 are app-layer |
| Boundary | **Ingest** | Ingest · RECEPTOR | ✅ app-layer (transducer, ParseRule data, dedupe, secret filter) · ⬜ in Rust |
| Cognition | **Monitor** | Monitor · PERCEPT | ✅ **detection pipeline + the engine that owns it** (2026-08-12, 2026-08-16) — `urgency = d(s,V)` on the fold → EWMA → CUSUM → the Monitor→Controller boundary, now held per-sustain by `MonitorEngine` and **driving the OODA loop's OBSERVE itself** ; **belief state under partial observability** (2026-08-17) — the observation update is EVT-10's snapshot collapse rather than a Kalman filter, silence widens without inventing dynamics, and `SENSOR_SILENT` rides the existing `Severity::escalates()` boundary as an admission rather than an alarm · ; **frequency-domain change** (2026-08-17) — a known cycle and its harmonics suppress an alarm the time-domain CUSUM demonstrably fires on, while an off-cycle change of the same magnitude still surfaces, and a cycle that is declared but unresolvable in the window reads `Indeterminate` rather than suppressing · ; **stream-window typology** (2026-08-17) — sliding and session built, tumbling **reused** from EVT-11 rather than duplicated, and all three closing only on a watermark · ; **observability** (2026-08-17) — the matrix **declined** on MON-11's grounds and replaced by reachability-to-a-measurement over the operator graph, conservative where effects are unsummarised and saying so · ; **and all four wired into `MonitorEngine`** (2026-08-17) — harmonics per tick, belief per tick through `ingest_at`, windowing and the structural observability check on demand · ; **the preattentive encoder** (2026-08-17) — `φ` maps five data dimensions onto five of Treisman's seven channels, with the set closed as a **type** so a non-preattentive channel is unspellable, **urgency → brightness taken from the real `d(s,V)`** and made field-relative so a widget can only brighten by genuinely being worse, and discriminability **run** as pairwise-distinct encodings rather than asserted · ⬜ traces (MON-3), the module's last open row — SPINE |
| Cognition | **Tenet (simulate + optimise)** | Tenet · Temporal Decision | ✅ **mathematics complete** (2026-08-16, three slices) — `T:S×A→Δ(S)`, the inversion point (can be **the viability kernel**), **backward induction** → `J` + a complete policy, the CTL-12 join, **scenario ensembles → invariant actions → decision nodes → dead drops**, and **signal functions + the temporal pincer** (re-plan on a declared cadence or a fired signal) · ⬜ TEN-11 only (pawa-efficiency, parked on the economy) — SPINE |
| Cognition | **Operative** | Operative · SCOUT | 🔨 the gate half ✅ (sandbox-under-gate, privilege, defer) · **approval token ✅** (OPV-30/31, 2026-08-12 — the snapshot previously still listed this as pending) · **agent layer OPEN 2026-08-16**: **`ω` ✅** (sharing constraint unrepresentable to violate; Prop 1 structural, Prop 2 at the type level) · **utility vector ✅** (Pareto frontier; a non-convex option kept that 1001 weight vectors all delete) · **boundary built**: **Goodhart guard ✅** (a gate-enforced invariant over `U`, not another scoring voice — damage there is invisible to every operative and refused only by the gate) · **present-the-frontier ✅** (no method returns one option; collapsing needs a declared rule and reports what it hid) · **sparse MoE router ✅** (structural sparsity — the router performs the call, so an unselected operative is unreachable, not just skipped; routing collapse walled off from the surface, proven byte-identical) · **criticality σ̂ ✅** (a read over the causal DAG; no expected-loss number offered at or above criticality — the risk arithmetic as a type) · **suited domains + coverage + disorder ✅** (disorder undeclarable by construction; an uncovered regime surfaces the gap and routes nobody) · ★★★ **§XV's THREE DUTIES ALL BUILT — Route ✅ HoldTheWhole ✅ PresentTheFrontier ✅** · ⬜ `Π` (OPV-4), `M_self`/`M_world` (OPV-11), attention (OPV-7), learning (OPV-6); `dom(s)`'s classifier is **CAP-13's, a separate layer** |
| Cognition | **Multiparty** | Multiparty · SLIME | ✅ **COMPLETE — every row, the first article module finished end to end.** substrate ✅ · **Signal ✅** · **Population ✅** · **Consensus ✅** · **Router ✅** (**MUL-15 COMPLETE**) · **vector clocks ✅** · **disaggregation ✅** (hysteresis band; departure removes an edge, never a node) · **division of labour ✅** (`rb > c` enforced as a refusal; the price of stability reported) · **CRDT family ✅** (G-counter · PN-counter · OR-set · RGA; every permutation and duplication folded to one state; never-erase-the-node as a theorem about `max`) |
| Surface | **Curated UI** | Curated UI · PERCEPT | ✅ app-layer (typed widgets, β, compose(r), knapsack, effect-capture) · **the typed load path in Rust, with the check ON it** (2026-08-17, UI-2) — `LoadedWidget` has no public constructor, so an untypechecked widget cannot reach a surface; `inputs ⊆ dim(S) ∧ emits ⊆ T` at load, a widget's own `emits` at emit, and a real typed `Edit` makes a loaded set report stale ; **β and `compose(r)` in Rust** (2026-08-17, UI-1) — event-first (an absent class is never scored), a **real** 0/1 knapsack proven optimal against brute force, every exclusion returning with its score, and ★★ **two gaming surfaces closed**: urgency is the household's `d(s,V)` and **cost is derived from the declared inputs rather than author-declared**, so grabbing dimensions to look urgent is priced out by the budget · ⬜ the drawing itself and UI-3 … UI-15 |
| Surface | **Editing engine** | Editing · SLOPE | 🔨 **engine half ✅ complete** (taxonomy, authority, version DAG, inverses, 3-valued rollback, μ + Expand–Migrate–Contract; **EDIT-11 replay-under-`D′` — the history check beside the state check, with the three remedies offered and none recommended** — 2026-08-17, and ★★ *migration cannot fix a history* / *grandfathering is unsound while the state is stranded* are enforced rather than documented) ; **EDIT-13 `π : D → G` — and it is a LENS: `π` forgets expression text and fine type detail, `put` takes the old `D` to restore them, GetPut and PutGet both executed, and a graph edit arrives at the gate as a TYPED edit** — 2026-08-17) · ★★★ **M-EDIT COMPLETE, every row** |
| Governance | **Controller** | Controller | ✅ **decision-math + the OODA loop + holarchic escalation** (2026-08-12, 2026-08-16) — Lyapunov stability, Sheridan levels, the SNR filter, the assembled OBSERVE→ORIENT→DECIDE→ACT cycle with the human structurally at DECIDE, and a violation that **climbs the holarchy until a level can act**; **damping — stability's frequency-domain face** (2026-08-17), which separates a loop that settles from one that rings while passing Lyapunov at every step · ⬜ IoT bridge (**relocated to a host-integration pass**), panels (**moved to Phase 3**) — SPINE |
| Economy | **Pawa · Mycelium · Arena** | Pawa · MYCELIUM · ARENA | ⏸️ deferred (meter ✅ app-layer; ledger has zero callers; publish is a second write path) |
| Cross-cutting | **Capstone** | Capstone · GAIA | ✅ the assembly map — 4 of the gate's 6 duties banked structurally in Rust |

*(Legend for the three SPINE flags: the master spec's operational spine is Monitor→Tenet→Controller — the system's observe / simulate-optimise / govern-decide depth. **All three now read ✅** (2026-08-16): **Monitor ✅ · Tenet ✅ · Controller ✅**, with Tenet's ensemble layer closing the last of the three cores plus the layer above it. **As of 2026-08-16 they are wired into one cycle** (`ooda.rs`, CTL-3) rather than three modules that could be. What remains is the surrounding assembly — the `MonitorEngine` that owns the detection chain per sustain, holarchy escalation, panels — not the mathematics and no longer the loop. This was the highest-attention gap on this document; it is now an assembly job.)*

---

## Delivery phasing (2026-08-17)

*An index over rows that already carry full detail below — the agreed order of attack, not a re-description. Row IDs are the contract; read the rows for what each one is.*

**Phase 1 — Foundation.** The substrate the rest stands on.
- ✅ **Operator: OP-3 DONE 2026-08-17** — the static author-time obligation ships as a flag, not a gate term (`obligation.rs`). OP-5 was already ✅.
- **Events & Time substrate:** ~~**EVT-6** (watermarks + per-window lateness)~~ ✅, ~~**EVT-11** (periods, `RRULE` recurrence)~~ ✅ mechanism, ~~**EVT-12** (replay + checkpointing)~~ ✅ — all 2026-08-17. ~~**EVT-10** (dimension-kind typing — snapshot vs accumulating; ties to M-DSL)~~ ✅ **done 2026-08-17** — **the block's attack list is complete.** Adjacent and open in the same layer: **EVT-1** (🔨 — six of seven fields since 2026-08-17; only `payload` remains), **EVT-14**, **EVT-15** (⬜ — blocks M-TEN and M-EDIT). **EVT-5 ✅**, **EVT-6 ✅**, **EVT-11's mechanism ✅**, **EVT-12 ✅** and **EVT-10 ✅** all closed 2026-08-17; EVT-11 stays 🔨 only for the rest of the RRULE grammar.
- **The firewall `F` — one predicate, two homes:** **CON-1** (🔨 — `C` ✅, `D` ✅, **`F` ⬜**) and **CON-2**'s standing *"`F` pending"*; **IMM-1** closes on the same term. ✅ **DONE 2026-08-17** — **SUS-7**, **SUS-8** and **OP-10** closed (`boundary.rs`), then **CON-1**, **CON-2**'s `F` term and **IMM-1**'s `F` closed on top of it (`flow.rs`). The boundary block of Phase 1 is complete. **OP-3 ✅ done 2026-08-17** — the Operator layer has no open rows but metering. **Remaining in Phase 1: the Events & Time substrate rows** (EVT-6/11/12/10, plus the adjacent open EVT-1/14/15). **EVT-5 ✅ done 2026-08-17** (`clocks.rs`) — the two clocks, the skew between them, and the negative reading surfaced and structurally unclampable; `t_ingest` + `source` landed additively on the record, taking **EVT-1 to six of its seven fields** (`payload` is the named residual — it lives on `EmittedEvent` and never reaches the durable `Event`). **EVT-6 ✅ done 2026-08-17** (`watermark.rs`) — *when may I stop waiting* now has a real answer: a window closes only on a watermark, `W(τ)=τ` is available only by declaring a zero bound explicitly, and the lateness policy is mandatory because a heuristic will sometimes be wrong. **EVT-11's mechanism ✅ done 2026-08-17** (`period.rs`) — recurrence, zone-id anchoring and store-the-rule all shipped; its residual is the rest of the RRULE grammar. **EVT-12 ✅ done 2026-08-17** (`checkpoint.rs`) — checkpoints `(s_k,k)`, `O(n−k)` countable, discardability and derived-never-authored both structural, and replay structurally unable to issue an external effect. **EVT-10 ✅ done 2026-08-17** (`dimension.rs`) — LWW attaches from the declaration, and delta-accumulation on a snapshot dimension is a **type error proven by a `compile_fail` doctest**, with a schema-level refusal for the declared-in-data half. ★★ **PHASE 1's ATTACK LIST IS COMPLETE.** Boundary (`SUS-7`/`SUS-8`/`OP-10`), firewall (`CON-1`/`CON-2`/`IMM-1`), Operator (`OP-3`/`OP-5`) and the Events & Time substrate (`EVT-5`/`EVT-6`/`EVT-11`/`EVT-12`/`EVT-10`) all closed on 2026-08-17. **What remains in the layer is declared residual rather than unstarted work**, and is listed as such: EVT-11's RRULE-grammar remainder + the EVT-10 DSL authoring surface (both **Phase 4 / M-DSL** territory), EVT-1's `payload`, EVT-14 effect journaling, and EVT-15 replay-under-`D′` (which **blocks M-TEN and M-EDIT**, so it is the one to schedule deliberately rather than let drift). **→ Phase 2 (Cognition assembly) is next.**

**Phase 2 — Cognition assembly.** The mathematics is done; this is wiring.
- **Monitor:** ~~**MON-8** (belief state / POMDP)~~ ✅ **done 2026-08-17 — Phase 2 opened here** (`belief.rs`); ~~**MON-13** (harmonics)~~ ✅ **done 2026-08-17** (`harmonics.rs`) — and it **corrected its own row**: harmonics does not and cannot consume `signal.rs` (an excitable medium over a graph, with no series in it); it reads the supplied `W` window `detect.rs` already uses. ~~**MON-4** (stream-window typology)~~ ✅ **done 2026-08-17** (`windowing.rs`) — sliding and session built; **tumbling reused from EVT-11 rather than duplicated**, and fixed-`T` tumbling shown to be the degenerate sliding case. ~~**MON-1** (observability)~~ ✅ **done 2026-08-17** (`observability.rs`) — the **matrix declined** (MON-2's precedent, MON-11's caveat) and the discrete reachability analogue built in its place. **MON-3** (three pillars — traces) is the module's last open row. **★★ The Monitor block of Phase 2 is complete; the Controller rows are next.** ~~★ **MON-7 (preattentive encoder) moved to Phase 3**~~ ✅ **done 2026-08-17** (`preattentive.rs`) — and the move was right for a second reason the row did not anticipate: ★★ **UI-13's *salience is never rendered as salience* is enforced IN it**, because brightness is field-relative and `VisualSpec` has no constructor, so the encoder is where that guarantee lives rather than the surface that was going to have to keep it. ★ **MON-3 is now the Monitor's only open row.** **MON-2 is not work** — the Kalman filter is the *declined import*, recorded as declined rather than pending, and ★ **re-confirmed by MON-8 shipping without it** at the one site where §VIII names it directly.
- **Controller:** ~~**CTL-11** (damping)~~ ✅ **done 2026-08-17** (`damping.rs`) — the frequency-domain face, which separates a loop that settles from one that rings while passing Lyapunov at every step. ★ **CTL-6 (IoT bridge) RELOCATED to a host-integration pass** — ADR-0001 keeps I/O out of the core, and the core-side half (*a device signal becomes a typed event*) is already the ingest door, so it is a declared-port/host-boundary slice rather than a mechanism. ★ **CTL-8 (persistent-panel invariant) MOVED to Phase 3** as a surface row, on the same argument that moved MON-7 — its real consumer is the Curated UI's attention budget. **CTL-9**'s host driver stays outside core by design.

★★★ **PHASE 2 IS COMPLETE (2026-08-17).** The Monitor block closed with MON-8 → MON-13 → MON-4 → MON-1, and the Controller block closes with CTL-11 now that CTL-6 and CTL-8 have found their right homes. **What remains in the cognition layer is declared residual rather than unstarted work**, and is listed as such: MON-3's Metrics/Traces (its Logs third is already ✅ via the event log), and — ~~the **`MonitorEngine` consolidation wiring**~~ ✅ **done 2026-08-17**, discharging that residual on all four rows at once, and finding a real seam on the way (a season-suppressed detection was still being handed to OBSERVE). **→ Phase 3 (Surface + Immune) is next**, and it has gained two rows on the way here: **MON-7** (preattentive encoder) and **CTL-8** (persistent panels), both surface rows whose consumer is the Curated UI.

**Phase 3 — Surface + Immune.** ★ **Opened 2026-08-17 with EVT-15** (`semantic.rs`), taken first because it was the one remaining item blocking EDIT-11.
- ~~**EVT-15** (general replay + replay-under-`D′`)~~ ✅ **done 2026-08-17** — the semantic reducer re-runs the logged Enzyme **calls** through the real gate under a chosen `Definition`, so a call admitted under `D` can be **refused** under `D′`. ★ **`EDIT-11 IS NO LONGER BLOCKED`** — the warning below is struck: its diagnosis was checked and confirmed, and the reducer it named now exists. What remains on EDIT-11 is its own decision (migrate / grandfather / refuse the edit), not a missing mechanism.
- ~~**EDIT-11**~~ ✅ **done 2026-08-17** (`stranding.rs`) — sequenced immediately after the reducer that unblocked it, and it turned out to be **a decision, not a mechanism**: both checks existed, the surface did not. ★★ *Migration cannot fix a history* and *grandfathering is unsound while the state is stranded* are both enforced.
- ~~**EDIT-13**~~ ✅ **done 2026-08-17** (`lens.rs`) — a **lens**, not a projection: `π` forgets, `put` takes the old `D` to restore it, both round-trip laws executed, and a graph edit reaches `admit_edit` as a typed edit with EDIT-11's stranding check firing on it. ★★★ **M-EDIT IS COMPLETE, every row.** EDIT-14/15/16 are Python-side defects recorded here, not Rust build items.
- **★ `G` is the surface block's input** — UI-in-Rust now has a real graph to draw and a safe way back, so the dependency is discharged before the block opens.
- ~~**MON-7**~~ ✅ **done 2026-08-17** (`preattentive.rs`) — **the surface block's door**, opened first on purpose so its conventions (what a channel is, what gets encoded, what deliberately is **not**) are set before fifteen UI rows lean on them. ★★ It also discharges **UI-13** in advance: *salience is never rendered as salience* is now a property of the encoder rather than a rule the surface must keep.
- **Surface, remaining:** ~~UI-2~~ ~~UI-1~~ ✅ **2026-08-17** — **UI-3 … UI-15** (with **UI-13 discharged in advance** by MON-7 and **UI-7 unblocked**, since `d(s,V)` now replaces its proxy) and **CTL-8** (persistent panel).
- **Curated UI in Rust:** ~~**UI-2**~~ ✅ **2026-08-17** (`widget.rs`) — taken **first** because it changes a real property, so every later UI row is built against a load path that type-checks; STEP-0 found there was **no load path at all**, so it became *build the typed path with the check on it*. ~~**UI-1**~~ ✅ **2026-08-17** (`curated.rs`) — β + `compose(r)` + the knapsack, ported from Slice 13 with three sharpenings: the ∪ as a **sum type**, the **typed population** UI-2 bought, and ★★★ **cost derived rather than author-declared**, which closes the gaming surface the reference's own docstring describes and does not compute. **UI-3 … UI-15** remain.
- ~~**IMM-7** (approval token in the immune framing)~~ ✅ **done 2026-08-17** — and ★★ two of its three clauses were already built (the token = OPV-30/31, `permitted` = `principal.rs`, both 2026-08-12); the unbuilt one was the **confused deputy**, now fixed with capabilities. ~~**IMM-6** is 🔨~~ ✅ **complete** — `skins` closed alongside it. **IMM-1**'s *not you* conjunct is complete in both forms.
- ~~**IMM-10**~~ ✅ **done 2026-08-17** (`learned.rs`) — ★★ ParseRules are **Python-only**, so this built the generic mechanism rather than porting one; trust became a real input to **both** precedence and privilege, and the learned tier now holds a capability **attenuated to a declared ingress bound**, consuming IMM-7. ★★★ **This closes M-IMM's engine half.**
- **Immune, remaining — both app-layer:** **IMM-11** (a different-*kind* OTP detector — layering is independence of failure, not count), **IMM-12** (detection over the log, correctly LAST — a detector over an unmediated system reports a hole it cannot close).

**Phase 4 — DSL.** The one large shallow chunk: **DSL-1**'s type-check half (⬜), **DSL-5**, **DSL-8**, **DSL-9**, **DSL-10**, **DSL-12**, **DSL-13**, then **DSL-14** (the friendlier authoring surface, correctly last). *Note: the predicate-grammar gaps the snapshot names — arithmetic and dynamic bracket indexing — have no row of their own; they sit under **DSL-1** and were disclosed as spec findings, not invented for this phasing.*

**⏸️ DOUBLE-BACK — deferred, and deliberately as one deep bundle.** **OPV-4** (`Π`, the operator-DAG) plus the rows that only mean anything once it exists: **OPV-6** (learning / `vary`·`select`·`retain`), **OPV-7** (attention), **OPV-11** (`M_self` / `M_world`). Splitting them would mean building three consumers of a thing that is not there.
- ★ **Return trigger, recorded so it is not left to memory:** Phases 1–4 cleared → **the deep bundle is the immediate next work**; **OR** any shallow row turns out to genuinely depend on `Π` → **do `Π` then, not later**. The second clause is the one that matters — a discovered dependency is a signal, not an inconvenience to route around.

**⏸️ PARKED — standing decision, documented rather than forgotten.** The whole **Economy layer** (`M-PAWA`, and `MYC-5`'s treasury/settlement schedule with it). Not sequenced into any phase above; it re-enters only by an explicit decision to un-park it.

---

## The WBD — by layer, in dependency order

*Dependency order (from the capstone §9.1): Events & Time → DSL → Sustain-design → Operator → Constraint → Ingest → Monitor → (Curated UI ∥ Tenet → Operative → Multiparty); Editing after the operator path; Controller threads through from the first gate check onward.*

### Delivery track — the Modeling Studio (A–F)

*Ratified 2026-08-05, folded in from the retired `SUSTENA_WBD.md`. The layer order above says what to build; this says in what order it reaches a person. The phone collapses to the **capture edge**; the desktop Studio grows to cover **every** engine (Monitor / Control / Simulate / Edit / Network / Complexity) — the Capstone's "every engine in one frame". The `/devui` front end is database-first, the pattern the Curated-UI article identifies as what killed VOS: **rebuild the surface, keep the engine.***

| | Phase | Covers |
|---|---|---|
| **A** | Authoring foundations | rich dimension types (file, url, list-of-records), Enzyme authoring, typed invariants, the config layer — **the Amber-generality track**: declare a structurally-unlike Sustain with zero engine changes |
| **B** | Studio shell + visual structural editor + code hatch | `π : D → G` (= **EDIT-13**). Adopts the preserved slice-1 shell |
| **C** | Agentic modeling assistant | generate-and-verify, **bounded by the types and the gate** (= OPV-23's proposal-distribution posture) |
| **D** | **Operative layer PROPER** | the article's ten: utility vectors, DAG memes, gated fork+sim, council with declared weights, orchestrator, two attentions, Pareto frontier, Goodhart guard, **approval token**, M_self / M_world (= **all of M-OPV**; today's operatives are a stub of this) |
| **E** | Capture edge | strip non-capture phone UI; wire the native listener/parser (= M-ING) |
| **F** | The Library | the meme/artefact library (= OPV-5/6, M-PAWA/Arena) |

**UI principles (Bonnie's design standard, ratified):** form follows function — novelty only when functional; simplify through **navigation, not clutter** (accordions, hidden sidebars, tabs, clear CTAs, pagination — not a wall of cards); **one view, one job** (a view is the answer to one question); progressive disclosure over mega-windows; **"why am I seeing this?" answerable on demand**; stunning, not dumbed down.

**Gate-1, the acceptance milestone:** a stranger uses Sustena on real data for **30 days**. (M3 = declare Amber with zero engine changes; M4 = a non-technical person productive without Bonnie explaining anything.)

### Layer 1 — SUBSTRATE

#### M-EVT · Events & Time  ✅ read
*Article: `3B1B — Events and Time - Formal Execution.md` (RECORD · GAIA). The append-only log everything folds over; state = fold(events).*

| # | Feature (from the article) | § | Rust status |
|---|---|---|---|
| EVT-1 | **Event record** `⟨id, type, payload, t_event, t_ingest, source, causes⟩` — immutable, append-only, ordered | I | 🔨 **six of seven fields, 2026-08-17** — `t_ingest` and `source` now ship on `Event` (`event.rs`), both `Option` + `#[serde(default)]` so a pre-slice wire record still deserialises, orders, dedupes and folds byte-identically (asserted against a literal old-shape JSON, not assumed). `Source` is `⟨id, kind ∈ {connector,device,enzyme,human}, trust⟩` — §I's *"which connector, which device, which Enzyme, at what trust level"*. ★ **Two corrections to this row's own prior text, found by reading the code before building:** `causes` was **already present** (it shipped with EVT-8's vector clocks and is the DAG they derive from) — the row was stale, not the code, and it was not re-added; and `payload` was listed as present but **is not on `Event`**. ★ **The honest residual:** `payload` lives on `EmittedEvent {name, payload}` at execution time and **never reaches the durable record** — there is no `From<EmittedEvent> for Event` and nothing bridges them, because minting the id and stamping the two clocks is host territory. Adding an unwritten `payload` field to close the row on paper would be a field nobody populates, so the row stays 🔨 with the gap named. |
| EVT-2 | **Stable intrinsic `id`** (M-Pesa code, not minted UUID) | I·V | ✅ (dedupe keys on the stable id) |
| EVT-3 | **Dot-protocol `type`** `event.<domain>.<type>` (3+ segments); dispatch on declared type, never raw payload | I | ✅ (carried from the Python contract) |
| EVT-4 | **State = fold(apply, s0, L)** — reproducibility; incrementality (cache == rebuild is a testable law) | II | ✅ (R1 event-fold; `rebuild==get_state` proven). **Note: §4J.2's *"❌ State is not a fold"* is SUPERSEDED** — the fold is the write path, not a diagnostic. Two details the Note pins down: the fold is **patch-level, not semantic** (an opaque `{op,path,old,new}` produced while the Enzyme ran, so replay *cannot* diverge from what the Enzyme did — the honest cost is that replay-under-`D′` is a **named, sequenced debt**, = EVT-15/EDIT-11); and all of a call's mutations attach to the **first** published event, so **the fold's true unit is the Enzyme call, not the domain event** |
| EVT-5 | **Two clocks + skew** `= t_ingest − t_event ≥ 0`; skew is normal/unbounded/heavy-tailed; **negative skew surfaced, never clamped**; backfill marked `provenance='legacy'` | III | ✅ **done 2026-08-17** (`clocks.rs`). `skew_of` is one subtraction — **no threshold, no rejection, no error type**, so §III's week-long forwarding-macro outage is read exactly like a millisecond of skew, and a week-late event still orders back into its own past by `t_event`. ★★ **Negative skew is surfaced and structurally unclampable:** `Skew::Observed` carries a **signed** `i64`, `skew_of` is the only constructor and floors nothing, and `clock_findings` takes **`&[Event]`** — an immutable slice — so writing a corrected time back is *not expressible from the surfacing path*. The clamp is unspellable rather than refused, the same discipline that made a one-sided clamp on a conserved dimension unrepresentable in `flow.rs`. ★ **Three readings, two of them deliberately not numbers:** `Observed(ms)` · `Inferred` (a backfilled row whose two clocks read equal — the arithmetic is 0 and *means nothing*) · `Unknown` (no arrival ever recorded). `Skew::millis()` returns `Option<i64>`, so neither can decay into a `0` a caller averages in as a punctual arrival. ★ Which clock is wrong is a **provenance** question — `ClockFinding::suspect()` reads the source's declared `Trust`: authoritative → the receiver, merely-reporting → the device, absent/derived → `Undetermined`, a real third answer. That is the one consumer of `Trust`, so it is a field with a reader rather than a field with a plan. **Note on the row's own formula:** the `≥ 0` in §III's display equation is the *normal case*, not a constraint — §III's third consequence requires the negative reading to survive, and it does. *Backfill:* `Event::backfilled` sets `t_ingest` from the legacy row's single stamp as an **observation** and infers `t_event` from it under `Provenance::Legacy`, so the marker attaches to the half that was inferred and the observed half is not disowned. |
| EVT-6 | **Watermarks** `W(τ)` monotone; perfect vs heuristic; **per-window lateness policy** ∈ {drop, late-fire, accumulate-and-retract}; no window closes by wall-clock hope | IV | ✅ **done 2026-08-17** (`watermark.rs`). ★★ **No window closes by wall-clock hope, structurally:** `Window::is_closed_by` and `WindowState::close` take a **`&Watermark` and nothing else** — there is no `close_at(tau)` overload and no `From<i64>`, so a caller holding only a processing time cannot ask the question. A watermark has exactly two origins: a **declared source guarantee** or an **estimate over observed skew**. ★★ That does not make `W(τ) = τ` impossible — a genuinely in-order source has a zero bound — it makes it **sayable only by saying it**: reproducing §4J.3's failure now requires declaring `SourceGuarantee { max_out_of_orderness: 0 }`, the assertion *this source never delivers out of order*, in as many words. A conformance case runs both guarantees at the same τ over the same window: the zero bound closes, the honest 2h bound does not. **The failure stops being a default and becomes a signature.** ★ **Monotonicity structural** — the mark is private, `WatermarkTracker::advance` is the only mover, and a retreating proposal returns `Advance::HeldBack` (reported, not swallowed); a test shows why the requirement exists rather than restating it: a retreat would un-close a window that had already answered. **Explicitly distinguished from EVT-5's clamp rule** — flooring a *measurement* hides a fact; non-decreasing **is the definition** of this object, and the rejected proposal is surfaced. ★ **Heuristic estimated from EVT-5's skew, never invented:** a declared percentile of the observed skews, reporting percentile/allowance/sample-size. `Unknown` and `Inferred` readings contribute nothing (they are not zero skew), and with no observed skew at all it returns `NoObservedSkew` rather than a zero bound — **an unmeasured source is not a punctual one**. ★★ And **no volume of data promotes an estimate**: `estimate_heuristic` can only ever return `Heuristic`, at 3 samples or 500, because heavy-tailed skew means the largest ever seen is not a bound on the next. ★ **The lateness policy is MANDATORY, not optional** — `Window::new` takes a `Lateness` and the type implements no `Default`, so the compiler stops a window existing without one. That is the honest completion of a heuristic: you cannot promise the bound holds, so you must say what you do when it does not. Each cost is real in the type: **only `AccumulateAndRetract` retains its emitted value**, so a `Drop` window cannot issue a retraction even by mistake, and a dropped fact still lands in the late log — dropping it from the *answer* is not pretending it never arrived. |
| EVT-7 | **Exactly-once EFFECT** = at-least-once delivery ∘ idempotent apply; **uniform dedupe on the stable id at the substrate** (repeat = no-op, never raise) | V | ✅ (R2 #18 uniform dedupe) |
| EVT-8 | **Causal order** — happens-before; **Lamport logical clock**; **vector clocks** for concurrency detection (biconditional, O(n)); `t_event` ≠ causal clock, both needed | VI | ✅ **complete 2026-08-16** — the Lamport `(t_event,id)` tie-break shipped in R2 #17; **vector clocks and genuine concurrency detection now ship in `vclock.rs`** (= MUL-6), derived from the `causes` graph the log already records. §VI's *`t_event` is a physical stamp and is NOT a causal clock — both are needed* is kept structurally: `Stamped` carries both, `order_key()` answers *which reading is newer*, the vector clock answers *did this depend on that*, and a test asserts a concurrent pair stays concurrent across a 999,999-unit physical gap — **a huge time gap is not causal evidence** |
| EVT-9 | **Convergence theorem** — snapshot dim `x=(v,τ)`, LWW register on total order `(τ,id)`; join-semilattice → **invariant under permutation & repetition** (order-independent replay; strong eventual consistency) | VII | ✅ (R2 #17 converging LWW join) — **and §VII's own honest limit is now addressed** (2026-08-16): *"two genuinely concurrent edits to the same field under LWW means one is silently discarded"*. With concurrency detectable, `vclock::merge` returns a `MergeResolution` whose `Concurrent` variant **carries both values**, and `Dimension` declares which reading applies — `Snapshot` collapses by the physical order (correct, and what the theorem is scoped to), `Contested` keeps both. The theorem is unchanged; what changed is that its scope is now checkable rather than remembered |
| EVT-10 | **Dimension kind declared** (snapshot vs accumulating) → LWW attaches automatically and **delta-accumulation on a snapshot dim is a type error**; honest limit: LWW scoped to observations, contested edits → §4B merge-CRDTs | VII→DSL | ✅ **done 2026-08-17** (`dimension.rs`). ★★ **The type error is real and PROVEN BY A `compile_fail` DOCTEST** — `SnapshotDim` carries one that calls `.add(...)` on a balance read off an SMS, and `cargo test --doc` executes it, so the claim is discharged by the code genuinely failing to build. There is no `add` on the type: the mistake is **unspellable**, not caught. ★ **And the honest completion:** a kind declared in a *spec* with an update arriving off a *wire* cannot be a Rust type error, so `DimensionSchema::check` refuses it there too — with the reason travelling along (*not idempotent, so a redelivered delta would be a wrong balance*). Both halves are needed; claiming the compile-time one covered the declared-in-data case would have been the dishonest version. ★★ **Derived, not decreed, and both halves measured:** naive addition's non-idempotence is run as arithmetic (`x+2δ ≠ x+δ`), and made safe it *is* the grow-only counter — a redelivered delta leaves the state **byte-identical**, at the price of an applied-id map that reaches 200 entries after 200 deltas, against LWW's constant **1** after 500 readings. §VII's cost argument as two numbers rather than a sentence. ★ **LWW attaches from the declaration** — the caller declares `snapshot` and never chooses the apply; a stale reading arriving last does not clobber. Convergence is proven with `crdt.rs`'s own `converges`, which folds **every** permutation and then every permutation again with each update **delivered twice**. ★★ **And §VII's emphatic limit is kept structural:** the three kinds are distinct, `Contested` refuses automatic apply **entirely** (any auto-apply is a machine deciding which human was right) and is backed by `crdt.rs`'s OR-set, so the same two concurrent edits keep **both** as contested and exactly **one** as snapshot. LWW cannot eat a contested edit. ★ **Reconciled, not forked:** `vclock::Dimension` gained the third variant rather than a parallel enum being invented; that forced `MergeResolution::resolve` open, which turned a bare `Vec` meaning two things into a typed `Resolved::{Superseded, BothCount, NeedsReconciliation}` — three call sites corrected in place. The `(τ,id)` tie-break is `Observation`'s existing one, reused. **Residual:** the **DSL authoring surface** for declaring a kind is M-DSL's (Phase 4), and `DimensionSchema` is **not yet wired into `State`/`StateAccessor`**, so nothing yet forces an operator through it — both named rather than claimed. |
| EVT-11 | **Periods** — half-open `[a,b)` windows partition time; **RFC-5545 `RRULE` recurrence** + `DTSTART;TZID` anchored to a **zone id** (not a fixed offset); one period primitive, many consumers | VIII | 🔨 **the mechanism is ✅ done 2026-08-17 (`period.rs`); the residual is the rest of the RRULE grammar.** §VIII's *"same object seen twice"* is now complete in one place: EVT-6 built `W ≥ b`, this builds `[a,b)`, and `Period` **wraps EVT-6's `Window`** rather than inventing a second interval type — so a consumer that can close a window on a watermark needs nothing new, and EVT-6's mandatory `Lateness` rides through from the rule. ★★ **The partition is structural, not checked:** the expansion emits `[occ[i], occ[i+1])`, so each period's end **is** the next one's start and a gap or overlap is unrepresentable. Dijkstra (EWD831) at the roll-up point: **a year is the sum of its twelve months, not the months plus twelve boundary instants** — asserted by summing durations against the span AND by checking every boundary lands in exactly one month. ★★ **Store the rule, recompute the expansion:** `Recurrence` holds **no instants** — no field for them, no constructor accepting them, verified through serialisation. And the reason is run rather than described: **one rule under two tzdata releases gives two different instants** while the local time is identical, so a persisted expansion would keep the older answer with nothing to detect it. ★★ **Zone id, not a bare offset:** `Anchor::Zone` resolves through the host's tz data and every period records the `tzdata_version` that derived it; `Anchor::FixedOffset` is kept — real data arrives carrying only an offset — as **a distinct lesser thing** that consults no tz data and carries `tzdata_version: None`. The trap is shown, not warned about: it agrees with the zone today and diverges under the revised release. ★ **The tz data is INJECTED, not bundled** (`TzProvider`), and §VIII's own argument is why — a bundled database pins one release *invisibly*, hiding exactly the versioning the section warns about. The line is drawn where the versioning is: civil-calendar arithmetic is a fixed algorithm and is computed (Hinnant, round-trip tested); timezone offsets are versioned data and are only ever asked for. **No new crate dependency** (`Cargo.toml` unchanged). ★ **DST exercised where it is not vacuous:** Nairobi observes none, so a Europe/Berlin counterparty gives a real **23-hour** spring-forward day and a **25-hour** fall-back day, both still partitioning exactly; a nonexistent local start is refused and an ambiguous one resolves by a **declared** `DstPolicy` (no `Default`, same discipline as `Lateness`). ★★ **The honest residual — the RRULE grammar is partial.** `FREQ=DAILY|WEEKLY|MONTHLY`, `INTERVAL`, `BYMONTHDAY`, `BYDAY` ship, with RFC-5545's skip-invalid-dates rule honoured (31 Jan → Mar → May, February skipped rather than clamped). **`COUNT`, `UNTIL`, `BYSETPOS`, `FREQ=YEARLY|HOURLY`, `BYMONTH`, `BYWEEKNO`, `BYYEARDAY`, `WKST`, `EXDATE`/`RDATE` and multi-value BY- lists do not**, nor does an `RRULE:` **text parser** (the rule is a typed value). And **no consumer has been migrated onto it** — budgets and Monitor could read it and do not yet, so this is a primitive that is ready rather than one in use. |
| EVT-12 | **Replay + checkpointing** `(s_k,k)`, cost O(n−k), always discardable | IX | ✅ **done 2026-08-17** (`checkpoint.rs`). `fold(s₀,L) = fold(s_k, L[k+1..n])` — §II's homomorphism — verified at **every** `k` from 1 to n, not one convenient one. ★★ **Always discardable, structurally:** `replay` takes `Option<&Checkpoint>`, so the fast path and the from-scratch path are the **same function** and discarding is passing `None` — there is no separate slow implementation that could drift. ★★ **Derived, never authored:** `Checkpoint::at` is the only constructor and it *folds the log*; there is no constructor taking a state, so a checkpoint claiming a state the log never produced is not writable. `events_applied()` makes the `O(n−k)` claim **countable** (2 instead of 5, in a test). ★ **The cheap/expensive split is stated rather than blurred:** attachment is checked in `O(1)` by the event id the checkpoint was taken through — catching the realistic failure, a checkpoint on the wrong log — while only `verify()` re-folds and catches a state corrupted afterwards, at `O(k)`, exactly what the checkpoint saved. Both offered; neither pretended to be the other. ★★ **Replay cannot reach an effect channel:** §IX's `ASSERT no_external_effects_were_issued()` is a fact about the signature, not a check at the end — `replay` takes `&[Event]`, returns a `Value`, and has no sink, no movement channel, no registry and no `execute` in reach. The contrast is the proof: an operator body receives `&mut Vec<EmittedEvent>` and `&mut Vec<Movement>` because emitting is what it is for. ★★ **A finding about §IX's own pseudocode:** `seen = set()` is per call, so ids folded into the checkpoint never enter it — a **late duplicate of an early event is skipped from scratch and applied from a checkpoint** (380 vs 100), which quietly breaks the same section's discardability claim. Carrying the folded id set would make the checkpoint grow with `k`, which is most of what it was for, so the fix is taken where §V already puts it: `replay` requires a substrate-deduped log and refuses otherwise. A conformance case runs the pseudocode **literally** to show both answers. ★ `replay_to` answers §IX's first consumer (Tenet's *stand in a chosen future*); a checkpoint past the point asked for is **ignored, not an error** — the log is always sufficient. |
| EVT-13 | **Purity of `apply`** — no ambient clock / randomness / I/O | IX | ✅ structural — the Rust core is pure; identity + time come from the host |
| EVT-14 | **External effects journaled as their own events** (decision-event + fact-of-sent event; apply never sends; no re-fire on replay) | IX | 🔨 partial — egress prepare/confirm exists (Python); Rust journaling model pending |
| EVT-15 | **General replay** consumed by **Tenet** (stand in a future) and **Editing** (replay under D′) | IX | ✅ **done 2026-08-17** (`semantic.rs`) — **Phase 3 opens here.** ★★ **The premise was checked in the code before anything was built on it, and all three parts held:** (1) the patch fold **is** `D`-independent — `checkpoint::replay` takes a log and a checkpoint and there is **no parameter through which a definition could enter**, which is a *property* rather than a shortfall and is precisely why the semantic layer sits **beside** it; (2) ★ the ingredient exists **in the reference, not here** — `operators_log` carries `operator_name` + `input_json` per call and the Rust `Event` carries neither, so `EnzymeCall` is defined here as the addition the reducer cannot exist without; (3) ★★ **`D′` needed no new type** — `editing::Definition` is already `⟨schema, invariants, operators⟩`, the gate's own three inputs, so replaying *under* `D′` is the same `execute` with a different `Definition`. ★★ **Same `D` reproduces; `D′` reinterprets.** The consistency check is asserted (re-running the calls lands where the patches did — without it every `D′` verdict would be about a different history), and under a `D′` that bites, the answer is **partly** inadmissible with the verdict pointed at the specific call: a floor of 1500 leaves the income admissible (1000→1500 clears it exactly) and refuses the allocation (→1200). `NotPermitted` is kept **separate** from `Refused`, because the allow-list and the gate answer different questions and collapsing them would tell an editor to fix the wrong thing. ★★ **Replay emits nothing, guaranteed twice over:** structurally this core has no sink, no egress and no send — **EVT-14 is not built in Rust**, stated rather than assumed away — and explicitly, `ReplayMode::Replay` **discards and counts** every emission, so the guarantee stays legible once an effect layer exists. `Live` must be *named*, the same discipline `Authorization::Unchecked` follows. ★ **Purity:** `is_deterministic` runs the calls twice and compares — which detects non-determinism and **does not prove purity**, stated as a limit rather than left to be discovered. ★ **Tenet's consumer was already served** by EVT-12 + the simulator; the genuinely new work is the Editing one. **Residuals:** the call log is **not wired to `execute`** (nothing yet records an `EnzymeCall` when an operator runs); `boundary`/`firewall` are not part of a `Definition`, so a `D′` replay checks schema/invariants/allow-list and not `μ` or `F`; and EVT-12's checkpoints do not transfer, since a checkpoint under `D` is not one under `D′`. |

**Summary:** the *convergence core* — fold, event-time LWW, dedupe, purity — is ✅ in Rust (the hardest part, already done). **Update 2026-08-16: the two-clock split is closed** (`vclock.rs`, EVT-8 = MUL-6). §VI's *`t_event` is a physical stamp and is NOT a causal clock — both are needed* is now structural: `Stamped` carries both, and they answer different questions. Vector clocks restore the biconditional the scalar clock cannot, and the gain is concrete rather than theoretical — the scalar answer reports a **false concurrent** across a transitive chain that crosses a node boundary, and a conformance case runs both answers side by side. That also sharpens **EVT-9 at the exact point §VII flags against itself**: *"two genuinely concurrent edits to the same field under LWW means one is silently discarded"* — with concurrency detectable, `merge` keeps **both**, and `Dimension` declares whether the snapshot reading (collapse, correct) or the contested one applies. Clocks are **derived from the `causes` DAG the log already records**, so no field was added to `Event` and every R1 parity vector keeps passing untouched.

**Update 2026-08-17 — the OTHER two-clock split is closed (EVT-5, `clocks.rs`), and it is a different pair from EVT-8's.** EVT-8 split *physical time* from *causal time*; this splits **when it happened in the world** from **when the system heard about it**, and the gap between them is the skew. ★★ The load-bearing property is that a **negative** skew survives: `Skew::Observed` is signed, `skew_of` floors nothing, and `clock_findings` takes an **immutable** slice so the surfacing path cannot repair what it reports — the clamp §III warns about is unspellable, not merely discouraged. ★ Three readings, and two are not numbers: an absent arrival reads `Unknown` and a backfilled row reads `Inferred`, neither collapsing to a `0` that would pass for a punctual arrival. Skew is unbounded on purpose — there is no threshold and no error type anywhere in the module, so §III's week-long outage is accepted as it must be, and **EVT-6 is unblocked without being pre-empted**. ★ Two stale claims in this module's own rows were corrected while reconciling: `causes` was already present (EVT-1 listed it pending) and `provenance` was already present (EVT-5 listed it pending) — both were re-read rather than re-built. And one went the other way: `payload` was listed as present on the record and **is not**, so EVT-1 stays 🔨 with that residual named. **Update 2026-08-17 (same day, straight on) — EVT-6 closed, and it is the row the whole substrate was pointing at.** ★★ *No window closes by wall-clock hope* is now **structural**: closing takes a `&Watermark` and nothing else, so a caller holding only a processing time cannot ask. §4J.3's failure — windows closing because a date rolled over — is exactly `W(τ) = τ`, and it remains **possible but no longer accidental**: it requires declaring `max_out_of_orderness: 0`, the claim *this source never delivers out of order*, out loud. A conformance case runs the zero bound and an honest 2h bound at the same τ over the same window and shows one closing and the other not. ★ Monotonicity is structural and **explicitly distinguished from EVT-5's clamp rule** — there, flooring a measurement hides a fact; here, non-decreasing *is* the definition, and a retreat is reported rather than discarded. ★ The heuristic is **estimated from EVT-5's surfaced skew**, honouring its three readings (an `Unknown` or `Inferred` skew contributes nothing, and with none observed the estimator errors rather than returning a zero bound — *an unmeasured source is not a punctual one*), and **no volume of data ever promotes it to a guarantee**. ★ And because a heuristic will sometimes be wrong, the **lateness policy is mandatory** — `Lateness` has no `Default`, so the compiler enforces it — with each cost real in the type: only `AccumulateAndRetract` retains its emitted value, so `Drop` cannot retract even by mistake. ★ The divergence is a **specific failure rather than an absence**: the reference has three real windows (`calendar.upcoming`, `tasks.due_soon`, `protege.urgent_window_hours`) and every one closes on `ctx.timestamp` — with a genuine credit alongside it, that clock is **injected not ambient**, so EVT-13's purity holds and the defect is the value assumed, not an ambient read. **Update 2026-08-17 (third slice the same day) — EVT-11's mechanism closed, completing §VIII's *same object seen twice* in one place.** `Period` wraps EVT-6's `Window`, so the two halves meet rather than sitting beside each other. ★★ The partition is **structural** — each period's end *is* the next one's start — which buys Dijkstra's point where it matters: **a year is the sum of its twelve months, not the months plus twelve boundary instants**. ★★ **Store the rule, recompute the expansion** is a property of the type (`Recurrence` holds no instants), and the reason is *run*: one rule under two tzdata releases yields two different instants with an identical local time. ★★ **Zone id over bare offset**, with `FixedOffset` kept as a labelled lesser thing whose trap is demonstrated — it agrees today and diverges under the revised release. ★ The tz data is **injected** (`TzProvider`), and §VIII's own argument is the reason rather than the ADR alone: a bundled database pins a release invisibly. Civil-calendar arithmetic is computed (fixed algorithm); timezone offsets are only asked for (versioned data). **No new dependency.** ★ DST is exercised by a Berlin counterparty since Nairobi's case is vacuous — a real 23-hour and 25-hour day, both still partitioning. **Update 2026-08-17 (fourth slice the same day) — EVT-12 closed** (`checkpoint.rs`). ★★ Discardability and derived-never-authored are both **structural**: one function with an `Option` argument, and no constructor that takes a state. ★★ Replay **cannot reach an effect channel** — no sink, no registry, no `execute` — so §IX's assertion about external effects is a property of the signature. ★★ And it found a real gap in §IX's own pseudocode: the per-call `seen` set makes the checkpointed and from-scratch paths **disagree on a late duplicate**, demonstrated by running the pseudocode literally; the fix is taken at the substrate, where §V already puts dedupe. ★ The counterweight is generous and exact: the reference **already runs the homomorphism** (`sustain_states` advances one call at a time — `fold(s_k, L[k+1..n])` at `k = n−1`) and its discardability is real and **tested** there (`rebuild_state()` regenerates the cache and the law is asserted). The genuine difference is that `_persist_state()` makes a wrong cache *writable* and forbids it by docstring, where `Checkpoint::at` makes it unwritable. **Update 2026-08-17 (fifth slice the same day) — EVT-10 closed, and with it the module's centrepiece stops being remembered and starts being checked.** ★★ Delta-accumulation on a snapshot dimension is a **type error proven by a `compile_fail` doctest** that `cargo test --doc` executes — there is no `add` on `SnapshotDim`, so the mistake is unspellable — with a schema-level refusal for the half a type system cannot reach (a kind declared in a spec, an update off a wire). ★★ *Never accumulate deltas* is **derived and measured**: non-idempotence run as arithmetic, and the safe version's applied-id map at 200 entries against LWW's constant 1. ★★ §VII's emphatic limit is structural — three distinct kinds, `Contested` refusing automatic apply entirely and backed by the OR-set, so **LWW cannot eat a contested edit**. ★ And the counterweight is the strongest in the set: §VII's own field note says the LWW behaviour shipped in the reference **first**, derived empirically after out-of-order SMS clobbered a balance — *"the convergence theorem is not being proposed to VOS; it is being written down about VOS."* This row did not bring a behaviour; it turned a hard-won rule into a checked one. ★ `vclock::Dimension` was **extended, not forked**, which turned `resolve` into a typed `Resolved` outcome.

**★★ The Events & Time module's attack list is now complete** — EVT-5, EVT-6, EVT-11's mechanism, EVT-12 and EVT-10 all closed on 2026-08-17, on top of the convergence core that was already ✅. **Declared residuals, all named rather than quietly open:** EVT-11's **RRULE-grammar remainder** (`COUNT`/`UNTIL`/`BYSETPOS`/`YEARLY`/`BYMONTH`/`EXDATE`/…, an `RRULE:` text parser, and migrating a real consumer onto the primitive); EVT-10's **DSL authoring surface** (M-DSL, Phase 4) and its `State`/`StateAccessor` wiring; **EVT-14** effect journaling; and **EVT-1's `payload`** on the durable record.

**Update 2026-08-17 — EVT-15 closed, and it opens Phase 3.** ★★ §IX's **second** consumer, which EVT-12 deliberately left: the **semantic** layer, built *beside* the patch fold rather than into it. The premise was checked in the code first and held in all three parts — the patch fold is `D`-independent (**no parameter through which a definition could enter**), the logged call exists **in the reference's `operators_log` and not in the Rust `Event`** (so `EnzymeCall` had to be defined), and **`D′` needed no new type** because `editing::Definition` is already the gate's own three inputs. ★★ Under the same `D` the calls reproduce the patch state (asserted — without it every `D′` verdict would be about a different history); under a `D′` that bites, the history is **partly** inadmissible with the verdict on the specific call, and `NotPermitted` is kept separate from `Refused`. ★★ Replay emits nothing, guaranteed twice: structurally (no sink, no egress, no send in this core — **EVT-14 is not built in Rust**) and explicitly (`ReplayMode::Replay` discards **and counts**, so the guarantee survives an effect layer being added). ★ Tenet's consumer was already served by EVT-12 + the simulator; the new work is Editing's. **EDIT-11 is unblocked.**

### Layer 2 — DEFINITION

#### M-SUS · Sustain design (S)  ✅ read
*Article: `3B1B — Sustain - Formal Execution.md` (CELL). The one recursive object `Σ = ⟨B, S, V, T, ⊕⟩`. (Naming: Enzyme=operator, Symbiont=operative, Embroidery=dsl — canonical terms, code aliases until rename.)*

| # | Feature (from the article) | § | Rust status |
|---|---|---|---|
| SUS-1 | **`Σ = ⟨B, S, V, T, ⊕⟩`** — the single recursive object; components of a Sustain are Sustains; no privileged top. Closes Phase 0 = ⟨S,T⟩ | I | 🔨 partial — S,V,T,⊕ present; **B (μ) partial** |
| SUS-2 | **S = typed state space** — product of named typed domains (record type, not JSON blob); path projection `π_p`; partial-state = projection onto subspace (enables modular/parallel eval) | II | ✅ (R2 #14 typed record state) |
| SUS-3 | **Type-checked predicates** — guard/invariant/postcondition referencing an undeclared dimension is a **load-time type error** (`bind(predicate_ast, schema)→errors`) | II | ✅ (predicate AST bound to schema at load) |
| SUS-4 | **Five required S properties** — serializable, addressable, composable, evolvable (schema versions without invalidating Enzymes), deterministic | II | 🔨 partial — serializable/addressable/composable ✅; **evolvable (schema versioning) → M-EDIT** |
| SUS-5 | **State = fold** `s(t)=fold(events[0..t])`; monoid homomorphism/catamorphism; reproducibility, incrementality (cache==rebuild law), time-travel; ops replace_root/set/append/remove; `FoldError` never silent | III | ✅ (R1 fold; rebuild==get_state proven) |
| SUS-6 | **Patch-level vs semantic events** — patch replay can't diverge from what the Enzyme did; semantic layer addable later without migration (named debt) | III | 🔨 patch-level ✅; semantic layer ⬜ (needed by M-EDIT replay-under-D′) |
| SUS-7 | **B = boundary ⟨scope, μ⟩** — μ a **membership predicate** deciding what the Sustain owns; private-vs-shared is structural (where a var sits relative to B), not a permissions bolt-on | IV | ✅ **built 2026-08-17** (`boundary.rs` · `Boundary`, `BoundaryDecl`, `Scope`, `Owned`, `BoundaryChange`, `BoundaryToken`, `preserves_membership`, `admit_boundary_change`) — **Phase 1's keystone.** `B = ⟨scope, μ⟩` is a real membership predicate over `Entities ∪ Paths`, replacing exactly the model §IV names by its symptoms: the reference's `{"owner_ids": ["system"]}` is *"a state dict with an owner field"*. ★ **`owns` keeps §IV's TWO conjuncts as two** — `scope.contains(x) ∧ μ(x)` — so a path μ claims but scope does not span is **not owned**, which catches a Sustain declaring ownership of something it has no business in. ★ **μ is subscripted by the state, which is what makes the law checkable:** the roster is state, so who belongs is something an operator could move; `BoundaryDecl::read` evaluates μ AT a state and `μ_{o(s)} = μ_s` compares two real values. ★ **μ has two halves with different natures, and saying so is the accurate thing:** the path half is DECLARED (changing it is a definition edit — that higher gate already exists in `editing.rs`), the entity half lives in STATE and is the half an ordinary Enzyme could move, so that is where the closure law bites. An **unreadable** register is distinguished from an **empty** one — *"nobody is a member"* and *"the roster is not where it was declared"* are different facts, and an operator that deletes the roster is refused under its own reason rather than silently emptying the boundary. ★ **Person-first nesting is a checkable property, not a convention:** `owns_interior_of` is always false — a parent owns the child and **not** the paths inside it, which is the whole of *"composes into Homestead and Vyyb boundaries without dissolving"* |
| SUS-8 | **Autopoietic closure law** — `∀o∈T,∀s: μ_{o(s)}=μ_s ∧ schema(o(s))=schema(s)`; Enzymes change values inside B, never B itself; changing B is a distinct **higher gate** (add member, adopt child) | IV | ✅ **completed 2026-08-17** — the schema half shipped with R2 #14 and the **μ half joins it**: `preserves_closure` is the whole conjunction `μ_{o(s)} = μ_s ∧ schema(o(s)) = schema(s)`, enforced in `operator::execute` before the invariants. ★★ **Structural at the gate, not a convention:** a registered operator that appends to the roster is **refused, state untouched, no mutations, no events**, tagged `boundary_closure` — proven with a real operator through `execute`, not a predicate call. The two halves are **tagged apart** (`boundary_closure` vs `organisational_closure`) because they fail for different reasons: one wants a migration, the other a `BoundaryToken`. ★ **The separate boundary-change gate is built and REUSES the definition gate's authority** — `BoundaryToken::mint` takes the same `Governance` and `EditAuthority`, so a shared Sustain's boundary needs a `CouncilMint` and no single actor converts into one. **Two paths mean nothing to exempt**: an ordinary operator cannot reach μ at all, where a single-path design would have to carve the boundary operators out of its own law |
| SUS-9 | **V = viable region** — box of per-dim intervals ∩ cross-dimension constraints; **a region, not a boolean list** → measurable / projectable / intersectable / searchable (VI–VIII need this); `in_region(s,V)` | V | ✅ **DONE in Rust** (`region.rs`, 2026-08-12) — **V is a first-class region**: `Region { intervals, relations, weights }`, with all four operations the article says a list cannot do. **Measured** (`distance` → `W`), **projected** (`Distance::binding` names which dimension binds), **intersected** (`fits_within_report` — child ⊆ parent, with the offending dimension named), **searched** ⬜ (needs the kernel, SUS-10). Membership is **three-valued** (`Interior` / `Boundary` / `Outside`), because `W ≡ 0` inside cannot rank anything |
| SUS-10 | **Viab_T(V) = viability kernel** — states from which *some* legal-move sequence keeps s∈V **forever**; greatest fixed point by descending iteration `D_{k+1}=Φ(D_k)`; the only honest target for "keep alive"; limits: exponential in dims (needs sampled/receding-horizon approx), finite-horizon `Viab^H` is an optimistic superset | VI | ✅ **DONE in Rust** (`kernel.rs`, 2026-08-12) — the greatest fixed point by **descending iteration** `D₀=V, D_{k+1}=Φ(D_k)`, terminating on an enumerated/discretised space. **Both of §VI's honest limitations are ENCODED, not worked around:** (1) exponential-in-dimensions → the exact computation runs over an **explicitly enumerated** space and claims nothing about states nobody enumerated; (2) `Viab^H` is a **superset** and therefore **optimistic** → `Kernel::Horizon` is a separate variant with **no constructor that turns it into `Exact`**, so reporting an optimistic set as a guarantee is *unspellable*. A vector proves `Viab^H ⊇ Viab_T(V)` at several horizons. **The headline: a state in `V` and outside the kernel — *fine right now, already lost*** |
| SUS-11 | **Lyapunov control** — `W(s)=d(s,V)`; pick Enzyme minimizing `W(o(s))`; descent ≥α ⇒ re-enter in ⌈W/α⌉ steps; weights `w` declared (not buried); inside V secondary objective = **kernel margin**; rollback = declared inverse to lower-W state | VII | 🔨 **`W(s)=d(s,V)` ✅ and the kernel margin ✅** (2026-08-12) — the weighted distance with `W(s)=0 ⟺ s∈V̄`, declared weights `w` (`undeclared_weights()` surfaces every silent `1.0`), and **`kernel_margin()` now supersedes the box margin**: `region.rs`'s `margin()` measures the edge of `V` and was labelled an honest **over**-estimate when it shipped; the kernel edge is nearer, and a vector measures both (20 → 10). **Still ⬜: the argmin-over-Enzymes control step and the α-descent guarantee** — those need `T` and belong with M-CTL/M-TEN |
| SUS-12 | **⊕ composition + ρ roll-up** — `S_P = own × ∏children × A`; aggregate `⊗_a` must be a **commutative monoid** (sum/min/max/count ok; avg → carry (sum,count), divide at end); computed fresh, never persisted | VIII | ✅ (Slice 7 + R2 composition/rollup) |
| SUS-13 | **Roll-up rules** — ρ is a function not a variable (no stale total, no reconciliation); **missing ≠ zero** (excluded + named); **nested viability** (whole viable ⟺ every level viable); **holon law** — parent value is a *fold over* members, never an *overwrite* (structural via monoid) | VIII | ✅ (excluded+named, is_self parent-fold, holon-by-construction) |
| SUS-14 | **Per-rule authority, advisory by default** — binding aggregate invariant refuses a child transition only on a **newly-caused** breach (person-first: already-breached parent doesn't block a non-worsening child action) | VIII | ✅ (R2 `_check_parent_binding_gate`, authority default advisory) |
| SUS-15 | **Determinism + canonical hash** — Enzymes pure (non-determinism captured into the event); `H(s)=hash(canonical(s))` makes verification a comparison; `fold(log)==cache` fails loudly on divergence | IX | 🔨 purity ✅ structural; **canonical-serialization hash `H(s)` ⬜** |
| SUS-16 | **Domain declaration (Cynefin)** — `domain: R→{clear,complicated,complex,chaotic}` **per region** of S; disorder = failure-mode to detect/surface; domain is a *reading* (Monitor detects shift via CUSUM); justified by Ashby requisite variety + Conant-Ashby "good regulator = model" | X | ⬜ **NOT STARTED** (declared in old spec §4D.6, absent from code) |
| SUS-17 | **Goodhart guard** — for every un-optimised dimension `d∉U` (U=∪ Symbiont utility dims), auto-emit a bound invariant into V so un-scored dims are gated structurally, not by review habit | XI | 🔨 **half built 2026-08-16** (= OPV-27, `goodhart.rs`) — `U` is computable, a guard over it is declarable, and the **gate enforces it**; a guard that watches nothing unwatched is refused. ★ **The AUTO-EMIT half is deliberately NOT built**, and the reason is a real one rather than scheduling: auto-emitting a *bound* invariant requires knowing **what bound**, and nobody declared one. A fabricated threshold over a dimension nobody was watching would be exactly the confident-number-from-nowhere this codebase refuses elsewhere (`0/0 ≠ r = 0`; a missing cost is not free; a missing reading is not zero). What could be auto-emitted honestly is the **finding** — *"this dimension is unwatched and has no guard"* — which is what `Coverage` already reports. Emitting a real bound needs a person to say what it is |

**Summary:** the core — typed S, fold, load-time predicate binding, composition/roll-up with the holon law + person-first authority — is ✅ in Rust. **Not started:** the viability kernel `Viab_T(V)`, Lyapunov homeostatic control (shared with Controller), the Cynefin domain field, the Goodhart guard, canonical-hash verification, and the full μ membership boundary. **★ Update 2026-08-17 — μ IS NOW BUILT, and it is Phase 1's keystone** (`boundary.rs`, SUS-7/SUS-8/OP-10): `B = ⟨scope, μ⟩` replaces the flat `owner_ids` §IV names by its symptoms, the **autopoietic closure law is enforced in both halves at the gate** (`μ_{o(s)} = μ_s ∧ schema(o(s)) = schema(s)`), and boundary change is a **separate higher gate reusing `editing.rs`'s authority** rather than a second model. Two paths mean **nothing to exempt** — an ordinary operator cannot reach μ at all. This **unblocks the firewall `F`** (CON-1), which was blocked on exactly this and is the immediate next slice. Region-as-first-class-object and schema-versioning (evolvable) are partial (→ M-CON, M-EDIT). **Update 2026-08-12:** **V is now a first-class region in Rust** (`region.rs`) — measurable, projectable and intersectable, with `W(s) = d(s,V)` and declared weights. That closes SUS-9 and the distance half of SUS-11, and it is the substrate SUS-10's viability kernel now has something to be a subset *of*. **Update 2026-08-12 (second):** **`Viab_T(V)` now exists** (`kernel.rs`) — the descending iteration to the greatest fixed point, with both of §VI's limitations encoded in the types rather than footnoted, plus **runway `R(s)`**, the cheap forward-scan clock the Operative article names as the best value per line of code once `V` is a region. Together these close SUS-10 and OPV-19, upgrade SUS-11's margin from the box to the kernel edge, and sharpen CTL-2. **This is the ingredient Tenet's backward induction needed.**

#### M-OP · Operator (T)  ✅ read
*Article: `3B1B — Operator - Formal Execution.md` (ENZYME). The guarded transition; the whole registry IS T. Guard→effect→emit.*

| # | Feature | § | Rust status |
|---|---|---|---|
| OP-1 | **Enzyme = ⟨guard g, effect e, emission ε⟩** — partial map `dom(o)={(s,p):g_o}`; partiality is where the law lives; **every effect must emit** (keeps state=fold honest) | I | ✅ (R1 operators; emit mandatory) |
| OP-2 | **Hoare triple `{g} e {Q}`** — pre/postcondition; refusal computed on a **copy**, judged, *then* committed (nothing durable until Q passes) | II | ✅ (R2 gate checks post on candidate) |
| OP-3 | **Weakest-precondition obligation** `g ⟹ wp(e,Q)` — static author-time claim the guard is strong enough; flag an Enzyme whose Q its guard can't guarantee | II | 🔨 **`wp` itself is built** (`compose.rs`, 2026-08-12) — the pullback stays inside the predicate fragment for an unchanged path, a `SetTo` (constant-folds to always/never) and a `ShiftBy` (moves the literal). **✅ COMPLETED 2026-08-17** (`obligation.rs` · `Soundness`, `ObligationReport`, `check_obligation`, `audit`, `flagged`; `OperatorMeta.effect`). The `EffectSummary` is **`Option`, absent by default** — no operator declaration was force-touched and no behaviour changed. ★★ **It FLAGS, it does not BLOCK**, which is the posture §III actually takes (*'should at least be flagged, not silently trusted'*): nothing here is called from `execute`, the per-call dynamic check is untouched, and a test asserts an unsound operator is **flagged at author time AND still runs**, caught dynamically after the fact — *'both are useful, and they are not the same thing.'* ★ **Three honest outcomes, and the third is the point:** `Sound`, `Unsound` (the flag), and **`Unavailable`** — because both other readings of *we cannot tell* are wrong. Silently passing an unsummarisable effect is exactly the *silently trusted* this row exists to prevent; flagging one would report a defect that has not been shown. **An unknown effect is not a proven-wrong one.** ★★ **What running it over the real registry FOUND:** every shipped operator is `Unavailable`, because Sustena's effects are **parameter-driven** — the amount comes from the call, which the fragment itself names `Opaque`. So the static obligation's reach today is **zero operators proven either way**, which is precisely why §III says *'the honest position for Sustena today is the dynamic one'* — the check does not contradict that sentence, **running it is what demonstrates it**. Asserted in a test, so a future summarisable operator breaks it and forces a look. Reuses `compose.rs`'s `wp` and `entails` unchanged; builds no prover. Limit recorded: **a summary is a declaration and can be wrong** — checking it against the body is a separate, larger row, which is why it is absent by default rather than inferred |
| OP-4 | **Determinism spectrum** pure/bounded/impure; **capture every non-deterministic input into the event** so replay sees recorded not fresh | III | ✅ structural — Rust core pure; identity+time from host (captured) |
| OP-5 | **Checked composition** `post(A) ⊨ guard(B)` at authoring time; composite guard `g_A ∧ wp(e_A,g_B)`; 3-valued **entailed/refuted/undecided**; decidable on the Embroidery fragment or via SMT | IV | ✅ **DONE in Rust** (`compose.rs`, 2026-08-12) — `compose(A,B)` returns `Result<Composed, Rejected>`, so a **refuted chain yields no composite value at all**, and `Pathway::try_chain` has **no constructor that takes steps on trust** — *a recipe whose third step can never fire cannot be held*, not merely rejected on save. All three parts derived: `g_A ∧ wp(e_A,g_B)`, `post(B)`, `ε_A · ε_B` (cost additive). **Associativity is checked, not assumed.** ⊨ is decided over conjunctions of path-vs-literal **interval** constraints; everything else returns **undecided** with `runtime_guard_required` + a reason — never a false claim. No SMT solver pulled in (a dependency-free core is what ships to a phone) |
| OP-6 | **Declared inverses** — `e_{o⁻¹}(e_o(s))=s` + `image(e_o)⊆dom(o⁻¹)`; reversible subset `T_r`; **generic patch-level inverse** from recorded `{op,path,old,new}`; ties to Controller (exact W restoration) | V | ✅ (R2 #15 patch-level inverse from the record) |
| OP-7 | **Effective guard** `ĝ = g_o ∧ ⋀ invariants(e_o(s)) ∧ permitted(actor)` — three distinct refusals: **not now / not ever / not you** (never collapse to "denied") | VI | ✅ — all three terms in Rust (guard ✅, invariants-of-result ✅, privilege ✅ R2 #10) |
| OP-8 | **Reference monitor** — complete mediation (every access checked, unbypassable), fail-safe defaults, least privilege; `e_o(s)∈V` in every gate ⇒ **V is an induction invariant** | VI | ✅ **structural win** — Rust core has one execution path, so complete mediation holds by construction (Python's was opt-in/bypassable) |
| OP-9 | **T = the transition model** `⟨S,T⟩` labelled transition system; reachability/viability/control/simulation/planning all defined over T | VII | ✅ |
| OP-10 | **Closure + completeness** — Enzymes preserve boundary+schema (autopoiesis); any mutation without an event is **forced into the log** (`event.system.unlogged_state_change`) | VII | ✅ **complete 2026-08-17** — structural (one write path), and the **boundary-change higher-gate now exists** (= SUS-7/SUS-8, `boundary::BoundaryToken`). Autopoietic closure is enforced in both halves at `operator::execute`, so *Enzymes preserve boundary + schema* is a refusal rather than a description |
| OP-11 | **Price of a move** — declared `cost(o)` (gas-style, up front) vs **metered ⟨compute,storage⟩** (odometer, after); **compute = work not wall-clock** (reproducible); guard is metered; refused = zero | VIII | ⏸️ economy-deferred (Python meter exists; Rust metering rides the economy layer) |

**Summary:** the Enzyme core is ✅ in Rust, and the effective guard is **more complete than Python** — Rust makes complete mediation *structural* (one path) and enforces privilege (the "not you" term). **Checked composition shipped 2026-08-12** (`compose.rs`, R2 #16): illegal chains are now discovered at WRITE time rather than by running them and watching step three refuse, and a refuted pathway is unconstructible rather than merely refused. **Update 2026-08-17: OP-3 is ✅** (`obligation.rs`) — the author-time obligation `g ⟹ wp(e,Q)` now runs on a single Enzyme, off the execution path, as the **flag** §III asks for rather than a gate term. Its finding is worth carrying: **every shipped operator reports `Unavailable`**, because Sustena's effects are parameter-driven and the fragment names that `Opaque` — which is exactly why §III calls the dynamic check the honest position today. **With OP-3 closed and the boundary/firewall rows before it, the Operator layer has no open rows left but metering** (economy-deferred). Python's audited gaps (opt-in gate, unread privilege, no inverse) are exactly what Rust closed by construction.

#### M-CON · Constraint engine (V binding)  ✅ read
*Article: `3B1B — Constraint - Formal Execution.md` (LAW). The gate that makes V binding; the induction that keeps V true. (Tolerance/self–non-self layer is M-IMM.)*

| # | Feature | § | Rust status |
|---|---|---|---|
| CON-1 | **Three predicate families** — **state** `C:S→bool` (A=∩C_i=V), **transition** `D:S×S→bool` (over the pair), **boundary/firewall** `F(φ,s)` (flow across B); none reduces to another; composition = **conjunction only** (monotone, no priority/override) | I | ✅ **state `C` ✅ and transition `D` ✅** (`transition.rs`, 2026-08-12) — `D` is the gate's third conjunct, evaluated over the (before, after) **pair**, with all three canonical shapes: **rate limit**, **monotonicity** and **conservation**. Proven strictly more expressive by a vector where the after-state is *individually valid* and only the STEP is refused. **Firewall `F` ✅ built 2026-08-17** (`flow.rs` · `Movement`, `Flow`, `Crossing`, `FlowRule`, `classify_movement`, `flows_of`, `check_flows`) — **all three families now ship**, and `F` got its own module for the same reason `D` did: `C` is over a state, `D` is over a pair, `F` is over **neither**. ★★ **Irreducibility PROVEN, not asserted:** 500 moved between two own pockets versus 500 out to a casino and 500 back produce **byte-identical** before and after states, and the case runs the SAME `Conservation` rule with `Tolerance::Exact` — `D`'s strongest form — over both and asserts it **passes both**. Only `F` separates them. ★ **A flow is defined relative to `μ`, not independently:** an operator declares a `Movement` (what moved, between whom) and `μ` decides whether it crossed `B` and which way — pocket-to-pocket is internal **because μ owns both ends**, not because the operator said so. Neither end owned is reported as `Foreign`, never silently dropped. ★ **Flows are DECLARED, never inferred:** the effect returns them (§II's `candidate, flows = o.effect(...)`), because if they were recoverable by diffing endpoints `F` would be reducible to `D`. Four declared rule shapes, no `eval`; `OnlyKnown` uses `F(φ,s)`'s **state** argument so a rule can be about the household's own contacts rather than a static allowlist. ★ **Conjunction only** — a more permissive rule cannot undo a stricter one, asserted directly. ★ **The clamp trap honoured by CONSTRUCTION:** `FlowRule` has **no clamp strategy at all**, so capping one side of a crossing — §III's money-minting move — is unrepresentable rather than refused. Wired into the gate as `admit_F`'s fourth conjunct; the household's real case is proven at `execute`, where the same operator and amount is admitted from a known counterparty and refused from a stranger, state byte-identical Composition is conjunction only, so adding a rule can only shrink the admissible region |
| CON-2 | **The gate** `admit(o,s) ⟺ g_o(s) ∧ o(s)∈A ∧ D(s,o(s))` (+ `⋀F(φ,s)`); effect on a copy → judge candidate → commit only if admitted | II | ✅ **all three conjuncts now built** — `g_o(s)` ✅, `o(s)∈A` ✅, **`D(s,o(s))` ✅ (2026-08-12)**, and **`⋀F(φ,s)` ✅ (2026-08-17)** — `admit_F(o,s) = admit(o,s) ∧ ⋀_{φ∈flows} F(φ,s)` is now the whole gate, all four conjuncts built. The effect returns `(candidate, flows)` exactly as §II's pseudocode writes it. Effect on a copy → judge the candidate → commit only if admitted, unchanged |
| CON-3 | **The induction** — `s_0∈A` + every change admitted ⟹ `s_k∈A ∀k`; **one skip deletes the theorem** → complete mediation mandatory | III | ✅ **structural** (Rust: one write path) |
| CON-4 | **Safety vs liveness** (Schneider) — a monitor enforces **exactly safety, never liveness**; "stays in V" ✅, "a move always exists" ✗ → needs the viability kernel (CON-10) | III | ✅ understood; kernel ⬜ |
| CON-5 | **Complete mediation / reference monitor** — always invoked, tamper-proof, small; **fail-safe defaults** (a constraint that fails to compile must NOT silently pass); economy of mechanism; least privilege | IV | ✅ — **Rust closed fail-open** (R2 #8: unreadable rule refuses) + one path |
| CON-6 | **Three strategies: refuse / clamp / defer** — clamp = projection `Π_A(s')` (only convex/interval constraints; **never clamp a conserved quantity**; report asked+did); defer = proposal, **re-admitted at commit** (verdict 4-valued) | V | ✅ **Rust win** (R2 #11 + 3 clamp safety rules; Python had refuse only) |
| CON-7 | **Conservation as a transition constraint** — `|SUM(after[q])−SUM(before[q])| ≤ tolerance`, refuse; money = integer minor units, tolerance 0; "shadow of a sameness," not a target | VI | ✅ **DONE in Rust** (2026-08-12) — **money is conserved is expressible for the first time**. `Tolerance::Exact` sums as `i128` over whole minor units, so a float cannot enter the arithmetic and *tolerance 0 is genuinely exact*; a **fractional** value is refused, never truncated (an integral float like `95000.0` is accepted — same count, differently written, and the engine's arithmetic promotes). **`TransitionRule` carries no strategy field at all**, so *never clamp a conserved quantity* is not a check that could be forgotten — the clamp is unspellable |
| CON-8 | **Holon-path composition** `admissible_global = ⋀_{H∈path} admit_H` along the membership DAG; monotone, order-independent, name-the-refuser; advisory-by-default, binding on **newly-caused** breach | VII | 🔨 one parent edge ✅ (R2); **full `⋀_H` over the DAG ⬜** |
| CON-9 | **Total, impoverished evaluator** — every predicate terminates, side-effect-free, deterministic (no `eval()`); static undecidable (Rice) → 3-valued entailed/refuted/undecided; **aggregates SUM/COUNT/AVG/MIN/MAX** over wildcard = same as roll-up ρ; scope-indexing + compiled caching | IX | ✅ **Rust unified the two evaluators** (R2 #4, aggregates on both paths); scope-indexing ⬜. **Per the Note's correction:** in Python the gap is *split* — `predicates.py` (invariant/gate path) implements all five aggregates; `constraints.py` (Enzyme **guard** path) still returns *"unsupported aggregate/block expression"*. **The divergence IS the finding**, and Rust closing it is the fix |
| CON-9b | **⚠ "Aggregate" carries TWO distinct meanings and they must not be conflated** — **(a)** the `SUM`/`COUNT`/`AVG` **functions** in the predicate fragment (CON-9); **(b)** `_compile_spec_invariants`'s **`is_aggregate` flag**, marking an invariant that references a **roll-up dimension** declared in `spec["aggregates"]` — a value folded from linked children, *never part of this sustain's own state*. Sense-(b) invariants are `enforced: False` in the sustain's own gate **by design**, and instead govern children through `_check_parent_binding_gate` | Note | ✅ both senses implemented correctly in Rust; recorded because conflating them silently breaks CON-8 |
| CON-10 | **Strong form** — substitute `Viab_T(V)` for A so admittance implies a surviving next move (safety→liveness-content); expensive, depends on honesty of T, horizon-limited optimistic | VIII | ⬜ **NOT started** (= SUS-10) |

**Summary:** the gate is ✅ and **better than Python in three ways** — refuse/clamp/defer, one unified evaluator with aggregates, and no fail-open, all structural in Rust. **Real gaps:** **transition constraints D** (rate-limit, monotonicity, **conservation of money — currently inexpressible**), ~~**firewall constraints F**~~ — **✅ built 2026-08-17** (`flow.rs`): all three predicate families now ship, with `F`'s irreducibility to `C` and `D` **proven** — identical endpoints, both conserving under `Tolerance::Exact`, and only `F` separates them, the **full holon-path DAG**, and the **strong viability-kernel form**.

#### M-DSL · DSL (Embroidery / Genome)  ✅ read
*Article: `3B1B — DSL - Formal Execution.md` (GENOME). The one language a Sustain is written in; a definition **denotes** ⟨S,T,V⟩ — compile or refuse, never partial.*

| # | Feature | § | Rust status |
|---|---|---|---|
| DSL-1 | **Compiler pipeline** text→lex→parse→AST→resolve→type-check→lower→⟨S,T,V⟩; `C: Text⇀Σ` = `Ok(Σ)` or `Err({ε})`, **never a partial Sustain** | I | 🔨 lex/parse/resolve/lower ✅; type-check ⬜ |
| DSL-2 | **Denotational semantics** — `⟦state_schema⟧=S`, `⟦invariant⟧=set`, `⟦operator⟧=⟨g,e,ε⟩`; **compositional** (no hidden control flow → licenses gate short-circuit/scope-index/candidate-eval); a definition is a **value**; the real argument against `eval()` | II | ✅ (immutable AST, no eval) |
| DSL-3 | **Grammar** — context-free BNF; precedence = grammar depth; ~LL(1); **small = a commitment** (buys totality, decidable entailment, economy, computable MDL) | III | ✅ (ported predicate grammar) |
| DSL-4 | **Name resolution** `Γ⊢p:τ` (wildcard resolves through a container); quantifier scoping `Γ⊕fields(τ_item)` right-biased; resolver + evaluator must agree; **undeclared path = load-time error naming the path** | IV | ✅ (bound to schema at load) |
| DSL-5 | **Type judgments `Γ⊢e:τ`** — comparison requires **same type + ordered**; "well-typed programs can't go wrong"; **the gate has no 4th outcome for "rule broken"** → without typing, a mistyped rule arrives as a *refusal* (can't tell a violated law from a broken document) | V | ⬜ **NOT built — the key DSL gap** ("one tree-walk away"); today a type error surfaces as a false constraint violation |
| DSL-6 | **★ Exactly ONE language** — two evaluators = the language stops denoting (`SUM` = a sum in an invariant, an error in a guard); Deutsch discrete-alphabet universality; Landin many-surfaces-one-core | X | ✅ **Rust win** (R2 #4 unified the evaluators — Python had 2 implementations + 3 syntaxes) |
| DSL-7 | **Lowering** `lower: Def→⟨S,T,V,s0,operatives⟩`; **genesis event** (seeded state emitted AS an event → `state=fold` from instant 0); total on well-typed input | VI | ✅ (instantiate + genesis) |
| DSL-8 | **Parameterized `Def(θ)`** — typed function into Sustains, **checked once**, instantiated freely (substitution lemma); vs textual `{{token}}` (no theorem; survives as literal) | VII | 🔨 instantiate ✅; typed-param checking ⬜ |
| DSL-9 | **Typed overlay `ρ`** — well-typed iff every path is a declared dim of that type; overlays compose (monoid); **lets you hand a person the *overlay*, never the definition** (Curated UI safety) | VIII | ⬜ not built — today `{{placeholder}}` substitution (unbound token = literal, caused the `role_in_family` bug) |
| DSL-10 | **Holon path type-checked as one program** — a parent's binding rule must type-check against child dims + declared roll-up dims; catches an aggregate over a missing dim / summing a string, at author time | IX | ⬜ not started |
| DSL-11 | **Totality + decidability** — no recursion/unbounded loop/user-function; quantifiers **bounded over declared finite containers**; type-check decidable; "does o preserve c" undecidable (Rice) → 3-valued | X | ✅ structural (total evaluator) |
| DSL-12 | **Whole-spec validator** (author-time) — bind Enzyme refs, Symbiont names, aggregate declarations, `ui_schema` widget names, `access_policy` at load; `imports` + enforced versioning declared, unbuilt | VI | 🔨 partial (user-authored path ✅; disk-load ⬜; imports/versioning ⬜) |
| DSL-13 | **Enzymes authored in Embroidery** — guards + effects declared in the DSL, not code (today a spec declares *which* Enzymes, never their guard/effect — `⟦operator⟧` is a name lookup) | §4F | ⬜ **not started** (large) |
| DSL-14 | **Friendlier surface syntax** — many surfaces over one AST (safe only once DSL-6 holds); JSON is the only surface today | X | ⬜ not started (correctly last) |
| DSL-15 | **Description-length / lift proposal (MDL)** — count how often a pattern recurs across Genomes; `ΔL(π)=n(L(body)−L(call))−L(def)`; **lift when ΔL>0** (over typed ASTs, human-confirmed, owes the migration predicate); learning = compression (same op as Symbiont memes) | XI | ⬜ not started (the `parse_rule` lift is the pattern, on a different artifact class) |

**Summary:** the front end (parse, resolve, lower, one unified evaluator) is ✅ — and Rust's single evaluator satisfies the article's hard "exactly one language" rule (DSL-6, a Python gap closed). **Key gaps:** the **type-checker `Γ⊢e:τ`** (DSL-5), the **typed overlay** (DSL-9), the whole-spec validator, holon-path joint typing, **Enzymes authored in the DSL** (DSL-13), and the MDL lift-proposal loop.

#### M-IMM · Immune / security (TOLERANCE)  ✅ read
*Article: `3B1B — Immune - Formal Execution.md` (TOLERANCE). Security is **not a module** — it's a property of B and the gate. Mediation, authority, detection across the recursive Sustain.*

**Key result: Sustena is "architecturally correct, evaluationally incomplete" — and Rust closes most of the evaluation gaps structurally. Article's build order: gate unconditional + fail-closed → principal term → grant-as-transfer → sign catalogue → detection LAST.**

| # | Feature | § | Rust status |
|---|---|---|---|
| IMM-1 | **Full admit()** `auth(α) ∧ permitted(α,o,Σ) ∧ g_o(s) ∧ o(s)∈V ∧ D(s,o(s)) ∧ ⋀F(φ,s)` — **six independent refusals** (not known / not you / not now / not ever / not that way / not across this wall); auth ≠ permitted | I | 🔨 guard/result/privilege ✅; auth (login) app-layer ✅; **D ✅** (`transition.rs`); **F ✅ built 2026-08-17** (`flow.rs`, =CON-1); ★★ **the *not you* conjunct is now COMPLETE in both its forms — 2026-08-17**: ambient (`permitted`, 2026-08-12) and **capability-borne** (`capability.rs`, IMM-7), and the six refusals gained three more names rather than being collapsed — `skin_resolves`, `capability_designates`, `capability_carries`, because *not a member*, *too little authority*, *unreadable bundle*, *wrong object* and *right unheld* send whoever reads the log to five different repairs — *not across this wall* is now a real refusal: a crossing to a counterparty the firewall does not admit is refused at the gate with nothing committed |
| IMM-2 | **Defence in depth = ⊕** `admit* = ⋀_{H∈path} admit_H` (monotone, person-first) — no separate layering construct | I | 🔨 one edge ✅; full path ⬜ (=CON-8) |
| IMM-3 | **Complete mediation / reference monitor** — always invoked, tamper-proof, small; one unmediated path **deletes** the induction | II | ✅ **structural win** — Rust one write path (closes Python's opt-in gate + second write path) |
| IMM-4 | **Fail-closed, not fail-open** — an uncompilable invariant/post_constraint must **refuse or defer, never silently skip** (Python's fail-open, newly named in this article) | II | ✅ (R2 #8 — Rust refuses an unreadable rule) |
| IMM-5 | **Authentication** — PBKDF2-HMAC-SHA256 (260k iters, per-user salt, constant-time), JWT with `token_version` = **real revocation** | IV | ✅ app-layer (users.py) — solid, not core-engine |
| IMM-6 | **Authorization = per-membership-edge privileges** (authority on the relationship, not the person) + **`skins`** (named RBAC bundles); `min_privilege` = the per-action binding | IV | ✅ **complete 2026-08-17** — `min_privilege` enforced since 2026-08-12 (R2 #10, edge-based and monotone across the holon path; Python's inert model is REAL in Rust), and ★ **`skins` closed with IMM-7**: `Skin` had shipped as a struct `MembershipEdge` referenced and **nothing read** — *'a field, not a check'*, the exact defect this module's own header criticises `min_privilege` for in the reference. `SkinRegistry` now resolves it, and where an edge carries both a tier and a skin the **weaker wins** (`max`, the same weakest-link algebra as the path walk) so a bundle cannot be a promotion route. ★★ **An edge naming a skin nothing resolves is REFUSED** (`Denial::UnresolvedSkin`), never demoted to its raw tier — falling back is how a typo'd role (`gest` for `guest`) becomes owner-by-accident, and IMM-4's *an unreadable rule refuses, never silently skips* is the same rule one layer up. |
| IMM-7 | **Approval token** `guard ∧ (effect_class=sandbox ∨ valid_token(approve(o,principal)))`; today "human decides" holds by **incapacity, not mediation**; **confused-deputy risk** once Symbionts can write → pass authority *with* the request (capability) | IV | ✅ **done 2026-08-17** (`capability.rs`) — and ★★ **the row had THREE clauses, two of which were already built when this slice opened; the STATUS here was stale, not the code.** **(a) The approval token shipped 2026-08-12 as OPV-30/31** (`approval.rs`: `ApprovalToken` with no public constructor, `EffectClass::{Sandbox,Live,Unchecked}`, `NonceLedger`, wired into `execute_admitted` with binding/principal/expiry checked **before** the body and the nonce spent **at commit**, so a gate refusal never consumes an approval). This row read `⬜ not started` for five days after it landed. **(b) `permitted(α,o,Σ)` also shipped 2026-08-12** (`principal.rs`, tracked correctly under IMM-6) — so the *authorization half was not the gap either*. ★★ **(c) The genuinely unbuilt clause was the confused deputy**, and that is this slice. Hardy 1988: a deputy applies its own authority to a name someone else chose; a Symbiont acting on a parsed SMS or an Arena artefact is that shape by construction. ★★ **`Authorization::Capability` carries NO `&Memberships`** — Miller-Yee-Shapiro's *no ambient authority* made a property of the type, so a deputy holding a capability cannot fall back on the issuer's wider authority even by mistake. Proven as a pair at the gate: the same principal, the same untrusted input and the same operator **commit** under ambient authority and are **refused** (`capability_designates`, state byte-identical) under a capability — the vulnerability is asserted so the fix has something to be a fix of. ★★ **Redesignation is unrepresentable** (`Attenuation` carries no sustain, so the deputy's own move is absent from the vocabulary, not guarded against); amplification is **refused and named** rather than silently clamped; issuing is a **transfer not a mint** (it consults a real edge, so a stranger gets `NoEdge`) and reuses `effective_privilege_with`, so it cannot route around ⊕. **Unforgeability is structural** — no public fields, no constructor, no `Deserialize`, proven by a `compile_fail` doctest. **Residuals, named:** unforgeability is *in-process*, not cryptographic (a wire form needs a MAC/key → **host-layer slot** under ADR-0001, the CTL-6 relocation); **this is not revocation** (a capability is valid until dropped; revoking the underlying edge does not invalidate one already issued); and **nothing issues capabilities to Symbionts yet** — the operative layer still calls with `Unchecked`/`Principal`, so the fix is *available* rather than *in force*. |
| IMM-8 | **Verdict set {Admit, Refuse, Clamp, Defer}** — **Defer = the tolerance mechanism**; over-refusal → users build a bypass, so over-refusal is a *security* failure | VI | ✅ (R2 #11 refuse/clamp/defer) |
| IMM-9 | **Anti-over-refusal** — advisory-by-default, binding refuses only on **newly-caused** breach (stops a breached parent freezing all children) | VI | ✅ (R2 `_check_parent_binding_gate`) |
| IMM-10 | **Fixed vs learned rules** — `R_fixed` = non-authorable pre-gate ordered first (the OTP secret filter); `R_learned` = ParseRules with `trust`+`provenance`, gated adoption (self-match + regression) | V | ✅ **done 2026-08-17** (`learned.rs`) — ★★ **and STEP-0 settled the scope before anything was designed: the ParseRule system is PYTHON-ONLY.** `ParseRule`, `parse_rule`, `transducer`, `OTP` and `R_fixed` are all **grep-0** in this core, so the Rust work is *not* to port a subsystem across to have somewhere to hang a bound — it is the **generic mechanism §V is really about**, of which a ParseRule is one instance: the fixed/learned split, the ordering, and the least-privilege bound. The regex library, the seed set and the adoption verifier stay app-layer, said plainly rather than quietly imported. ★★ **`R_fixed` stays a fixed point, and it is structural rather than enforced:** `Edit::apply` is `(&Definition) -> Definition`, acting on `𝒮`/`Inv`/`T`, with **no parameter through which a rule set could enter and no return through which one could leave** — the same shape as EVT-15's finding about the patch fold. A `FixedRule` is not part of a `Definition`, so `∀e, ∀r ∈ R_fixed: e(r) = r`, exercised over all **eight** edit kinds and compared byte-for-byte rather than argued from the signature. ★ And a fixed rule carries **no operator and no capability** — a pre-gate refuses, it does not act, so asking for one is an `Err` rather than an empty capability that would read as *bounded* where the truth is *not applicable*. **Ordered-before is structural too**: `screen` is the only entry, the fixed tier runs first, and the order is not a parameter. ★★ **Gap 1 — trust is now a real decision input, and the finding was verified rather than quoted:** across **47** `trust` hits in the reference, **not one** is a comparison, branch, sort key or ordering (the only operator-shaped hit is the `<...>` in a docstring listing the tuple's fields). `TrustPolicy` is the evaluator, and it drives **two** decisions — **precedence** (a `Shipped` rule wins a match over a `ProposedConfirmed` one even when declared second) and **privilege** (a machine-synthesised, human-clicked-yes rule gets a strictly weaker capability than an authored one). ★ Both derive from **one** declared thing, so they cannot drift into two orderings that disagree, and `TrustPolicy` implements **no `Default`** — what a trust level ought to reach is a deployment's policy, and a library that invented one would decide it invisibly (the `Lateness` reason). ★★★ **Gap 2 — least privilege on the learned tier, the keystone, and it CONSUMES IMM-7:** `IngressBound` declares the operators reachable from ingest at all (small, declared, HRU), and a learned rule holds a capability **attenuated to that bound** rather than the ambient authority of whoever it acts for — the confused-deputy configuration IMM-7 built the capability for, with the learned tier as the deputy. Proven **end to end at the real gate**: an ingest-tier rule invoking an out-of-bound operator reaches `execute_as` and is refused with `capability_carries`, no mutations, no events, state byte-identical — *and the in-bound call still commits*, asserted in the same test so the bound cannot quietly become an outage. ★★★ **No trust level lifts the bound** — asserted across all three levels, because a ceiling with an exception is not a ceiling — and ★ the bound **never exceeds the principal it acts for**, since a capability is a transfer: the policy sets a ceiling, never a floor. **Residuals:** no matcher library (substring only, deliberately — a richer one would invite porting the reference's regexes); **adoption gating is not built here** (verify + regression need the matcher and the stored corpus, both app-layer, and both already exist there); adoption and authority are kept **separate** on purpose, so a rule outside the bound is adopted-and-visible rather than silently dropped; and **nothing hands the capability to a running rule engine**, because this core has no ingest loop — the same threading residual IMM-7 already carries. |
| IMM-11 | **Layered controls = independence of failure** (not count) — the 3 OTP layers share **one** detector (10 hand-synced regexes) → add a **different-kind** structural detector | III | ⬜ (app-layer) |
| IMM-12 | **Detection over the log** — anomaly/negative-selection; **base-rate limit** (output must be *attention* not *denial* → route to Defer); **heartbeats** = declared expectation checked deterministically (no base-rate problem) | VII | ⬜ **NOT started, correctly LAST** (a detector over an unmediated system reports a hole it can't close) |
| IMM-13 | **Forged credentials** — Sybil (grant must be a **transfer, not a mint**), unsigned Arena artefacts (**content hash + signature**; integrity ≠ authenticity ≠ quality) | VIII | ⏸️ economy-deferred |

**Summary:** the security *shape* is right, and Rust closed most *evaluation* gaps structurally — complete mediation (one path), fail-closed, refuse/clamp/defer, enforced privilege all ✅. **Pending:** transition/flow constraints (=M-CON), a second-kind OTP detector, and detection/heartbeats (built **last**) — **both app-layer**. Sybil-grant + Arena-signing are economy-deferred.

**Update 2026-08-17 — IMM-7 closed, and the reconcile that opened it is worth keeping.** ★★ The row named three clauses and **two were already built**: the approval token (OPV-30/31, 2026-08-12) and `permitted(α,o,Σ)` with per-edge privileges (`principal.rs`, the same day, tracked under IMM-6). The status line said `⬜ not started` for five days after the first of those landed, so STEP-0 corrected **the tracker, not the code** — and corrected the slice brief too, which expected authorization to be the gap. ★★ The genuinely unbuilt clause was **the confused deputy** (Hardy 1988), and it is the one that matters once Symbionts can write: an operative acts on a person's authority over inputs *it did not choose*, so ambient authority applies everything that person holds to a name from untrusted text. The fix is Dennis & Van Horn's — designation and rights in one unforgeable token — with Miller-Yee-Shapiro's sharpening made **structural**: `Authorization::Capability` carries **no `&Memberships`**, so the ambient fallback is *absent* rather than merely unused. ★ Three properties are unrepresentable rather than checked: **redesignation** (no sustain on `Attenuation`), **widening rights** (no such method), and **forging one** (no `Deserialize`, proven by a `compile_fail` doctest). Asking for a stronger *tier* is representable because a caller may sincerely ask, and is **refused and named** — silently clamping would hide that someone asked for more than they had, the trap `admission.rs` warns about a layer down. ★ The same slice closed **IMM-6's `skins`** — declared 2026-08-12 and read by nothing, which is *'a field, not a check'* turned back on this codebase — with the unresolvable case **failing closed**. ★★ **The divergence's counterweight is two things and the first is genuine parity**: authentication is the reference's (PBKDF2-HMAC-SHA256, 260k iterations, per-user salt, constant-time, JWT with `token_version` = real revocation) and nothing here improves on it — which is exactly §IV's point that `auth` and `permitted` are separate conjuncts; and SUS-7's boundary `μ` is at parity on both sides, which is what gives a membership edge something to be an edge *of*. ★ **The reference's one real ownership check is credited precisely**: `_assert_owns_sustain()` 404s for both not-found and not-owned so ids cannot be enumerated — careful work, and **single-owner, binary, and at the route layer** rather than a conjunct of `admit()`. ★★ **The grep that would most mislead a re-reader is named**: `skin` returns **16 hits and zero real** — every one is the substring in *asking*, the sharpest false positive since CTL-11's `gain`/*again*; and `permitted`'s 11 hits are the **allow-list**, which is §IV's conflation stated out loud (`sustain_engine.py:1618`, *'operator is permitted by this sustain's spec'*). **Residuals:** in-process unforgeability only (a wire form needs a key → host-layer slot); **no revocation**; and **nothing issues capabilities to Symbionts yet**, so the confused-deputy fix is available rather than in force — that threading is the sequenced next step.

**Update 2026-08-17 — IMM-10 closed, and with it M-IMM's ENGINE HALF.** ★★ STEP-0 settled the scope first: **the ParseRule system is Python-only** (`ParseRule`/`parse_rule`/`transducer`/`OTP`/`R_fixed` all grep-0 here), so the Rust work is the **generic mechanism** §V is about rather than a port — the split, the ordering, and the bound; the regex library, the seed set and the adoption verifier stay app-layer. ★★ **The fixed tier stays a fixed point structurally**: `Edit::apply` is `(&Definition) -> Definition` and a `FixedRule` is not part of a `Definition`, so no edit's range includes it — exercised over all eight edit kinds. Ordered-before is structural too (one entry point, order not a parameter), and a fixed rule holds **no capability at all**, because a pre-gate refuses rather than acts. ★★ **Trust became a real input, and §V's finding was verified rather than trusted**: across **47** `trust` mentions in the reference, **zero** are a comparison, branch, sort key or ordering. `TrustPolicy` now drives **precedence** and **privilege** from one declared mapping, with no `Default` — the meaning of a trust level is a deployment's policy, not a library's. ★★★ **And the keystone consumed IMM-7 exactly as intended**: the learned tier is a **deputy**, so it holds a capability attenuated to a declared `IngressBound` instead of ambient authority. An ingest-tier rule invoking an out-of-bound operator is refused **at the real gate** (`capability_carries`, state byte-identical) while the in-bound call still commits. **No trust level lifts the bound**, and the bound never exceeds the principal it acts for — a ceiling, never a floor. ★★ **The counterweight is the strongest in the module and it belongs to the reference**: the fixed/learned *split* is at parity, and the fixed tier there is its best security work — `contains_sensitive_secret()` runs **first in `parse_message()`** (before any parser, so no learned rule can see an OTP first) **and again in `capture()`** (before the raw text is ever written, so a rejected message leaves no row and no `raw_payload`), non-authorable, with a mirrored Java copy on the device so the secret never leaves the phone. Nothing here improves on any of it. ★ Adoption gating (verify + regression) is real there too. ★ **Two greps worth carrying**: `trust`'s **47 hits with zero decisions** (the count reads like a live subsystem and is entirely storage and display), and `R_fixed`'s **grep-0 that is not an absence** — the name appears nowhere while the thing is excellent, so a reviewer grepping the name would conclude the opposite of the truth. **What remains in M-IMM is app-layer only:** IMM-11 (a different-*kind* OTP detector — layering is independence of failure, not count) and IMM-12 (detection over the log, correctly last).

### Layer 3 — BOUNDARY

#### M-ING · Ingest & external integrations (RECEPTOR)  ✅ read
*Article: `3B1B — Ingest - Formal Execution.md` (RECEPTOR). B seen from outside: a total transducer with the gate behind it. Once past it, a sensor reading and a hand-typed spend are the same kind of thing.*

**Status: heavily built (Slices 5/11/13/14 — transducer, parse rules, one door, egress). Key gaps: heartbeats, device-as-Sustain, intrinsic-id key.**

| # | Feature | § | Rust status |
|---|---|---|---|
| ING-1 | **Boundary as sensor** `τ:R→C` / `σ:E→R`; a transducer produces a **candidate `C≠S`**, not state; **no ingest write path** (same gate + fold as an internal write) | I·VI | ✅ structural (capture→execute_operator) |
| ING-2 | **Total τ, five-way result** `Mapped | ParsedUnmapped | Informational | Unparsed | Rejected`; **"I don't know" is a value, not a drop**; fail-safe default = **surface, not drop** | II | ✅ app-layer (transducer.py, deterministic) |
| ING-3 | **Secret pre-gate** — `secret(r)` **first, before persistence**; content-based; **fixed, non-authorable**; two independent impls (device + server) | III | ✅ app-layer (before INSERT + device twin) — the IMM-10 R_fixed |
| ING-4 | **Sender-strict dispatch** — source decided by the **sender**, never the wording (`argmax textual-fit` forbidden) | IV | ✅ app-layer (`_PARSERS_BY_SOURCE`) |
| ING-5 | **Idempotent intake** — at-least-once ∘ idempotent = exactly-once **effect**; key `⟺ same fact`; **intrinsic id (txn code) first**, text-hash fallback (a UUID partitions by *arrivals* — always wrong); duplicate = no-op | V | 🔨 substrate dedupe ✅ (R2 #18); **intrinsic-id-as-key ⬜** |
| ING-6 | **One write path** `commit(Mapped)=execute_operator`; mapped-but-invalid is **quarantined (named rule), never forced**; message id stamped on the event | VI | ✅ (Rust one path; refusal quarantines) |
| ING-7 | **Parse rules as declared primitives** — `Γ⊢r` (pattern compiles ∧ status=mapped ⟹ operator∈Registry ∧ params⊆declared); ordered first-match; examples = regression corpus | VII | ✅ app-layer (parse_rule.py, 22 shapes as data) |
| ING-8 | **★ Liveness / staleness / heartbeats** — `staleness>θ(c)`, `θ=Percentile_p(gaps)`; **heartbeats convert a statistical detector into a deterministic one** (`≤h+δ`); never guess a cadence; seen when it *speaks*, not when it parses | VIII | 🔨 staleness partial ✅; **θ-learned ⬜; HEARTBEATS ⬜ — the residue of the week-long outage** |
| ING-9 | **Device-as-Sustain** `Σ_dev` (connectivity/battery/**queue_depth**/t_last_ack); staleness = ordinary invariant; **queue_depth = the leading health metric**; a liveness invariant is evaluated by the **receiving** Sustain (a dead device can't self-report) | IX | ⬜ not started (ingest_sources is a plain table) |
| ING-10 | **Ack + transactional outbox** — ack = **durably committed**; retry backoff+jitter; queue-depth reporting | V·IX | 🔨 effectively ack-on-commit ✅; no ack state machine / backoff / queue-depth ⬜ |
| ING-11 | **Egress asymmetry** — can't make someone else's receiver idempotent → **irreversibility declared**, no blind auto-retry; **human gate** (approval token = a term inside `admit()`) | X | 🔨 egress human-gated ✅ (Slice 11); irreversibility flag + approval-token-in-admit ⬜ (=IMM-7) |
| ING-12 | **Cross-source correlation** — one transfer → two senders' SMS → two candidates; needs a fact-based key (amount + shared M-PESA ref) | IV | ⬜ disclosed, not fixed (double-counts) |
| ING-13 | **`t_event` at the boundary** — lift the SMS's own date/time into event time (today only `received_at`, so skew isn't computable) | VIII | ⬜ — **but the core half landed 2026-08-17 (EVT-5), and this row is now the whole remaining gap.** ★ Sharper than the row's own wording: the reference does not merely lack the event time, it **captures and discards** it — `(?P<date>…)\s+at\s+(?P<time>…)` appears in **13** real M-Pesa/KCB patterns, and every handler's `parsed_fields` omits both groups one line later. So the cheapest first step is not a parser change at all: keep the two groups the regexes already match. |

**Summary:** Ingest is among the most-built modules — transducer, five-way result, secret pre-gate, sender-strict dispatch, single door, parse-rules-as-data, human-gated egress all ✅. **Residue: heartbeats** (would have caught the outage), **device-as-Sustain** (queue-depth), the **intrinsic-id dedup key**, cross-source correlation, and the approval-token term.

### Layer 4 — COGNITION

#### M-MON · Monitor  ✅ read  ★ SPINE (observe)
*Article: `3B1B — Monitor - Formal Execution.md` (PERCEPT). Observe true state under noise; escalate only when the math demands. CUSUM is the formal Monitor→Controller boundary.*

**Status: the detection chain, the per-sustain ENGINE that owns it, and the escalation into the OODA loop are all built (2026-08-12, 2026-08-16) — OBSERVE is self-driving. Still ⬜: the belief tracker (MON-8), the preattentive encoder (MON-7), observability/traces/stream windows, and harmonics (MON-13, waiting on nothing but its own build).**

| # | Feature | § | Rust status |
|---|---|---|---|
| MON-1 | **Observability** — `is_observable()` (observability matrix rank = n); partial when rank<n → distribution not point; **"unmeasured state is ungoverned state"** | I | ✅ **done 2026-08-17 via the Sustena-native form, with the MATRIX DECLINED** (`observability.rs`). ★★ **§I's `rank([C; CA; …; CAⁿ⁻¹]) = n` is a DECLINED IMPORT, not a gap** — the same position MON-2 takes on the Kalman filter, resting on MON-11's ratified caveat that *the observability and Kalman apparatus of §I–II assumes a continuous linear system*. ★ And the concrete reason beyond the categorical one: **`CAᵏ` needs a single `A` to take powers of, and there is not one** — the dynamics here is a *choice* (which operator someone runs), not a fixed linear map, so any `A` would have to be picked and picking one would be a fabrication. This row's own warning settles it: *a rough answer to a rank question is worse than none.* ★★ **What ships is the discrete analogue, and it is a translation rather than an approximation:** `C` → dimensions a declared source measures; `A` → one operator (reads `X`, writes `Y` ⇒ `X → Y`); `CAᵏ` → a measurement reachable in `k` operator hops; `rank(O)=n` → every governed dimension reaches a measurement. The verdict **names the operator chain** that carries a value, so the claim is checkable rather than taken on trust, and the search is breadth-first because *which block row* is a question about the smallest `k`. ★★ **Conservative where the effects are unsummarised, and it says so:** writes come from OP-3's optional `EffectSummary`, so an operator without one contributes **no edges** — a dimension can read unobservable that a summary would have shown observable. That is the safe direction (the Goodhart support-matching discipline), and `Observability::Unobservable` **carries the operators whose summaries were missing** with `is_conservative()` to say whether the verdict rests on them, so *unobservable* and *we could not see* stay distinguishable. ★ **Over today's registry that is the whole picture** — no shipped operator declares a summary, so every non-measured dimension reads conservative, asserted so a future summarised operator breaks the test and forces a look. ★ **Where it meets MON-8:** §I's *partial observability → a distribution, not a point* is the belief, and this row says **which** dimensions need one (`requiring_belief`). And the distinction from `ungoverned()` is kept sharp rather than called an extension: that is a **runtime** question (*has anything spoken yet* — answerable by waiting), this is a **structural** one (*could anything ever* — waiting will not help). **Residuals:** root-name granularity (coarse in the *observable* direction, the less safe one, and stated); a path is not an estimator (reachability does not say how to invert the chain); and a path is not an estimator.  ★★ **WIRED into `MonitorEngine` 2026-08-17** (the Phase-2 consolidation) — declared per sustain on `SustainWatch`, `Option` and absent by default so a sustain declaring none behaves exactly as before. ★★ **Runs on demand, and is structural** — `MonitorEngine::observability(sustain, registry, sources)` reads the **operator graph**, which this sustain's state cannot change, so running it per tick would burn work on an answer that cannot have moved. The governed set is the region's own declared intervals, and over today's registry the report says **conservative** per finding rather than in a footnote. |
| MON-2 | **Kalman filter** — optimal estimate under noise (predict/update, gain K); show `x_hat` not raw | II | ⬜ **deliberately not built** — MON-11's fit caveat is now discharged *without* it: the Sustena-native form (distance-to-V on the fold + CUSUM/EWMA) ships as of 2026-08-12. Kalman assumes a continuous linear ODE, which discrete event-sourced state is not, so this is a **declined import**, not a gap. ★ **Re-confirmed 2026-08-17 at the one place it would have been easiest to slip in:** §VIII's belief-state pseudocode calls `kalman_filter.update(observation)` by name, and MON-8 shipped **without it** — the `point_mass` half is served by EVT-10's snapshot collapse instead, which is the Sustena-native rule for what an observation of an external authority means. ★★ **And re-confirmed a second time the same day by MON-1**, which declined §I's *observability matrix* on the identical grounds — MON-11's caveat names the two together (*'the observability matrix and the Kalman filter are a loose import'*), so declining one and importing the other would have been incoherent. The declination now has **three** concrete sites: the detection pipeline, the belief state, and the observability analysis. |
| MON-3 | **Three pillars** — Metrics `(name,value,t,labels)`; Logs (ordered append-only); Traces (span DAG; `Span`, `TraceCollector`) | III | 🔨 Logs ✅ (=EVT); Metrics/Traces ⬜ |
| MON-4 | **Stream processing** — tumbling/sliding/session windows; `SlidingWindowAggregator` (count/rate/by_type/error_rate); watermarks + late-event policy | IV | ✅ **done 2026-08-17** (`windowing.rs`) — the typology, on top of EVT-6's watermarks + lateness (same day). ★★ **Tumbling is not a third mechanism and is not rebuilt.** `[kT, (k+1)T)` **is** a half-open period partition, so calendar-aligned tumbling is `tumbling_from_recurrence`, a **one-line delegate** to EVT-11's `Recurrence::expand` — which matters concretely rather than aesthetically, since a month is not a fixed number of milliseconds and a duplicate would have had to get calendar length right a second time or quietly get it wrong. And **fixed-duration tumbling is the degenerate sliding case where `step == size`**: a conformance case checks `Sliding{DAY, DAY}` and an EVT-11 daily expansion produce **byte-identical** windows, so the claim is measured rather than argued. `Sliding::is_tumbling()` merely *reports* which case a spec is. ★★ **Sliding overlaps honestly:** size 60m stepping 15m gives an `overlap_factor` of 4, and `windows_containing` returns a `Vec` — an event is counted in **each** window containing it, because that is what makes a rolling rate roll. A **`step > size` is refused** (`StepExceedsSize`), since gaps between windows would silently drop every event that fell in one. ★★ **Session is data-driven, and its end is not known until the gap is.** A session ends **τ after its own last event**, not at the event that revealed the gap; the last session in a batch is `SessionState::Open` — *not* an empty session and *not* a closed one, the same *we cannot answer yet* EVT-6's `Closing::NotYet` reports. ★★ **All three close on a watermark, never a clock.** Sliding and tumbling windows **are** EVT-6 `Window`s, so `is_closed_by` is inherited rather than re-implemented. A session is the case that could have gone wrong — its end is unknown in advance — so `close_on` takes a **`&Watermark` and nothing else**: a processing clock two hours past the gap carrying an honest 90-minute bound does **not** close it. **A clock is not evidence**, and there is no *expire after a while* path, which would be the wall clock wearing a different name. ★ **This row's own wording corrected, second time:** it names `SlidingWindowAggregator (count/rate/by_type/error_rate)` as if shipped, and it is **grep-0 on both sides**. Aggregation is a **consumer** of the typology, not part of it — building one in would tie a window shape to a particular thing being summed — so it stays deliberately unbuilt and the wording is corrected rather than satisfied. **Residuals:** sub-daily fixed-`T` tumbling routes through `Sliding` because EVT-11's grammar has no `HOURLY` (that row's named residual, not closed here); `sessionise` **reads the order it is handed** (as `fold` does — `event::order` exists for the caller); no session merging across batches.  ★★ **WIRED into `MonitorEngine` 2026-08-17** (the Phase-2 consolidation) — declared per sustain on `SustainWatch`, `Option` and absent by default so a sustain declaring none behaves exactly as before. ★ **Runs on demand**, over the timed readings the engine records — `windows()` and `readings_in()`. The engine holds readings rather than an event log, and **only a timed reading can be placed in a window**: a pass through `ingest` carries no event time, and putting it in one would be inventing it. |
| MON-5 | **EWMA smoothing** `α·y+(1−α)·EWMA`, half-life ≈0.693/α; per-widget α | V | ✅ **DONE in Rust** (`detect.rs`, 2026-08-12) — `EWMA[k] = α·y[k] + (1−α)·EWMA[k−1]`, seeded on the first observation (starting from zero would fabricate a ramp-up transient in the one signal everything trusts). **`α` is a declared per-detector field**, not a constant — *a budget ring can be cosmetically smooth; a constraint-health indicator must respond within seconds* — and `half_life()` states what the chosen value actually buys, since *"α = 0.1"* says little and *"t½ ≈ 7 updates"* says what it costs |
| MON-6 | **CUSUM detector** — `S⁺/S⁻` sums; fires on **persistent** shift; `classify()`→CRITICAL/WARNING (→Controller) vs INFO (stay); **the formal Monitor→Controller boundary** | VI | ✅ **DONE in Rust** (`detect.rs`, 2026-08-12) — **the formal Monitor→Controller boundary now exists.** `S⁺`/`S⁻` per §VI, with **μ₀, δ and h declared** (same discipline as the region weights: `δ` is the smallest shift worth detecting, `h` is how much evidence before interrupting someone). `Severity::escalates()` IS the seam — CRITICAL/WARNING cross to the Controller, INFO stays in the Monitor — and it is what **CTL-7** will consume. **The proof of value is pinned by a vector, not prose:** a sustained small drift alerts, while a spike **4.5× larger** that corrects never does. ⚠ **The article's pseudocode zeroes `S_pos` BEFORE `classify()` reads it**, so every upward shift would report INFO and never escalate; implemented per the prose (excess captured at the alert, then reset) and recorded rather than silently fixed |
| MON-7 | **Preattentive visual encoding** — map data dims → hue/size/motion/orientation/brightness; `WidgetVisualEncoder`; Tufte data-ink | VII | ✅ **done 2026-08-17** (`preattentive.rs`) — **the door into the surface block**, and it establishes that block's conventions. ★★ **The *subject to* clause is a TYPE:** §VII's model maximises discriminability **subject to** the attributes coming only from the preattentive set, and `Channel` has exactly seven variants with **no `Other(String)`** — so an encoding that reached for a non-preattentive channel (a number to read, a label to parse) **cannot be written down**. That closure is load-bearing rather than decorative: leaking onto a non-preattentive channel does not degrade the glance, it ends it. ★★★ **Urgency is the household's, not the widget's, and three things make it structural:** `VisualSpec` has **no public constructor and no setters** (a widget cannot build itself a bright spec because it cannot build a spec at all); **the map is FIXED, not a parameter** (a configurable map is a gaming surface — whoever chose it could route urgency onto a channel nobody looks at); and ★★★ **brightness is field-relative**, with `encode_field` the only entry point. The consequence is the property worth having: **a widget can only get brighter by being WORSE** — asserted in both directions, since a peer moving further from `V` makes it **dimmer** while it is unchanged. Attention is zero-sum across the field and cannot be manufactured. That is UI-13's *salience is never rendered as salience* enforced at the encoder, and the encoding-side twin of Flight 401: where the Curated UI stops a loud-but-safe widget **winning a slot**, this stops it **looking urgent** once it has one. ★★ **Discriminability is RUN, not claimed** — the necessary condition (distinct state-classes encode distinctly) is asserted over every class this encoder can see, with an identical peer held constant so the peer cannot be what makes them differ; `Hue` has three variants so *two near-identical hues* is unrepresentable. The psychophysical half is **named as out of reach** rather than smuggled in. ★ **Encoder, not renderer:** `Hue::Amber` never `#E8A020`, a `Fraction` never a pixel radius — asserted by checking no `#`/`px`/`rgb`/`hsl` appears in a spec. And Tufte is honoured by **leaving saturation and enclosure unassigned**: there is no sixth or seventh data dimension, and giving them a value would be decoration wearing an encoding's clothes. ★ **It consumes the real engine**: a conformance case declares a real `Region`, runs a real `MonitorEngine` pass, and encodes its `Ingested` — so *this encodes `d(s,V)`* is checked against the thing that computes it. **Residuals:** the psychophysics is not checkable here; the map is deliberately non-configurable; a one-item field carries **no brightness and no size** (nothing to be brighter than, and `1.0` would be a fabricated maximum); nothing calls this from the engine yet, and should not — encoding has a surface's cadence, and a per-ingest encode would be a field of one; and trend is a **one-step** reading (`w` against its own EWMA level), since fitting a slope is MON-3's territory and would be the fabricated-model trap CTL-11 declined. |
| MON-8 | **Belief state (POMDP)** — `BeliefStateTracker`: Kalman point-mass on observation, `predict_forward`+`widen` on silence; `SENSOR_SILENT` past threshold | VIII | ✅ **done 2026-08-17** (`belief.rs`) — **Phase 2 opens here.** ★★ **The observation update is EVT-10's snapshot collapse, NOT a Kalman filter.** §VIII's pseudocode reads `point_mass(kalman_filter.update(observation))`; the Kalman half is **MON-2, the declined import** (see that row), and what survives is the `point_mass` — for which Sustena already has a rule: §VII's *a newer reading of an external authority supersedes*. `Belief` **holds an EVT-10 `SnapshotDim`** and collapses through it, so the coherence is structural rather than parallel and *'an observation arrived'* has one meaning in this codebase. The visible consequence is a real behaviour: **a stale reading arriving late does not collapse the belief** (`Collapse::Stale`), losing on `(τ,id)` exactly as it would on the state dimension. ★★ **Silence widens without inventing dynamics.** `σ = elapsed × DRIFT_RATE`, `variance = σ²` — §VIII taken literally, asserted **monotonic across 24 consecutive hours** rather than at one convenient point. `predict_forward` is only honest where a transition model exists, so `Dynamics` is **declared with no `Default`**: `Unknown` **holds the mean** and grows only the band (*we know less*, not *we know something else*), `LinearDrift` moves it only where someone said how. Fabricating a model to fill the pseudocode's shape would put a made-up trajectory behind a real-looking mean. ★ **The escalation is an admission, not an alarm.** A `SilenceAlert` carries the variance as its *reason* and rides the **existing** boundary — `detect::Severity::escalates()` — but is deliberately **not** a `detect::Alert`, because that type carries a `Shift` and **nothing shifted**: the boundary is reused, the shape is not. And it **can never be `Info`** — the one severity that would contradict §VIII is refused at declaration. ★ **§I is served, not claimed:** `ungoverned()` answers the concrete half that needs no matrix — a declared-governed dimension no sensor has ever spoken about. **Residuals:** not a joint POMDP belief (the tractable per-dimension mean+variance §VIII's own render asks for); the point-mass collapse **assumes the observation is authoritative**, which for a noisy sensor is a modelling assumption made and not checked — and modelling measurement noise is precisely where Kalman would come in, so the trap and the limit are the same decision seen twice; the drift rate is declared, not learned; and the point-mass collapse assumes the observation is authoritative.  ★★ **WIRED into `MonitorEngine` 2026-08-17** (the Phase-2 consolidation) — declared per sustain on `SustainWatch`, `Option` and absent by default so a sustain declaring none behaves exactly as before. ★★ **Runs per tick — but only through `ingest_at`**: a belief collapses on an *observation*, which needs an event time, and `ingest` has none to give. Rather than invent one (no clock in core), `ingest_at` is the door that carries a time and `ingest` **documents that it updates no belief** — a caller that never uses it has beliefs that were never observed, which is exactly what `ungoverned()` says about them. The engine offers every dimension the region declares, through `BeliefTracker::observe`, so **a stale reading does not collapse a belief inside the engine** for the same reason it does not on the state dimension. `silent(now)`/`silence_observations(now)` are **queries at a time**, not per-tick work. |
| MON-9 | **MonitorEngine** — per-sustain filters/detectors/trackers; `ingest()` pipeline **Kalman→EWMA→CUSUM + belief + Controller.escalate on WARNING+**; `render_widget()`, `tick()` | IX | ✅ **the engine, `ingest()` and the escalation are built 2026-08-16** (`monitor.rs` · `MonitorEngine`, `SustainWatch`, `flatten_holarchy`, `ingest`, `drive`) — **and OBSERVE is now self-driving**: an escalating ingest hands an `OodaObservation` straight to the OODA loop (CTL-3), which is what the chain lacked. The pipeline is the **native** one, `W = d(s,V) → EWMA → CUSUM` — **no Kalman stage**, per the Additions' fit caveat (= MON-2, a declined import, not a gap this leaves open). Per-sustain `α`/`μ₀`/`δ`/`h`/`θ_fire`, so one household's drift cannot move another's accumulator. §III's *"the Monitor is always running"* is honoured structurally: watching continues while the loop is parked mid-cycle and `held_back` names the phase that declined the hand-off. **Deliberately NOT built and NOT stubbed** — the belief tracker (= MON-8), `render_widget()`/the visual encoder (= MON-7), and `tick()` (the host's driver, same boundary CTL-9 draws). Disclosed: history is retained un-truncated, because dropping the oldest would silently change what a bounded `Never` concludes |
| MON-10 | **★ Urgency = distance-to-V** — `urgency(s)=d(s,V)`; **Monitor computes it**; consumed by Curated UI (salience) + Controller (escalation); ungameable (no second notion of "important") | Add. | ✅ **DONE in Rust** (`region.rs`, 2026-08-12) — `urgency(s) = d(s,V)` is real, and it is **the** salience quantity: a fact about the household's own declared region, so it cannot be gamed by a widget or an operative competing for attention. Replaces the `pct = spent/allocated` proxy **and its concrete defect** — the reference returns `0.0` whenever `allocated <= 0`, reporting an overspent unfunded pocket as maximally calm; as a region it is simply outside V by exactly the overspend. Consumed by M-UI (salience, UI-7) and M-CTL (escalation, CTL-7) |
| MON-11 | **Sustena-native form (fit caveat)** — Kalman/observability assume a continuous linear ODE; Sustena state is discrete/event-sourced → native form is **distance-to-V on the fold, with CUSUM/EWMA over that series** (same boundary, no ODE) | Add. | ✅ **DONE in Rust** (2026-08-12) — **the native form is complete**: distance-to-V computed on the fold, with **CUSUM/EWMA over that series**, and **no ODE assumed anywhere**. That is the article's fit caveat discharged end to end — the observability/Kalman apparatus stays a loose import, deliberately not built (see MON-2) |
| MON-12 | **Watching metered, looking spends a budget** — surfacing spends a working-memory slot (~4) + pawa; the CUSUM crossing is economic, not only statistical | Add. | ⏸️ economy-adjacent |
| MON-13 | **Frequency-domain change (harmonics)** — read the fold in the frequency domain (DFT over a windowed series) to separate a genuine shift from an expected cycle | Add. | ✅ **done 2026-08-17** (`harmonics.rs`). ★★ **A correction to this row's own prior text, found by reading before building:** it said this *'consumes `signal.rs` rather than defining its own pulse'*. **It does not, and it cannot** — `signal.rs` is the excitable-medium primitive (`Phase`/`Pulse`/`Field`, refractoriness, a wave across a **graph of nodes**) and produces no time series to transform. The confusion is traceable to the Additions paragraph running two sentences together: the harmonics sentence, then *'The Signal primitive Monitor relays is defined formally in §4B'* — a **cross-reference about where Signal is defined**, not a dependency. What the article says to transform is *'the fold [...] over a windowed series'*, which is the same `W = d(s,V)` series `detect.rs` already runs over and `MonitorEngine` already keeps a history of. So the window is **supplied** (`&[f64]`), exactly as `criticality.rs` takes one — **no parallel windowing is defined**, which was the real substance of the instruction. ★★ **The payoff, and it is proven not argued:** a monthly bill arriving on schedule reads as an `ExpectedCycle` and **does not escalate**, while a conformance case runs the **real `Cusum` from `detect.rs`** over the identical series and asserts it **fires**. So *'the time-domain detectors cannot do this alone'* is a measured fact about this codebase's own detector. The same magnitude arriving **off-cycle** surfaces as a `GenuineShift`, and a **new rhythm on top of the known one** still surfaces — so suppression is not a blanket mute. ★★ **A cycle must be earned twice over, and the two requirements are different:** **authorised** (`CycleProvenance::Declared` — an EVT-11 monthly period is exactly this, via `KnownCycle::from_periods` — or `Established` with enough observed cycles) **and resolvable** (the window must carry at least two full periods; a DFT bin `k` has period `N/k`). ★ **Declaration authorises a cycle; it does not make a short window able to measure it** — subtracting energy at an unresolvable bin would remove a real shift under the name of a season. Both failures return `CycleVerdict::Indeterminate` — *not* 'no cycle' and *not* 'a shift', the same third-answer family as `Skew::Unknown` and `Soundness::Unavailable`. ★★ **Testing forced a correction inspection would not have:** attributing a known cycle to one bin reported an **on-schedule bill as a shift**, because a bill is a periodic **impulse** and an impulse train puts energy at every multiple of its fundamental — which is what the row is *named* for. Fixed by attributing the harmonic series `k, 2k, 3k, …`, **with the cost named where it lives**: a genuine shift landing exactly on a harmonic of a known cycle is absorbed with it, inherent rather than a defect. ★ It **refines the existing boundary rather than opening one**: a genuine shift is classified with `detect::classify`, the same `excess → Severity` table, so it crosses via `Severity::escalates()`. A direct `O(N²)` DFT in plain `f64` — **no dependency**; an FFT is a named scaling slot. **Residuals:** ★★ **WIRED into `MonitorEngine` 2026-08-17** (the Phase-2 consolidation) — declared per sustain on `SustainWatch`, `Option` and absent by default so a sustain declaring none behaves exactly as before. ★★ **Runs per tick**, inside `ingest`, over the `W` series the chain already produces — kept rather than discarded. ★★ **And the wiring revealed a real seam, which is why integration is not the same as four passing unit suites:** `Ingested::observation()` gated on the `OodaObservation`'s **own** `escalates()`, so a detection explained by a known season would have been suppressed by `Ingested::escalates()` **and still handed to OBSERVE**. Two answers disagreeing about one reading is exactly the bug MON-13 exists to prevent; `observation()` now honours the suppression, and `suppressed_by_cycle()` reports it so a suppression is never *silent*. Still open: no window taper (leakage is not smoothed away, because a taper is another declared choice and its absence is more legible than a silent one); the mean is removed but a **linear trend deliberately is not**, since removing it silently would suppress the slow drift a Monitor most wants to see. |

**Summary:** **Monitor's detection pipeline completed 2026-08-12; the engine that OWNS it landed 2026-08-16, and with it OBSERVE became self-driving.** Two slices built the chain: `region.rs` made `urgency(s) = d(s,V)` a real quantity (MON-10), and `detect.rs` added **EWMA** (MON-5), **CUSUM** (MON-6) and the severity classification that **is** the formal watching→looking boundary. **MON-9 then supplied what was missing** — not more mathematics, but an object with a lifetime: `MonitorEngine` holds one chain per sustain, reads each sustain's **own** declared `α`/`μ₀`/`δ`/`h`/`θ_fire`, and hands an escalating reading to the OODA loop as an `OodaObservation`. Before it, the chain existed and nothing carried its state between observations; a `Watch` had to be built, fed and interpreted by whoever happened to call it. **The pipeline is the native one** — `W = d(s,V) → EWMA → CUSUM`, with **no Kalman stage**: MON-2 is a **declined import**, not a gap this engine leaves open, and MON-11's fit caveat is discharged without it. §III's *"the Monitor is always running"* is structural rather than aspirational: watching continues while the loop is parked mid-cycle, and `held_back` names the phase that declined the hand-off, so a deferred escalation is visible instead of looking like a dropped reading. Two escalation inputs, not one — a trajectory signal can fire on a dimension `V` does not bound at all, which a distance cannot measure. **Deliberately NOT stubbed:** the belief/POMDP tracker (MON-8) and the preattentive encoder (MON-7) have no placeholder fields, because an empty map reads as built-and-idle rather than absent; `tick()` stays with the host, the same boundary CTL-9 draws. **Update 2026-08-17 — MON-8 shipped, and Phase 2 opened on it** (`belief.rs`). ★★ The decisive choice was **not** following §VIII's pseudocode literally: its full-update line calls `kalman_filter.update(observation)` **by name**, and MON-8 shipped **without it** — so the declined import now has a second, concrete site rather than only this pipeline. What survives is the `point_mass`, and Sustena already had the rule for it: `Belief` **holds an EVT-10 `SnapshotDim`** and collapses through it, which makes *an observation arrived* one thing in this codebase rather than two. The behavioural proof is that **a stale reading arriving late does not collapse the belief**. ★★ Silence widens (`σ = elapsed × DRIFT_RATE`, monotonic across 24 hours) **without inventing dynamics** — `Dynamics::Unknown` holds the mean and grows only the band, because filling `predict_forward`'s shape with a fabricated model would put a made-up trajectory behind a real-looking mean. ★ And the alert **reuses the boundary without reusing the shape**: it rides `Severity::escalates()` but is not a `detect::Alert`, because that carries a `Shift` and nothing shifted — it is *we have lost sight of this*, not *this moved*. It can never be `Info`. The prior note above — *'deliberately NOT stubbed... an empty map reads as built-and-idle rather than absent'* — held exactly as intended: the tracker arrived as a real object, and **wiring it into `MonitorEngine` is named as the residual** rather than quietly assumed. **Update 2026-08-17 (second Phase 2 slice) — MON-13 shipped** (`harmonics.rs`), and it corrected its own row on the way in: the row said harmonics *consumes `signal.rs`*, and it cannot — `signal.rs` is an excitable medium over a **graph**, with no time series in it. The Additions paragraph runs the harmonics sentence and a Signal **cross-reference** together, and the row read the second as a dependency of the first. What this actually reads is the `W = d(s,V)` window `detect.rs` already runs over, **supplied** rather than re-windowed. ★★ The payoff is proven rather than argued: the **real `Cusum`** fires on the on-schedule monthly bill while the frequency-domain reading calls it an expected cycle — complement, not veto — and the same magnitude off-cycle still surfaces. ★★ A cycle must be both **authorised** and **resolvable in this window**, and failing either gives `Indeterminate` rather than a suppression: declaration does not make a short window able to measure. ★★ And testing forced the correction inspection would not have — a bill is a periodic **impulse**, so its energy sits at every multiple of its fundamental; attributing one bin reported an on-schedule bill as a shift. The fix is the harmonic series, with its cost stated: a shift on a harmonic of a known cycle is absorbed with it. **Update 2026-08-17 (third Phase 2 slice) — MON-4's typology shipped** (`windowing.rs`). ★★ The reconciliation is the substance: **tumbling is not a third mechanism** — calendar-aligned tumbling is a one-line delegate to EVT-11, and fixed-duration tumbling is **the degenerate sliding case where `step == size`**, proven by two routes producing byte-identical windows. So the genuinely new work was **sliding** and **session**. ★★ Sliding overlaps honestly (an event counted in every window containing it; `step > size` refused because it would silently drop events), and a session's end is **τ after its own last event**, with the last session left `Open` because no gap has been observed. ★★ And EVT-6's guarantee carries to the one type that could have lost it: `SessionState::close_on` takes a **`&Watermark` and nothing else**, so a clock two hours past the gap does not close a session — **a clock is not evidence**. ★ This row's `SlidingWindowAggregator` wording was corrected a second time: grep-0 **on both sides**, and deliberately unbuilt, because aggregation is a consumer of the typology rather than part of it. **Update 2026-08-17 (fourth Phase 2 slice) — MON-1 shipped, and the Monitor block of Phase 2 is COMPLETE** (`observability.rs`). ★★ The substance is a **declination plus a translation**: §I's observability matrix is declined on MON-11's ratified grounds — and with a concrete reason, that **`CAᵏ` needs a single `A` and the dynamics here is a choice, not a linear map** — while the discrete analogue is built term for term (`C` → measured dimensions, `A` → one operator's read→write edge, `CAᵏ` → `k` hops, `rank(O)=n` → every governed dimension reaches a measurement). ★★ It is **conservative where effects are unsummarised and says so**, carrying the unsummarised operators in the verdict itself — and over today's registry *every* verdict is conservative, since no shipped operator declares an `EffectSummary`. That is OP-3's finding seen from a second angle, and it is asserted rather than noted. ★ It also completes §I's implication where MON-8 started it, with the distinction kept sharp: `ungoverned()` is **runtime** (*nothing has spoken yet*), MON-1 is **structural** (*nothing ever could*). ~~**Still ⬜ in this module:** MON-7 (preattentive encoder — **moved to Phase 3**) and traces (MON-3)~~ **★★ Phase 2 moved to the Controller rows next, and CTL-11 closed them.**

**Update 2026-08-17 — MON-7 shipped, and it is the door into the surface block** (`preattentive.rs`). ★★ §VII's model maximises discriminability **subject to** the attributes coming only from Treisman's set, and the *subject to* is the load-bearing half: leaking onto a non-preattentive channel does not weaken the glance, it **ends** it. So `Channel` has exactly seven variants and **no `Other(String)`** — a non-preattentive encoding **cannot be written down**, the same discipline as EVT-6's missing `close_at(τ)`. ★★★ **Urgency is the household's, not the widget's**, and three things make that structural rather than conventional: `VisualSpec` has **no public constructor and no setters**; the channel map is **fixed, not a parameter** (a configurable map is a gaming surface); and **brightness is field-relative**, with `encode_field` the only entry point. The consequence is the property worth having — **a widget can only get brighter by being WORSE**, asserted in both directions, since a peer moving further from `V` makes it *dimmer* while it is itself unchanged. Attention is zero-sum across the field and cannot be manufactured. ★★ That **discharges UI-13 in advance**: *salience is never rendered as salience* is now a property of the encoder rather than a rule the surface has to keep. ★★ **Discriminability is RUN** — the necessary condition (distinct state-classes encode distinctly) over every class this encoder can see, with an identical peer held constant so the peer cannot be what makes them differ; `Hue` has three variants so *two near-identical hues* is unrepresentable. The psychophysical half is **named as out of reach**, not smuggled in. ★ **Encoder, not renderer** — `Hue::Amber` never `#E8A020`, asserted by checking no `#`/`px`/`rgb`/`hsl` reaches a spec; and Tufte is honoured by leaving **saturation and enclosure unassigned**, because there is no sixth or seventh data dimension and giving them one would be decoration. ★ It consumes the **real engine**: a conformance case runs a real `MonitorEngine` pass over a real `Region` and encodes its `Ingested`, so *this encodes `d(s,V)`* is checked against the thing that computes it. ★ **The counterweight belongs to the reference and it is the important one**: Slice 0 derived the single urgency signal from real data, and ★★ **Slice 13's Curated UI reused that formula verbatim** rather than inventing a second — so the reference already has **one** urgency and already feeds it to **selection**. This row adds the other consumer, the **encoding**. Two consumers, one signal, which is exactly why it cannot be gamed at either end. ★ And the contrast worth naming: `colour_rule` (`"amber_if_below_20pct"`) is a **widget-chosen** string hint, declared per field, serialised to the client and **evaluated by nothing on either side** — grep-0 in the web frontend. Even wired, it would let a widget decide when it turns amber; this row makes that shape unrepresentable. **★ MON-3 (traces) is now this module's only open row.**

**Update 2026-08-17 (the Phase-2 consolidation) — the four readings are WIRED into `MonitorEngine`.** Each of MON-8, MON-13, MON-4 and MON-1 had named *not wired into `MonitorEngine`* as its residual; all four are discharged here, **as wiring rather than new mechanism** — every module is the one it already was, reached through `Option` declarations on `SustainWatch` that leave an undeclaring sustain byte-identical to before. ★★ **What runs when is stated rather than implied, because three different answers are true:** harmonics **per tick** (it reads the `W` series the chain already produces); belief **per tick but only through `ingest_at`** (a belief needs an event time and `ingest` has none — no clock in core, so the door that carries one is explicit and `ingest` documents that it updates no belief); windowing and observability **on demand**, the latter **structural** over the operator graph. ★★ **And the wiring found a real seam, which is the argument for consolidating at all:** `observation()` gated on the `OodaObservation`'s own `escalates()`, so a detection explained by a known season would have been suppressed by `Ingested::escalates()` **and still handed to OBSERVE**. Two answers disagreeing about one reading. Fixed, with `suppressed_by_cycle()` making the suppression **visible** — a suppression nobody can see is indistinguishable from a detector that failed. ★ Belief's `SENSOR_SILENT` and harmonics' shift both cross the **existing** `Severity::escalates()` boundary; no parallel alarm channel was opened. **The spine reads: Monitor observes ✅, Tenet models ✅, Controller decides ✅ — and as of these two slices the cycle runs itself from a state update to a surfaced decision.**

#### M-TEN · Tenet — simulate + optimise  ✅ read  ★ SPINE
*Article: `3B1B — Tenet - Formal Execution.md` (Temporal Decision Architecture). Invert from a chosen future to the next optimal action. (Value function written **J**; **V** reserved for viability.)*

**Status: M-TEN's MATHEMATICS IS COMPLETE (2026-08-16, three slices). Δ(S), the inversion point, backward induction and the Controller join; then scenario ensembles, invariant actions, decision nodes and dead drops; then signal functions and the temporal pincer. The ONE open row is TEN-11 — the pawa-efficiency term — and it is parked on the economy, not on Tenet.**

| # | Feature | § | Rust status |
|---|---|---|---|
| TEN-1 | **Transition model `T: S×A→Δ(S)`** — a **probability distribution** over next states (MDP), not a single state | I·II | ✅ **built 2026-08-16** (`tenet.rs` · `Distribution`, `TransitionModel`) — a distribution that does not sum to 1 is **refused, not normalised** (normalising invents probabilities nobody declared, in the one input the optimisation reads as evidence). The R1 deterministic simulator is `Distribution::certain`, a **point mass** — a special case of this model rather than a competitor |
| TEN-2 | **Inversion point** `IP: S→{0,1}` — a precise target-state predicate that makes the inversion computable | III | ✅ **built 2026-08-16** (`InversionPoint`) — a declared predicate, **the viability kernel**, or both. The kernel reading is the one that matters: *"worth reaching"* becomes *"from here you can keep going"*, so the optimiser aims at survivability rather than at a number somebody picked. This is why SUS-10 had to land first |
| TEN-3 | **Scenario ensemble** `Ω={best, base, worst}` — each a `TransitionModel(p_…)`; `SimulateForward(s_0, T(ω), steps)` | IV | ✅ **built 2026-08-16** (`ensemble.rs` · `ModelTemplate`, `Scenario`, `Ensemble`) — a scenario is a **declared assignment of named probabilities** (`p_funding=0.85`), exactly as §IV writes it, pushed through one shared `ModelTemplate`. The three futures therefore differ **only** in their numbers over identical structure, which is the premise the whole comparison rests on. An unbound parameter is **named, never defaulted**; an incoherent one is refused by `Distribution::new`'s own rule. `Ensemble::new` **refuses fewer than two scenarios** — with one future every action is trivially invariant and nothing is ever a decision node |
| TEN-4 | **Backward induction (Bellman)** `J_t(s)=max_a[R+γΣP(s'|s,a)J_{t+1}(s')]` → value `J` + complete policy `π` (optimal action ∀ state×time) | V | ✅ **built 2026-08-16** (`backward_induct` → `Plan`) — sweeps from the terminal condition back, returning `J` **and a complete policy** (every state, every `t` — a policy answers off-path where a path cannot). `γ`/`R`/`H` are **declared spec fields**, and `Plan::horizon()` carries the window rather than implying forever. **The article's `V` is written `J`** per the Additions — `V` is the viable region everywhere else in the series. A dangling outcome is **refused**, not scored 0 |
| TEN-5 | **Invariant actions** `I(s_0)={a: π_A(s_0)=π_B(s_0)=π_C(s_0)}` — optimal across all scenarios = unconditional priorities | VI | ✅ **built 2026-08-16** (`EnsembleAnalysis::invariant_actions` → `InvariantSet`) — the intersection of the per-scenario optimal actions, run off TEN-4 unchanged, once per scenario. **The honest limit is structural, not a footnote:** `InvariantSet` carries the scenario names it was computed over and there is **no accessor returning the actions without them**, so *"this action is invariant"* full stop is unspellable. `the_one()` states §VI's at-most-one property (the article's own pseudocode intersects singletons) so nobody builds a picker over it |
| TEN-6 | **Decision nodes** `D={s: ∃ω_i,ω_j π_i(s)≠π_j(s)}` — where the optimal action diverges across scenarios | VII | ✅ **built 2026-08-16** (`decision_nodes`, `is_decision_node` → `DecisionNode`) — a node carries **what each scenario wants**, not merely that they disagree: the disagreement is the actionable part and is what a dead drop is written against. **A finding the fixture produced rather than confirmed:** divergence is **horizon-dependent** — a state splits the futures at `t=0,1` and not at `t=2,3`, because too near the end of the window no future can repay the costly move, so *"is this a decision node?"* is a question about a state **and a time** |
| TEN-7 | **Dead drops** — pre-committed `δ(D_i)=a*` chosen now under clarity; `ExecuteAtNode` falls back to `π(state)` off-node | VIII | ✅ **built 2026-08-16** (`DeadDropBook::pre_commit` / `execute_at` → `Resolution`) — Schelling's argument applied **to the pre-commitment itself**: a drop is refused now if it does not name a real decision node, names an action the model does not admit there, or carries a guard that does not parse — all obvious now, all expensive at the node. Guards reuse the existing predicate language (no new expression syntax). `Resolution` **never blends** a pre-commitment with a fallback: *"you decided this in advance"* and *"this is what the base scenario suggests"* carry different weight, the fallback is the **declared** base rather than a guess, and a drop that declined is **named**. A drop whose action no future proposed is **allowed and flagged** (`is_hedge`) — human judgment is permitted, invisibility is not |
| TEN-8 | **Signal functions** `σ_k: S^t→{0,1}` over the trajectory; `Monitor(history,signals)` fires → `TRIGGER Inversion` (HMM-style latent-scenario inference) | IX | ✅ **built 2026-08-16** (`pincer.rs` · `Trajectory`, `Signal`, `SignalMonitor`) — a predicate over the **trajectory**, as a small temporal layer (`Ever`/`Always`/`Never`/`At`/`Count` + connectives) over the single-state predicates already in the crate; no new expression language, no `eval`. **★ A correction to the article:** §IX's `IF σ(history) == 1` is two-valued, and read literally its own example *"no customers by week 6"* is **TRUE at week 0** — every warning fires on an empty history. `SignalVerdict` is three-valued (`Holds`/`Fails`/**`Undetermined`** with the step that settles it), early-determining in both directions; an **unbounded** `Never` can only ever be falsified, never confirmed. **Consumes `signal.rs` (MUL-15) as the firing mechanism**, and load-bearingly: a warning is persistent, so the refractory window is the **re-plan debounce** — a suppressed trigger is reported, not dropped. `MonitorReport::watched` carries the declared list, so *"nothing fired"* can never be quoted as *"nothing is wrong"* |
| TEN-9 | **Temporal pincer** — forward execution ∥ periodic backward recompute (reassess, update scenario probs, rerun BackwardInduct, refresh dead-drops, recalibrate signals) | X | ✅ **built 2026-08-16** (`run_pincer`) — all five backward steps real: a fired signal **reclassifies** the ensemble's base (§IX's HMM reading — an observable emission infers the latent scenario), `backward_induct` re-runs, and a dead drop whose node is no longer a decision node is **retired and named** rather than left aiming at a state every future now agrees about. `Recompute::policy_changed` is the observable proof a trigger did something. Runs on a **declared cadence** or on a signal. **Two disclosures:** "parallel" is **interleaved deterministically** — core has no threads, and that is §X's semantics rather than a simplification of it; and `T(s,a).sample()` takes its draw from an injected `Draws` (no RNG in core, same reason `approval.rs` takes `now`), which is also what makes a stochastic run reproducible enough to pin in a vector |
| TEN-10 | **Fork runs under the gate** — every simulated step passes the same `admit` gate; the sandbox is bounded by the household's own law | Add. | ✅ (R1 `simulate()` enforces the gate) |
| TEN-11 | **Pawa is the efficiency axis** — `Pawa(π,s)=Σ pawa(o,θ,s̃)`; `score(π)=Σ wᵢuᵢ(sim(π)) − λ·Pawa(π,s)` (optimal includes efficient) | Add. | 🔨 partial (per-branch pawa exists; ⏸️ full score ties to the economy) — **as of 2026-08-16 this is the ONLY open row in M-TEN**, and it is parked on the economy rather than on Tenet: every other row is built |
| TEN-12 | **Bellman = optimal control** — discrete case of min-accumulated-cost / stationary-action, **shared with the Controller (§3)** (Lyapunov/Pontryagin) | Add. | ✅ **joined 2026-08-16** (`rewards_from_region`) — with `R(s,a)=W(s)−E[W(s')]` the `H=1` sweep's `argmax J` **is** CTL-2's greedy `argmin W`. **One mechanism, not two that agree** — and a conformance vector *checks* it (computing the greedy choice independently) rather than asserting it. What the horizon buys is the ability to take a **worse first step for a better end**, which a one-step rule structurally cannot see |

**Summary:** **M-TEN's mathematics is complete as of 2026-08-16 — three slices took it from the single largest not-started module to one open row.**

*Slice 1, the optimiser core (TEN-1/2/4/12).* `Distribution`/`TransitionModel` made `T` a genuine **Δ(S)**, with the R1 forward simulator re-expressed as a point mass *inside* the model rather than beside it; `InversionPoint` made the terminal condition computable and it can be **the viability kernel** — *"worth reaching"* read as *"from here you can keep going"*, which is why SUS-10 had to land first; `backward_induct` returns `J` and a **complete policy**, with `γ`/`R`/`H` declared and the horizon carried; and `rewards_from_region` closes **TEN-12/CTL-12** — the `H=1` sweep **is** the Controller's greedy Lyapunov step, checked by a vector rather than asserted.

*Slice 2, the ensemble layer (TEN-3/5/6/7).* Runs that sweep **unmodified, once per declared scenario** and reads the disagreement. A scenario is a declared assignment of named probabilities pushed through one shared template, so the futures differ only in their numbers over identical structure. `I(s₀)` is the intersection, `D` the states where they split, and a dead drop is a decision taken **now, under clarity**, for a node not yet reached.

*Slice 3, the when-to-re-plan layer (TEN-8/9).* Signals are predicates over the **trajectory**, not the state — a small temporal layer over the crate's existing single-state predicates. The pincer runs forward on a sampled `T` and backward on a declared cadence or a fired signal: **reclassify → re-invert → refresh the dead drops**, with `policy_changed` as the observable proof a trigger did something. It **consumes the Signal primitive** (MUL-15) rather than defining a pulse, and load-bearingly: a warning condition is persistent, so §III's refractory window becomes the **re-plan debounce** — the reason MUL-15 was worth building first.

**Honest limits are carried in the types rather than the prose.** Invariance holds only over the **declared** scenarios (`InvariantSet` has no accessor returning the actions without the names); a single-scenario ensemble is **refused**, because one future makes every action trivially invariant; `Resolution` never blends a pre-commitment with a policy fallback; `MonitorReport::watched` means *"nothing fired"* can never be read as *"nothing is wrong"*; and every `Recompute` states its horizon, because re-running the sweep more often does not make it see further.

**Three article defects were levelled up rather than copied**, each recorded in the module docs and the vectors: §V's value function is written **`J`, not `V`** (the Additions' rename — `V` is the viable region everywhere else, including in the Lyapunov term this joins to); a distribution that does not sum to 1 is **refused rather than normalised**; and §IX's two-valued `σ(history)==1` is **three-valued**, because read literally its own example *"no customers by week 6"* is true at week 0 and every warning fires before the run begins. **Two findings came from running the fixtures rather than predicting them:** decision nodes are **horizon-dependent**, and a costly first step can be right at `H=3` and wrong at `H=1`.

**Still ⬜:** only **TEN-11**, the pawa-efficiency term — per-branch pawa exists and the full `score(π)` ties to the economy, so it is parked *there* rather than here. **The spine reads: Monitor observes ✅, Tenet models ✅, Controller decides ✅.** Its 4th sub-module (S/T "fundamental forces") = M-SUS (S) + M-OP (T), already built.

#### M-OPV · Operative (Symbiont)  ✅ read
*Article: `3B1B — Operative - Formal Execution.md` (SCOUT). Agents over the **same** ⟨S,T⟩, differing only in `u_i`. "A shared world, N preferences, a search, a sandbox, an aggregation rule, and a lock the agents do not hold."*

**Status: the most-built module after Sustain-design — in PYTHON. In Rust: the gate half exists (sandbox-under-same-gate, privilege), and as of 2026-08-16 the AGENT LAYER IS OPEN and its BOUNDARY IS BUILT — `ω` + the utility vector (OPV-1, OPV-3), the Goodhart guard (OPV-27), the presentation duty (OPV-29) and the sparse MoE router (OPV-28) — so §XV's three duties are all at least partial.**

| # | Feature | § | Rust status |
|---|---|---|---|
| OPV-1 | **`ω = ⟨u, Π, attention, M_self, M_world, dom⟩`** + **sharing constraint** `∀i,j: S_i=S_j ∧ T_i=T_j`; Prop 1 **no private move**, Prop 2 adding an agent only **re-ranks** `R(s)`, never enlarges it | I | ✅ **built 2026-08-16** (`operative.rs` · `Operative`, `Omega`, `Shared`, `LegalMove`, `Proposal`, `Cynefin`) — **the agent layer is open.** ★ **The sharing constraint is not checked, it is unrepresentable to violate:** `Operative` has **no `S` field and no `T` field**, because both live on the one `Shared` world that `Omega` owns. There is nowhere to put a private state space or a private move set. ★ **Prop 1 (no private move) is structural:** `LegalMove` has private fields and **no public constructor**; the only source is `Shared::legal_move`, and a `Proposal` carries one — so a proposal naming a move outside `T` is not rejected, it is **unconstructible**. (At parity in *effect* with the reference, where `execute_operator` refuses an unregistered operator — but that is a runtime refusal at the far end; here there is nothing to refuse.) ★ **Prop 2 (monotone in nothing) is at the type level:** `Shared::reachable` takes **no operative and no population** — `Ω` is not an argument, so adding a member cannot change a function it is never passed to. The test asserts the **pair** the proposition is actually about: reachable set byte-identical, ranking genuinely different (one operative drops the rest-heavy option as dominated; adding a second puts it back on a frontier **without adding a single move**). *An operative is a preference and a search procedure, not a capability.* ★ **OPV-16's recorded trap honoured at the point it would have been sprung:** homestead.json line 380 reads `"domain": ["finances","budget","savings"]` — a live subject-matter tag list — so the Cynefin axis is named **`Cynefin`**, and both stay expressible. `supp(u_i)` is real (an objective declares which dimension it reads), so `O = ⋃ supp(u_i)` and `U = dim(S) \ O` are computable now and **reported, not acted on** — §XIV is explicit that the fix is not another operative. **Declared slots deliberately NOT stubbed:** `Π` (OPV-4), `M_self`/`M_world` (OPV-11), attention (OPV-7), learning (OPV-6) have **no placeholder field**, because a declared-but-inert field reads as built |
| OPV-2 | **Default set** `Ω_default = {Orchie} ∪ {Mentor, Protégé, Attaché, Curator, Navigator}`; Corollary 1 — six by default add **no capability and no risk** | I | ⬜ Rust. **Note Correction 1 — five classes ship, not seven:** `_OPERATIVE_MAP` holds exactly mentor/protege/attache/navigator/curator; `chama_secretary.py` is **not in the tree** (a stale `.pyc` only). **Orchie is not an operative class either** — it has a graph spec, operators and routes, but **no class and no seat in the map**. Sharp against §4G.7 item 5: the member that is *structurally required* — the only path to a person, and therefore to a token — is the one with no seat. Corollary 1 holds for what ships |
| OPV-2b | **Note Correction 2 — 21 graph specs ship, not "15+"** — ten evaluation/deliberation pairs (five operatives × two), `orchie_evaluation`, and **ten sub-operative graphs**: financial_risk_assessment, burn_rate_analysis, investment_advisory, asset_valuation, deadline_analysis, priority_sorting, trust_assessment, relationship_tracking, route_optimization, delivery_tracking | Note | ✅ app-layer (a **correction upward** — the strategy layer is more built than the audit said) |
| OPV-3 | **Utility is a VECTOR** `u_i:S→ℝ^m`; Pareto frontier `𝒫(X)`; scalarisation recovers only the **convex hull** → non-convex options no weight vector can ever select; `Δu_i(o,s)` is transition-shaped | II | ✅ **built 2026-08-16 — the keystone** (`operative.rs` · `Utility`, `Objective`, `Sense`, `Dominance`, `pareto_frontier`, `nash_product`). `u_i : S → ℝ^m` is a **vector and stays one**: `Utility::at` and `Utility::delta` both return `Vec<f64>`, and **there is no method on `Utility` that returns a scalar**. `scalarise` exists as a free function documented as *what it deletes*, and nothing in the module calls it. ★ **Proof of value — the convex-hull defect executed, not repeated:** three options (10,0), (0,10), (4,4); the middle is genuinely non-dominated (`Dominance::Incomparable` — the verdict a scalar structurally cannot return), sits in a **non-convex notch**, and is swept against **1001 weight vectors without ever being the argmax**, while `pareto_frontier` keeps all three. Das & Dennis (1997) exactly: *collapsing to a scalar does not hide a trade-off, it deletes options.* A **convex companion case** is swept too, so the finding reads as *scalarisation misses exactly the non-convex ones* rather than *scalarisation never works*. `Δu_i(o,s) = u_i(o(s)) − u_i(s)` is **transition-shaped** as §4E's `D(s,s')` is, and still a vector. A dimension a point does not carry is **refused, not read as zero** — zero is a value. ★ **Nash recorded precisely, in the reference's favour and against it:** `nash_utility_score` is genuinely correct for the degenerate 1-D case and genuinely tested (4 tests) — **AT PARITY**, as §II says. Two nuances recorded rather than glossed: (1) the disagreement point is **named in three docstrings** (council.py, curator.py, protege.py) and **subtracted in none** — the arithmetic is `exp(mean(ln u))` over raw utilities, so a party gaining *exactly nothing* zeroes Nash's product (`NoBargain`) and does not dent a geometric mean, which is the single property the product is most known for; (2) it is **not on a production decision path** — the only non-test references are its own definition and one docstring, while `aggregate_delegated_votes` (confidence-weighted) is what actually runs, so *at parity* must not be read as *in the loop*. ★ **A tension found while grounding, stated not resolved by fiat:** Nash takes **one scalar per party**, so applying it to vector utilities requires the very collapse §II warns against — hence frontier **first**, losslessly, and bargain among frontier points only if a single pick is genuinely required, which is why *present the frontier, not the winner* is its own duty (OPV-29) |
| OPV-4 | **Strategy `Π` = operator-DAG** ⟨N,E,entry,exit⟩, conditional topological walk, `$dot.path` injection, cycle guard; **Prop 3 — a strategy adds structure, not capability** | III | ⬜ (✅ app-layer: `operative_graph.py` is Π in production, every node in the registry) |
| OPV-5 | **Meme = four kinds** — Model `S×T→Δ(S)` / Strategy (DAG) / Resource (∈S) / Network (edge in ⊕); Dawkins-Campbell condition: **copyable, variable, differentially retained**; Popper–Deutsch criticizability (anti-rational meme = one that escapes criticism) | III | ⬜ **not started** (graph specs are hand-authored static JSON, no provenance, no library) |
| OPV-6 | **Learning = search over meme space** `M(t+1)=retain(select_{u,Viab}(vary(M(t))))`; steps on **results, not a clock**; **No Free Lunch** ⇒ libraries are per-operative and per-Sustain, provenance is part of a meme's meaning | IV | ⬜ **not started** (no vary/select/retain anywhere) |
| OPV-7 | **Two attentions, inside EVERY operative** `⟨narrow, broad⟩`, both non-empty; narrow returns a **score**, broad returns a **re-framing trigger**; a council of one-attention agents is a **council of half-minds** | V | ⬜ (shape exists in Python by convention: cheap `should_evaluate()` scan vs expensive `evaluate()` — undeclared) |
| OPV-8 | **Attention meter = breadth × depth × resolution** over the ⊕ tree; `pawa(a)=κ_a·b·d·ρ ≤ B_att`; **iso-cost** — deep-and-narrow and broad-and-shallow are the same spend; **the working set is attention's footprint** (scan priced here, hold is §4H's K≈4) | V | ⏸️ **κ_a is a declared coefficient with no measurement wired to it** — `pawa_meter` charges over mutation/event/constraint-eval counts and bytes, **nothing keyed to breadth, depth or sampling rate**. The Note's own framing: *the system has the **hold** (knapsack, `DEFAULT_BUDGET = 4`, built) without the priced **scan** that fills it* — the inverse of the usual gap, and a small seam to close from both sides |
| OPV-8b | **★ κ_t — the LLM token term — is the meter's missing half, and both halves already exist on separate paths.** `pawa_meter.py` ships `1.0·compute + 0.01·storage` with **no token term**; meanwhile `_call_claude` already writes `model`, `input_tokens`, `output_tokens`, `llm_tokens_used` per call to `operators_log`. *"Both halves of the measurement exist on two paths joined by nothing; adding κ_t and routing token counts into the meter is small, well-defined work"* — and it is **the whole metering half of §4G.7 item 6**. (The **charging** half does not exist: `PawaLedger.charge()` is scaffolded and unwired.) Related: **model selection is a Python default argument** (`model: str = HAIKU` in `operatives/base.py`, passed explicitly at every call site) — *exactly the shape item 6 says it should not be* | Note · XII | ⬜ **not started** — the single cheapest item in this module, and it needs no new machinery |
| OPV-9 | **One duality at three scales** ℓ0 inside an operative / ℓ1 operative↔sub-operatives / ℓ2 **Orchie ↔ Council**; the two quantifiers (A) over agents and (B) over scales both hold — **reading (B) as licence to violate (A) rebuilds the half-mind council** | VI | ⬜ not started |
| OPV-10 | **Sub-operative = ⊕, not a new primitive** — `Σ_ω = (⊕_j Σ_ω'_j) ⊕ Σ_ω^own`, `V_ω'` within `V_ω`, admittance composes along the holon path; **Corollary 2 — depth adds conjuncts and cost, never power** | VI | 🔨 the ⊕/admit-along-path machinery ✅ in Rust; **the agent-layer instance ⬜** (Python's sub-operatives are graph *file paths*, so ⊕ here is named, not structural) |
| OPV-11 | **`M_self` is a Sustain; `M_world ≈ Σ̂`** — Conant–Ashby: a good regulator **is** a model; Sustena's M_world starts **declared** (only the residual is learned) — easier and more brittle, "wrong in ways it cannot notice"; **one M_world per household = the Tenet**, forked per councillor | VII | ⬜ **not started** (neither object exists; the shared-model correlation is not measurable) |
| OPV-12 | **Forks buy independence of SAMPLING, not of ASSUMPTIONS** — every fork forks one shared M_world, so residual correlation presents as **unanimity**; effective N < headcount by an amount nobody inside can measure | VII·XIII | ⬜ (the qualification cannot be represented until M_world exists) |
| OPV-13 | **Chaos bounds the simulator** — `H* ≈ (1/λ)·ln(ε/|δ0|)`, **logarithmic in precision**; a simulator **must report the horizon** over which its output is meaningful; past H* it is compute spent on noise | VIII | ⬜ **not started** (= M-TEN; no horizon reporting) |
| OPV-14 | **Self-organised criticality** — `P(s)∝s^-τ`; `τ≤2 ⇒ E[s]` diverges, `τ≤3 ⇒ Var[s]` diverges ⇒ **"expected loss" is not a usable summary near criticality**; three detectors: **branching ratio σ̂ (a read over the log's `causes`)**, tail shape (Clauset MLE, not a log-log eyeball), **critical slowing down** (variance + lag-1 autocorr — the Monitor's own CUSUM/EWMA moments) | VIII | 🔨 **σ̂ + detector 3 built 2026-08-16** (`criticality.rs` · `branching_ratio`, `BranchingReading`, `CascadeSummary`, `Regime`, `CriticalitySpec`, `critical_slowing_down`); **tail shape deliberately ⬜, for a structural reason.** The dependency was **verified before building**: `causes` is on the Rust `Event` and `assign_clocks` derives the DAG from it, so σ̂ adds no field. ★★ **The risk-arithmetic point is a TYPE, not a caveat:** there is **no method that returns a bare expected cascade size** — `cascade_summary()` returns a `CascadeSummary` whose `Mean` variant is unreachable at or above criticality. Not a wider interval, not a caveated number — **no number**. And withheld **at** the band, not only above it: being inside it is precisely not knowing which side of `τ ≤ 2` you are on. The observed largest cascade is reported as a **FLOOR**, since §VIII says the bound is the system's, *"not history"*, and an observed maximum is exactly history. ★★ **A reading surfaces, never triggers** — structural, not conventional: everything takes `&[Event]`/`&[f64]`, so the borrow checker enforces that a reading cannot change what it read; no engine, no operator, no `&mut` in the module. ★ **Window edges handled where this estimate quietly goes wrong:** a cause outside the window is **dangling and reported**, never an offspring nobody had; a tree over a finite window is pinned as **never supercritical** (`n` nodes, `n−1` edges); the critical band is **declared** rather than a hidden epsilon, and the same tree reads critical at 0.2 and subcritical at 0.05 — the band is the judgement, and it is arguable. ★ **Tail shape is slotted for a STRUCTURAL reason, not scheduling:** CSN's goodness-of-fit step is a semi-parametric **bootstrap**, which needs a random source ADR-0001 forbids in core. The MLE and lognormal likelihood-ratio are closed-form and could ship — but without GoF there is no answer to *"is a power law even a plausible fit"*, and shipping the two computable thirds would license a `τ` that reads as a fitted power law and is not one. **The constraint a future slice must satisfy is already recorded: the verdict type may not carry an absolute "power law" variant, only a comparison.** Out of scope and named: the `⟨λ,β,γ⟩` weight modulation (§VIII's scoring *implication*, which the article marks as design not derivation) and `scenario_ensemble` (§VIII-iii, constructive chaos) |
| OPV-15 | **Scenario ensemble** — `vary` applied to **initial conditions**: sample declared perturbations, fork, simulate under the **same** admit, return the **Pareto front + H\***; the bridge from complicated-domain optimisation to **complex-domain probing** | VIII | ⬜ **not started** (= M-TEN) |
| OPV-16 | **Suited domains** `dom_i ⊆ {clear, complicated, complex, chaotic}`; `match(i,s)`, mismatch ⇒ **abstain**; **coverage condition** `⋃dom_i` must cover all four or **surface the gap** rather than route to the least-bad scorer; *disorder* is a detection target | IX | ✅ **built 2026-08-16** (`cynefin.rs` · `ResponseMode`, `DomainReading`, `Suitability`, `Mismatch`, `DomainCoverage`, `Disorder`, `DomainRouting`, `assess`, `domain_coverage`, `route_on_domain`, `transition_risk`) — **and this closes §XV's three duties.** ⚠ **The collision this row recorded in advance HELD**: `operatives.<n>.domain` is still the subject-matter tag list, the axis was named `Cynefin` when `ω` shipped, and both stayed expressible — a flag raised before either existed and honoured at the moment it would have been sprung. ★★ **This layer routes ON a domain reading and does NOT produce one.** §IX assigns `dom(s)` to the Monitor and gives **no** classification algorithm; inventing a confident one would be the confident-classification-from-nowhere refused everywhere else here (`0/0 ≠ r=0`; a missing cost is not free). So `DomainReading` is **supplied**, and the classifier is **CAP-13 / §4D.6 — a separate LAYER, not an agent-layer gap.** ★★ **What actually closed Route:** an **indeterminate** reading is now representable, so a caller with no reading surfaces disorder instead of **fabricating a regime** to call `route()` at all — that was the real agent-side gap, and naming it is what makes the `Built` flip honest rather than asserted. ★ **The abstention's reason is derived, not canned:** each response mode carries what it *presupposes*, so a `Mismatch` names the presupposition that fails against the regime that denies it — §IX's *"not merely less useful; confidently wrong, because its method presupposes analysability"* reachable from declared data. ★ **Disorder is UNDECLARABLE by construction**, not by rule: `Cynefin` has exactly four variants, so `dom_i = {disorder}` has no value to write. It arrives two ways — an uncovered regime, or an indeterminate reading — and both **route nobody**, keeping the mismatches, because *why* nobody was suited is the gap's content. ★ **Coverage reconciled to ONE source of truth:** `mixture::coverage_gaps` (OPV-28) folded in a copy when §XIII's condition had no home; the duplicate is **removed** and it now delegates here. Named `domain_coverage`, since `goodhart::coverage` already means `O`/`U` over utility supports — two real notions, and the newcomer takes the longer name. ★ **OPV-14 wired in for the ONE inference §XIII licenses:** a critical σ̂ on a *complex* reading flags **complex → chaotic** and nothing else, and **never reclassifies** — there is no path here that produces a `DomainReading`. **Slot:** the `Π`-shape mismatch check (*"an optimisation DAG has no business declaring chaotic"*) needs `Π` = OPV-4; the declarations are **enumerable and auditable now** (`claimants`), and inferring a graph's shape from an operative's id or objectives would be guessing |
| OPV-17 | **Sandbox = fork + simulate under the SAME gate**, tagged `effect_class = sandbox`; ungated simulation makes the fork a **second write path** — "a gate with a simulation bypass is a gate with a bypass" | X | ✅ **structural in Rust** — one execution path, so a fork cannot skip the gate (Python's council rides the ungated `simulate_run_path` — Note Correction 3) |
| OPV-18 | **The score** `score_i = Δu_i + β·1[∀k: s̃_k ∈ Viab_T(V)] + γ·ΔReach_H` — viability is a **trajectory** property, not an endpoint; `γ=0` **refuses every beaver dam** | X | ⬜ **not started** (no Viab, no Reach) |
| OPV-19 | **Runway = the viability kernel with a clock** `R(s)=min{k: s_k∉V}` under the null policy; `Viab_T(V)={s: ∃ admitted policy with R=∞}`; runway is **cheap** (one policy, forward scan) and an **under-estimate in a declared direction** — report the bound with its sign | X | ✅ **DONE in Rust** (`runway()`, 2026-08-12) — **`R(s) = min{k : sₖ ∉ V}` under the null policy: one policy, one forward scan**, exactly as the article costs it. Obligations fall due whether or not anyone acts, which is what makes runway a **clock** rather than a restatement of `W`. Reported as an **under-estimate in a declared direction** (`is_under_estimate()`) — a real policy can only last longer. Surviving the scan returns `None`, not infinity: the absence of evidence, not evidence of absence. **The article's own example is a vector**: W = 0, box margin 500, rent on Thursday, `R(s) = 4` |
| OPV-20 | **Positioning `ΔReach_H`** under a **declared measure μ** (never raw cardinality); horizon-limited ⇒ **under**-estimate, biasing against long-horizon positioning; in a chaotic regime bounded by **H\*, not by budget** | X | ⬜ **not started** |
| OPV-21 | **The Symbiont's mind = the machinery already built, named** — ten **appraisal axes** (Joy/Sadness/Fear/Anger/Disgust/Anxiety/Envy/Ennui/Embarrassment/Nostalgia) as components `u_ik` with a distinguished evaluation context; OCC + Lazarus core relational themes; **Russell's caution — the ten names are an interface, not natural kinds**; **an emotion is an appraisal with a sign and a weight, not a quale** | XI | ⬜ (adds no object; becomes measurable once `u_i` is a vector) |
| OPV-22 | **Where the mind analogy BREAKS — build neither** — (1) an appraisal axis **has no write path**: it can make a move look good, never make it; the gate does not read the mood; (2) **an agent that IS one emotion is the half-mind council** — invert the casting: N whole minds carrying the full vector, differing in weights | XI | ✅ (1) structural in Rust — scoring cannot reach the write path; (2) a design rule to honour when OPV-1 is built |
| OPV-23 | **LLM = a proposal distribution** `π ~ g(·|ctx)`, accepted iff `verify_sandbox(π)`; Metropolis–Hastings / AlphaGo posture — **correctness does not depend on g, only efficiency does**; "the model was confident" is not an argument | XII | ⬜ (Python's `_call_claude` is a **method call, not an element of T** — Note Correction 4: as written the gate *could not* mediate it) |
| OPV-24 | **LLM admissibility + policy** — `allowed(llm.*,κ) ⟹ (∄o∈T covering κ) ∨ (perf(o,κ)<θ_κ)` with **θ_κ declared**; per-task-class `policy(κ)=⟨allow, B_κ juul, model, maxtok⟩`; guard is an **ordinary §4E constraint**; **deactivation is a policy, not a switch — the fallback edge must be declared** or deactivation is an outage; `maxtok` is the enforceable per-call cap (a budget without one is overshot by exactly one call) | XII | ⏸️ economy-deferred (the guard itself is one more predicate on the existing seam) |
| OPV-25 | **Council = social choice** — Arrow's wall; the **declared escape** is cardinal interpersonally-weighted utilities (Sen/Harsanyi) with **declared `w_i`**, `w_human = 0.51`; **U ranks, it does not decide** — compute and present **𝒫**, the human's residual chooses within it | XIII | 🔨 weighted aggregation + resolution ✅ in Rust (R1 council); **vector u, Pareto frontier, DSL-declared weights ⬜** (Python's weights are hardcoded) |
| OPV-26 | **Condorcet needs independence** — correlated councillors give the comfort of a large N with the accuracy of a small one ⇒ **each councillor gets its own fork**; mitigations: declare the model's fidelity claim, let councillors differ in M_world where they legitimately should | XIII | ⬜ (✅ app-layer: `_create_sandbox()` forks per councillor with a unique `fork_id`) |
| OPV-27 | **Goodhart guard** — `O=⋃supp(u_i)`, `U=dim(S)\O`; **no agent's score changes when U moves** — damage there is *unobserved by every scoring function*; the fix is **NOT another operative** (that just makes a smaller U) but an **invariant in V enforced by the gate** | XIV | ✅ **built 2026-08-16** (`goodhart.rs` · `Coverage`, `coverage`, `GoodhartGuard`, `referenced_dimensions`) — unblocked the moment `supp(u_i)` existed, exactly as this row predicted. ★ **The file layout is the design statement:** §XIV's whole point is that the fix moves the check **out of the agent layer**, so this is its own module beside the gate rather than part of `operative.rs`. A `GoodhartGuard` **is** a `ConstraintDecl` and `check()` is a thin call to the **existing** `admit_one` — if it did anything the gate does not, the module would have re-invented what §XIV says to reuse. It carries **no utility, no vote, no score, no rank**: there is nothing on the type that could become another scoring voice. ★★ **Proof of value, two-part because the claim is two-part:** sleep falls 8 → 3 and **every operative reads the two states as identical** (`u_i(before) == u_i(after)`, Δu the zero vector) — so nothing in the agent layer could rank against it however many operatives are added — **and** the gate refuses it via the declared U-invariant, while admitting the healthy state. ★ **A guard that watches nothing unwatched is REFUSED** at authoring time: watching what an operative already scores is a redundant voice, not a guard, and calling it one would be false assurance about the exact blind spot §XIV is about. ★ **§XIV's reason for refusing the obvious fix is demonstrated in both directions**, not quoted: adding an operative for a `U` dimension really does move it to `O` and shrink `U` — *a benefit and not a solution* — and it is still non-empty; then `S` gains a dimension one step later and a fresh `U` appears. Limits recorded: `U` is only over **declared** dimensions (no report finds what the model does not contain), and support matching is **root-name granular** — coarse in the **safe** direction, since it can refuse a real guard (visible) rather than silently accept a fake one |
| OPV-28 | **Orchestrator = sparse mixture-of-experts gate** `top-k(softmax(α·relevance + η·Δû + ζ·match))`; sparsity matters because evaluating an operative forks state and is metered; **relevance (subject) and match (Cynefin) are different axes — conflating them makes one inexpressible** | XV | ✅ **built 2026-08-16** (`mixture.rs` · `Mixture`, `Dispatch`, `Ticket`, `Contender`, `GateWeights`, `EstimateRule`, `subject_relevance`, `coverage_gaps`) — **§XV's third duty, and the one that made Route reachable at all.** ★★ **Sparsity is STRUCTURAL, not a post-filter:** scoring all `N` then truncating is a ranked list, because the expensive work already happened. So the router **performs** the call — `Dispatch::run` applies the caller's function to the selected tickets and nothing else, `Ticket` has private fields minted only inside `route`, and no method hands one to an unselected operative or iterates the population with `f`. Proven with a counting closure: **entered 2 of 4**, the other two never entered. ★★ **The routing scalar is walled off from presentation.** `Δû` is a vector and the gate needs a scalar — exactly the collapse OPV-29 just stopped a surface doing. The permission is narrow and stated: routing chooses **whom to ask, never what to do**. The wall is structural (`Dispatch` carries **no `Alternative` and no frontier**, so nothing in it can reach `present()`) and **proven** — present before and after routing, frontier **byte-identical**, including the non-convex option a routing-style weighted sum would delete. The collapse rule is a named `EstimateRule` so which collapse ran is auditable. ★ **The two axes cannot substitute:** `subject_tags: BTreeSet<String>` and `dom_i: BTreeSet<Cynefin>` are different fields of different types — §XV's warning (and OPV-16's collision) unrepresentable to violate. `subject_relevance` **generalises `is_relevant` and agrees at both boundaries** (untagged request → 1.0, untagged operative → 0.0), so it is > 0 exactly when the hard test is true and soft in between. ★ **Coverage folded in because it is cheap and load-bearing:** when `dom(s)` is in no operative's `dom_i`, `route` returns **zero tickets and names the gap** — §XIII says routing to the least-bad scorer is *confidently wrong*, not degraded. A domain mismatch withholds under **its own reason**, never scored, kept distinct from losing the top-`k`. ★ **Borrowed for selection, deliberately not for combination:** Jacobs et al. compute `y = Σ g_i f_i`; averaging expert outputs is the §II collapse one layer on, so `g_i` is reported and never weights an answer — and **softmax is monotone**, so it does not choose, it normalises. **Slots named, not stubbed:** `dom(s)` is **supplied** (a Monitor output needing OPV-14), `Δû` is **supplied** (computing it means forking — the thing routing avoids), *disorder* detection is OPV-16, and **Shazeer's load balancing is NOT built** — it is a training loss and there is no training here; `never_reachable` is the honest adjacent read of the dead-expert problem it exists to prevent |
| OPV-29 | **Orchie's three duties** — **route** sparsely, **hold the whole** (domain reading + criticality signal), **present the frontier not the winner**; showing only the top-ranked option silently re-collapses the vector — "a presentation bug with the consequences of an architecture bug" | XV | 🔨 **presentation duty ✅ built 2026-08-16** (`presentation.rs` · `present`, `present_joint`, `Presented`, `CollapseRule`, `Collapsed`, `Duty`) — **route is OPV-28 and stays ⬜**. ★★ **`present()` returns 𝒫(X), and there is NO method on `Presented` that hands back a single id on its own terms.** Reaching one option requires `collapse()`, which (1) demands a **declared `CollapseRule`** — *"because it was first"* is a rule and has to be said out loud; (2) returns `hidden`, everything the frontier held and the collapse dropped, **non-empty whenever there was a genuine choice**; (3) **refuses `SolePoint` on a frontier of more than one**, so the one honest reason to collapse cannot be borrowed to justify a real deletion. ★ **`would_reinstate_the_collapse()` is the §XV diagnostic**, and the proof ties §XV back to §II by computation rather than assertion: the option **1001 weight vectors deleted last slice** is the option a single-winner surface hides here. Choosing a **dominated** option is refused (that is losing a trade-off, not resolving one); presenting **nothing** is refused rather than returning an empty frontier, which would read as *"there is no good choice"* when the truth is *"you gave me no choices"*. `present_joint` is §XV's **hold the whole** at population level — an option only one operative rates non-dominated is still a real choice. ★ **The three duties are recorded with what is NOT built:** `discharged_by_this_build()` reports Route → `NotBuilt` (naming OPV-28), HoldTheWhole → `Partial` (the Cynefin domain reading shipped with `ω`; §VIII's criticality signal is OPV-14), PresentTheFrontier → `Built`. A status read that states its absences; nothing is stubbed. Limits: **presentation is not ordering** (an order over non-dominated options is a preference about preferences, and inventing one would be the collapse wearing a different hat), and which of the single/joint surfaces to show is the caller's decision |
| OPV-30 | **The approval token INSIDE admit()** — `admit ⟺ §4E ∧ (effect_class=sandbox ∨ valid_token(approve(o, principal)))`; `τ=⟨hash(o,θ), principal, proposal_id, nonce, expiry⟩` — **bound to (o,θ)** (else it approves a *shape* of act), **single-use** (at-least-once delivery makes a replayable approval one you gave once and spent twice), **expiring with re-admission at commit** | XVI | ✅ **DONE in Rust** (`approval.rs`, 2026-08-12). `EffectClass::Live` **carries** the token, so *"live and unapproved" is not expressible*; the binding is kept **structurally** (operator + the real params, compared by equality) rather than as a digest, so there is **no collision surface**; expiry is re-checked at commit against a host-supplied `now` (the core has no clock); the nonce is spent **at commit only**, so a refusal never burns an approval. Defer + re-admission already ✅ (R2 #11). **Python unchanged — recorded as a rust-ahead divergence in `approval.json`** |
| OPV-31 | **The trace invariant** `∀ live e: ∃ e_sim ≺ e_vote ≺ e_approve ≺ e, τ(e_approve)=τ(e)` — **prefix-closed ⇒ a safety property** (Alpern–Schneider) ⇒ **in the enforceable class**, so it can be structural rather than promised; depth cannot be used to get around it (Corollary 2) | XVI | ✅ **DONE in Rust as STRUCTURE, not a runtime check** — `Simulated → Voted → approve()`, each stage **consuming the previous by value**; `ApprovalToken` has **private fields and no public constructor**, so the only route to one is the full chain. A refused sandbox run cannot advance, and only `PASSED` advances — **`DEFERRED` is never an auto-approval**, because a deadline passing is not a decision. Depth adds a conjunct rather than escaping one (Corollary 2): there is one gate, and this clause is inside it |

**Summary: the gate half was already ✅ and better than Python; as of 2026-08-16 the AGENT LAYER IS OPEN.** The gate half stands as it did — the sandbox cannot bypass the gate (structural, one path), privilege is enforced, defer+re-admission ships, and OPV-30/31's approval token closed the module's strongest single safety gap on 2026-08-12. **What is new is the foundation the rest of the agent layer hangs off.** `ω` is built with **no `S` and no `T` on it**, so §I's sharing constraint is unrepresentable to violate rather than checked; **Prop 1 is structural** (a move outside `T` is unconstructible, not rejected) and **Prop 2 is at the type level** (`reachable` is never passed a population, so adding an operative cannot change it — asserted as the pair: capability byte-identical, ranking genuinely different). And **`u_i` is a vector that stays one**: no method returns a scalar, `scalarise` exists only to demonstrate what it deletes, and the demonstration is real — a non-dominated option in a non-convex notch, kept by the frontier and selected by **none of 1001 weight vectors**. Two things were recorded rather than smoothed over: Nash's degenerate case is **at parity and correct** (§II's own verdict) but its disagreement point is named in three docstrings and subtracted in none, and it is not on a production decision path; and **Nash needs one scalar per party**, so it belongs *after* the frontier, not instead of it. **OPV-16's `domain` collision was live and is honoured** — the Cynefin axis is named `Cynefin`, so the reference's subject-matter tag list stays sayable. **And on 2026-08-16 the layer's BOUNDARY was built on both sides.** ★ **Outward (OPV-27):** the Goodhart guard, and the file layout is the argument — §XIV says the fix moves *out* of the agent layer, so it is its own module beside the gate, a `ConstraintDecl` with **no utility, no vote, no score**, whose `check()` is a thin call to the gate that already exists. The proof is two-part because the claim is: damage to `U` leaves **every** operative's utility vector byte-identical (Δu the zero vector — nothing in the agent layer *could* see it, however many operatives are added), and only the declared invariant refuses it. A guard watching nothing unwatched is refused at authoring time; and §XIV's reason for refusing *"just add an operative"* is demonstrated both ways — `U` shrinks and stays non-empty, then a new dimension refills it. ★ **Inward (OPV-29):** the presentation cannot silently undo what the vector bought. `present()` returns 𝒫(X) and **no method hands back one id on its own terms**; collapsing needs a **declared rule**, reports **what it hid**, and refuses to borrow *"there was only one"* when there were four. The proof ties §XV back to §II by computation: the option 1001 weight vectors deleted is the option a single-winner surface hides. **Still ⬜ in the agent layer:** `Π` the operator-DAG (OPV-4), `M_self`/`M_world` (OPV-11), attention (OPV-7), learning (OPV-6), suited-domain routing + the coverage condition (OPV-16), — **and §XV's three duties are now all at least partial.** ★ **Route (OPV-28) shipped the same day** it was named as the gap: a declared-weight top-`k` softmax gate whose **sparsity is structural** (the router performs the expensive call, so an unselected operative's path is unreachable rather than merely skipped — proven by a counting closure entering 2 of 4), with the **routing collapse walled off from presentation** and the wall *proven* rather than asserted (frontier byte-identical before and after routing). It surfaces a coverage gap instead of routing to the least-bad scorer, keeps *not suited* distinct from *out-scored*, and takes the mixture for **selection only** — averaging expert outputs would be the §II collapse one layer on. It is `Partial` rather than `Built` for one honest reason: `match(i,s)` needs `dom(s)`, which is **supplied** until OPV-14 feeds the Monitor's domain reading — **the same absence that keeps *hold the whole* partial**, so one row now closes two-thirds of a duty. ★★★ **MILESTONE — §XV's THREE DUTIES ARE CLOSED.** `discharged_by_this_build()` now reports **Route ✅ · HoldTheWhole ✅ · PresentTheFrontier ✅**, all `Built`. Orchie routes sparsely on a domain reading, holds the integrating view including the criticality signal, and presents the frontier rather than a winner — with a person choosing inside it.

★★ **And the route there is left visible, including a wrong prediction of this summary's own.** It previously said OPV-14 was *"the single highest-leverage row, since it moves both Route and HoldTheWhole to `Built`."* **It did not** — §XIII calls the criticality detector *"one of the inputs"* to a domain reading, not the classification — and a related string conflated `dom_i` (an operative's **suited** domains, which `ω` carries) with `dom(s)` (the Sustain's **current** regime). **OPV-16 closed them, and for a specific reason worth keeping:** an **indeterminate** `dom(s)` became representable, so a caller with no reading surfaces **disorder** instead of fabricating a regime to route at all. That was the real agent-side gap. **`dom(s)` is still supplied** — §IX assigns the classifier to the Monitor (CAP-13 / §4D.6), which is a separate **layer**, not an agent-layer gap, and the distinction is what makes `Built` honest rather than asserted. **Criticality (OPV-14) is ✅ for σ̂ and detector 3**, tail shape slotted structurally. One item stays *cheap and high-value once V is a region*: **runway** (OPV-19).

#### M-MUL · Multiparty  ✅ read
*Article: `3B1B — Multiparty - Formal Execution.md` (SLIME). Compose person-holons into shared wholes using only local information — and never erase the node.*

**Status: the convergence substrate is ✅ in Rust (event-time, dedupe, LWW join). The distributed-coordination layer above it — quorum, consensus, routing — is unbuilt everywhere.**

| # | Feature | § | Rust status |
|---|---|---|---|
| MUL-1 | **Node + population** `n_i=⟨s_i, N(i), act_i⟩`; **no node ever sees P** — coordinated global state from **local information exchange only**; `act_i` is the node's own gated Enzyme set — *"a composed body may ask, price, aggregate, and refuse, but the move itself is always taken by the node"* | I | ✅ **built 2026-08-16** (`population.rs` · `Population`, `LocalView`) — **the hard constraint is structural**: a commit decision is computed from a `LocalView` built from `N(i)` and nothing else, and there is no field on that type that could carry the roster. `committed_fraction()` exists and is **named for the observer** — a cascade that consulted it would be reaching coordinated global state by reading the global state. `N(i)` carries a **declared** `d(i,j)` per edge, which is what keeps `λ` from being inert. Disclosed scope choice: neighbourhoods here are **symmetric**, though §I permits any subset |
| MUL-2 | **Quorum sensing / threshold commitment** `c_i = Σ_{j∈N(i)} σ_j e^{-λd(i,j)}`, `commit = 1[c_i>θ]`; mean-field `ẋ = -x + f(βx)` ⇒ a genuine **phase transition** at β\* | II | ✅ **built 2026-08-16** (`quorum_step`, `cascade`, `sweep`) — §II's pseudocode exactly: sense `c`, commit if `c > θ`, relay. **Below θ nothing; above it one seed takes the whole ring**, spreading a neighbourhood per tick because decisions read the **pre-step** lattice (otherwise a single step sweeps the graph and a broadcast wears a cascade's clothes). `θ` and `λ` are **declared**. **The bifurcation is OBSERVED, not predicted:** §II's mean-field `ẋ = −x + f(βx)` describes a well-mixed population and `β*` depends on the topology, so `sweep` reports the transition **for the graph that was declared** and `PhaseTransition` carries its size. Consumes `signal.rs` for the medium and for **debounced announcements** — but deliberately **not** for the commitment lattice: §II's `σ` is standing where §III's pulse is transient, and routing commitment through a refractory field would make it die out like a wave instead of latching |
| MUL-3 | **Two different quorums — confusing them is the classic error** — §II's is a **count against a threshold** (does a body form at all); §V's is an **intersecting majority** (what a formed body may ratify). Neither substitutes for the other | II·V | ✅ **honoured structurally 2026-08-16** — §II's θ is an `f64` **concentration** and a commit is a comparison of two real numbers; §V's quorum is a property of **sets** (that any two intersect). A scalar comparison cannot express a set-intersection property, so the classic error is **not spellable** through this type. Sharpened by the reference engine itself, which sits on the other side of the line: `CouncilSession` counts votes on a raised proposal — that is §V's quorum, and it is real. §V's own build is the consensus follow-on |
| MUL-4 | **Excitable media = the Signal primitive** — RESTING→EXCITED→REFRACTORY; FitzHugh–Nagumo; **the refractory term is load-bearing**: it makes the wave directional and stops a relayed message re-exciting its sender; no broadcaster required | III | ✅ **built 2026-08-16** (`signal.rs` · `Field`, `SignalSpec`, `Pulse`, `StepReport`) — and the article's central claim is **tested where it could have failed**: `connect` is **symmetric**, so nothing in the topology prefers a direction and the wave is directional anyway. Directed edges would have made it directional for a reason unrelated to refractoriness, leaving §III's claim unexamined while appearing to hold. A node that received inbound and declined **says why**, so no-self-re-excitation is *observed* (`RefusalReason::Refractory`) rather than inferred from a missing second excitation. `θ_fire` and the refractory window are **declared**; a **zero window is refused** — zeroing the load-bearing term deletes the mechanism and keeps the vocabulary. Logical ticks, no clock in core |
| MUL-5 | **Lamport happens-before + logical clock** — `a→b ⇒ C(a)<C(b)`; tie-break by node id gives a **total order every node computes identically from local data alone** | IV | ✅ (R2 #17 — `(t_event, id)` total order + causal stamps) |
| MUL-6 | **Read the implication one way only** — `C(a)<C(b)` does **not** mean a influenced b; the scalar clock manufactures an order over genuinely concurrent events and then cannot say which those were. **Vector clocks** recover it at O(k) metadata | IV | ✅ **built 2026-08-16** (`vclock.rs` · `VectorClock`, `CausalVerdict`, `assign_clocks`) — the biconditional restored: `a → b ⟺ V(a) < V(b)` and `a ∥ b ⟺ V(a) ≰ V(b) ∧ V(b) ≰ V(a)`. **★ The defect is named and demonstrated, not just described:** the earlier scalar answer reports a **FALSE CONCURRENT** across a transitive chain — `a` on node-1 → `b` on node-2 → `c` on node-2 means `a → c`, and the scalar clock says *concurrent* because `c` does not name `a` and they sit on different nodes. A vector clock gets it right, and a conformance case runs **both answers side by side** against the real `Event::happens_before`. Genuinely concurrent events stay concurrent — the fix does not simply order everything. `assign_clocks` derives clocks from the **recorded `causes` DAG**, so no field is added to `Event` and no wire format changes; a causal cycle is refused, a cause outside the supplied set is history rather than a deadlock |
| MUL-7 | **FLP impossibility** — no deterministic protocol guarantees consensus under full asynchrony with one faulty node; every real system buys around it with **timeouts or randomisation, never magic** | V | ✅ **recorded 2026-08-16** (`consensus.rs`) — there is nothing to *implement*: FLP is an impossibility result. What is built is the honesty it demands. **`propose()` returns a `Round` whose `Failed` variant is a NORMAL outcome**, not an `Err` a caller could `?` into a crash — a lost race is the protocol behaving correctly under asynchrony. And **nothing retries**, because retrying is exactly the timeout-or-randomisation §V says must be *bought*, and it is bought outside core where there is a clock. **Safety guaranteed, liveness not** — stated in the module docs, the vector, and a test |
| MUL-8 | **Quorum agreement / Paxos** — overlapping majorities so two conflicting decisions cannot both ratify; PREPARE→PROMISE→ACCEPT→ACCEPTED | V | ✅ **built 2026-08-16** (`consensus.rs` · `Body`, `Ledger`, `propose`, `Decision`) — §V's protocol exactly. **The safety property is a property of the BODY, not the run, and is made unspellable**: `Body::new` refuses `2q ≤ k`, so a body from which two disjoint quorums could be drawn **cannot be constructed**. Two proposers race; overlap forces the second to adopt the first's value via *"v = highest-numbered accepted value seen"*, and a chosen value cannot be un-chosen. **The re-choosing is NOT latched** — `decision()` recomputes from acceptor state every time, so the property is the protocol's rather than a cache's. Quorum size is **declared** per body |
| MUL-9 | **Byzantine bound** `k ≥ 3f+1` — the price of trusting a body whose parts might betray it; plain quorum is the working default at household scale, **the bound is the limit to remember before the federation includes anyone you don't fully trust** | V | ✅ **recorded 2026-08-16**, and *not* built — **this is not a BFT protocol**, and `ByzantineBound::THIS_BUILD_TOLERATES_BYZANTINE` is a `false` a test asserts: Paxos survives a node that *stops*, not one that *lies*. What **is** built is the **arithmetic** of `k ≥ 3f+1` — what a BFT protocol on this body would tolerate, and how many nodes a target `f` would need — because §V calls it *"the design limit to remember before the federation includes anyone you don't fully trust"*, and a limit you cannot compute is not one you can remember. Household scale ⇒ plain quorum/Paxos is the working default, as §V says |
| MUL-10 | **CRDTs — join-semilattice, LUB merge**: commutative ∧ associative ∧ idempotent ⟺ **arrival order, duplication and retry cannot change the result** ⇒ strong eventual consistency; CAP tax: partition ⇒ availability + eventual convergence, the right call for a household that must work offline | VI | ✅ **COMPLETE 2026-08-16 — the whole family.** The LWW register came first (R2 #17 — three laws asserted, every permutation checked) and was **sharpened 2026-08-16** so a genuinely concurrent pair is detected and both values kept rather than one silently overwritten by the total order (= MUL-6/EVT-9). The **wider family now ships** (`crdt.rs` · `JoinSemilattice`, `GCounter`, `PnCounter`, `OrSet`, `Rga`): **G-counter** exactly as §VI's own pseudocode writes it (per-node `max`, `value = Σ`), the **OR-set** and **RGA** from the Shapiro et al. 2011 / Roh et al. 2011 citations §VI makes rather than from its prose — a distinction recorded, not blurred. **★ The three laws are the proof obligation, not documentation:** `laws_hold` checks all three on a real triple of every type, and `converges` does the stronger thing §VI's sentence actually claims — folds **every permutation** of a delivery set, then folds every permutation **again with each update duplicated**, and asserts one state out of all of it. Four updates = 24 orderings = 48 folds, one answer. Arrival order, duplication and retry, tested rather than quoted. **★ "Never erase the node IS the merge" is now a THEOREM, not a property of how the code happens to be written:** `join(a,b)[id] ≥ a[id]` for every id and every pair, because `max` is monotone — asserted against a smaller, an empty and a zero reading. **★ Where §VI's roll-up correspondence stops being exact, closed rather than footnoted:** the example is *grow-only* by name and a real balance falls, so `PnCounter` (two monotone halves, the standard family answer) is here — `max(10,4)` keeps 10, which is correct for a counter and silently wrong for a pocket. **★ The vclock payoff:** `merge_stamped` takes the LUB regardless of the causal verdict, so the two readings the previous slice had to tell apart collapse — `a ⊔ successor(a) = successor(a)` means the LUB agrees with LWW exactly where LWW is right and loses nothing exactly where LWW is wrong. The clock is no longer load-bearing for the *answer*; it is still reported, because *why* two replicas differed stays worth knowing. **OR-set: add wins, and not by a tie-break** — a remove tombstones only the tags it *observed*, so a concurrent add was never what the remove was about. **RGA: the read is a pure function of the merged set**, so convergence of the set is convergence of the sequence; an element whose anchor has not arrived is **reported pending, never dropped and never placed by guess** (guessing would give two replicas holding the same set two different reads). Limits recorded: **no RNG in the crate** so tags/ids are caller-supplied `(node, seq)`; **tombstones are never collected** (a vanished tag could not suppress a retry, a vanished element would orphan a concurrent insert) and GC needs a causal-stability barrier this build does not have; and `converges` **refuses above 7 updates rather than sampling** — a convergence proof over a sample is not a proof |
| MUL-11 | **The holon invariant IS the merge** — because the group value is a **merge over member entries** and never an overwrite, the member's own number survives every aggregation **by construction**. "Not a policy sitting on top of the merge. It *is* the merge." | VI | ✅ **and as of 2026-08-16 it is a THEOREM, not only structural**: `GCounter::join` is per-node `max`, `max` is monotone, so `join(a,b)[id] ≥ a[id]` for **every** id and **every** pair — no merge exists that could lower a member's entry, so there is no rule anyone has to remember not to. Asserted across every permutation against a smaller, an empty and a zero reading. Previously ✅ structural (R1/Slice-7 roll-up is a fold over members; parent never overwrites) |
| MUL-12 | **Division of labour — why a node spends itself** — kin selection `rb > c`; **ESS**: stability is a checkable property of the assignment, not a hope about members; costly signalling; role assignment as `min Σcost s.t. coverage, rb>c, redundancy≥ρ`. **An assignment violating `rb>c` is not merely unfair, it is unstable and will be defected out of** | VII | ✅ **built 2026-08-16** (`division.rs` · `Division`, `Role`, `SharedInterest`, `StabilityCheck`, `Assignment`, `Unfillable`) — §VII's optimisation with all three constraints, and the third built as a **refusal, not a warning**: `Assignment` has private fields and **no constructor taking a role→member map on trust**, and `assign()` filters unstable pairings out of the candidate set *before* searching — an unstable split is not a cheaper option that lost, it is not in the space. `diagnose()` reports everything about a hand-proposed split, with the numbers, and still yields **no** `Assignment`. **★ Proof of value:** a split that is genuinely **cheaper**, **covered** and **redundant**, failing only `rb > c`, is refused — and `price_of_stability()` reports what refusing it cost, computed against the same search run with the constraint off. Stability is *bought*, and the price is a number, not a claim. **★ `r` is DERIVED, not asserted:** `SharedInterest::from_rollup` normalises the composition roll-up's per-member contributions, so `Σr = 1` by construction — a member cannot get back more than the whole. Two boundaries fall out of the arithmetic and are §VII's actual content: a single-member body has `r = 1` so stability collapses to `b > c`, and the larger the body the smaller each share, so **cooperation is harder to sustain the more diluted your stake is**. **`rb > c` IS the ESS condition** for the deviation §VII names (payoff `r·b − c` against a defector's `0`), *proved rather than asserted* — with the honest limit that it assumes the defector is **pivotal**, which `ρ > 1` structurally makes them not (the free-rider tension, disclosed; resolving it needs the group's *response* to defection, which §VII does not specify). **Costly signalling made checkable** rather than noted: `signals_commitment()` is false for a free role — stable trivially, therefore evidence of nothing. Three exact pre-flight checks decide impossibility *by counting* before any search (a member with no stable role is **named**, not discovered by exhausting a space that never held an answer). Composed on the `Population`'s node set (`Division::over`, the same one-topology-two-readings move `Router::over` makes); a node with no declared share is **refused in both directions** rather than defaulted to `r = 0` or `r = 1`. Limits recorded: coverage is the `ρ = 1` case of redundancy, one role per member, and the search **refuses rather than approximates** above its declared limit |
| MUL-13 | **Physarum conductance-adaptive routing** `Q_ij = (D_ij/L_ij)(p_i−p_j)`, `dD/dt = f(|Q|) − D` — flow thickens a channel, unused channels decay; entirely local feedback; provably solves shortest path. **This is Mycelium**: money, tasks, compute and storage routed by reinforcing what carries load | VIII | ✅ **built 2026-08-16** (`router.rs` · `Router`, `Reinforcement`, `physarum_step`, `converge`) — §VIII exactly: a Kirchhoff flow solve (weighted Laplacian, sink grounded) then `dD/dt = f(|Q|) − D` per edge. **★ Proof of value:** two routes start with identical conductivity and differ only in length; flow alone reinforces the short one and decays the long one below the cutoff — §VIII's *provably solves shortest path*, with no planner consulted. **What is global is stated rather than glossed:** the reinforcement is strictly local (`f` sees one edge's own flux), and the flow solve is a conservation law — physics, not planning. **Composed, not rebuilt:** `Router::over` takes the Population's declared `N(i)` as the graph and its `d(i,j)` **is** `L_ij` — one topology, two readings. Three honest limits: it is a **fixed-point iteration** (`converged` is reported), decay is **asymptotic** so *pruned* is a reading at a **declared cutoff** and nothing is deleted, and a disconnected source/sink is **refused, not zeroed**. `dt`, `f` and `L` declared |
| MUL-14 | **Disaggregation with hysteresis** — aggregate at `θ↑`, disperse at `θ↓ < θ↑` so the body doesn't flicker; the gap is the design parameter, bought with responsiveness. On dispersal CRDT state settles and each node carries its portion home — **the composition is destroyed; the members are not. Departure removes an edge, never a node** | IX | ✅ **built 2026-08-16** (`disaggregation.rs` · `Band`, `Assembly`, `BandPosition`) — **MUL-2's closing bracket**: that slice built `commit = 1[c > θ]`, this one `disperse = 1[c < θ↓]`, and the two thresholds are deliberately not the same number. **★ Hysteresis is a property of where you ARE:** `BandPosition::Within` means *nothing changes* — a formed body stays formed, a dispersed one stays dispersed, and the same reading means different things depending on which state it arrives in. `Band::new` **refuses θ↓ ≥ θ↑** (a zero-width band is one threshold wearing two names; an inverted one would form and disperse in the same breath — both are flicker). The gap is the **declared** design parameter, bought with responsiveness. **Proof:** eight readings across the band → exactly **two** transitions, against five for a single threshold, asserted side by side. **★ Departure removes an edge, never a node:** `members_removed` is **always 0** and a test asserts it rather than trusting the sentence; every member's portion comes home untouched. The settle runs through the **vclock merge** — one final value for a snapshot dimension (§IX's own phrasing), and what the merge preserves for a contested one, since collapsing there is what §VII warns against |
| MUL-15 | **★ This paper is the canonical home of two primitives the core modules reference rather than redefine** — **(1) the Signal primitive (§III)**, a relayed refractory pulse needing no broadcaster, is the **shared** signal that **Monitor (§2), Controller (§3) and Tenet (§4) all ride**, and those papers cross-reference §III rather than restating it; **(2) the conductance-adaptive router (§VIII)** is Mycelium's flow-reinforced routing, which the Mycelium whitepaper **builds on**. Build Signal **once, here** — three modules inherit it | Add. | ✅ **COMPLETE 2026-08-16 — both halves built.** **(1) The Signal primitive** (`signal.rs`, = MUL-4) is the shared relayed refractory pulse, and building it once here discharged a dependency in three spine modules: **MON-13, CTL-3 and TEN-8** — TEN-8 was blocked on it and shipped on it the same day, and a **fourth** rider joined that was not on the original list (`population.rs` uses the medium for debounced commitment announcements). **(2) The conductance-adaptive router** (`router.rs`, = MUL-13) is Mycelium's flow-reinforced routing, which the Mycelium whitepaper builds on. The row's own argument held exactly as written: one primitive discharged a dependency in three modules, and the second half runs on the same declared topology the coordination layer already had |
| MUL-16 | **§VI's shared-CRDT roll-up IS §4D's person-first ρ** — the two are the same operation named from two sides, not a coincidence to be reconciled later. *(The §VI worked example is generalised — no household is named.)* | Add. | ✅ structural (= **MUL-11**; roll-up is a fold over members, parent never overwrites) |

**Summary: ✅ M-MUL IS COMPLETE — every row, end to end. The first article module fully finished.** Nine sittings, built from the substrate up. The *substrate* came first — Lamport ordering, the total order, dedupe, the LWW join, and the holon invariant as a structural property of the merge. **The Signal primitive (§III)** is the shared relayed refractory pulse **Monitor, Controller and Tenet all ride**, and its own argument held as written: TEN-8 was blocked on it and shipped on it the same day, and a fourth rider joined that was not on the list. **The Population (§I–§II)** gave a node a local neighbourhood and **no view of P**, with threshold commitment and a cascade whose phase transition is *observed* on the declared graph. **Consensus (§V)** made safety a property of the *body* — a quorum that need not overlap cannot be constructed — and **closed EDIT-8's `CouncilMint` loop**. **The Physarum router (§VIII)** completed MUL-15: a Kirchhoff solve plus a strictly local reinforcement rule, routing with no planner. **Vector clocks (§IV/§VI)** restored the biconditional the scalar clock cannot, and sharpened the LWW register at the point §VII flags against itself. **And disaggregation (§IX)** closed the bracket the Population opened: `disperse = 1[c < θ↓]` with a **hysteresis band** — eight readings across it produce two transitions where a single threshold produces five — and *departure removes an edge, never a node*, with `members_removed` asserted at 0 rather than described. Its settle runs through the merge the previous slice built, which is why the two belong in sequence. **And division of labour (§VII)** answered the question the whole coordination layer had been assuming away — *why a node would spend itself* — by making the answer **checkable**: `rb > c` is enforced as a refusal rather than printed as a warning (`Assignment` cannot be constructed on trust, and unstable pairings are filtered out of the search space before searching), `r` is **derived from the composition roll-up** rather than asserted, and the proof of value is a split that is genuinely cheaper, covered and redundant, fails only stability, is refused — with `price_of_stability()` reporting what refusing it cost. **Bounds and limits recorded rather than implied away throughout:** FLP means liveness is bought, never guaranteed; `k ≥ 3f+1` is computed but deliberately unmet; vector clocks cost `O(n)`; the router's *pruned* is a reading at a declared cutoff, not a deletion; and `rb > c` assumes the defector is **pivotal**, which `ρ > 1` structurally makes them not — the free-rider tension is disclosed rather than papered over, because resolving it needs a response to defection §VII does not specify. **And the CRDT family (§VI)** closed the last row and the module with it: the G-counter exactly as §VI writes it, plus the PN-counter that keeps its roll-up correspondence honest, the OR-set and the RGA — with the three laws executed rather than quoted (every permutation, every duplication, one state) and *never erase the node* turned from a property of how the fold happens to be written into a **theorem about `max`**. Its LUB also retires the choice the vclock slice had to make: a concurrent pair neither loses an edit nor comes back as two. **Bounds and limits recorded rather than implied away throughout:** FLP means liveness is bought, never guaranteed; `k ≥ 3f+1` is computed but deliberately unmet; vector clocks cost `O(n)`; the router's *pruned* is a reading at a declared cutoff, not a deletion; `rb > c` assumes a **pivotal** defector, which `ρ > 1` structurally makes them not; and CRDT tombstones are never collected, because GC needs a causal-stability barrier this build does not have. **Nothing is ⬜.** Multiparty no longer only *rides* the pieces that exist — **six modules now ride pieces of it**, and it is the first article whose every row is built, proven and recorded.

### Layer 5 — SURFACE

#### M-UI · Curated UI engine  ✅ read
*Article: `3B1B — Curated UI - Formal Execution.md` (PERCEPT). A view is a **computed value**, not a stored page; attention is a budget and selection is a knapsack.*

**Status: mostly BUILT — in Python, and the article corrects the old audit on all four headline items. In Rust: not started (a surface module). One precise gap: the widget type-checker is not on the load path.**

| # | Feature | § | Rust status |
|---|---|---|---|
| UI-1 | **Widget as a typed declaration** `w = ⟨id, render, inputs, emits, event_class|unit, cost, description⟩`; `Γ ⊢ w` — every `inputs` path walked against the declared `state_schema`, every `emits` name in the registry, must declare `event_class` **or** `unit`; reports **all** errors rather than raising on the first | I | ✅ **done 2026-08-17** (`curated.rs`, with the schema half in `widget.rs` from UI-2) — the **compose engine**, ported from Slice 13's Python so this row had a reference implementation to measure against rather than only a spec. ★★ **STEP-0 settled β's key:** there is **no `EventClass` in this core** (`EventClass`/`event_class` grep-0; `Event` carries only `name: String`) — ★ and **the reference has none either**: its `event_class` is a plain string and `binding_key` returns it or a `UNIT` sentinel, both as `dict` keys. So there was no type to port and inventing an event taxonomy would have been this core deciding how the reference should name its events. β keys on `Event.name`, with **one** sharpening: `BindingKey::{Unit, Event(String)}` makes `EventClass ∪ Unit` genuinely the ∪ the notation claims. The sentinel collision that removes is **theoretical** (nobody names an event `__unit__`, and that is said rather than dressed up); the real gain is that a widget with **no** binding becomes unrepresentable, which the reference must reject at runtime. ★★ **Event-first in behaviour, not only shape:** an event-bound widget whose class is absent from the recent log is **never scored** — asserted via `candidates_considered`, not via the selection — so compose cannot degrade into *scan everything and rank*. Unit-bound widgets are always eligible and carry their own urgency. ★★★ **Urgency is the real `d(s,V)`, and this is where the port improves on its source.** The reference's urgency is an explicitly-**declared proxy** (`pct = spent/allocated`) and UI-7 says why — *'because CUSUM/EWMA don't exist'*. In this core they do, and so does `region.rs`, so a widget's urgency is its **share of `d(s,V)`**: the per-dimension weighted excess over the dimensions it declares, as a fraction of the sustain's total — *the fraction of the household's trouble this card can show you*. Inside `V`, every urgency is 0. ★★★ **Two gaming surfaces closed structurally.** (1) A widget **cannot set its own urgency** — its only lever is which dimensions it declares, and UI-2 already checked those against `dim(S)`; the distance is the household's. (2) ★★★ A widget **cannot declare itself cheap**: the reference's `cost` is an **author-declared integer defaulting to 1** while its own docstring describes a *'Hick-Hyman superlinear per-item cost'* — **the formula is written down and never computed**, a gaming surface of exactly the `colour_rule` kind. Here `attention_cost` is **derived** from `inputs.len()`, superlinear at `1 + n(n−1)/2`. ★★ Together they close the hole share-of-distance would otherwise open: grabbing more dimensions raises urgency **and** raises cost superlinearly, so the budget prices the strategy out — a greedy all-dimensions widget costing 4 is **excluded** while two focused ones costing 1 each are both selected. **The greedy move is self-defeating**, asserted rather than argued. ★★ **The knapsack is real and proven twice:** greedy-by-score takes 0.90 and stops where the DP takes 0.60+0.55 for 1.15, and optimality is asserted **against brute force** over every feasible subset. Every exclusion returns **with its score**, so *'N stayed quiet'* has something true to say. ★ One behaviour found by testing and kept: a widget scoring exactly 0 is **never selected** (the DP takes an item only when it strictly improves the total) — right, because `K ≈ 4` is scarce, and **at parity** with the reference; it returns in `excluded` with its `0.0`. ★ **Read-only by signature** (shared refs in, no `Registry`, so nothing here could run an operator) and asserted anyway across two calls, which also proves the view is deterministic. ★ **UI-2's typed population is the ordering benefit realised**: `compose_view` takes a `WidgetSet`, so **a widget with a made-up input cannot enter the knapsack** — it never loaded. **Residuals:** `render` stays **opaque**, so this ranks rather than renders (the reference's per-render `WIDGET_RENDERERS` belong to a surface with a display); urgency-as-a-**share** is a modelling choice and is named as one; `relations_violated` contributes **no** urgency, inherited from `region.rs`'s refusal to score a boolean; `device` is carried and **not** used to rank; relevance is token overlap, not meaning; and nothing calls `compose_view` from an engine — a host assembles `r`, at a surface's cadence. |
| UI-2 | **★ The type-checker is not on the load path** — `validate_all_widget_schemas()` is called only from tests; `sustain_engine.py` does not import `curated_ui` at all. *"A check that is called somewhere is not a monitor."* **The highest-value small change in the module** | I | ✅ **done 2026-08-17** (`widget.rs`) — **first of the surface rows**, taken first because it changes a real property rather than adding a surface, so every later UI row is built against a load path that actually type-checks. ★★ **STEP-0's crux decided the scope:** there is **no widget code in this core at all** — `curated` is **grep-0**, and all twelve `widget`/`Widget` and three `salience` hits are **prose in doc comments** (`editing.rs` explaining why `RebindWidget` is absent, `monitor.rs` naming MON-7's row, `region.rs` calling `d(s,V)` the salience quantity). No widget type, no binding table, no `compose(r)`, no knapsack, **no load path**. So porting `validate_widget_schema` would have produced a checker **beside nothing**, which checks nothing and cannot have the property this row is about. The row is the **typed load path with the judgment on it**. ★★★ **The check is on the path structurally:** a `WidgetDecl` is what an author writes; a `LoadedWidget` is what a surface may draw, and it has **private fields and no public constructor**, so the only way to obtain one is `WidgetSet::load`, which type-checks `inputs ⊆ dim(S) ∧ emits ⊆ T`. *A widget that does not typecheck cannot be rendered* is a fact about the types, not a rule to remember — the `Capability`/`ApprovalToken`/`VisualSpec` discipline. A typo'd dimension produces **no widget**, not one that renders blank or reads `null` later. ★★ **Two containments, nested, at different times:** at **load**, `emits ⊆ T` (a widget may not even name an operator this definition forbids); at **emit**, the operator must be in **its own** declared list — proven by a widget refused for `budget.record_income`, which the definition *does* permit and the widget never asked for. Same shape IMM-10 gives a learned rule. And `WidgetEmission` has no public constructor either, so a request naming an operator no widget declared cannot be forged and handed to the gate. ★ **Three distinct load errors**, kept apart for the reason EVT-15 splits `Refused` from `NotPermitted`: *not a path*, *not a declared dimension*, *not permitted* versus *not registered* — each sends an author to a different repair. **Every** error is reported, not the first, and **one bad widget refuses the whole set**, because a partial load is **silent degradation**: a missing widget is indistinguishable from one that decided it had nothing to show. ★ **Reuse, not redefinition:** `dim(S)`/`T` are read from `Definition` (the same two EDIT-13's `π` walks) and inputs are parsed with `predicate::parse_state_path`, which exists precisely so a second wildcard-path grammar does not appear in this crate. ★ And EDIT-13 pays off immediately: a real typed `Edit` retiring a dimension or an operator makes a loaded set report **stale** (`is_current_for`), while adding an invariant correctly does **not**. ★★★ **STEP-0 also caught this repository overstating itself:** MON-7's vector, written the day before, recorded *urgency → selection* as **`AT PARITY — curated_ui.rs's equivalent here`**. There is no `curated_ui.rs`. **Corrected in that vector**, not quietly left — a vector that overstates parity is the one failure mode this discipline exists to catch. **Residuals:** this is the **load path, not a surface** (`compose(r)`, β and the knapsack are later rows); `render` is an **opaque string** by design, so a misspelled render tag is **not** caught here and that cost is stated; **emission params are not type-checked**, because `OperatorMeta` carries no params schema in this core — the gate still catches it, just later; and nothing wires a `WidgetEmission` to `execute`, the same core→host seam every other row draws. |
| UI-3 | **The binding table β : (EventClass ∪ Unit) → 𝒫(W)** — keyed on **event class, never on Enzyme name**; a formal **inversion** of the old operator→widget `ui_schema`; additive, so the old binding is untouched | II | ⬜ Rust (✅ app-layer) |
| UI-4 | **`compose(r)`, `r = ⟨state, query, device⟩`** — a view is computed fresh per call and **never stored**; reads state/events/rollup/needs-attention and **writes nothing**; an event-bound widget becomes a candidate only if its class appears in the recent window | III | ⬜ Rust (✅ app-layer; ⚠ `limit=25` hard-codes the meaning of "recently" — a widget whose event scrolled out cannot be summoned) |
| UI-5 | **Honest empty-slate suppression** — a card **withdraws itself** rather than render an unexplained `0`; the grounding condition is enforced *before* any ranking | III | ⬜ Rust (✅ app-layer — and it came from live use) |
| UI-6 | **Salience `score(w) = α·urgency + λ·relevance`** — urgency dominant by design | IV | ⬜ Rust (✅ app-layer) |
| UI-7 | **★ Urgency is a proxy, with a named blind spot** — `spent/allocated`, deliberately reusing the Monitor's Slice-0 signal because CUSUM/EWMA don't exist; **`allocated ≤ 0` returns 0.0, so an unfunded pocket can never be urgent**; unmapped captures pinned at `1.1` — a policy living in a module constant | IV·X | ⬜ (blocked on M-MON; = backlog #23) |
| UI-8 | **A budget is the right model** — Miller/Cowan bounded working memory; Hick–Hyman (choice time grows with log options) and Fitts as the interaction costs; attention as a capacity pool that is **allocated and spent** | V | ⬜ Rust (✅ app-layer, `DEFAULT_BUDGET = 4`, Cowan-cited in source) |
| UI-9 | **Selection is a 0/1 knapsack** — NP-hard (Karp), solved by a genuine DP: integer costs, scores scaled ×1000, descending capacity loop, `keep[i][c]` table **and a backtrack reconstruction**; returns **both `selected` and `excluded`** with scores and reasons | VI | ⬜ Rust (✅ app-layer — verified genuine, not a sort dressed up) |
| UI-10 | **Progressive disclosure as an FSM** `δ` — state is the accumulated `known` map, transitions are answers, `required_params_satisfiable` decides F | VII | ⬜ Rust (🔨 app-layer: `infer()` *is* a δ but **hand-rolled per flow** — no abstraction a new flow can inherit) |
| UI-11 | **Effect-first capture `ℐ : ε → (o, θ)`** — deterministic, no LLM, no `eval`; **θ from the Enzyme's real `inspect.signature`** so never more fields than it declares; pocket names matched against the Sustain's own live state; a hint that eliminates *every* candidate is treated as **no hint**; history may pre-fill but **never silently and never skips the confirm** | VIII | ⬜ Rust (✅ app-layer — `infer()` never writes; confirm calls `execute_operator`) |
| UI-12 | **Bounded generation** — a generated view constrained to the declared schema (the LLM proposes, the schema disposes) | IX | ⬜ **absent entirely** |
| UI-13 | **`WidgetVisualEncoder` — salience computed then NOT rendered as salience** — the engine ranks under a budget and hands the result to a renderer that **draws everything alike**; the last mile (rank as a pre-attentive visual difference, per Treisman feature-integration) is missing | X | ⬜ **not started** (grep-confirmed absent) |
| UI-14 | **What the budget does not buy** — a budget bounds *what is shown*, not whether the underlying state is understood; and **two widget notions coexist** (`curated_widgets` with typed `emits` vs `ui_schema`'s untyped `ctas` display strings) — the one-language-two-implementations divergence at the UI layer | X | ⬜ (= backlog #36, 4 render systems) |
| UI-15 | **Coverage** — only `homestead.json` declares `curated_widgets`; every other spec honestly composes to *"no curated widgets declared for this sustain yet"* | Note | ⬜ (honest empty state, not a defect) |

**Summary:** the article's headline correction stands: **this module was specced as mostly-unbuilt and is mostly-built** — typed widgets, β keyed on event class, `compose(r)` as a pure computed value, a genuine knapsack, and deterministic effect-first capture all exist in Python. Nothing is in Rust yet (it is a surface module, correctly sequenced after the engine). **Update 2026-08-17 — UI-2 closed, and Rust now has a widget layer.** ★★ STEP-0's crux: there was **no load path in this core at all** (`curated` grep-0; every `widget`/`salience` hit prose in a doc comment), so the row became *build the typed load path with the check on it* rather than *port a checker to sit beside nothing* — which is also why it was cheap, since there was no untyped path to retrofit. ★★★ `LoadedWidget` has **private fields and no public constructor**, so an untypechecked widget cannot reach a surface; `inputs ⊆ dim(S) ∧ emits ⊆ T` is checked at load, and a widget's **own** declared `emits` again at emit — two nested containments. ★ `dim(S)`/`T` are read from `Definition` and paths from `predicate::parse_state_path`, so nothing is redefined; and a real typed `Edit` retiring a dimension makes a loaded set report **stale**. ★★ The counterweight is that **the judgment and the schema shape are the reference's own** — this core invented neither — so the gap was **one word wide**: *on*. ★★★ STEP-0 also caught MON-7's vector claiming a `curated_ui.rs` that does not exist; corrected there. **Remaining:** UI-1 and UI-3 … UI-15 in Rust. ★★ **UI-13 is discharged in advance by MON-7** (2026-08-17) — brightness is field-relative and `VisualSpec` has no constructor, so a widget can only look more urgent by genuinely being further from `V`. ★ And **UI-7's urgency blind spot is no longer blocked**: it was blocked on M-MON because CUSUM/EWMA did not exist, and they have since shipped (`detect.rs`, MON-5/MON-6) — so the row is now buildable rather than waiting, and its `allocated ≤ 0 ⇒ 0.0` policy is a live decision rather than a deferred one.

#### M-EDIT · Editing engine  ✅ read
*Article: `3B1B — Editing - Formal Execution.md` (SLOPE). Change the definition while it runs — the meta-operator, one level above the Enzyme.*

**Status: the CENTRAL safety property is BUILT in Python — the migration predicate is real. The sharpest gap is that a definition edit is an in-place overwrite: there is no history, so there is nothing to roll back to.**

| # | Feature | § | Rust status |
|---|---|---|---|
| EDIT-1 | **The definition is a value** `D = ⟨schema, Inv, T_decl, …⟩` — editable, versionable, diffable; an edit is a **meta-operator** on D, subject to its own gate | I | ✅ **DONE in Rust** (2026-08-12) — `Definition` is a first-class value, and now genuinely **versionable and diffable**: `VersionDag` holds the append-only history and `Edit` is the typed diff. What remained of this row after EDIT-8 was the *versionable* half, and `version.rs` supplies it |
| EDIT-2 | **The typed edit taxonomy `e`** — AddDim / DropDim / RetypeDim / AddInv / DropInv / TightenInv / LoosenInv / AddEnzyme / … each with its own safety class | II | ✅ **DONE in Rust** (`Edit`, 2026-08-12) — the **structural / regional / transitional** families, each with its own `applicable(e,D)` guard and `apply()`. `ModifyOp` and the **peripheral** family are deliberately absent: guards and effects are Rust fns in this core, not data, and there is no utility/widget model — a variant whose typing obligation could not be discharged would be a stub wearing a type's clothes. **Python still has no `e` at all** |
| EDIT-3 | **The migration predicate** — an edit is safe iff **every live instance's actual current state** satisfies every candidate invariant; returns the **witness set** `{sustain_id, invariant_id, expression, reason}` | III | ✅ **DONE in Rust** (`safe()`) — witness set `{instance_id, invariant_id, expression, reason}`, matching the reference's own shape, **and since 2026-08-12 it judges `μ(sᵢ)` for a declared μ** (EDIT-12), which is what §III's *migratable* case actually needs. The `μ = id` half is **AT PARITY**, not rust-ahead — the reference genuinely implements it |
| EDIT-4 | **Fail-safe on an uncompilable candidate** — a candidate invariant that **fails to compile** is reported as a violation against **every** live instance rather than skipped | III | ✅ **DONE in Rust** — an uncompilable candidate invariant is reported as a violation, never skipped (fail-safe defaults at the meta-level too) |
| EDIT-5 | **Refusal vs error, in the return type** — a refusal is an *expected everyday outcome returned as data*; a `ValueError` is a programmer/input error and is *raised*. The Embroidery distinction (a fact about the household vs a fact about the document) honoured in the signature | III | ✅ **DONE in Rust, structurally** — the distinction is carried by *types*, not by raise-versus-return: a refusal is data (`EditAdmission.verdict`, `Rollback`, `Compatibility`) and a malformed declaration is a separate error type (`Irreversible`, `TransitionDeclError`, `MigrateError::ExpandIsNotAWeakening`). A fact about the household and a fact about the document cannot be confused because they do not share a type |
| EDIT-6 | **The safe-change asymmetry** — **loosening is free by proposition** (a weaker V cannot strand a state already inside a stronger one); tightening must be checked | IV | ✅ **DONE in Rust, and sharpened 2026-08-12** — `Edit::can_strand()` now names exactly three kinds (`AddInv`, `ModifyInv`, `RetypeDim`), because `Safe` evaluates the *candidate's invariants* against live states, so an edit strands only if it **adds or tightens an invariant** or changes what a value means. That precision is what makes EMC's `e₊` **auto-safe with no check at all** rather than merely cheap. **Python still pays the full `|inst| × |Inv|` scan on every edit** |
| EDIT-7 | **Versions, inverses and the algebra of rollback** — `D_n`, `e⁻¹`, and the **3-valued rollback** Total / Partial / Unavailable over an append-only definition DAG | V | ✅ **DONE in Rust** (`version.rs`, 2026-08-12) — **closes N3, the module's sharpest gap.** `VersionDag::commit` **appends**; nothing is overwritten, and `head` is a **pointer, not a counter** (two edits can branch from one parent, which an integer cannot express). Each node records the full `⟨D, parent, e, μ, author, t⟩`. `e⁻¹` is derived over the EDIT-8 taxonomy against the **parent** node, and round-trips are checked (`e⁻¹(e(D)) = D`). **Rollback is three-valued and `Total` has NO Δ field** — `Rollback::classify` is the only constructor, so *reporting Partial as Total* is unspellable, not merely guarded. Both pre-image escapes work: **folding the log** (free — the `AddDim` that introduced a dimension is already in the history, so μ⁻¹ is a lookup) and **explicit journalling** (paid). The algebra is a **groupoid**: `path_between` returns `None` for branches that never meet, and a path is no more restorable than its worst link |
| EDIT-8 | **Edit authority — the gate on the definition** — an edit is admitted through `admit()` with its own authority term, not checked inline | VI | ✅ **DONE in Rust** (`editing.rs`, 2026-08-12) — **the fifth duty on the §4E seam**. `admit_edit()` implements all four terms; **`EditEffect` has NO `Unchecked` variant** (the operator gate needed one for R1 parity, the meta-gate is new, so no bypass exists at all). §VI's *authority does not lift from below* is **structural**: `EditToken::mint` refuses a `Governance::Shared` definition from any single actor, and the only other route is a `CouncilMint` — private fields, constructible only from a **passed AND quorate** decision. **Quorum arithmetic was the declared M-MUL composition point — and it is CLOSED as of 2026-08-16**: `CouncilMint::from_consensus` takes a `consensus::Decision` (private fields, no public constructor, obtainable only from a ledger where acceptors genuinely accepted, drawn from a `Body` whose quorum size could not have been declared without satisfying `2q > k`), so a shared definition's rule-change quorum is now **derived, not asserted**. The `quorum_met` bool survives as one path among two, for a host whose consensus lives elsewhere |
| EDIT-9 | **The new definition governs immediately** — after a successful update, every live instance's spec cache is invalidated, because *"a version bump that doesn't reach the gate is a version bump that lies"* | VI | ✅ **structural in Rust — there is no cache to invalidate.** `VersionDag::current()` reads the head node, so a committed edit governs the next call by construction. Python needs `self._specs.pop(...)` per live instance precisely because it has a per-instance spec cache; the Rust core has none, so *"a version bump that doesn't reach the gate"* is not expressible |
| EDIT-10 | **Typing on the authoring path** — compile every invariant against the candidate schema and **raise on the first bad one**; reject unknown dimension types, duplicate dimension names, an `authority` outside {binding, advisory}, and an Enzyme name absent from the registry | VI | ✅ **whole-definition typing DONE in Rust** — `typecheck(D)` runs `bind()` over **every** invariant against the candidate schema, on `D'` entire, every time. Proven by the case that matters: **`RetireDim` invalidating an invariant that never mentioned the edit**. Guard/effect/widget obligations remain out of reach while operators are code rather than data |
| EDIT-11 | **Replay under `D′`, and the limit that blocks it** — patch replay is **D-independent by construction**, so it cannot answer "what would this history mean under the new definition"; needs the **semantic** reducer | VII | ✅ **done 2026-08-17** (`stranding.rs`) — ★★ **and the mechanism was finished before this slice opened, twice over; what was missing was the DECISION.** **(a)** The **state** half has shipped since 2026-08-12: `editing::safe` is `Safe(e,μ) ⟺ ∀i ∈ inst(D): μ(sᵢ) ∈ V_{D′}`, judging the **migrated** state, reporting a witness set naming instance AND rule AND reason, treating an uncompilable candidate invariant as a violation against every instance rather than skipping it, and wired into `admit_edit` as its fourth conjunct. **(b)** The **history** mechanism shipped hours earlier as EVT-15 (`semantic::replay_under`). **(c)** The rest of the engine — taxonomy, authority, version DAG, inverses, 3-valued rollback, μ + EMC — was already ✅. ★ So this row is narrow by construction, exactly as IMM-7's reconcile taught. ★★ **What was ACTUALLY missing, and it is two things:** nothing called `replay_under` from the edit path (the history question was answerable and unasked), and `admit_edit` had exactly **two** outcomes — admitted, or `WouldStrand` — with `Migration` an *input the caller had already chosen* rather than a *reported outcome*. Of §III's three classifications the engine could express *compatible* and *rejected* and had **no surface at all for the choice**. ★★ **The crux, proven:** `D′` adds `balance >= 500`; an instance recorded 300 then 700 and sits at **1000**. `safe` passes it; `replay_under` refuses call `c1`, because the balance was 300 when that call committed. Same instance, same `D′`, two correct answers to two different questions — kept in **separate lists**, never summed, because a stranded state wants a migration and a stranded history wants a policy decision. ★★ **And the distinction recurs in the REMEDIES, which is the stronger statement: migration cannot fix a history.** μ transforms where an instance *is* and cannot reach back, so `Remedy::Migrate` carries the whole history-stranded list as residue even when it clears the states completely. **Grandfathering is withheld while the state is stranded** — a soundness argument, not a policy: scoping `D′` to new actions cannot help an instance already sitting somewhere `D′` forbids, since the next call refuses anyway; there, `Refuse` is the only option, which is exactly what a silent default would have hidden. ★ **No default, structurally:** `remedies()` is the only accessor, returns a `Vec`, has no `best()`/`recommended()` and no ordering that reads as a ranking, and `Remedy` implements no `Default` — the OPV-29 frontier discipline applied to editing. **Residuals:** the remedies are **offered, not applied** (threading a chosen one back through `admit_edit`, and giving the engine somewhere durable to remember that a history was grandfathered, is host state and the sequenced next step); **grandfathering has no enforcement arm** here (per-instance effective-definition pointers are host state); **nothing verifies a supplied history is the instance's own** (EVT-15's *call log not wired to `execute`*, seen from the consumer side); and ★★ the history check **cannot see `μ` or `F`** — EVT-15's limit carries unchanged, because boundary and firewall are not part of a `Definition`. |
| EDIT-12 | **Expand–Migrate–Contract** — the three-phase safe schema change (add the new shape, move the data, remove the old) | VIII | ✅ **DONE in Rust** (`migrate.rs`, 2026-08-12) — **μ finally has a representation**, so the classification is no longer binary and the discipline is no longer *refuse-if-unsafe*. `Migration::Apply(Mu)` makes `Safe(e,μ)` judge **μ(sᵢ)** rather than `sᵢ`, which is the whole difference between refusing an edit and carrying the instances into it. **`Emc::run` is one call** — there is no public way to run the contract alone, so the ordering *is* the safety property. `e₊` is **auto-safe with no scan** (EDIT-6 refined: only `AddInv`/`ModifyInv`/`RetypeDim` can strand), `D†` is a **real recorded version** with both shapes, and `Safe(e₋,id)` is **verified** rather than trusted. Backward/forward compatibility are computed at **plan** time, so *"this commits you"* is told, not discovered. **Knock-on: a μ that journals its pre-image collapses EDIT-7's `Partial` region to `Total`** |
| EDIT-13 | **`π : D → G` — the definition as an editable graph** | IX | ✅ **done 2026-08-17** (`lens.rs`) — ★★ **and it is a LENS, not a projection.** `π` alone is easy; the difficulty is that it **forgets**, so the inverse cannot be a function of the graph alone. The editor is the pair `⟨get, put⟩` (Foster/Greenwald/Moore/Pierce/Schmitt 2007), and ★★ **`put` takes the old `D` as well as `G′` precisely so it can restore what `get` discarded** — a naive *regenerate D from G′* would destroy every detail the graph never carried. That asymmetry is the whole row. ★★ **STEP-0 corrected what `π` forgets:** §IX names *Enzyme bodies* first, and in this core it forgets them **for free** — `Definition.operators` is an allow-list of **names**, and the struct's own docstring already says *'operator bodies are not'* data here (they live in the `Registry`). So a round-trip proof over Enzyme bodies would have been theatre. ★ What `π` forgets that is **real here** is the **invariant expression text** (the graph draws *that there is a rule called `floor` touching `finances`*, never `finances >= 500`) and the **fine type detail** (`Number { lo, hi }` draws as the label `number`) — and those are what the preservation proof actually uses. ★★ **Both laws are executed, not asserted:** **GetPut** in the stronger form the return type allows — an untouched graph yields the **empty edit list**, not an edit that happens to be a no-op — and **PutGet** *literally*, `get(put(G′,D)) == G′`. ★ That literalness came from a deliberate design choice: the detail travels **beside** the graph in `Supplied` rather than as an optional field on a node, because `π` would always leave such a field `None` while an editor's node carried `Some`, and PutGet would have had to be weakened to *equal after clearing the fields π never fills* — a weaker claim wearing the same name. Keeping `G` purely structural also makes it exactly what a surface draws. ★★★ **The preservation, proven:** remove an operator on the graph, put it back, and the invariant's expression text and the dimension's `lo`/`hi` bounds return **byte-identical**, recovered from the old `D` the graph never carried them in. ★ And where the detail is genuinely absent — a **new** node — `put` **refuses and names what is missing** rather than inventing a plausible default; the one addition needing nothing supplied is an operator, because there `π` forgot nothing, which is the asymmetry made concrete. ★★ **A graph edit is a TYPED edit:** `put` returns `Vec<Edit>`, never a rebuilt definition, so §II's *an untyped JSON replacement has no kind* is honoured — proven end to end by a graph-added invariant becoming a typed `AddInv`, reaching `admit_edit`, and being **refused with `WouldStrand`** naming the instance and the rule (EDIT-11's own check firing on a graph edit), while the same edit against a compliant instance is admitted. A modified expression becomes one `ModifyInv` rather than a drop-and-add, which matters because `DropInv` cannot strand and `AddInv` can. ★ **Reuse over reinvention:** the edges are **derived from the parsed predicate** via `goodhart::referenced_dimensions`, so an edge is a fact about what a rule reads rather than a substring match, and the graph cannot disagree with the Goodhart guard about it. **Residuals:** this is the **lens, not the editor** — the interactive Edit zone is the surface block's work (UI-in-Rust), and what it gets from here is a real graph and a safe way back; there are **no operator→dimension edges**, because that needs an `EffectSummary` and MON-1 already found no shipped operator declares one (uniformly absent, so drawing them would be fabrication); an unparseable invariant appears as an **unattached node** rather than being hidden; and `put` does not reorder, since `Edit` has no move or rename kind. |
| EDIT-14 | **⚠ The embryo's fence, and two holes in it** — `edit.operator_spec` fences `_EDITABLE_FIELDS` and explicitly immutabilises name/constraints/fn/protocol (the fence does a type system's work, honestly). But it **permits editing `pawa_cost` and `license_tier` at runtime, free, with no authority check** — *"changing an Enzyme's price currently costs less than using it. That is §VI backwards"* — and it mutates the **in-memory registry**, so the change is process-local and lost on restart | Note | ⬜ (Python-side; recorded as a real defect) |
| EDIT-15 | **⚠ `edit.state_patch`'s `remove` sets the path to `None` rather than deleting** — under a typed schema that is **a type violation deposited into state** for something downstream to trip over | Note | ⬜ (Python-side; Rust's typed state + organisational closure would refuse the shape change) |
| EDIT-16 | **⚠ The live example of why edits must be typed** — `NetworkZone.jsx:47`: when `owner_ids` holds the unsubstituted token `"{{owner_ids}}"`, `?.[0]` indexes the **string** and yields `"{"`, which is truthy so the `|| 'owner'` fallback never fires — **`"{"` is posted as the `user_id` that provisions child sustains**. A defect in the definition becomes wrong ownership on real children, nothing refused, nothing logged | Note | ⬜ **a real bug sitting in the tree today**; two independent checks would each have caught it (typed overlay, typed edit taxonomy) |

**Summary:** **the ENGINE half of M-EDIT is complete as of 2026-08-12** — EDIT-1 through EDIT-10 and EDIT-12 are all ✅ in Rust. The arc, in order: the migration predicate was already real; **EDIT-8** made an edit a meta-operator through the gate; **EDIT-7** closed N3 by giving definitions an append-only history with declared inverses and three-valued rollback; **EDIT-12** gave `μ` a representation, so the engine can now *carry instances into* an edit rather than only refusing it. **Two rows genuinely remain.** ★★ **EDIT-11 (replay under `D′`) is UNBLOCKED as of 2026-08-17** — its own diagnosis was checked and **confirmed** (the patch fold really is `D`-independent by construction, and the ingredient really is in the Enzyme log — though in the reference's `operators_log`, not the Rust `Event`), and **EVT-15 built the reducer it named** (`semantic.rs`): `replay_under` re-runs the logged calls through the real gate under a chosen `Definition` and reports, per call, whether it would still have been admissible. ★ What remains is this module's own and is a **decision rather than a mechanism** — migrate the instance, grandfather the history, or refuse the edit — and it should be reconciled with `check_definition_edit_safety()`, which already asks the same question of live **states** where this asks it of **histories**; and **EDIT-13** (`π : D → G`) is a surface projection, not engine.

**Update 2026-08-17 — EDIT-11 closed, and the sentence above turned out to be the whole specification of the work.** ★★ It really was a decision rather than a mechanism: `editing::safe` (states, 2026-08-12) and `semantic::replay_under` (histories, EVT-15) both existed, so `stranding.rs` wires them and builds the surface neither had. ★★ **The crux is now a running test rather than a note in two vectors**: `D′` adds `balance >= 500`, the instance recorded 300 then 700 and sits at 1000 — `safe` passes it, the history check refuses call `c1`. Two correct answers to two different questions, kept in separate lists because they send an editor to different repairs. ★★ **The distinction recurs in the remedies, which is the finding worth carrying: migration cannot fix a history.** μ moves where an instance *is*; it cannot make a past call admissible, so a `Migrate` always reports the history residue rather than reading as *fixed*. And **grandfathering is withheld while the state itself is stranded** — soundness, not policy: the next call refuses regardless, so offering it would be offering a fix that cannot work. ★ **No remedy is recommended**, structurally — no `best()`, no ordering, no `Default` — because which one is right is a policy question about a particular household, and a library that picked one would be making that decision invisibly. ★★ **The counterweight belongs to the reference and it is strong**: `check_definition_edit_safety()` is fail-closed, runs before persistence, invalidates the per-instance spec cache on success, and its `blocked_by` already carries a real **witness set** — the reference had that discipline before Rust did. And it **names this exact gap about itself**: *'Only refuse is built... the safety check can only say no, not offer a guided fix.'* **Residuals:** the remedies are offered, not applied; grandfathering has no enforcement arm (per-instance effective-definition pointers are host state); nothing verifies a supplied history is the instance's own; and the history check cannot see `μ` or `F`. **With this, only EDIT-13 (`π : D → G`, a surface projection) remains — the ENGINE half of M-EDIT is complete with nothing outstanding.**

★★★ **Update 2026-08-17 — EDIT-13 closed, and with it THE WHOLE EDITING MODULE.** ★★ It turned out to be a **lens rather than a projection**: `π` forgets, so the way back cannot be a function of the graph alone, and `put` takes the old `D` precisely to restore what `get` discarded. ★★ STEP-0 corrected the brief's own framing — §IX names *Enzyme bodies* first and this core forgets them **for free**, because `Definition.operators` is a list of names and the bodies were never `D`'s; what `π` really forgets here is **expression text** and **fine type detail**, and those are what the preservation proof uses. ★★ **GetPut** holds in the stronger empty-edit-list form and **PutGet** holds *literally*, which came from putting the supplied detail **beside** the graph rather than in an optional node field — an optional field would have forced the law to be weakened to *equal after clearing what π never fills*. ★ A new node with no supplied detail is **refused, not defaulted**; an operator addition needs nothing, which is the forgetting made concrete. ★★ **And the graph edit is a typed edit all the way to the gate:** a graph-added invariant becomes `AddInv`, reaches `admit_edit`, and is refused with `WouldStrand` naming the instance and the rule — EDIT-11's check, firing on a change made by dragging a node. **`M-EDIT IS COMPLETE — every row.`** EDIT-14/15/16 remain recorded here as Python-side defects, not Rust build items. EDIT-14/15/16 are Python-side defects recorded here, not Rust build items.

### Layer 6 — GOVERNANCE

#### M-CTL · Controller — govern / decide  ✅ read  ★ SPINE
*Article: `3B1B — Controller - Formal Execution.md`. The cybernetic feedback loop closed through a human; where the person decides. Supervisory control at Sheridan levels 5–6.*

**Status: NOT STARTED — the article's own note: "the current code ships neither the table nor the four functions." Execute/rollback/council exist; none of the governance decision-math.**

| # | Feature | § | Rust status |
|---|---|---|---|
| CTL-1 | **Feedback control system** `C=⟨Plant, Sensor, Governor, Actuator⟩`; loop `e=r−y, u=G(e), y'=P(y,u)` — the DESCRIBE→…→OBSERVE loop closed through a human | I | ⬜ not started |
| CTL-2 | **Lyapunov stability / `is_stable_intervention`** `V(s)=‖s_desired−s_actual‖²`, valid iff `V_after ≤ V_before` — the **formal definition of a good decision**; rollback = restore lower-V | II | ✅ **DONE in Rust** (`controller.rs`, 2026-08-12) — `is_stable_intervention`: **stable ⟺ `W(after) ≤ W(before)`** against the **viable region**, not an ad-hoc point (§II's `‖s_desired−s‖²` levelled up per the Additions). `should_rollback` is derived as `!stable`. **Sharpened the same day by SUS-10:** `kernel::is_stable_toward_kernel` is the strictly stronger condition — descent toward `V` says *closer to being alive today*, descent toward the **kernel** says *closer to being able to stay*. A vector shows a move that leaves `W` unchanged and **improves** the box margin while walking out of the kernel |
| CTL-3 | **OODA loop** state machine — OBSERVE (Monitor) → ORIENT (Council + Simulator + score) → **DECIDE (surface, human authorizes)** → ACT; **human enters at DECIDE**; faster OODA wins → minimal pre-oriented prompts | III | ✅ **built 2026-08-16** (`ooda.rs` · `Ooda`, `OodaPhase`, `δ` as typed methods) — **pure assembly: it computes almost nothing.** §III's separation is **structural, not observed**: `OodaObservation` has no constructor from plain data (only `from_monitor`, taking a real `Severity` + `MonitorReport`), and `Orientation` only `from_plan`, taking a real `Plan` and ranking by `J` — so the machine **cannot** invent an observation or a ranking. The one phase with logic of its own is `decide`, which calls `controller::route`. **The human seam is what the type cannot do:** `Surface` parks the machine, `act()` refuses while awaiting authorisation, the token's binding must match the surfaced act, and **nothing on `Ooda` mints a token** — that needs `approval.rs`'s chain. **Boyd encoded as a refusal:** `decide` refuses without an orientation, so a person never sees raw options. The three routes out stay distinct — Surface parks, **Withhold executes nothing and interrupts nobody**, Automate goes straight to ACT under `EffectClass::Unchecked` (the declared table IS the prior decision) with `human_asked: false` recorded so an action nobody was asked about is visible |
| CTL-4 | **Sheridan automation levels** — `AUTOMATION_LEVEL` table (ROUTINE→execute+notify [6]; FINANCIAL/CRITICAL/IOT_WRITE/ROLLBACK→execute-if-approved [5]); `route_action()` gates on it | IV | ✅ **DONE in Rust** (2026-08-12) — `SheridanLevel` 1–10 with `requires_approval()` at the **level-5 line**, and `AutomationTable` as a **declared spec field** (the article's own ROUTINE→6, FINANCIAL/CRITICAL/IOT_WRITE/ROLLBACK→5). **Fail-safe at the governance layer: an undeclared action type routes to `ExecuteIfApproved`** and is reported by `undeclared()` — forgetting a declaration must not be how something starts running unattended |
| CTL-5 | **Holarchic hierarchy + escalation** — recursive `Sustain⟨…,children⟩`; invariant (local constraints ∧ consistent with parent); **`holarchy_escalation` propagates a violation upward until it reaches a human who can act** | V | ✅ **built 2026-08-16** (`holarchy.rs` · `escalate`, `Levels`, `Capacity`, `validate_holarchy`) — **no escalation graph was introduced**: §V says *"the escalation path IS the holarchy"*, so the walk is over the `MonitorEngine`'s parent chain, validated at construction since MON-9 and **retained from this slice, because until now nothing walked it**. At each level the question is *can this level act?*; `Capacity::CannotAct` **requires a reason**, so a level cannot absorb a breach silently — the failure §V exists to prevent. **Reaching the root unresolved is an `EscalationOutcome`, not an `Err`**: the loudest thing the system can say must not be `?`-able into a log line. `Breach::from_ingested` returns `None` unless the reading really crossed, so an escalation cannot be manufactured. The observation handed to the acting level carries the **origin's** severity — a homestead decision about a soil sensor has to be about the sensor. `validate_holarchy` checks both clauses, with the `⊆` reading **disclosed as an interpretation** (§V never defines it). Composes MON-9 + CTL-3 + the region machinery |
| CTL-6 | **Cyber-physical / IoT bridge** `CPS=⟨cyber, physical, interface⟩`; `mqtt_bridge()` turns a device signal into a typed event → constraint check → escalate/record | VI | ⬜ **RELOCATED 2026-08-17 to a host-integration pass**, out of Phase 2. ADR-0001 keeps I/O out of the core — an MQTT bridge is a host concern, and the core-side half of it (*a device signal becomes a typed event*) is already what the S4/S5 ingest door does. So this is a **declared-port/host-boundary** slice rather than a mechanism, and it belongs with the other host work rather than with cognition. Not a deferral of the row; a relocation of it. |
| CTL-7 | **Governance SNR (Shannon)** — maximize `SNR_gov`; **`should_surface(e)=Sheridan≤5 ∧ urgency≥θ`**; **`compute_urgency = base × lyapunov × time_pressure`** (Lyapunov-derived, not heuristic) | VII | ✅ **DONE in Rust** (2026-08-12) — `route()` implements `φ(e)=1 ⟺ level ≤ 5 ∧ urgency ≥ θ_user`, with `compute_urgency = base × lyapunov × time_pressure` where **lyapunov is `W=d(s,V)`** (MON-10) — *not heuristic, it follows from the stability condition*. `θ_user` and `URGENCY_WEIGHTS` are declared. **★ The critical distinction:** `Routing` has a separate **`Withhold`** variant — an action needing approval but below θ does **not** proceed; folding *"not surfaced"* into *"automated"* would turn an attention filter into an auto-approver. A passed deadline is **refused, not clamped** (dividing by zero would invert the ranking) |
| CTL-8 | **Persistent-panel invariant** — `I(p)=H(state|absent)−H(state|present)`; unconditionally-high-info panels always shown (console, health, IoT, sessions); `PANEL_POLICY` ALWAYS vs WHEN_ACTIVE | VIII | ⬜ **MOVED 2026-08-17 to Phase 3**, on the same argument that moved MON-7: this is a **surface** row whose real consumer is the Curated UI's attention budget, so it belongs with the surface work rather than the cognition layer. The row's own text already said *(surface/UI)*; the phasing now matches it. |
| CTL-9 | **The full Controller loop** — observe → orient (rank by urgency) → filter by SNR → decide (surface, await, validate Lyapunov) → act (execute, record, holarchy-propagate) | IX | 🔨 **three of the five stages exist** — observe (M-MON), orient/rank by urgency (CTL-7), decide/surface-and-await (CTL-7 + the N1 token), and a conformance vector walks **Monitor → Controller → human → gate** end to end. **Still ⬜: the assembled loop itself** — the OODA state machine (CTL-3), holarchy propagation (CTL-5) and the act/record stage as one running object — **largely discharged 2026-08-16 by CTL-3**: `ooda.rs` IS the assembled loop object; what remains of this row is the host's `WHILE system.running` driver and the surfaced-queue drain, both deliberately left outside core (no clock, no scheduler, and no hidden backlog a person cannot see) |
| CTL-10 | **Control is metered** — worth = Lyapunov gain net of pawa; cheaper equal-gain intervention wins | Add. | ⏸️ economy-deferred |
| CTL-11 | **Frequency-domain stability (damping)** — Lyapunov descent alone permits an oscillating over-corrector; keep the loop damped (poles in the unit circle) so corrections settle, not ring | Add. | ✅ **done 2026-08-17** (`damping.rs`) — **and it closes Phase 2.** ★★ **The gap is not hypothetical, and the proof runs CTL-2's own checker.** Under `e_{k+1} = (1−g)·e_k`: at `1 < g < 2` the pole is inside the unit circle but negative, so `W` strictly decreases every step **while the state flips side every step** — it converges *and* rings; at `g = 2` the pole is exactly `−1`, so `W_{k+1} = W_k` and **Lyapunov's `≤` holds forever, with equality, while nothing settles**. A conformance case runs the **real `is_stable_intervention`** over both and asserts `stable` at every step. *Lyapunov says the gap closes; damping says it closes without oscillation.* ★★ **Honest fit, on MON-1's precedent.** *Poles in the unit circle* is a **linear** characterization needing a fixed gain. **Where a gain is declared** the pole is exact and is computed — `Damped` / `Deadbeat` / `Underdamped` / `Undamped` (with `sustained` marking the pole exactly on the circle) — because declining a real model would be as dishonest as fabricating an absent one. **Where there is none, nothing is fitted:** there is deliberately **no `fit_gain(trajectory)`**, since taking poles of a fitted model is precisely the fabricated-`A` trap MON-1 declined the observability matrix to avoid. ★ **The native form for the no-gain case** is ringing read from the trajectory: alternating-sign `ΔW` (`read_trajectory`) and repeated boundary crossings (`crossings`), with fewer than three points reading **`Indeterminate`** rather than damped — two points cannot show a reversal. And **overshoot** is the one-step form: 50 → 230 on a [100,200] band drops `W` from 50 to 30, so CTL-2 passes, and the state has crossed to the far side needing a correction back. ★★ **A finding from testing rather than design, and the reason both native readings ship:** `W` is a **magnitude**, so a loop flipping side every step while converging has a *monotonically falling* `W` — `read_trajectory` calls it `Damped` and is not wrong to. The **side** series is what carries the ringing there. Shipping only the magnitude reading would have made the underdamped case invisible **in exactly the way Lyapunov already makes it invisible**. ★ **Complement, not replacement** — CTL-2, `region.rs`'s `W`, and the EWMA series are reused, none rebuilt. **Slot:** the Additions' *Lyapunov gain net of pawa* is economy-adjacent and parked. |
| CTL-12 | **Potential + action** — `W(s)=d(s,V)` a potential well; viable path minimizes accumulated cost; Lyapunov descent = greedy one-step, **Tenet backward induction = the full optimal-control solution** | Add. | ✅ **closed 2026-08-16** — the potential well ships (`region.rs`) and is the Controller's Lyapunov function (CTL-2); **the accumulated-cost reading is now built too** (`tenet.rs::rewards_from_region`): with `R(s,a)=W(s)−E[W(s')]` the `H=1` sweep's `argmax J` **is** CTL-2's greedy `argmin W`, so the two are **one** optimal-control core. A vector computes the greedy choice independently and checks the sweep agrees — falsifiable, not asserted (= M-TEN TEN-12) |

**Summary:** **the Controller is now decision-math AND the loop that runs it** (2026-08-12, then 2026-08-16). The math came first: `is_stable_intervention` (CTL-2) defines a good decision against the **viable region** rather than an ad-hoc point; `AutomationTable` (CTL-4) is the declared *"how much does the human decide"* dial with a fail-safe default; `route()` (CTL-7) is the SNR filter with a Lyapunov-derived urgency; and **CTL-12's join is closed** — `rewards_from_region` makes `is_stable_intervention` and `backward_induct` **one** optimal-control core, checked by a vector. **CTL-3, the OODA machine, landed 2026-08-16 and is pure assembly** — it computes almost nothing, and that is the point: §III says Monitor owns OBSERVE, the Simulator owns ORIENT, and *"the Controller is specifically the DECIDE surface"*, so `OodaObservation` and `Orientation` are **constructible only from real Monitor and Tenet artifacts** and the machine cannot fabricate either. **The human seam is a property of what the type cannot do**: it parks at DECIDE, refuses to act while awaiting authorisation, requires a token bound to the surfaced act, and **has no method that mints one**. Boyd is encoded as a refusal — `decide` without an orientation is an error, so nobody is ever shown unscored options. A conformance vector walks the whole cycle end to end: **a signal fires at OBSERVE → Tenet's sweep ranks at ORIENT → the Controller surfaces and a human authorises at DECIDE → the gate commits at ACT → back to OBSERVE**, with the commit asserted rather than assumed. **One widening is disclosed rather than buried:** an action above the approval line executes under `EffectClass::Unchecked`, because the declared `AutomationTable` is the prior human decision — and the trace records `human_asked: false` so it is never invisible. **CTL-5 landed 2026-08-16 and completes the escalation half**: a violation at a nested sustain climbs the `MonitorEngine`'s parent chain — *"the escalation path IS the holarchy"*, so no escalation graph was introduced — asking each level whether it can act, until it reaches one that can (ultimately the level with a human at it). A level that declines **must say why**, and reaching the root unresolved is an outcome rather than an error, because it is the loudest thing the system can say. **Update 2026-08-17 — CTL-11 shipped and Phase 2 closes on it** (`damping.rs`). ★★ The row's substance is that **CTL-2 has a blind spot the article names**: under `e_{k+1} = (1−g)·e_k`, a gain in `(1,2)` makes `W` fall every step **while the state flips side every step**, and a gain of exactly 2 makes `W` *never change* — so `W(after) ≤ W(before)` holds for ever, with equality, and nothing settles. A conformance case runs **`is_stable_intervention` itself** over both trajectories and asserts `stable` at every step. Lyapunov is not wrong; it is answering a different question. ★★ **Honest fit on MON-1's precedent:** where a gain is **declared** the pole is exact and is computed; where there is none, **nothing is fitted** — there is deliberately no `fit_gain(trajectory)`, because taking poles of a fitted model is the fabricated-`A` trap. The native form reads ringing from the trajectory instead, with `Indeterminate` below three points. ★★ **And a finding from testing:** `W` is a magnitude, so a converging-but-ringing loop has a *monotonically falling* `W` — the magnitude reading calls it `Damped` and is not wrong to; the **side** series is what carries the ringing. Both native readings ship because each sees what the other cannot, and shipping only the magnitude one would have reproduced Lyapunov's own blind spot. ★ **A framing correction the greps forced, recorded rather than smoothed:** the obvious counterweight would be *the time-domain half is at parity*, and it is not — `lyapunov` is grep-0 on the reference side and its four `stable` hits are a stable sort, a stable fingerprint and two testables. CTL-2 is a **Rust** row; the real counterweight is **intra-Rust**. **Row phasing settled 2026-08-17:** **CTL-6 relocated to a host-integration pass** (ADR-0001 keeps I/O out of the core, and the core-side half is already the ingest door) and **CTL-8 moved to Phase 3** as a surface row, on the same argument that moved MON-7. **Still ⬜ in this module:** those two, in their new homes; CTL-9's assembled loop object is largely **discharged by CTL-3**. **The spine reads: Monitor observes ✅, Tenet models ✅, Controller decides ✅** — wired into one cycle by CTL-3, fed by MON-9's engine, and as of CTL-5 that cycle reaches **every level of the holarchy**: a breach at a nano-sustain finds its way to whoever can act on it.

### Economy (deferred)

#### M-PAWA · Pawa & Juul economy  ✅ read  ⏸️
*Articles: `3B1B — Pawa - Formal Execution.md` (the meter, then the gas), `3B1B — Mycelium - Formal Execution.md` (the network as a Sustain), `3B1B — Arena - Formal Execution.md` (the market as a Sustain). **Held with the economy layer** per ADR-0001 D5, pending legal counsel — but captured in full, because the meter half is already live and the boundary between "buildable now" and "a later human-authorized act" is drawn in the article itself.*

**Status: ⏸️ deferred. The pawa METER is built and wired (Python, Slice 15) — the article's own Correction says so. Everything downstream of the meter — the gas, the ledger's use, the treasury, the market's settlement — is unbuilt.**

##### Pawa — the meter and the coin

| # | Feature | § | Rust status |
|---|---|---|---|
| PAWA-1 | **`pawa(o,θ,s) = κ_c·compute + κ_s·storage`** — the meter of one Enzyme run; **metering, not estimating**: measure on real runs and record `⟨operator, ts, compute, storage, sustain, principal⟩` | 1 | ⏸️ ✅ app-layer (Slice 15; κ declared and **explicitly uncalibrated**; wall-clock deliberately excluded so the reading is reproducible) |
| PAWA-2 | **Juul as the unit of account** — `balance ← balance − pawa`, admitted only if `balance ≥ pawa`; **a utility token by design**, defined by *use*, deliberately clear of investment-contract hallmarks (the gas-token posture, written into the design) | 2 | ⏸️ ⬜ not started |
| PAWA-3 | **The out-of-pawa gate clause** — `admit = guard ∧ constraints ∧ token_ok ∧ balance ≥ pawa` — one more conjunct on the existing seam | 2 | ⏸️ ⬜ not started (the seam exists in Rust; the clause does not) |
| PAWA-4 | **Pawa as a fitness term** `Pawa(π,s) = Σ pawa(o)` accumulated **in the sandbox, live state untouched**; `score(π) = Σ w·u(sim(π)) − λ·Pawa(π,s)` — efficiency ranked **before** commit | 3 | ⏸️ 🔨 the simulator accumulates per-branch pawa (Slice 15 step 2 ✅ app-layer); the **fitness term in a scorer ⬜** (= M-TEN) |
| PAWA-5 | **Contribution as a programmable flow** — `licence ∈ {Free, Royalty(rate, payee)}`; a run debits `pawa(o)` from the user **and** transfers `r` user→contributor; an optional declared **treasury slice** on royalty runs bridges the private economy and the public commons | 4 | ⏸️ ⬜ not started |
| PAWA-6 | **The treasury is itself a Sustain** `Σ_T` — `allocate`/`grant`/`disburse` Enzymes, a **reserve floor and category caps in `V_T`**, the protocol fee fraction φ_p, a periodic budget; **every disbursement rides the same enforcing gate and approval token** | 5 | ⏸️ ⬜ not started (a clean instance of the recursion — the treasury is admitted like any Enzyme and simulated like any strategy) |
| PAWA-7 | **Genesis** `g : principals → Juul` — the starting distribution, declared in the open once and auditable forever; the ledger begins with `g` and every later balance is a **fold of transfers over it** | 6.1 | ⏸️ ⬜ not started |
| PAWA-8 | **Monetary policy — issuance denominated in work** — new Juul enters by **rewarding hosts who serve pawa**; the schedule (fixed / decaying / target-rate) is a **declared, revisable parameter** changed only through governance | 6.2 | ⏸️ ⬜ not started |
| PAWA-9 | **Many hosts, one meter** — the decentralized network needs no new machinery: **Multiparty** carries membership and consensus, **Events-&-Time merge semantics** carry cross-host state. *"A global meter over a coordinated membership with well-defined merge is the whole of what a world computer needs at this layer."* | 6.3 | ⏸️ 🔨 **both halves are now ✅ in Rust as of 2026-08-16, and the row's own claim is what closed them.** Merge semantics: R2 #17/#18 (event-time, dedupe, LWW convergence) plus the **wider CRDT family** (MUL-10 — G-counter/PN-counter/OR-set/RGA, strong eventual consistency proven over every permutation). Membership + consensus: **M-MUL is complete** (Population, quorum/Paxos, vector clocks, composition). So *"a global meter over a coordinated membership with well-defined merge"* now has every named part built — what remains is **host wiring, not machinery**: the reference engine is still a single-process singleton over one SQLite file, which is the thing a second host actually changes |
| PAWA-10 | **Incentive alignment + security from the same meter** — out-of-pawa gating stops runaway work at the gate; **pricing-via-processing makes Sybil strategies uneconomic** (Dwork–Naor); the efficiency term rewards lean strategies | 6.4 | ⏸️ ⬜ not started |
| PAWA-11 | **Governance — the parameters are declared Sustains** — κ_c, κ_s, φ_p, φ_r, fee rates, the issuance schedule, the treasury budget: **a parameter change is an Enzyme**, admitted through `admit` with its approval token, recorded, auditable by replay | 6.5 | ⏸️ ⬜ not started |
| PAWA-12 | **The migration path — single host outward** — (1) single host, (2) federated, (3) open. *"The meter, the unit of account, the efficiency term and the royalty flow are defined once, on a single host, and extend outward without redefinition."* | 6.6 | ⏸️ ⬜ (the path is the plan, and step 1's meter is done) |
| PAWA-13 | **★ The honest line, stated once** — the **software** (meter, ledger, treasury accounting, licence/royalty settlement, out-of-pawa gating, parameter governance) is buildable now on a single host, before any coin is public. **Issuing a real, public, transferable token is a distinct, later, human-authorized act with legal and financial weight** | 6.7 | ⏸️ **this line is exactly ADR-0001 D5's boundary**, written by the article itself |

##### Mycelium — the network as a Sustain

| # | Feature | § | Rust status |
|---|---|---|---|
| MYC-1 | **The network is a Sustain** `Σ_net` — not a new kind of thing; addressing and discovery over it | I·II | ⏸️ ⬜ not started (⚠ Python's `NETWORK_TREASURY_ID = "sustena.network_treasury"` carries Bonnie's own TODO to rename to `sustena.mycelium.treasury`) |
| MYC-2 | **Propagation — gossip + publish/subscribe** | III | ⏸️ ⬜ (✅ app-layer *in-process* only: `events.py` enforces the dot protocol and dispatches hierarchical wildcard subscriptions — real topic pub/sub, one process) |
| MYC-3 | **Membership and health** | IV | ⏸️ ⬜ not started |
| MYC-4 | **What routes, and the law that separates it** — the Physarum router applied to money, tasks, compute, storage | V·VIII | ⏸️ 🔨 **the mechanism is ✅ built** (MUL-13 shipped 2026-08-16 — `router.rs`, a Kirchhoff flow solve plus strictly-local reinforcement, provably finding the short route with no planner). What remains is **application, not machinery**: declaring money, tasks, compute and storage as the flows it routes |
| MYC-5 | **Treasury and settlement — the ratified two-revenue-type royalty schedule (2026-08-04).** A **pawa charge (usage)** splits `70 / 15 / 5 / 5 / 5` and a **licence sale (access)** splits `80 / 10 / 3 / 2 / 5` across contributor / treasury / validator / proposer / referrer. **Integer-floor arithmetic with the remainder assigned to the validator share**, so the split is **exactly conserving by construction** — a rounding leak is a conservation-invariant violation (of `V_arena`'s fourth conjunct), not a cosmetic bug. The schedule adds the **proposer** share and **separates the two revenue types**, which the earlier single four-way split could not express. Read the 70% as what it is economically: **the price the commons pays for variation** — selection (§VI) can only act on variation that exists | Myc §VI · Are §IV · Pawa *Add.* | ⏸️ 🔨 **the shipped `PawaLedger.charge` still runs the earlier four-way `70/20/5/5`** and has **zero callers** — the articles now all carry the ratified schedule; migrating the code is an explicit **Track-3 migration**, and the conservation constraint (shares sum to 100, remainder deterministic) is what makes it safe (= backlog E1/E2) |
| MYC-6 | **★ Grants are mints, and the distinction is not pedantic** — a **transfer** (Σ Δ = 0) is bounded by the treasury and self-limiting; a **mint** (Σ Δ = +100) is unbounded, inflates the unit, and **makes Sybil registration directly profitable**. Both are legitimate designs; **only one of them is legitimate *undeclared*** | VI | ⏸️ ⬜ (the Onboarding Grant's kind must be declared before it ships) |
| MYC-7 | **A commons with a subsidy** — the fee funds the commons that the fee is taken on | VII | ⏸️ ⬜ not started |
| MYC-8 | **Two surfaces, one network** — Mycelium (orchestrator, laptop, full complexity) and Orchie (curated, phone) are **two projections of the same object, differing only in budget** | IX | 🔨 ✅ app-layer as UI surfaces; the *network* reading ⬜ |
| MYC-9 | **Foundation and Institute as network-scale Symbionts** — their niche happens to be the whole graph; **nothing else about them is different**. The no-special-power rule is a **structural claim** (complete mediation), not a governance promise — *"a mother tree with override privileges would not be a forest; it would be a plantation"* | X·XI | ⏸️ ⬜ (the structural claim is already true in Rust: one path, no bypass) |

##### Arena — the market as a Sustain

| # | Feature | § | Rust status |
|---|---|---|---|
| ARE-1 | **The Arena is a Sustain**; **artefact taxonomy** `kind ∈ {operative, operator, spore, widget}` | I·II | ⏸️ ⬜ Rust (✅ app-layer: `arena_packages` with the four kinds, spec_json, author, version, tags) |
| ARE-2 | **★ Publish is an Enzyme, not an upload** — today it is a **route**, i.e. **a second write path into `S_arena`** — the exact failure complete mediation forbids | III | ⏸️ ⬜ **not started** (the Capstone records this as a *seventh gate duty*) |
| ARE-3 | **★ Two prices are conflated in one column, and they are different objects** — `price(a) = p_access(a)` (**once, to acquire**) ⊕ `p_use(a)` (**per invocation, metered**). The shipped `pawa_cost` is used as **both**: the surfaces say "Pawa to install" while `PawaLedger.charge(pawa_cost, contributor_id)` is a **per-component-call royalty**. Different economics — an access price is a one-time signal and a one-time transfer; a usage price is a metered stream, and it is the one that makes an artefact's cost proportional to the value it actually delivers over time. **They should be separate fields**, and this is why MYC-5's schedule is revenue-*typed* | IV | ⏸️ ⬜ **not started** — a real, concrete schema gap, not just "pricing pending" |
| ARE-3b | **The free tier is the dangerous half** — Spence (1973): a signal separates types only if it is **differentially costly** (cheaper for the high type than the low). A signal that costs everyone the same **separates nothing**, because the low type sends it too | IV | ⏸️ ⬜ not started (ties to ARE-4's adverse selection) |
| ARE-4 | **Trust, reputation and adverse selection** — the **unravelling** (Akerlof): without a quality signal the market degrades to lemons; four countermeasures and how each is defeated; **what a trust score has to be** to survive | V | ⏸️ ⬜ **not started** — `trust_score` is a **dead constant** (Float default 0.0), used only to `ORDER BY` (= backlog E5) |
| ARE-5 | **Delisting is not deletion, and that is correct** | V.4 | ⏸️ ⬜ not started |
| ARE-6 | **Selection dynamics** — artefacts compete and are differentially retained (the Arena is §III's meme selection at market scale) | VI | ⏸️ ⬜ not started (`download_count` is likewise a dead constant) |
| ARE-7 | **No Free Lunch ⇒ per-niche ranking** — a single global ranking is provably wrong; libraries are per-niche by necessity | VII | ⏸️ ⬜ not started (ranking is a single global `ORDER BY trust_score DESC`) |
| ARE-8 | **Market design — biological markets; what matching theory does and does not give** | VIII | ⏸️ ⬜ not started |
| ARE-9 | **Two separated lifecycles, and the schema already anticipated them** — `product_total` / `pawa_total` as separate columns and `product_status` / `package_status` as separate sequences: `PLACED|PROCESSING|DELIVERING|DELIVERED` (rival, physical) vs `PLACED|INSTALLING|SANDBOXED|LIVE` (non-rival, digital) | IX | ⏸️ 🔨 **the schema is right**; the lifecycles do not run — **orders do not settle** (= backlog E6) |
| ARE-10 | **Licence keys at order time** — `LIC-` + eight uppercase alphanumerics, stored and returned per non-free package item | IX | ⏸️ ⬜ Rust (✅ app-layer) |
| ARE-11 | **★ Germination is ⊕, and that is the whole safety story.** A purchased spore is not *installed*, it is **composed**: `germinate(g, Σ_p) = Σ_p ⊕ Σ_g`, where `Σ_g = instantiate(Genome(g))` — subject to three conditions, **none of which is new machinery**: (1) **viability composes** — `V_g` holds within `V_p` (the roll-up condition, `compute_rollup`); (2) **membership stays acyclic** (Mycelium §VII; shipped limitation: `link_child` refuses a second parent, so composition is a **tree** and a germinated spore has **at most one container**); (3) **the parent's gate admits it** — the same `admit()` as every other write, `admissible = ⋀_{H∈path} admit_H`. **"There is no special install path, and there must not be one"** — complete mediation (Saltzer & Schroeder) at the market boundary. **`package_status` already encodes exactly the right lifecycle**: **SANDBOXED** is Operative §X's fork-and-replay *under the same gate*, and the transition to **LIVE** is **where the approval token belongs** (= N1/OPV-30). The three classes germinate differently, and the difference is the security model: **spore** → a new Σ by ⊕; **Enzyme** → a new element of **T**, arriving with **its own guard, written by its author** — the counterfeit surface, and §X's subject; **Symbiont** → a new element of Ω which by Operative §I Props 1–2 **cannot enlarge `R(s)` at all, only re-rank it**. The article's load-bearing sentence: ***"A Symbiont from an untrusted author is bounded by your T; an Enzyme from an untrusted author is an addition to it."*** This is why §V.4's *"the market cannot recall what it sold"* is survivable rather than alarming — **the buyer was never protected by the catalogue's judgement; they were protected by the gate on their own boundary, which the market has no path through** | IX.4 | ⏸️ 🔨 **the ⊕ half is ✅ and structural in Rust** (admit-along-the-path; a bypass is not expressible) — and the Note confirms `instantiate` / `link_child` / `provision_declared_children` / `compute_rollup` all exist, so *"§IX.4's ⊕ is real on the Sustain side."* **The lifecycle ⬜:** per the Note's item 12, `package_status` is set to `PLACED` and **never advances** — `INSTALLING`/`SANDBOXED`/`LIVE` are unreachable and there is **no install or germinate endpoint**, so *"'no special install path' is currently true in the strongest and least useful sense: there is no install path at all."* **A correctness property, not merely economic** (ties CAP-7, OPV-17) |
| ARE-12 | **Market security exposure (thin)** — the *mechanisms* belong to **M-IMM**; only the **market's exposure** is here. **(1) Counterfeit & name confusion** — content-addressing defeats *substitution* (verify the bytes) but **not** *confusion* (the wrong name, verified perfectly); typosquatting and author impersonation dominate in practice (Ohm et al. 2020). Mitigations: **namespace ownership, signature, human-readable provenance at the decision point**. **(2) Sybil authorship** (Douceur 2002) — manufacture authors, manufacture raters, manufacture adoptions; **bounded only by making identity costly** (Mycelium §VII owns this as a network invariant), and **it fails silently if identity is free**. **(3) Trust gaming** — rating rings, whitewashing, review-bombing; countered by **purchase-gated ratings, stake weighting, decay, graduated sanctions**. **(4) The malicious update** — trust earned on v1.0, payload shipped in v1.1: **the strongest argument for per-version trust and `(name, version)` write-once**. **(5) The irreducible residue** (Thompson 1984) — *a signature chains trust to an author, not to the author's toolchain*; **no provenance system closes this, it can only be moved**. **The standing structural defence:** nothing purchased can act except as an **admitted transition inside the buyer's own viable region** — the market makes good artefacts **findable**, the gate makes bad ones **inert**; merging the two deletes the property the rest depends on | X | ⏸️ ⬜ — **per-version trust** and **`(name,version)` write-once** are the two concrete unbuilt points (and `trust_score` is a dead constant today, ARE-4). The standing defence itself is **✅ structural in Rust** (complete mediation, one write path) |

##### Additions — ratified 2026-08-05 (Pawa *Additions*; Mycelium §II·§VI; Arena §IV)

*Every row here is **article-derived**, with its § cited — **no exceptions**. An earlier pass folded these from the retired `SUSTENA_WBD.md` and labelled them "not article-derived"; that was wrong, and the label is corrected. **ADD-2b was the last holdout and is no longer one:** it was recorded as "in no article body," which was itself an **under-read of Mycelium §XI** — the same error class as the Pawa miss. §X and §XI already carried the Foundation-with-zero-authority rule, `CouncilSession`/`StakeholderSession`, per-edge privileges, skins, quorum, the FLP/Paxos/BFT bounds, and proposal→vote→veto. Only the RFC-workflow / earned-weight / `sustena_core`-meta sliver was genuinely new, and it has since been written into **§XI ("RFC change control")**, so the whole row now cites canon.*

| # | Feature | § | Rust status |
|---|---|---|---|
| ADD-1 | **Juul is a KES-pegged stablecoin** — §2's gas-token posture fixes what Juul is *for*; the peg fixes its **stability**. Fiat-collateralised against the **Kenyan Shilling**, reserves in permitted instruments with a **segregated bank floor**, redeemable on M-Pesa / bank rails, so a household's costs — which are in KES — are shielded from FX and crypto volatility. Kenya now regulates the pathway (**VASP Regulations 2026**, including a stablecoin regime), and the peg is **migratable behind the settlement switch** to a CBK digital shilling or a basket at scale. *Mycelium §VI states the same requirement from the network side: Juul is a **stable** unit, not a volatile asset* | Pawa *Add.* · §2 · §6.7 · Myc §VI | ⏸️ ⬜ not started — and **§6.7's "distinct, later, human-authorized act" governs it**: this is a legal commitment as much as an engineering one |
| ADD-2 | **Governance — parameters.** Every open parameter (κ_c, κ_s, φ_p, φ_r, fee rates, the issuance schedule, the treasury budget) is a **declared, revisable Sustain**; a parameter change is an **Enzyme admitted through `admit` with its approval token**, recorded in the ledger, auditable by replay — *"the on-chain-governance analog, expressed in Sustena's own construct"* (= **PAWA-11**) | Pawa §6.5 | ⏸️ ⬜ not started |
| ADD-2b | **Governance — RFC change control.** *"Changing the network's own rules is itself a Transition, so it is admitted like any other."* A change to `sustena_core` — a shared parameter, an Enzyme, a Sustain definition — is raised as an **RFC**, discussed, put to a **`CouncilSession`** (plus a **`StakeholderSession`** wherever interests genuinely conflict), and **on quorum finalized and anchored**: the proposal is an Enzyme, `admit` **with its approval token** is the gate, the ledger records it, auditable by replay. **Weight is earned, not bought** — voting weight rises with **merged contributions and Arena reputation** (the same earned trust the royalty economy already meters) and may carry a **bounded, declared** pawa/Juul stake, **capped so that no holding buys control**: reputation primary, stake a minor revisable axis, **the person-first default holds**. **Graduated rights — proposer / reviewer / maintainer — ARE skins**: named bundles of **edge** privileges, least-privilege by default (so per **CAP-17** the weight attaches to the **membership edge**, not the member). And **the convening network-scale Symbiont has, per §I, the authority of any Symbiont: none — it runs the process, it does not decide it** | Myc §XI · §X · Pawa §6.5 | ⏸️ ⬜ not started — needs `StakeholderSession` (⬜) on top of the existing `CouncilSession`, plus the approval token (**N1/OPV-30**) it shares a gate with |
| ADD-3 | **Discovery — a hybrid behind ONE resolution interface**, and the article is sharper than "registry then DHT": the correct design is *"**not** registry-or-DHT but a hybrid behind one resolution interface"* — `resolve(name) → holder`, so the backend changes **without touching a single caller**. A registry/index serves the fast common case (**10⁰–10³**: O(1) lookup, O(n) state at the registry); a **Kademlia DHT plus gossip** carries scale and resilience as n crosses ~10³ (**10³–10⁶**: O(log n) hops, O(log n) state — Kademlia's XOR metric makes the routing table self-maintaining from ordinary traffic); **Babychain anchoring (§VI) sits above both**. Build the interface and the registry now with the overlay **mocked behind it**, so the DHT is a drop-in. **The threshold is a design trigger, not a guess:** *a registry stops being correct the moment its single operator's availability becomes a network-wide invariant — when the registry's failure would violate `V_net`* | Myc §II | ⏸️ ⬜ not started (this **supersedes MYC-1**'s generic "addressing and discovery") |
| ADD-4 | **Settlement rides the layered stack, behind a swappable adapter** — Sustena executes locally; **Babychains** are the L2 (aggregation, rollup, zk-proofs); **Ethereum is the finalizer**, receiving only state-root hashes. *The ⊕ hierarchy **is** the rollup batching structure* — a homestead folds its habitats, a Babychain folds its Sustains, and only key transactions and periodic roots reach L1 (§V's value/information law read at the settlement layer). Two disciplines make it shippable: **(i)** the whole L1/L2 stack sits behind a settlement interface with a **mock** — pawa/Juul run as live utility tokens today behind an adapter to Ethereum standards (ERC-20 / rollup / finalizer), the chain switched on later **with no rewrite**, the same swap-behind-a-stable-interface pattern as ADD-3; **(ii)** **finality is layered, so latency is too** — local transactions final in milliseconds, global finality at the anchoring cadence, and most transaction classes never need L1 at all | Pawa *Add.* · §6 · Myc §VI | ⏸️ ⬜ not started (= the build strategy for **CAP-16**) |
| ADD-5 | **Correction to the Sustena Note** — *"none of pawa/Juul exists yet"* **predates Slice 15**: the **pawa meter is built and wired into `execute_operator`**. The rest of the additive plan stands | Pawa *Add.* | ✅ app-layer (= **PAWA-1**; status already correct) |

**Summary:** the economy layer is **⏸️ deferred wholesale**, and the article draws the deferral line itself (PAWA-13): the meter, ledger, treasury accounting and gating are buildable now; **issuing a real transferable token is a separate, later, human-authorized act**. What already exists is the **meter** (Slice 15, wired into `execute_operator`, coefficients honestly uncalibrated) and a real append-only `PawaLedger` — whose `charge()` runs the **superseded four-way split** and **has zero callers**. **A main-body sweep on 2026-08-12** — a section-by-section cross-check of all 18 articles against this WBD — found **17 modules faithful and two genuine gaps, both in the Arena**: `ARE-11` (§IX.4, germination is ⊕) and `ARE-12` (§X, market security exposure), added here. Four items are worth carrying forward even while deferred, because they are **correctness rather than economics**: **ARE-2** (publish is a second write path), **ARE-11** (germination has no special install path — and the approval token belongs on the SANDBOXED→LIVE transition), **MYC-6** (a grant that mints rather than transfers makes Sybil registration profitable, and must be *declared*), and **MYC-5**'s conservation property (integer-floor remainder, assigned to the validator share, is what makes the split exactly conserving). **As of 2026-08-12 that property is finally checkable**: `CON-7`'s conservation law is the mechanism a royalty split's exact-conservation would be declared against. **ADD-1–5** carry the 2026-08-05 Additions — the KES peg, parameter governance, **RFC change control**, the discovery hybrid and the settlement adapter — **all article-derived with their § cited, none excepted**. Two prior mislabels are corrected here: the peg/schedule/settlement stack (folded from a retired tracker, actually Pawa's own *Additions*) and **ADD-2b**, recorded as "in no article body" when it was an under-read of **Mycelium §X·§XI** — now cited, and completed in canon by §XI's *"RFC change control"*.

### Capstone / cross-cutting

#### M-CAP · The whole machine  ✅ read
*Article: `3B1B — Capstone - Formal Execution.md` (GAIA). The assembly — one object, seventeen times. Not a module to build: the argument that the others compose, plus the seams where leverage concentrates.*

**Status: this section is the assembly view. Its build content is the SEAMS — and the gate seam is where Rust has already banked the largest structural wins.**

| # | Feature | § | Rust status |
|---|---|---|---|
| CAP-1 | **The primitive restated; the module map — one object, seventeen times** — every article is `Σ = ⟨B,S,V,T,⊕⟩` at a different scale | I·II | ✅ the premise the Rust core is built on |
| CAP-2 | **The recursion theorem + the four lifts** — the construction, the theorem, and the fixpoint "stated without mysticism", with **two honest caveats** on it | III | 🔨 ⊕/ρ ✅ in Rust; the theorem is the design's justification, not code |
| CAP-3 | **The six-layer frame** — Substrate (Events&Time) → Definition (Sustain, Enzyme, Constraint, Embroidery) → Boundary (Ingest) → Cognition (Monitor, Tenet, Symbiont, Multiparty) → Surface (Curated UI, Editing) → Governance (Controller) | IV | ✅ **this WBD's own layering** |
| CAP-4 | **The dependency order** `Events&Time → Embroidery → Sustain → Enzyme → Constraint → Ingest → Monitor → (Curated UI ∥ Tenet → Symbiont → Multiparty)`; **Editing after the Enzyme path**; **the Controller is NOT a late layer — it threads through from the first gate check onward** | IV | ✅ **the Rust build order followed it**: fold → state → rules → operators+gate → council |
| CAP-5 | **Mycelium, Arena and Immune are not a seventh layer** — Mycelium and Arena are Σ at larger scales; **Immune is a property of the Boundary and Definition layers**, not a member of any | IV | ✅ (recorded — this is why M-IMM's items land inside the gate) |
| CAP-6 | **The trace — the composition proof** — one M-Pesa message end to end, **every engine appearing exactly once**, using only machinery the specs already contain. *"The trace is the argument: if any step required machinery no spec provides, the series would have a hole"* | V | ✅ the trace runs today in Python (capture → τ → dedupe → gate → fold → compose) |
| CAP-7 | **★★ SEAM 1 — the gate has SIX duties** — (1) admit operators to live state, (2) enforce invariants and post-conditions, (3) **check the approval token**, (4) **replay sandboxed simulations under the same rules**, (5) **gate edit authority**, (6) **mediate the boundary firewall F**. *"The single highest-leverage object in the design. Build it once, and six obligations across five sections are discharged"* | VI.1 | 🔨 **Five of the six §VI.1 duties are banked in Rust** — 1 ✅ admit, 2 ✅ invariants + post-conditions **and now `D(s,s')`** (`transition.rs`), 3 ✅ the bound single-use approval token (`approval.rs`), 4 ✅ sandbox-under-the-same-gate (structural: one execution path), 5 ✅ edit authority (`editing.rs`). **Only 6, the boundary firewall `F`, remains** — blocked on `μ`, the membership predicate (= CON-1's third family, SUS-*). The two later-found duties (CAP-8) are separate |
| CAP-8 | **Two further gate duties the later articles found** (flagged as assembly, not §9.3 canon) — (7) the **LLM task-class budget** is an ordinary constraint, one more predicate on the same seam; (8) **`publish` is a route, not an Enzyme**, so the seam has a **market-side duty** too | VI.1 | ⬜ both not started (7 ⏸️ economy-deferred; 8 = ARE-2) |
| CAP-9 | **★ SEAM 2 — fork+sim is used three ways** — Tenet's scenario replay, the Symbiont sandbox, and Editing's simulate-before-commit. **One mechanism, three consumers, and the correctness condition is that all three run under the same gate** — which is exactly where the Python code diverges | VI.2 | ✅ **structural in Rust** — there is one execution path, so all three consumers must ride it (this is the concrete form of backlog #12) |
| CAP-10 | **SEAM 3 — the log sits under three things at once** — state (as a fold), definitions (as versioned history), audit (what happened, when, from whom); and per TOLERANCE it is **immune memory**: the substrate on which *this has happened before* and *this has never happened before* both become answerable | VI.3 | 🔨 state-as-fold ✅; **definitions-as-versioned-history ⬜ (= EDIT-7)**; audit/immune-memory reads ⬜ |
| CAP-11 | **SEAM 4 — Embroidery is the single declaration source** — S dimensions, V invariants, guards and effects, utilities and weights, widget schemas and bindings, connectors, domains. *"One place to say what is true — which is the only way the type-checker can be one type-checker"* | VI.4 | 🔨 Rust has **one** predicate parser/evaluator and a typed schema (R2 #14) — the property this seam is about; the **authoring language ⬜** (= M-DSL) |
| CAP-12 | **SEAM 5 — provenance threads the whole length** — device tokens → event principals → approval decisions. *"Break the thread anywhere and the audit becomes a list of assertions"* | VI.5 | 🔨 event `provenance` ✅ (R2 #17 legacy marker) + principals ✅ (R2 #10); **device tokens → approval decisions ⬜** |
| CAP-13 | **Behaviour varies by domain — the domain × engine matrix** (Observe/Model/Learn/Respond × clear/complicated/complex/chaotic); **domain detection is a Monitor function** (a shift is a regime change — what CUSUM exists to catch); **disorder is a first-class reading**, surfaced as *confidence over the domain estimate* | VII | ⬜ **not started** (= SUS-16, OPV-16) |
| CAP-14 | **★ The honest tension at assembly scale** — *"Most of Sustena is complicated-domain machinery… a household and a business are complex systems."* The complex row is deliberately **not** the complicated row with more compute; Ashby: variety by **nesting** grows combinatorially where variety by **enumeration** runs out — *"the load-bearing reason the primitive is recursive"* | VII·IX.3 | ✅ recorded as the design's own stated limit |
| CAP-15 | **Two surfaces, one truth** — Mycelium and Orchie are **projections of the same object differing only in budget** | VIII.1 | ⬜ Rust (✅ app-layer) |
| CAP-16 | **Settlement: Local → Babychain → finalizer** — each Sustain executes locally and almost all activity stays there; a **Babychain** aggregates and **anchors periodically**, committing the **state-root hash** (optionally with a validity proof) rather than the transactions; *"Ethereum as finalizer does not run the system; it finalises its truth"* | VIII.2 | ⏸️ economy-deferred |
| CAP-17 | **Governance — local defines, global enforces** — a DAO is a Sustain with embedded consensus; **CouncilSession** for a single-human Sustain, **StakeholderSession** added when interests are genuinely in tension; **authority attaches to the membership EDGE, not the member**; a **skin** is a named bundle of edge privileges (RBAC); bounds: FLP, and `k ≥ 3f+1` | VIII.3 | ✅ **edge-authority + skins built in Rust (R2 #10)**; **consensus ✅ built 2026-08-16** (= MUL-7/8/9 — quorum/Paxos with `2q > k` structural, FLP and `k ≥ 3f+1` recorded as the bounds they are), and EDIT-8's `CouncilMint` now derives a shared-definition quorum from a real `Decision` rather than asserting one; StakeholderSession ⬜ |
| CAP-18 | **Foundation and Institute — the no-special-power rule is structural** — complete mediation, no bypass. *"A privileged-Symbiont exception is not a feature with a risk; it is the deletion of the property that makes the rest of the argument true"* | VIII.4 | ✅ **structural in Rust** (one path, no privileged bypass) |
| CAP-19 | **The thesis, made precise** — *"Any system that can be described can be modeled, simulated, and optimized before execution"*; the loop **DESCRIBE → MODEL → SIMULATE → OPTIMISE → EXECUTE → OBSERVE**, closed, forever | IX.1 | 🔨 DESCRIBE/EXECUTE ✅; **SIMULATE partial, OPTIMISE ⬜, OBSERVE ⬜** (= M-TEN, M-MON, M-CTL) |
| CAP-20 | **Why one recursive primitive is the economical route** — the argument is about **interfaces, and it is countable** | IX.3 | ✅ the justification for the whole build |

**Summary:** the Capstone is not a module to build but the **map of where leverage concentrates**, and it names the two seams Rust has already banked: **the gate** (four of six duties discharged, structurally) and **fork+sim under one gate** (a bypass is not expressible). The remaining gate duties are the **bound approval token** (OPV-30), **edit authority** (EDIT-8) and the **firewall F** (CON-1) — and the article adds two more found later: the **LLM policy predicate** and **publish-as-an-Enzyme** (ARE-2). The build order this WBD follows is the article's own (CAP-4), including its two insistences: **Editing comes after the Enzyme path**, and **the Controller is not a late layer**. CAP-14 is the honest frame for everything: most of the machinery is complicated-domain, most households are complex, and the matrix (CAP-13) is how the design faces that rather than papering over it.


---

## Completeness checklist — every technical article mapped

**All 18 ✓ — every technical article has been fully read and its features extracted into the WBD above. The breakdown is complete.**

- ✓ Events and Time → M-EVT
- ✓ Sustain → M-SUS
- ✓ Operator → M-OP
- ✓ Constraint → M-CON
- ✓ Immune → M-IMM
- ✓ DSL → M-DSL
- ✓ Ingest → M-ING
- ✓ Monitor → M-MON
- ✓ Tenet → M-TEN
- ✓ Operative → M-OPV
- ✓ Multiparty → M-MUL
- ✓ Curated UI → M-UI
- ✓ Editing → M-EDIT
- ✓ Controller → M-CTL
- ✓ Pawa → M-PAWA
- ✓ Mycelium → M-PAWA
- ✓ Arena → M-PAWA
- ✓ Capstone → M-CAP

---

## New gaps found by the final eight reads

*Not on `R2_BACKLOG.md`. Recorded here so the backlog can absorb them on its next pass; each is marked ★ or ⚠ in its own module above.*

| # | Gap | Module | Why it matters |
|---|---|---|---|
| N1 | **The approval token — ✅ SHIPPED IN RUST 2026-08-12** (`sustena-core/src/approval.rs`). Was grep-0 everywhere. Built bound to `(o,θ)`, single-use and expiring, with the trace invariant `sim ≺ vote ≺ approve` enforced by **move semantics** rather than a runtime scan — prefix-closed, hence a safety property, hence structural. **Still open in Python**, recorded as a documented rust-ahead divergence | OPV-30/31, EDIT-8, CAP-7 | The gate's third duty. One object discharges obligations in three articles |
| N2 | **`publish` is a route, not an Enzyme** — a **second write path into the Arena**, the exact failure complete mediation forbids | ARE-2, CAP-8 | A seventh gate duty the Capstone found after §9.3 |
| N3 | **A definition edit destroys its predecessor — ✅ RESOLVED IN RUST 2026-08-12** (`sustena-core/src/version.rs`). Was an in-place `UPDATE … SET spec_json` with a bare counter, so rollback had nothing to roll back *to*. Now an append-only DAG with a head pointer, declared inverses over the typed taxonomy, and three-valued rollback whose `Total` cannot carry a Δ. **Still open in Python**, recorded as a documented rust-ahead divergence | EDIT-7 | Blocks the whole rollback algebra |
| N4 | **`edit.operator_spec` lets `pawa_cost` and `license_tier` be changed at runtime, free, with no authority check** — and mutates the in-memory registry, so the change is process-local and lost on restart | EDIT-14 | *Changing an Enzyme's price costs less than using it* |
| N5 | **`edit.state_patch`'s `remove` writes `None` instead of deleting** — under a typed schema that deposits a type violation into state | EDIT-15 | Rust's organisational closure would refuse it |
| N6 | **Live bug — `NetworkZone.jsx:47`** — `owner_ids?.[0]` on the unsubstituted token `"{{owner_ids}}"` indexes the *string* and yields `"{"`, which is truthy, so the fallback never fires and `"{"` is posted as the `user_id` provisioning child sustains | EDIT-16 | Wrong ownership on real children today; nothing refused, nothing logged |
| N7 | **The `domain` key collision** — `operatives.<n>.domain` is a subject-matter tag list; the article's `dom` is the **Cynefin axis**. Reusing the key makes one of them inexpressible | OPV-16, CAP-13 | Cheap to avoid now, expensive after specs ship |
| N8 | **A grant that mints is not a grant that transfers** — a transfer conserves and is treasury-bounded; a mint is unbounded and **makes Sybil registration directly profitable**. Both are legitimate; *only one is legitimate undeclared* | MYC-6 | ⏸️ economy, but it is a correctness property, not a pricing choice |
| N9 | **`compose(r)`'s recency window is a hard-coded `limit=25`** — a widget whose triggering event has scrolled out cannot be summoned at all | UI-4 | A policy living in a call argument |
| N10 | **Urgency returns 0.0 when `allocated ≤ 0`** — an **unfunded pocket can never be urgent**, which is backwards | UI-7 | Rides on M-MON; worth fixing at the proxy meanwhile |

**Two cheap, high-value items** surfaced that are *not* new gaps but are newly costed: **runway** (`R(s)=min{k: s_k∉V}` — one policy, one forward scan, OPV-19) and the **branching-ratio criticality read** (σ̂ is a read over the log's `causes`, OPV-14). Both become available the moment V is a region and `causes` is on the event record.

---

*Created 2026-08-12 as the frame; **completed 2026-08-12** — all 18 technical articles read and extracted. Historical source: `Projects/SUSTENA_UPGRADE_SPEC.md` (the Python-upgrade synthesis). Python engine stays live+untouched as the reference until Rust ships.*
