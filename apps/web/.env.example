# Sustena XII — frontend environment
# Copy to .env.local and fill in your values.

# DevUI backend admin token (matches ADMIN_TOKEN in the backend .env)
# In dev, the backend defaults to: ci-admin-token
VITE_ADMIN_TOKEN=dev-token
