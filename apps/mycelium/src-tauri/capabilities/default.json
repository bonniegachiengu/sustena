{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The cockpit's own window. No filesystem, no network, no shell -- the engine is compiled in, so it needs none of them.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
