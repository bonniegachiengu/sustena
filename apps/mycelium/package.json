{
  "name": "mycelium",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "bindings": "cargo run --manifest-path src-tauri/Cargo.toml --features dev-bins --bin export_bindings",
    "smoke": "cargo run --manifest-path src-tauri/Cargo.toml --features dev-bins --bin smoke"
  },
  "dependencies": {
    "@fontsource/dm-mono": "^5.3.0",
    "@fontsource/inter-tight": "^5.3.0",
    "@tauri-apps/api": "^2",
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vanilla-extract/css": "^1.16.1",
    "@vanilla-extract/vite-plugin": "^5.2.6",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vite-plugin-solid": "^2.11.0"
  }
}
